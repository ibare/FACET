/**
 * 원형 버퍼 되감기 학습 설명 — 단일 마크다운.
 *
 * `{facet:circularBufferWrap}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 * 조각이 전제하는 것 (칸 다섯, 처음부터 두 자리가 차 있고 tail 이 이미 감겨 있다) 은
 * 화면의 각주가 아니라 이 글이 밝힌다 (S-piece).
 */

export const circularBufferWrapDescription = `## 마지막 칸 다음은 어디인가

배열은 끝이 있다. 다섯 칸짜리 배열의 4 번 칸 다음은 없다. 그런데 **자리 번호를 셀 때
\`(i + 1) % 5\` 를 쓰면** 4 다음이 0 이 되어, 끝이 앞으로 이어진다. 칸은 그대로 다섯인데
가리키는 자리만 돌고 도는 것이다.

{facet:circularBufferWrap}

### 무엇이 도는가

도는 것은 값이 아니라 **자리**다. 넣을 자리(tail)와 뺄 자리(head) 두 개만 앞으로
나아가고, 칸에 들어앉은 값은 옮겨지지 않는다. 큐를 배열로 만들 때 앞에서 하나를 빼고
나머지를 전부 한 칸씩 당기면 뺄 때마다 n 번을 옮겨야 하지만, 자리 번호만 밀면 한 번이다.

위 그림은 칸 다섯에 값 둘이 든 상태에서 시작한다. 앞의 세 자리는 이미 빠져나가 비어
있고, tail 은 4 번 칸을 지나 0 번 칸으로 감긴 뒤다. 그 자리에 새 값이 들어가는 것이
곧 **빠져나간 자리를 다시 쓰는 일**이다.

### 가득 참과 텅 빔

되감기에는 대가가 하나 있다. head 와 tail 이 같은 자리를 가리킬 때, 그것이 텅 빈
것인지 가득 찬 것인지 번호만 봐서는 알 수 없다. 그래서 실제 구현은 칸 하나를 늘 비워
두거나, 들어 있는 개수를 따로 센다.

### 어디에 쓰이나

- **오디오·센서 버퍼** — 초당 수만 번 들어오는 표본을 고정 크기 자리에 돌려 담는다.
- **로그 링 버퍼** — 최근 n 건만 남기고 오래된 것 위에 덮어쓴다.
- **커널 큐** — 메모리를 새로 잡지 않고 정해진 자리만 재사용해야 하는 곳.
`;
