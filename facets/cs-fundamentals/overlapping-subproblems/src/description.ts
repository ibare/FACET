/**
 * 중복 부분 문제 조각의 학습 설명.
 *
 * 조각이 전제하는 것 — 무엇을 줄여 그렸고 무엇을 그리지 않았는지 — 은 화면
 * 각주가 아니라 이 글이 밝힌다 (S-piece).
 *
 * `{facet:overlappingSubproblems}` 토큰은 호스트가 인라인 시각화로 치환한다.
 */

export const overlappingSubproblemsDescription = `## 중복 부분 문제 (Overlapping Subproblems)

**재귀 정의를 곧이곧대로 따르면, 서로 다른 가지에서 같은 부분 문제를 몇 번이고 다시 푼다.** 정의 자체는 짧고 우아하다. 낭비는 정의가 아니라 정의를 펼친 자리에서 생긴다.

{facet:overlappingSubproblems}

### 정의는 짧고, 펼친 것은 크다

피보나치의 정의는 두 줄이다.

- \`f(0) = 0\`, \`f(1) = 1\`
- \`f(n) = f(n-1) + f(n-2)\`

이 정의를 그대로 함수로 옮기면, \`f(5)\` 하나를 부르는 데 호출이 **15 번** 일어난다. 그런데 그 15 번이 다루는 서로 다른 항은 **여섯** (\`f(0)\` ~ \`f(5)\`) 뿐이다. 나머지 아홉 번은 이미 답을 낸 적 있는 것을 처음인 양 다시 푸는 일이다.

가장 심한 것은 \`f(1)\` 로, 혼자서 다섯 번 풀린다. \`f(0)\` 과 \`f(2)\` 가 각각 세 번, \`f(3)\` 이 두 번이다. 그렇게 해서 얻는 답은 **5** 라는 수 하나다.

### 왜 겹치는가

\`f(5)\` 는 \`f(4)\` 와 \`f(3)\` 을 부른다. 그런데 \`f(4)\` 도 자기 몫으로 \`f(3)\` 을 부른다. 두 갈래가 서로를 모르므로 같은 \`f(3)\` 을 각자 처음부터 다시 푼다. 그 아래로 내려갈수록 겹침은 배로 불어난다 — 작은 항일수록 더 여러 가지가 그것을 필요로 하기 때문이다.

조각의 선반을 보면 이 성질이 그대로 나타난다. 왼쪽(작은 항)의 더미가 높고 오른쪽(큰 항)의 더미가 낮다.

### 무엇이 문제인가

호출 수는 \`f(n)\` 에 대해 대략 \`2·f(n+1) - 1\` 로, **지수적으로** 자란다. \`f(5)\` 는 15 번이지만 \`f(30)\` 은 269 만 번 (2,692,537) 이다. 재귀가 느린 것이 아니라, **같은 것을 다시 푸는 것**이 느리다.

여기서 다음 이야기가 필요해진다 — 한 번 푼 답을 적어 두면 두 번째부터는 그것을 꺼내 쓰면 된다. 메모이제이션과 동적 계획법은 이 그림의 주황색 노드를 전부 없애는 장치다.

### 이 조각이 보여 주는 것 · 보여 주지 않는 것

- 화면의 나무는 **호출이 일어나는 순서**(전위)로 자란다. 실제 실행에서는 아래로 내려갔다가 값을 들고 되올라오지만, 이 조각은 **되올라오는 길을 그리지 않는다.** 여기서 세는 것은 "몇 번 불렀는가" 이지 "언제 답이 정해지는가" 가 아니다.
- 노드 모서리의 숫자는 그 항이 **몇 번째로 나타났는지**다. 1 이면 처음 푸는 것, 2 이상이면 이미 푼 것을 또 푸는 것이다.
- \`n = 5\` 는 한 화면에 담기는 가장 큰 값에 가깝다. 겹침의 **비율**은 \`n\` 이 커질수록 훨씬 나빠지므로, 여기 보이는 15 대 6 은 문제의 축소판이지 최악이 아니다.
`;
