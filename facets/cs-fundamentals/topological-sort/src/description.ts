/**
 * topological-sort 완결형의 글.
 *
 * `{facet:topologicalSort}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const topologicalSortDescription = `
## 위상 정렬

강의를 듣는 차례, 빌드가 도는 차례, 설치가 되는 차례. "이것보다 저것이 먼저" 라는
조건이 잔뜩 있을 때 모두를 만족하는 한 줄을 뽑는 일이다. 조건은 화살로 적는다 —
\`0 → 2\` 는 "0 이 2 보다 먼저" 라는 뜻이다.

칸(Kahn) 알고리즘은 이것을 아주 단순하게 푼다. **들어오는 화살이 없는 것은 지금
바로 해도 된다.** 그것을 꺼내고, 그것이 걸어 두었던 조건을 지운다. 그러면 또 다른
것의 조건이 다 풀린다. 그 되풀이가 전부다.

{facet:topologicalSort}

## 수 하나만 들고 있으면 된다

정점마다 "아직 나를 가리키는 화살이 몇인가" 하나만 세어 두면 된다. 그 수가 0 이면
앞을 막는 것이 없다는 뜻이고, 곧바로 줄로 **떨어져 나온다**.

하나를 꺼낼 때마다 그것이 가리키던 것들의 수를 하나씩 줄인다. 화면에서 화살이
한 번씩만 흐려지는 것을 보라 — 화살 하나는 딱 한 번 쓰이고 끝난다. 그래서 이
알고리즘이 하는 일의 총량은 **정점 수 + 간선 수** 를 넘지 않는다.

여기서는 정점 여섯에 화살 일곱이다. 처음에 0 이 되어 있는 것은 0 과 1 이고,
0 을 꺼내니 2 가 풀리고, 1 을 꺼내니 3 과 4 가 풀리고, 그 셋이 다 나간 뒤에야
5 가 풀린다. 5 는 화살 셋을 받고 있으므로 셋이 모두 빠질 때까지 기다린다.

## 줄은 배열 하나와 색인 둘이다

풀린 것들을 담아 두는 줄은 대단한 물건이 아니다. **배열 하나**와 **head · tail**
두 색인이면 된다. tail 은 다음에 놓을 자리, head 는 다음에 꺼낼 자리다. 둘이
같으면 줄이 비어 있고, \`head < tail\` 인 동안 꺼낼 것이 남아 있다.

코드 패널에 \`queue_push\` 나 \`queue_pop\` 같은 이름이 없는 것은 그래서다. 감싸
버리면 큐가 무엇으로 되어 있는지가 코드에서 사라진다. 여섯 언어 어느 쪽을 열어도
\`queue[tail] = v\` 와 \`tail = tail + 1\` 두 줄이 그대로 보인다.

head 는 한 가지 일을 더 한다 — 지금까지 꺼낸 개수이기도 하다. 그래서 결과에서의
자리가 바로 \`order[head]\` 이고, 마지막 판정도 head 하나로 끝난다.

## 못 뽑는 경우가 있다

\`A → B\`, \`B → C\`, \`C → A\` 처럼 조건이 고리를 이루면 어떤 차례도 만들 수 없다.
셋이 서로를 막고 있어서 어느 것의 수도 영영 0 이 되지 않는다.

그때 이 알고리즘은 멈추지 않고 **조용히 덜 뽑는다**. 줄이 비면 while 이 끝날 뿐,
고리가 있다고 소리치지 않는다. 그래서 마지막 한 줄이 필요하다.

\`\`\`
if head < n:
    return -1
\`\`\`

꺼낸 수가 정점 수에 못 미치면 남은 것들이 고리를 이루고 있다는 뜻이다. 화면
오른쪽 아래의 \`꺼낸 것 6 / 6\` 이 그 대조를 그대로 보여 준다. 이 판정이 없으면
답이 틀렸다는 사실을 아무도 모른 채 짧은 줄을 답으로 쓰게 된다.

## 차례가 하나뿐인 것은 아니다

이 그래프에서는 0 과 1 이 둘 다 처음부터 자유롭다. 어느 쪽을 먼저 꺼내도 조건을
어기지 않으므로 올바른 차례가 여럿이다. 여기서는 큐가 먼저 들어온 것을 먼저
내보내고(FIFO), 처음 훑을 때 번호가 작은 것부터 넣었으므로 0 · 1 · 2 · 3 · 4 · 5
가 나온다.

번호가 작은 것을 반드시 먼저 뽑아야 한다면 큐 대신 최소 힙을 쓴다. 그러면 배열
하나와 색인 둘이라는 단순함은 사라진다 — 무엇을 얻고 무엇을 잃는지가 여기서
갈린다.
`;
