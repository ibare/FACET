/**
 * dfs 완결형의 글.
 *
 * `{facet:dfs}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const dfsDescription = `
## 깊이 우선 탐색

갈림길이 나오면 어느 쪽으로 갈 것인가. 깊이 우선 탐색의 대답은 "일단 하나를 골라
끝까지 가 본다" 이다. 더 갈 데가 없으면 **왔던 길을 되짚어 나와** 아직 안 가 본
갈래로 든다. 그것이 전부다.

{facet:dfs}

## 부르는 일과 돌아오는 일

코드에서 파고드는 것은 \`dfs(adj, visited, nb)\` 한 줄이다. 함수가 자기를 부른다.
그 한 줄이 끝나기 전까지 아래 갈래가 통째로 다녀오고, 그동안 부른 쪽은 자기가
몇 번째 이웃을 보던 참이었는지를 그대로 붙들고 기다린다.

붙들고 기다리는 그 자리가 **호출 스택** 이다. 화면 오른쪽에 쌓이는 것이 그것이고,
파고들 때 하나 쌓이고 돌아 나올 때 하나 빠진다. 그래프 위의 노란 사슬이 같은
것을 다른 방식으로 그린 것이다 — 뿌리에서 지금 자리까지의 길이 곧 스택이다.

돌아오는 일은 그냥 "다음으로 넘어가는 일" 이 아니다. 함수 하나가 끝나고 부른
쪽으로 제어가 되돌아가는 사건이며, 되돌아간 쪽은 멈춰 두었던 반복문을 **그 다음
이웃부터** 이어서 돈다. 간선마다 남는 두 화살이 그 기록이다. 아래를 가리키는
것은 들어간 자국, 위를 가리키는 것은 나온 자국이다. 끝나고 보면 들어간 자국이
있는 간선에는 나온 자국이 반드시 함께 있다.

## 코드에 없는 줄

여기에 이 그림의 어려운 대목이 있다. 파고드는 것은 코드에 줄이 있는데 **돌아오는
것은 줄이 없다.** 함수는 그냥 본문이 끝날 뿐이다.

그래서 코드 패널의 마지막 \`return\` 은 없어도 도는 줄이지만 일부러 적어 두었다.
화면에서 프레임이 빠지는 순간에 짚을 자리가 필요했기 때문이다. 실제 코드에서
그 줄을 지워도 프로그램은 똑같이 돈다 — 다만 지우고 나면, 돌아오는 일이 언제
일어나는지가 코드 어디에도 보이지 않게 된다.

## 이미 본 자리

\`visited\` 표는 재귀 호출들이 나눠 본다. 한 갈래에서 본 자리는 다른 갈래에서도
본 자리다. 이 그래프에서는 정점 5 가 두 곳에서 가리켜진다 — 4 에서 한 번, 2 에서
한 번. 먼저 닿은 4 쪽으로 들어가고 나면, 나중에 2 에서 5 를 볼 때는 이미 본
자리라 들어가지 않는다. 화면에서 그 간선 하나만 빨간 점선으로 남는다.

이 표시가 없으면 어떻게 되는가. 고리가 있는 그래프에서는 같은 자리를 영원히
맴돈다. 고리가 없어도 같은 갈래를 몇 번이고 다시 세게 되어 일이 폭발한다.
정점 여덟에 간선 여덟인 이 그래프에서 걸음이 딱 여덟 번의 방문과 여덟 번의
간선 검사로 끝나는 것은 표 덕분이다.

## 깊이가 값이다

화면의 "가장 깊었던 스택" 은 5 에서 멈춘다. 0 → 1 → 4 → 6 → 7 로 내려간 그
순간이 가장 깊었고, 그 다섯 개의 프레임이 그때 메모리에 함께 살아 있었다.

이것이 깊이 우선 탐색이 치르는 값이다. 너비 우선 탐색이 "같은 거리의 모든 것" 을
큐에 담아 두는 대신, 깊이 우선 탐색은 **지금 파고든 한 갈래의 길이만큼** 만
붙들고 있으면 된다. 넓게 퍼진 그래프에서는 이쪽이 훨씬 적게 든다. 반대로 길게
늘어진 그래프에서는 스택이 정점 수만큼 깊어져서, 재귀로 짠 코드가 스택을 넘겨
터지기도 한다.

## 방문 차례는 이웃 목록이 정한다

이 그림의 방문 차례는 0 · 1 · 3 · 4 · 5 · 6 · 7 · 2 다. 왜 2 가 맨 마지막인가.
0 의 이웃 목록이 \`[1, 2]\` 라서 1 을 먼저 보기 때문이다. 1 로 들어간 뒤에는 그
아래가 다 끝나야 0 으로 돌아오고, 그제서야 2 차례가 온다.

목록을 \`[2, 1]\` 로 바꾸면 차례가 통째로 달라진다. 깊이 우선 탐색이 돌려주는
차례는 그래프의 성질이 아니라 **이웃을 적어 둔 차례** 의 성질이기도 하다.
`;
