/**
 * 순차 접근 학습 설명 — 단일 마크다운.
 *
 * `{facet:traverseFromHead}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const traverseFromHeadDescription = `## 처음부터 따라가야 닿는다

**연결 리스트에서 n 번째 노드에 닿으려면 n 번 옮겨 가야 한다.** 세 번째 뒤의 것이 필요하면 세 번, 백 번째 뒤의 것이 필요하면 백 번이다. 지름길이 없다.

{facet:traverseFromHead}

### 왜 건너뛸 수 없나

배열은 원소가 한 덩어리로 붙어 있어서 **시작 주소 + 칸 크기 × 인덱스** 라는 셈 한 번이면 어디든 곧장 닿는다. 인덱스 3 이든 3000 이든 드는 값이 같다.

연결 리스트는 그렇지 않다. 노드가 저마다 다른 자리에 흩어져 있고, 어디에 있는지는 **앞 노드만 안다.** 그러니 셈할 것이 없다. 앞 노드에게 물어 다음 자리를 알아내고, 거기서 또 물어야 한다.

### 그래서 갈리는 것

- **읽기** — 배열은 어디든 한 번. 리스트는 앞에서부터 세는 만큼.
- **가운데 끼워 넣기** — 배열은 뒤를 전부 밀어야 하지만, 리스트는 링크 둘만 고쳐 쓰면 된다. 다만 **그 자리까지 가는 데 드는 값은 여전히 따라간 만큼이다.**

한쪽이 다른 쪽보다 나은 것이 아니라, 무엇을 자주 하느냐로 갈린다. 자리를 두고 왔다 갔다 읽는 일이 잦으면 배열, 중간을 자주 헤집으면 리스트다.

### 링크를 따라가는 값을 줄이려면

리스트 구조를 그대로 두고 건너뛸 길을 얹는 방법이 있다. 몇 칸씩 건너뛰는 링크를 층층이 더 얹는 **스킵 리스트**가 그것이고, 그때도 여전히 "따라간다" 는 성질은 남는다 — 다만 한 걸음의 보폭이 커질 뿐이다.
`;
