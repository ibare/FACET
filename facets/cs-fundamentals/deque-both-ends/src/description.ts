/**
 * 양방향 큐 조각 학습 설명 — 단일 마크다운.
 *
 * `{facet:dequeBothEnds}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const dequeBothEndsDescription = `## 양끝이 모두 열려 있다

**양방향 큐(deque)는 문이 둘이고, 그 둘이 저마다 넣기와 빼기를 겸한다.** 큐는 뒤로 넣어 앞으로만 빼고 스택은 한쪽으로만 드나드는데, 양방향 큐는 두 끝이 다 열려 있어 어느 끝에서든 넣고 어느 끝에서든 뺀다.

{facet:dequeBothEnds}

### 문은 둘, 조작은 넷

문이 둘인데 조작이 넷인 것은 문 하나가 두 몫을 하기 때문이다. 앞에 넣기 · 앞에서 빼기 · 뒤에 넣기 · 뒤에서 빼기 — 이 넷이 두 끝에 두 개씩 붙는다. 그림은 자리 넷짜리 통으로 줄여 그렸고, 값이 들고 날 때 **이미 앉은 값은 움직이지 않는다.** 양끝 조작이 나머지를 건드리지 않아서 넣기도 빼기도 한 번에 끝난다.

### 스택으로도 큐로도

한쪽 문만 쓰면 넣은 자리에서 그대로 빼게 되니 스택이고, 한 문으로 넣어 반대 문으로 빼면 먼저 들어온 것이 먼저 나가니 큐다. 그래서 양방향 큐 하나로 두 자료 구조를 다 흉내 낼 수 있다 — 반대로 스택이나 큐로는 양방향 큐를 흉내 내지 못한다.

### 어디에 쓰이나

- **슬라이딩 윈도** — 창 밖으로 밀려난 값은 앞에서 버리고, 새 값은 뒤로 넣는다.
- **되돌리기 기록** — 뒤로 쌓다가 기록이 너무 길어지면 앞에서 오래된 것을 잘라 낸다.
- **작업 훔치기** — 자기 일감은 자기 끝에서 꺼내 쓰고, 남는 일감은 반대 끝에서 다른 일꾼이 가져간다.
`;
