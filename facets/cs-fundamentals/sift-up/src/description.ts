/**
 * 상향 재배치(sift-up) 학습 설명 — 단일 마크다운.
 *
 * `{facet:siftUp}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const siftUpDescription = `## 자리를 찾을 때까지만 오른다

힙에 값을 넣을 때 새 값은 일단 **맨 끝자리**에 앉는다. 그런 다음 자기 부모와
견주어, 자기가 더 앞서면 부모와 자리를 맞바꾸며 한 칸 오른다. 앞서지 못하는
부모를 만나면 거기가 자기 자리다.

{facet:siftUp}

### 꼭대기까지 가지 않는다

모든 값이 꼭대기까지 오르는 것은 아니다. 부모보다 크면(또는 같으면) 그 자리가
곧 최종 자리이고 더 비교하지 않는다. 최악의 경우에도 오르는 칸 수는 트리의
높이 — 원소가 n 개면 대략 log n — 를 넘지 않는다.

### 어디에 쓰이나

- **우선순위 큐** — 힙으로 구현한 우선순위 큐가 원소를 넣을 때 이 과정을 그대로 쓴다.
- **힙 정렬** — 힙을 만드는 단계와 값을 하나씩 빼는 단계 모두 이 재배치에 기대어 있다.
- **스케줄러** — 마감이 이른 작업이 항상 앞자리에 오도록 이 방식으로 유지한다.
`;
