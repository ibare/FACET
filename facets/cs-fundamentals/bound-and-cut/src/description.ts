/**
 * bound-and-cut 의 글.
 *
 * `{facet:boundAndCut}` 토큰은 호스트가 그 자리에 이 조각의 화면을 앉히라는
 * 표식이다. 화면이 말하지 않는 전제 — 자르는 조건이 엄격 부등호라는 것, 무게가
 * 넘친 갈래는 아예 그리지 않았다는 것 — 은 화면 각주가 아니라 여기서 밝힌다.
 */

export const boundAndCutDescription = `
# 한계 기반 가지치기

가지치기라고 하면 보통 **조건을 어긴 자리**를 떠올린다. 무게가 넘쳤으니 더 볼 것이
없고, 같은 줄에 퀸이 둘이니 더 볼 것이 없다. 어겼으니 접는 것이다.

여기서 접는 이유는 다르다. **아직 아무것도 어기지 않았다.** 그런데도 접는다 —
이겨 봐야 못 이기기 때문이다.

{facet:boundAndCut}

## 한계를 어떻게 재는가

갈래 하나를 붙들고 "여기서 가장 잘 되면 얼마인가" 를 묻는다. 정직하게 답하려면
그 아래를 다 뒤져야 하지만, **문제를 살짝 느슨하게 풀면** 답이 금방 나온다.

물건을 쪼갤 수 있다고 치는 것이다. 남은 물건을 값/무게가 큰 것부터 담되 마지막
하나는 쪼개서라도 한도를 꽉 채운다. 실제로는 쪼갤 수 없으므로 진짜 답이 이 값을
넘을 길은 없다. 그래서 이 값이 그 갈래의 **한계**다.

화면의 막대가 두 토막인 것이 그 때문이다. 아래 채워진 토막은 이미 담기로 한 값,
위 점선 토막은 쪼개서라도 채웠다 치고 얹은 몫이다. 둘을 합한 높이가 한계다.

## 무엇이 잘렸나

한도 5 인 가방에 A(무게 2·값 12) · B(3·15) · C(4·16).

- A 와 B 를 담으면 무게 5 에 값 27. 이것이 최고가 된다.
- **A 를 담고 B 를 뺀 갈래** — 아직 무게 2 라 한도에 여유가 넉넉하다. 아무것도
  어기지 않았다. 그런데 남은 C 를 3/4 만큼 쪼개 넣어도 한계가 24.0 이라 27 을
  못 넘는다. 잘린다.
- **A 를 아예 안 담는 갈래** — 여기도 성하다. B 를 담고 C 를 2/4 쪼개 넣으면
  23.0. 역시 못 넘는다. 잘린다.

자르기 두 번으로 여덟 조합 중 여섯을 안 봤고(나머지 하나는 A·B·C 를 다 담으면
무게 9 가 되어 애초에 갈래가 생기지 않는다), 그러고도 최적 27 을 놓치지 않았다.

## 최고가 기준이다

자르는 기준은 고정된 숫자가 아니라 **지금까지의 최고**다. 화면 오른쪽 칸이
재생 도중에 0 에서 12 로, 다시 27 로 올라가는 것이 그것이다. 기준이 올라가면
더 많은 갈래가 그 밑에 깔린다.

그래서 **좋은 답을 일찍 찾는 것이 곧 속도**다. 값/무게가 큰 것부터 보는 순서,
욕심껏 먼저 한 번 담아 보고 시작하는 것 — 답을 개선하려는 수단이 아니라 자를
칼을 일찍 세우려는 수단이다.

## 전제

- 한계 셈이 상한이 되려면 남은 물건이 **값/무게 내림차순**이어야 한다. 화면은
  물건을 그 순서로 세워 두고 셈한다.
- 한계가 최고와 **같을 때는 자르지 않는다.** 같은 값의 다른 답을 지우지 않기
  위해서다. 같은 것도 버려도 좋다면 자르기가 조금 더 늘어난다.
- 담으면 한도를 넘는 선택은 갈래가 **생기지도 않는다.** 그것은 조건 위반이라
  여기서 말하는 자르기와 다른 일이다.
`;
