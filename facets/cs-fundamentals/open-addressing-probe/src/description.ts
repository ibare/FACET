/**
 * 개방 주소법 조각의 학습 설명 — 단일 마크다운.
 *
 * `{facet:openAddressingProbe}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 * 조각이 화면에 각주를 두지 않으므로 (S-piece), 전제는 이 글이 밝힌다.
 */

export const openAddressingProbeDescription = `## 자리가 차면 다음 자리를 본다

해시 표에서 두 열쇠가 같은 자리를 가리키는 일은 피할 수 없다. 자리는 유한하고 열쇠는 그렇지 않으니, 언젠가는 부딪힌다. 그때 **표 밖에 사슬을 달아 매다는 방법**이 있고, **표 안에서 끝내는 방법**이 있다. 뒤엣것이 개방 주소법이다.

규칙은 한 줄이다 — 제 자리가 차 있으면 **다음 자리를 본다.** 거기도 차 있으면 또 다음을 본다. 빈 자리를 만날 때까지 옆으로 걸어간다. 표 밖으로 나가는 것도, 다른 상자를 매다는 것도 없다. 열쇠는 언제나 표 안 어딘가에 앉는다.

{facet:openAddressingProbe}

### 앉은 자리가 제 자리가 아니다

이 방법의 대가는 그림 마지막에 드러난다. \`fig\` 의 제 자리는 4 번인데 5 번에 앉는다. \`fig\` 는 누구와도 부딪힌 적이 없다 — 4 번을 차지한 \`mango\` 야말로 제 자리가 2 번인, 밀려온 손님이다. **남의 충돌이 나에게 번진다.** 이렇게 밀려난 자리들이 서로 붙어 덩어리를 이루면 (군집, clustering) 그 구간에 떨어지는 열쇠마다 걸음 수가 길어진다.

그래서 찾기도 같은 걸음으로 해야 한다. \`fig\` 를 찾을 때 4 번만 보고 없다고 답하면 틀린다. **넣을 때 걸었던 길을 찾을 때도 똑같이 걸어야** 하고, 빈 자리를 만나야 비로소 없다고 말할 수 있다. 지우기가 까다로운 것도 이 때문이다 — 중간을 그냥 비우면 뒤에 앉은 열쇠로 가는 길이 끊긴다. 그래서 지운 자리에는 대개 "여기 있었다" 는 표를 남긴다.

### 어느 쪽을 고르나

- **적재율이 낮을 때** — 표에 여유가 있으면 걸음이 한두 칸에서 끝난다. 사슬의 포인터 추적보다 빠르다. 값이 메모리에 나란히 놓여 캐시에도 유리하다.
- **적재율이 높아지면** — 군집이 자라 걸음이 급격히 길어진다. 표가 가득 차면 앉을 곳 자체가 없다. 그래서 개방 주소법은 대개 적재율 상한(0.5 ~ 0.75)을 두고 넘으면 표를 키워 다시 배치한다.
- **한 칸 옆이 아닌 방법** — 한 칸씩 걷는 것을 선형 탐사라 한다. 군집을 흩기 위해 걸음 폭을 제곱으로 늘리거나(이차 탐사), 두 번째 해시 함수로 걸음 폭을 정하는(이중 해싱) 변형이 있다.

### 그림의 전제

버킷은 8 개, 걸음은 한 칸씩 오른쪽(\`(i + 1) % 8\`)이다. 해시는 자바 \`String.hashCode\` 값이고 자리는 \`(h & 0x7FFFFFFF) % 8\` 로 구한다. 넣는 차례는 \`apple\` · \`elder\` · \`mango\` · \`fig\` 이며, 순서가 달라지면 앉는 자리도 달라진다.
`;
