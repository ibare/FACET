/**
 * guess-by-value 학습 설명 — 단일 마크다운.
 *
 * 화면이 밝히지 않는 전제 — 값이 고르게 퍼져 있어야 겨눔이 맞는다는 것 — 을
 * 여기서 말한다. 조각은 여러 글에 불려 다니므로 각주를 화면에 새기지 않고
 * 함께 딸려 오는 이 글이 맡는다 (S-piece).
 *
 * `{facet:guessByValue}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const guessByValueDescription = `## 가운데는 아무것도 모른다

정렬된 배열에서 값을 찾을 때 가운데를 짚어 절반을 버리는 방법을 먼저 배운다. 짚을 때마다 남은 구간이 반으로 줄어드니 열 개짜리 배열은 서너 번, 백만 개짜리도 스무 번이면 끝난다. 이보다 나은 것이 있을 것 같지 않다.

그런데 가운데를 짚는다는 것은 **찾는 값이 무엇인지 전혀 보지 않는다**는 뜻이기도 하다. 90 을 찾든 15 를 찾든 첫 걸음은 똑같이 한가운데다. 사람은 그러지 않는다. 사전에서 "하마"를 찾을 때 한가운데를 펴지 않고 앞쪽 어림잡은 자리를 편다. 값의 크기가 자리를 짐작하게 해 주기 때문이다.

{facet:guessByValue}

### 비율이 자리가 된다

아래 줄이 하는 일이 그것이다. 남은 구간의 두 끝 값 — 여기서는 10 과 100 — 을 자의 눈금 삼고, 찾는 값 90 이 그 자 위 어디쯤인지를 잰다. \`(90 − 10) / (100 − 10)\` 은 \`8/9\` 다. 자리도 같은 비율만큼 가면 \`0 + 8/9 × 9 = 8\`, 곧 자리 8 이다. 거기에 90 이 있다. **한 번 짚고 끝난다.**

위 줄은 같은 목표를 향해 50 · 80 · 90 을 차례로 짚어 세 번 걸린다. 세 번과 한 번의 차이는 이 배열이 작아서 작아 보이지만, 값이 고르게 퍼져 있는 백만 개짜리 배열에서 가운데를 짚는 방법이 스무 번쯤 걸릴 때 값으로 겨누는 방법은 서너 번이면 닿는다.

### 겨눔이 맞는 조건

화면의 그림은 한 가지 전제 위에 서 있다 — **값이 고르게 퍼져 있다는 것.** 자의 두 끝을 첫 칸과 끝 칸의 한가운데에 맞춰 두었기 때문에, 값 간격이 일정하면 비율로 잰 자리와 칸의 한가운데가 정확히 겹쳐 선이 곧게 떨어진다. 이 조각의 데이터가 10 씩 늘어나는 것은 그래서다.

값이 고르지 않으면 겨눔은 빗나간다. \`[1, 2, 3, 4, 5, 1000000]\` 에서 1000000 을 찾으면 비율은 언제나 맨 끝을 가리키고, 구간은 한 칸씩만 줄어든다. 최악의 경우 배열 길이만큼 짚게 되어 가운데를 짚는 방법보다 오히려 나쁘다. 값의 분포를 모르거나 한쪽으로 쏠려 있다면 가운데를 짚는 쪽이 안전하다 — 그쪽은 값을 보지 않는 대신, 어떤 값이 들어와도 절반씩 줄어드는 것을 보장한다.

정리하면 이렇다. 가운데를 짚는 방법은 **자리만** 쓰고, 값으로 겨누는 방법은 **자리와 값을 함께** 쓴다. 정보를 더 쓰는 쪽이 더 빠른 것은 그 정보가 참일 때뿐이다.
`;
