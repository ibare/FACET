/**
 * 인접 교환 학습 설명 — 단일 마크다운.
 *
 * `{facet:bubbleAdjacentSwap}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 * 화면이 밝히지 않는 전제 — 훑기가 한 번뿐이라는 것 — 도 여기서 말한다 (S-piece).
 */

export const bubbleAdjacentSwapDescription = `## 옆끼리만 견주는데도 가장 큰 것이 끝까지 간다

버블 정렬을 처음 보면 "가장 큰 값을 찾아서 끝으로 보낸다" 로 읽힌다. 그런데 코드 어디에도 **찾는 단계가 없다.** 있는 것은 옆칸 둘을 견주고, 왼쪽이 더 크면 자리를 맞바꾸는 것뿐이다.

{facet:bubbleAdjacentSwap}

### 선두는 한 칸씩만 간다

화면 아래를 달리는 표시가 **선두** — 지금까지 본 것 중 가장 큰 값이 놓인 자리다. 견줌 한 번마다 이 표시는 예외 없이 한 칸 오른쪽으로 간다.

- 왼쪽이 더 크면 그 값이 이웃을 넘어 한 칸 오른쪽으로 옮겨 간다. 선두는 **값을 데리고** 간다.
- 오른쪽이 이미 더 크면 아무 값도 움직이지 않는다. 선두만 **더 큰 이웃에게 넘어간다.**

어느 쪽이든 한 칸이다. 그래서 칸이 다섯이면 견줌 네 번으로 선두가 왼쪽 끝에서 오른쪽 끝까지 닿는다. 화면 아래 자취선이 끊긴 데 없이 이어지는 것이 그 사실이다 — 멀리 건너뛴 구간이 한 군데도 없다.

### 그래서 끝자리가 확정된다

선두는 언제나 그때까지 본 것 중 가장 큰 값이고, 훑기가 끝나면 그것이 오른쪽 끝에 있다. 곧 **전체에서 가장 큰 값이 마지막 칸에 있다.** 그 자리는 다시 볼 필요가 없다.

찾는 일과 옮기는 일이 따로 있었던 것이 아니라, 옆끼리 견주는 일 하나가 두 몫을 다 한 것이다.

### 여기서 보인 것은 한 번의 훑음이다

이 그림은 왼쪽 끝에서 오른쪽 끝까지 **한 번** 훑고 멈춘다. 그 결과 배열은 아직 정렬되지 않았고 — 오른쪽 끝 한 자리만 확정됐다.

정렬을 마치려면 남은 구간을 대상으로 같은 훑기를 되풀이한다. 훑을 때마다 오른쪽 끝에서부터 한 자리씩 확정되므로, 칸이 N 개면 훑기가 N-1 번이고 견줌은 대략 N²/2 번이다. 버블 정렬이 **O(N²)** 인 까닭이 여기 있다.

### 왜 옆칸이어야 하는가

멀리 떨어진 둘을 견줘 맞바꾸면 한 번에 더 많이 움직일 수 있다. 실제로 셸 정렬은 그렇게 한다. 대신 옆칸끼리만 다루는 방식은 **서로 넘어간 적 없는 두 값의 순서를 절대 뒤집지 않는다** — 같은 값이 여럿 있어도 원래 순서가 보존된다(안정 정렬). 옆칸이라는 제약이 느림의 원인이자 안정성의 근거다.
`;
