/**
 * 제자리 정렬 학습 설명 — 단일 마크다운.
 *
 * `{facet:inPlaceVsExtra}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const inPlaceVsExtraDescription = `## 정렬은 자리를 얼마나 쓰는가

정렬 알고리즘을 견줄 때 우리는 대개 비교 횟수와 교환 횟수를 센다. 그런데 같은 답을 내는 두 방식이 **차지하는 자리**에서 갈리는 일이 있다. 하나는 처음 받은 칸 안에서 일을 끝내고, 다른 하나는 결과를 적을 띠를 따로 얻어 거기에 옮겨 적는다.

{facet:inPlaceVsExtra}

위쪽은 값 하나를 잠깐 들어 올리고, 그보다 큰 값들을 오른쪽으로 건너뛰게 한 뒤, 빈자리에 내려놓는다(삽입 정렬). 들고 있을 자리 **하나**가 필요하고, 그 자리는 라운드가 몇 번이든 다시 쓰인다. 아래쪽은 남은 값 중 가장 작은 것을 새 띠에 옮겨 적는다. 옮겨 적을 때마다 칸이 하나씩 더 필요하므로, 끝나고 보면 **원본만큼**의 자리를 빌렸다.

두 그림에서 값이 움직이는 부산함은 비슷하다. 다른 것은 아래에 깔린 넓이뿐이다.

### 제자리(in-place)라는 말

**제자리 정렬은 입력 크기와 무관하게 상수 칸만 더 쓰는 정렬이다.** 자리를 하나도 안 쓴다는 뜻이 아니다 — 값을 맞바꾸려면 잠깐 들고 있을 변수가 필요하고, 반복문에는 색인이 필요하다. 중요한 것은 그 수가 **입력이 백만 개가 되어도 늘지 않는다**는 점이다. 이것을 보조 공간 **O(1)** 이라고 적는다.

반대편은 보조 공간 **O(n)** 이다. 값이 열 개면 열 칸, 백만 개면 백만 칸을 더 얻어야 한다. 이 조각에서는 넷이라 넷이었다.

### 왜 이것이 고르는 이유가 되는가

메모리 이야기는 "성능의 곁가지" 로 흘려듣기 쉽다. 그러나 자리가 없으면 알고리즘은 **실행되지 않는다**. 느린 것은 기다리면 되지만 모자란 것은 기다려도 오지 않는다.

- **자리를 빌리는 쪽** — 병합 정렬이 대표다. 합치려면 결과를 적을 띠가 있어야 하므로 **O(n)** 을 쓴다. 대신 안정 정렬이고 최악에도 **O(n log n)** 을 지킨다.
- **제자리 쪽** — 퀵 정렬과 힙 정렬이 그렇다. 퀵 정렬은 재귀 깊이만큼 스택을 쓰므로 엄밀히는 **O(log n)** 이고, 힙 정렬은 상수다.

자리가 넉넉한 서버에서는 이 차이가 잘 드러나지 않는다. 드러나는 자리는 따로 있다.

- 램이 킬로바이트 단위인 마이크로컨트롤러. 정렬할 배열 하나를 더 못 만든다.
- 메모리에 다 올라가지 않는 큰 데이터. 복사본을 만드는 순간 디스크로 밀려나며 속도가 자릿수로 무너진다.
- 캐시에 얹혀 도는 짧은 배열. 복사본은 캐시 줄을 두 배로 쓴다.

### 견줄 때 같이 볼 것

제자리라고 언제나 낫지는 않다. 값을 서로 건너뛰게 하는 방식은 원래의 순서를 흐트러뜨려 **안정성**을 잃기 쉽고, 옮겨 적는 방식은 자리를 빌리는 대신 병렬로 나눠 처리하기 쉽다. 그래서 실제 라이브러리는 두 성질을 놓고 고른다 — 자리를 아낄 것인가, 순서와 최악 보장을 지킬 것인가.
`;
