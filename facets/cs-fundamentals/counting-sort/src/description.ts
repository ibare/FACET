/**
 * counting-sort 완결형의 글.
 *
 * `{facet:countingSort}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const countingSortDescription = `
## 카운팅 정렬

지금까지의 정렬은 모두 값을 견주었다. 두 값을 대 보고 큰 쪽을 뒤로 보내는 것이
그 알고리즘들의 전부였다. 카운팅 정렬은 **한 번도 견주지 않는다.** 값마다 몇
개인지 세고, 그 셈을 앞에서부터 더해 각 값이 시작할 자리를 얻고, 그 자리에 곧장
놓는다. 화면의 견줌 수가 끝까지 0 으로 남는 것이 이 알고리즘의 정체다.

{facet:countingSort}

## 개수를 더하면 자리가 된다

값 \`v\` 가 앉을 첫 자리는 \`v\` 보다 작은 값들의 개수를 모두 더한 것이다.
0 이 둘이면 1 은 2 번 자리부터, 2 는 그 뒤부터 — 이 덧셈이 **누적합** 이다.

여기서 개수가 0 인 값이 재미있다. 화면의 자료에는 1 과 4 가 하나도 없다.
개수 줄에서 그 두 칸은 빈 채로 남고, 시작 자리 줄에서는 **앞 값과 같은 자리**를
받는다 — 1 도 2 번 자리, 2 도 2 번 자리다. 자리 하나를 아무도 쓰지 않고 넘긴
것이 아니라, 애초에 1 에게 줄 자리가 없었던 것이다. 더하기가 저절로 그 일을
한다.

## 놓을 때마다 자리를 한 칸 민다

시작 자리는 "첫 자리" 일 뿐이다. 같은 값이 여럿이면 두 번째는 그 다음 자리에
앉아야 한다. 그래서 하나를 놓을 때마다 그 값의 자리 번호를 한 칸 민다.

이 한 줄이 **안정성**을 만든다. 입력을 앞에서부터 훑으면 먼저 나온 3 이 먼저
자리를 받고, 뒤에 나온 3 은 밀린 자리를 받는다. 같은 값끼리의 원래 순서가 그대로
보존된다는 뜻이다. 값이 숫자 하나뿐일 때는 티가 나지 않지만, 값에 딸린 다른
정보가 있을 때 — 이름을 성적순으로 다시 세우는 것 같은 일 — 이 성질이 결과를
가른다.

## 견주지 않는 대신 무엇을 쓰는가

자리다. 값의 가짓수만큼 칸을 마련해야 하므로, 값이 0..5 처럼 좁은 범위일 때만
쓸 수 있다. 값이 0..1000000 이면 칸 백만 개를 먼저 만들어야 한다. 견줌을 없앤
값이 그것이다.

그래서 이 알고리즘은 **범위가 좁고 정수인 값**에 쓴다. 자리 수의 기수 정렬이
카운팅 정렬을 자릿수마다 되풀이하는 것도 같은 이유다 — 한 자리의 값은 언제나
0..9 라 칸이 열 개면 족하다.

## 코드에서 볼 것

\`count[arr[i]] = count[arr[i]] + 1\` 과 \`output[start[arr[i]]] = arr[i]\` 두 줄이
전부다. 인덱스 안에 인덱스가 들어앉은 이 모양이 "값을 주소로 쓴다" 는 말의 실물
이다. 값끼리 크기를 대 보는 \`<\` 나 \`>\` 가 어디에도 없다는 것을 코드 패널에서
직접 확인해 보라.
`;
