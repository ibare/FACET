export const mutuallyReachableDescription = `
# 서로 오갈 수 있는 무리

방향이 있는 그래프에서 **한쪽으로만 갈 수 있는 사이는 한 무리가 아니다.**
\`a\` 에서 \`d\` 로 갈 수 있다는 것만으로는 둘을 묶을 수 없다. 묶으려면
\`d\` 에서 \`a\` 로도 돌아올 수 있어야 한다.

{facet:mutuallyReachable}

## 화면이 하는 일

두 정점을 짚어 **양쪽 방향을 따로 묻는다.** 갈 수 있으면 토큰이 간선을 타고
움직이고 지나온 길이 켜진다. 갈 길이 없으면 토큰이 뜨지 않는다 — 대신 출발점이
닿을 수 있는 곳으로 얼룩이 번지고, 다 번진 자리에 점선 벽이 닫힌다. 그 벽이
"여기서 바깥으로 나가는 길이 없다" 는 뜻이다.

양쪽이 다 통하면 위쪽 검사기의 칸 둘이 띠로 묶이고, 한쪽이 막히면 칸 사이에
금이 가며 서로 밀려난다. 마지막에 정점들이 자리를 옮겨 무리끼리 갈라서고,
둘을 잇던 간선 하나만 빈 사이를 건넌다. 그 간선의 화살촉은 한쪽뿐이다.

## 전제

- 정점 다섯, 방향 간선 여섯짜리 작은 그래프다. 이 크기에서는 "서로 갈 수
  있는가" 를 짝마다 직접 물어도 걸음이 얼마 되지 않는다.
- 그래서 화면은 코사라주나 타잔을 따르지 않는다. 그 둘은 간선을 뒤집어 두 번
  훑거나 low-link 수를 굴리는데, 빠른 대신 **왜 그것이 한 무리인지를 화면에서
  감춘다.** 정점이 수만 개로 늘면 그쪽이 옳고, 여기서는 정의를 그대로 묻는 편이
  주장에 가깝다.
- 모든 짝을 다 묻지는 않는다. 서로 오갈 수 있다는 관계는 동치관계라, 무리마다
  대표 하나를 세워 그하고만 견주면 된다. 또 막힌 답사가 그려 보인 덩이는
  통째로 후보에서 빠진다 — 그 안의 어느 것도 바깥으로 나가지 못하기 때문이다.
`;
