/**
 * 비교와 교환 학습 설명 — 단일 마크다운.
 *
 * `{facet:compareAndSwap}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 * 조각이 화면에 각주를 두지 않는 대신, 전제는 여기서 밝힌다 (S-piece).
 */

export const compareAndSwapDescription = `## 견주는 일과 옮기는 일은 다르다

**정렬은 값을 옮기는 일처럼 보이지만, 옮김은 견줌이 참일 때만 일어난다.** 견줌 자체는 아무것도 바꾸지 않는다. 답을 하나 얻을 뿐이다.

{facet:compareAndSwap}

짝 셋을 차례로 다뤘는데 자리를 바꾼 것은 하나뿐이다. 첫 짝은 큰 값이 앞에 있어 어긋나 있었고, 둘째는 이미 순서가 맞았고, 셋째는 두 값이 같아 바꿀 이유가 없었다. 견줌은 세 번, 맞바꿈은 한 번이다.

### 자리와 값

화면에서 점선 칸은 **자리**고 그 위의 타일은 **값**이다. 견주면 두 값이 자리에서 살짝 들리는데, 그것이 판정하는 동작이다. 판정이 참일 때만 두 값이 동시에 엇갈려 지나가 서로의 자리로 건너간다. 한쪽이 다른 쪽을 밀어내는 것이 아니라 둘이 함께 움직인다.

### 왜 이것을 갈라 두는가

정렬 알고리즘의 비용을 셀 때 견줌 횟수와 맞바꿈 횟수를 따로 세는 이유가 여기 있다. 둘은 비례하지 않는다. 이미 거의 정렬된 입력에서는 견줌이 그대로여도 맞바꿈이 거의 없고, 값을 옮기는 비용이 비싼 자료(큰 구조체 배열 등)에서는 맞바꿈 횟수가 실제 속도를 가른다.

- **견줌** — 두 값의 순서를 묻는다. 상태를 바꾸지 않는다.
- **맞바꿈** — 답이 "어긋났다" 일 때만 일어나는 쓰기 연산이다.

### 전제

- 오름차순으로 맞춘다. 왼쪽이 오른쪽보다 **클 때만** 바꾼다.
- 값이 같으면 바꾸지 않는다. 이 선택이 정렬의 안정성(stable)을 지킨다 — 같은 값끼리의 원래 순서가 뒤집히지 않는다.
- 여기서는 서로 떨어진 짝 셋을 따로 다루지만, 실제 정렬에서는 한 배열 안의 이웃한 두 칸에 같은 일을 되풀이한다.
`;
