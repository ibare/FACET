/**
 * 프림 학습 설명. 호스트가 마크다운으로 렌더한다.
 *
 * `{facet:primMst}` 토큰은 같은 패키지 `facet.ts::id` 와 같아야 한다 (C4).
 */

export const primMstDescription = `# 프림 — 한 나무를 키워 얻는 최소 신장 트리

정점을 전부 이으면서 간선 무게의 합이 가장 작은 이음새를 **최소 신장 트리**라
한다. 프림은 그것을 나무 하나를 키워서 얻는다. 시작은 정점 하나. 그 다음부터는
**나무 밖으로 나가는 간선 가운데 가장 가벼운 것**을 골라 반대쪽 끝을 나무에
붙인다. 정점이 n 개면 n-1 번 붙이고 끝난다.

{facet:primMst}

## 고를 수 있는 것은 그때마다 다르다

여기서 자주 어긋나는 곳이 하나 있다. 프림이 고르는 것은 **그림 전체에서 가장
가벼운 간선이 아니다.** 지금 나무의 경계를 넘는 간선, 그러니까 한쪽 끝은 나무
안이고 다른 쪽 끝은 나무 밖인 간선만이 후보다.

그림에서 주황으로 진하게 그려진 것이 그 후보들이고, 흐린 점선은 지금은 고를 수
없는 것들이다. 두 끝이 모두 나무 밖이면 아직 차례가 아니고, 두 끝이 모두 나무
안이면 이제 고리를 만들 뿐이라 쓸모가 없다. 한 자리가 붙을 때마다 이 경계가
움직이므로 **주황도 옮겨 다닌다.** 그것을 보는 것이 이 그림의 요지다.

## key 배열 — 나무에서 여기까지 한 간선

후보를 매번 다시 훑는 대신, 정점마다 값을 하나 들고 있는다.

    key[v] = 지금 나무에서 정점 v 로 건너가는 가장 가벼운 간선 하나의 무게

처음엔 출발점만 0 이고 나머지는 전부 ∞ 다. 한 자리 u 가 나무에 붙을 때마다 u 에서
나가는 간선을 훑어, 더 가벼운 길이 생겼으면 key 를 줄인다. 그러면 다음 회에는
"나무 밖에서 key 가 가장 작은 정점" 하나만 찾으면 된다.

## 다익스트라와는 한 줄이 다르다

코드 패널을 보면 바깥 뼈대가 다익스트라와 똑같다. 아직 굳지 않은 것 중에 값이
가장 작은 것을 반복문으로 찾고, 그것을 굳히고, 그 이웃의 값을 고친다. 갈리는
곳은 값을 고치는 한 줄뿐이다.

    if w[u][v] > 0 and inTree[v] == 0 and w[u][v] < key[v]:   ← 프림
    if dist[u] + w[u][v] < dist[v]:                           ← 다익스트라

프림은 **간선 하나의 무게**를 재고, 다익스트라는 **출발점에서의 누적 거리**를
잰다. \`dist[u] +\` 가 있느냐 없느냐. 이 저장소에는 다익스트라도 완결형으로 있으니
두 코드 패널을 나란히 열어 보면 좋다.

값의 뜻이 달라지는 것만으로 답이 다른 나무가 된다. 위 그래프에 다익스트라를
0 에서 돌리면 최단 경로 나무는 \`0-1 · 0-3 · 1-2 · 1-4 · 3-5 · 5-6\` 이고 간선
무게의 합은 **44** 다. 프림이 낸 최소 신장 트리는 \`0-3 · 3-5 · 0-1 · 1-4 ·
4-2 · 4-6\` 이고 합은 **39** 다. 겹치는 간선도 있지만 끝의 두 간선이 다르다 —
다익스트라는 6 을 5 를 거쳐 매달아야 0 에서 가깝고(22), 프림은 4 에서 매다는
쪽이 가볍다(9). 최단 경로 나무는 무게 합이 가장 작은 나무가 아니고, 최소 신장
트리는 출발점에서의 거리를 지켜 주지 않는다.

## 우선순위 큐를 쓰지 않은 까닭

프림을 힙으로 쓰면 \`O(E log V)\` 가 된다. 여기서는 일부러 배열을 매 회 훑는
\`O(V²)\` 를 적었다. 힙을 넣는 순간 "가장 작은 것을 고른다" 가 자료구조 안으로
숨어, 다익스트라와의 대비가 코드에서 사라지기 때문이다. 정점이 빽빽한 그래프
(간선이 V² 에 가까운 그래프) 에서는 이 단순한 판이 오히려 빠르기도 하다.

## 같은 무게가 여러 번 나올 때

이 자료에는 무게 7 이 둘, 5 가 둘, 9 가 둘 있다. 그래서 최소 신장 트리가 여럿일
것 같지만, 이 그래프의 답은 하나뿐이다 — 같은 무게를 가진 간선들이 **한 회에서
맞붙은 적이 한 번도 없기 때문**이다. 예를 들어 무게 7 인 0-1 과 1-4 는 연달아
붙지만, 0-1 이 붙는 회에 정점 4 의 key 는 아직 8 이었다. 4 가 7 이 된 것은 1 이
붙고 난 뒤다.

그래도 규칙은 정해 두어야 한다. 무게가 같은 후보가 실제로 맞붙는 그래프는 얼마든지
있고, 그때 최소 신장 트리는 여럿이 되며 어느 것을 고르든 무게 합은 같다. 화면은
하나만 그릴 수 있으므로 — \`key[v] < key[u]\` 를 **엄격 부등호**로 두고 훑기를
0 번부터 올린다. 그러면 **무게가 같을 때 번호가 작은 정점이 이긴다.** 코드도 화면도
같은 규칙을 쓰므로 그림이 코드와 다른 말을 하지 않는다.

## 왜 이 방법이 맞는가

**자르기 성질(cut property)** 하나면 된다. 정점을 두 무리로 가르는 어떤 자름에
대해서도, 그 자름을 건너는 간선 중 가장 가벼운 것은 어떤 최소 신장 트리에 반드시
들어 있다. 프림이 매 회 하는 일이 바로 그것이다 — "나무 안" 과 "나무 밖" 이라는
자름을 놓고, 그 자름을 건너는 가장 가벼운 간선을 집는다. 그래서 붙일 때마다
아직 최소 신장 트리로 자랄 수 있는 상태가 유지된다.

## 크루스칼은 나무를 여럿으로 시작한다

같은 최소 신장 트리를 얻는 길이 하나 더 있다. 크루스칼은 나무를 하나 키우는
대신 **정점 일곱을 저마다 홀로 선 나무 일곱으로 두고**, 간선을 가벼운 것부터
훑으며 서로 다른 나무를 잇는 간선만 받아들인다.

{facet:kruskalMst}

프림에게는 "지금 나무 밖으로 나가는가" 가 판정이고, 크루스칼에게는 "두 끝이
서로 다른 나무에 있는가" 가 판정이다. 둘 다 뿌리는 같다 — 자르기 성질이다.
프림은 자름을 "나무 안 / 나무 밖" 으로 고정해 두고 매 회 그 자름의 최소를
집고, 크루스칼은 간선을 먼저 정렬한 뒤 그 간선이 어떤 자름의 최소인지를
나중에 확인한다.

그래서 프림은 **경계를 훑고**(위 그림에서 주황이 옮겨 다니는 것) 크루스칼은
**무게를 훑는다.** 이 그래프에서는 두 방법이 같은 여섯 간선에 닿는다 —
답이 하나뿐인 그래프라서다.

## 붙는 차례

이 그래프에서 나무는 이렇게 자란다.

| 회 | 붙는 간선 | 무게 | 누적 |
| --- | --- | --- | --- |
| 1 | (출발점 0) | 0 | 0 |
| 2 | 0-3 | 5 | 5 |
| 3 | 3-5 | 6 | 11 |
| 4 | 0-1 | 7 | 18 |
| 5 | 1-4 | 7 | 25 |
| 6 | 4-2 | 5 | 30 |
| 7 | 4-6 | 9 | 39 |

무게 5 인 간선 4-2 가 여섯 번째에야 붙는다는 것을 보라. 그것은 그림 전체에서 가장
가벼운 축인데도, **나무가 4 에 닿기 전까지는 후보가 아니었다.** 프림이 고르는 것이
전체의 최소가 아니라 경계의 최소라는 말이 이 한 줄에 있다.
`;
