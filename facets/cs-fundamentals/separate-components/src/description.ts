/**
 * 연결 요소 학습 설명 — 단일 마크다운.
 *
 * `{facet:separateComponents}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const separateComponentsDescription = `## 한 번의 탐색이 닿는 곳까지

BFS 든 DFS 든, 한 번의 탐색은 **출발점에서 간선을 따라 갈 수 있는 곳** 만 본다. 간선이 없는 곳으로는 건너가지 못한다. 그래서 탐색이 정상적으로 끝났는데도 아직 보지 못한 정점이 남아 있는 일이 생긴다. 버그가 아니라, 그래프가 여러 덩어리로 나뉘어 있다는 뜻이다.

{facet:separateComponents}

### 남은 것에서 다시 출발한다

그래프 전체를 다 보려면 방법은 하나다. 탐색이 끝난 뒤에도 아직 켜지지 않은 정점이 있으면 **그중 하나에서 새로 출발한다.** 그 일을 남은 것이 없어질 때까지 되풀이한다.

\`\`\`
for v in vertices:
    if not visited[v]:
        bfs(v)          # 새 출발 = 새 덩어리
        components += 1
\`\`\`

정점을 앞에서부터 훑는 바깥 루프는 정점마다 한 번씩만 돌고, 안쪽 탐색은 각 정점과 간선을 통틀어 한 번씩만 밟는다. 그래서 전체 비용은 여전히 O(V + E) 다 — 여러 번 출발한다고 해서 여러 배로 비싸지지 않는다.

### 출발 횟수가 곧 덩어리 수

바깥 루프가 탐색을 시작한 **횟수**가 연결 요소의 개수다. 각 탐색이 켠 정점의 수가 그 덩어리의 크기다. 무언가를 따로 세지 않아도, 언제 새로 출발했는지만 세면 나온다.

### 어디서 쓰이나

- **섬 세기 / 영역 채우기** — 격자를 그래프로 보면 칠하지 않은 칸에서 다시 출발한 횟수가 영역의 수다.
- **친구 관계망** — 서로 닿지 않는 무리가 몇 개인지.
- **회로·배관 점검** — 한 지점에서 신호를 흘려 닿지 않는 부분을 찾는다.

### 그림에 대해

정점 여덟과 간선 여섯은 이야기를 짧게 하려고 고른 것이다. 그림은 탐색 순서를 너비 우선으로 잡았지만, 깊이 우선으로 바꿔도 **어디까지 닿고 어디서 멈추는지**는 똑같다 — 덩어리의 경계는 탐색 방식이 아니라 간선이 정한다.
`;
