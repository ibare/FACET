/**
 * sort-stability 학습 설명 — 단일 마크다운.
 *
 * 본문 안의 `{facet:sortStability}` 토큰은 호스트(Tiptap NodeView) 가
 * 시각화 노드로 치환해 인라인 마운트한다.
 *
 * 화면이 밝히지 않는 전제를 여기서 밝힌다 (S-piece) — 조각은 선택 정렬의
 * 흔한 구현 하나를 예로 들 뿐이고, 선택 정렬을 안정하게 고쳐 짤 수도 있다.
 */

export const sortStabilityDescription = `## 정렬 안정성

정렬 결과는 하나라고 여기기 쉽다. 값이 모두 다르면 그 말이 맞다. 그런데 값이
같은 항목이 둘 있으면 옳은 결과가 여럿 생긴다 — 그 둘을 어느 순서로 두든 값의
차례는 어긋나지 않기 때문이다.

**안정 정렬** 은 그중 하나만 고른다. 값이 같으면 입력에서의 앞뒤를 그대로 지킨다.

{facet:sortStability}

값만 그려서는 이 차이를 볼 수 없다. 두 결과 모두 1 · 1 · 3 · 3 이라 글자 하나
다르지 않다. 차이는 "이 3 이 어디서 온 3 인가" 에만 있고, 그래서 그림은 값이
아닌 것 — 이름표 — 을 들고 있어야 한다.

### 왜 중요한가

두 번 정렬할 때 드러난다. 이름으로 한 번 정렬한 뒤 부서로 다시 정렬하면, 안정
정렬은 같은 부서 안에서 이름 차례를 그대로 남긴다. 안정하지 않으면 앞 정렬이
지워진다.

### 어느 쪽이 안정한가

- **안정** — 병합 정렬, 삽입 정렬, 계수 정렬, 기수 정렬
- **불안정** — 선택 정렬, 힙 정렬, 흔한 방식의 퀵 정렬

여기 그린 선택 정렬은 "남은 구간의 최솟값을 맨 앞과 맞바꾼다" 는 가장 흔한
구현이다. 멀리 있는 항목을 끌어와 앞자리에 꽂는 맞바꿈이 그 사이에 있던 같은
값들의 앞뒤를 뒤집는다. 맞바꾸는 대신 밀어 넣도록 고쳐 짜면 선택 정렬도 안정해
지지만, 그때는 옮기는 비용이 늘어난다.

불안정한 정렬을 안정하게 쓰는 방법은 하나 더 있다. 값 옆에 입력에서의 자리를
붙여 두고, 값이 같으면 그 자리로 견주는 것이다. 조각이 이름표를 그리는 것과
같은 이야기다.
`;
