/**
 * avl-tree 완제품의 글.
 *
 * `{facet:avlTree}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const avlTreeDescription = `
## AVL 트리

이진 탐색 트리인데, 넣고 뺄 때마다 **자리마다 양쪽 높이 차를 재서** 그 차가
−1·0·+1 을 벗어나면 그 자리에서 고친다. 그래서 한쪽으로만 자라 줄이 되는 일이
없다.

{facet:avlTree}

재는 것은 내려갈 때가 아니라 **되돌아 올라올 때**다. 잎에 새 자리를 앉힌 뒤
왔던 길을 거슬러 오르며 한 자리씩 높이를 다시 셈하고 차를 본다.

## 네 가지 경우

기운 방향과 **그 자식이 기운 방향**이 무엇을 할지 정한다.

- 둘이 같으면 (LL · RR) 한 번 돌면 끝난다.
- 어긋나면 (LR · RL) 한 번으로 안 된다. **안쪽을 먼저 돌려 방향을 맞춘 뒤**
  바깥을 돈다. 이중 회전이라 부르는 것이 이것이다.

화면에서 회전 칸을 보면 방금 어느 경우였는지가 남는다. \`10 · 20 · 30\` 순으로
넣으면 RR 이 나오고, \`30 · 10 · 20\` 순으로 넣으면 RL 이 나온다.

## 넣기와 지우기는 다르다

넣을 때는 깨진 자리 중 **가장 낮은 것 하나만** 고치면 위쪽이 저절로 풀린다.
회전이 한 번이면 끝난다.

지울 때는 그렇지 않다. 한 자리를 고쳐도 그 위가 다시 깨질 수 있어 **뿌리까지
올라가며** 고친다. 회전 수를 보면 그 비대칭이 드러난다.

## 얼마나 낮은가

높이 칸은 지금 높이를 **이론 최소**와 나란히 보인다. AVL 이 지키는 것은 "가장
낮다" 가 아니라 "그것에서 멀지 않다" 는 것이다 — 최악이라도 1.44배 안쪽이다.
`;
