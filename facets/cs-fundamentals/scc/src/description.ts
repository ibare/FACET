/**
 * scc 완결형의 글.
 *
 * `{facet:scc}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const sccDescription = `
## 강한 연결 요소

방향이 있는 그래프에서 **서로 오갈 수 있는** 자리끼리 묶은 것이 강한 연결 요소다.
\`a\` 에서 \`b\` 로 갈 수 있고 \`b\` 에서 \`a\` 로도 돌아올 수 있으면 둘은 한 무리다.

정직하게 확인하려면 자리 짝마다 양쪽으로 길이 있는지 물어야 한다. 자리가 여덟이면
쉰여섯 번, 천이면 백만 번에 가깝다. 타잔 알고리즘은 그것을 **깊이 우선 순회 한
번**으로 끝낸다.

{facet:scc}

## 되짚어 닿는 간선

순회하며 자리마다 번호를 매긴다. 먼저 만난 자리가 작은 번호를 갖는다. 그리고
자리마다 값을 하나 더 들고 다니는데, **여기서 뻗어 나가 되짚어 닿을 수 있는 가장
이른 번호** 다. 코드에서는 \`num\` 과 \`low\` 다.

뻗어 나간 끝에서 이미 지나온 자리로 이어지는 간선이 나오면 — 화면에서 붉게 그어지는
그 간선이다 — 거기서부터 여기까지가 고리라는 뜻이다. 고리 위의 자리는 모두 서로
오갈 수 있으므로 한 무리다. 그 사실이 \`low\` 를 타고 위로 전해진다.

## \`low[v]\` 가 아니라 \`num[v]\`

낮은값이 갱신되는 자리는 둘인데, 둘이 서로 다른 값을 쓴다. 이 알고리즘에서 가장
자주 틀리는 자리다.

- **아직 안 가 본 곳으로 내려갔다 왔을 때** — 자식이 닿은 곳을 그대로 물려받는다.
  \`low[u] = low[v]\`.
- **이미 스택에 있는 곳으로 되짚어 닿았을 때** — 그 자리의 **번호** 를 쓴다.
  \`low[u] = num[v]\`.

둘째에서 \`low[v]\` 를 쓰면 안 되는 까닭은, \`v\` 가 어디까지 닿는지는 아직 정해지지
않았기 때문이다. 그 순간 확실한 것은 \`v\` 가 아직 스택에 남아 있다는 사실 하나뿐이고,
그래서 확실한 값인 \`num[v]\` 만 쓴다. 코드 패널에서 두 줄을 나란히 보라 —
감싸지 않고 그대로 펼쳐 두었다.

## 스택에 있느냐가 전부다

이미 번호가 매겨진 자리로 가는 간선이라고 다 되짚어 닿는 간선은 아니다. 그 자리의
무리가 이미 확정되어 스택에서 나갔다면, 그리로는 갈 수 있어도 돌아올 수 없다.
화면에서 이런 간선은 흐린 점선으로 남고 아무 값도 옮기지 않는다.

그래서 \`onstack\` 이 따로 필요하다. "가 봤는가" 와 "아직 무리가 안 정해졌는가" 는
다른 물음이다.

## 답이 맞아 버려서 살아남는 실수

앞의 두 대목은 이 알고리즘에서 가장 자주 틀리는 자리다. 그런데 **이 그래프에서는
둘 다 틀려도 답이 그대로 셋으로 나온다.** 되짚는 갈래에서 \`num[v]\` 대신
\`low[v]\` 를 써도, \`onstack\` 검사를 통째로 지워도, 무리는 \`{0,1,4}\` ·
\`{2,3,7}\` · \`{5,6}\` 그대로다.

그것이 이 두 실수가 오래 살아남는 까닭이다. 틀린 답을 내서 걸리는 것이 아니라
**맞는 답을 내서 안 걸린다.** 값을 보는 검사는 통과하고, 통과했으니 아무도 다시
보지 않고, 입력이 바뀐 어느 날 조용히 다른 답을 낸다.

**답이 맞는 것과 코드가 맞는 것은 다르다.** 그래서 이 그림은 값이 아니라 코드를
함께 보인다 — 아래 패널에서 \`low[u] = low[v]\` 와 \`low[u] = num[v]\` 가
서로 다른 갈래에 나란히 서 있는 것을 눈으로 확인하는 편이, 무리가 셋이라는 결과를
확인하는 것보다 많은 것을 보증한다.

## 무리가 통째로 정해지는 순간

어떤 자리에서 \`low\` 가 자기 \`num\` 과 같으면, 거기서 뻗어 나간 어디에서도 자기보다
위로 되짚어 닿지 못했다는 뜻이다. 그 자리가 무리의 뿌리이고, **스택에서 그 자리부터
위까지가 통째로 한 무리** 다. 화면에서 대괄호가 쳐지는 순간이 그것이다 — 몇이
들어갈지는 이미 정해졌고, 그 다음은 꺼내는 일만 남는다.

여덟 자리를 한 번 훑는 동안 무리 셋이 이렇게 확정된다. 순회가 끝나면 모든 자리가
정확히 한 무리에 들어 있다.
`;
