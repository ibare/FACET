/**
 * 분할 정복 학습 설명 — 단일 마크다운.
 *
 * `{facet:divideConquerCombine}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 *
 * 화면이 밝히지 않는 전제는 여기서 밝힌다 (S-piece — 각주는 화면에 두지 않는다).
 */

export const divideConquerCombineDescription = `## 내려갔다 올라온다

분할 정복은 흔히 두 토막으로 배운다. **쪼갠다** 를 배우고, **합친다** 를 배운다. 그런데 그 둘이 어떻게 한 절차인지는 잘 이어지지 않는다.

이어지는 자리는 **방향**이다. 쪼개는 것은 내려가는 길이고 합치는 것은 올라오는 길이며, 둘은 같은 나무의 같은 자리를 두 번 지난다. 내려갈 때 그 자리가 들고 있는 것은 **문제**이고, 올라올 때 들고 있는 것은 **답**이다.

{facet:divideConquerCombine}

### 답은 올라오는 동안에만 생긴다

내려가는 동안에는 어떤 자리도 답을 갖지 못한다. 더 작은 문제로 갈라 놓았을 뿐이다. 바닥에 닿아 값이 하나만 남았을 때 비로소 답이 하나 생긴다 — 값 하나짜리 줄은 이미 줄이 서 있기 때문이다. 이것이 재귀의 바닥이고, 여기서 진행 방향이 뒤집힌다.

거꾸로 오르며 만나는 것은 언제나 **이미 답이 된 둘**이다. 그래서 합치는 일은 처음부터 다시 줄을 세우는 것이 아니라 줄 선 둘을 하나로 잇는 일이 된다.

### 순서가 뒤집힌다

이 절차에서 가장 놓치기 쉬운 것이 순서다.

- 맨 처음 자른 자리 — 나무의 꼭대기 — 는 **맨 마지막에** 합쳐진다.
- 늦게 잘린 아래층일수록 **먼저** 합쳐진다.

화면 양옆에 남는 \`↓n\` 과 \`↑n\` 이 그것이다. 꼭대기에 \`↓1\` 과 \`↑3\` 이 함께 남는 것이 이 조각이 하려는 말 전부다.

### 화면이 줄인 것

**층 단위로 묶어 보인다.** 실제 재귀는 깊이 우선이라, 왼쪽 가지가 끝까지 내려갔다 올라온 **뒤에야** 오른쪽 가지가 시작된다. 화면은 같은 층을 한꺼번에 내려보내고 한꺼번에 올려 보낸다 — 그렇게 하지 않으면 오른쪽이 아직 쪼개지는 중에 왼쪽에는 이미 답이 있어, "내려가는 동안에는 답이 없다" 는 이 그림의 주장이 무너진다. 어느 자리가 먼저이고 나중인지는 그대로다.

**합치는 순간의 견줌은 보이지 않는다.** 줄 선 둘을 하나로 잇는 일은 앞끼리 견주어 작은 쪽을 먼저 꺼내는 절차인데, 이 조각은 그 안쪽을 열지 않는다. 여기서 보이는 것은 값이 어디서 어디로 올라가는가뿐이다.

**값이 넷이라 나무가 반듯하다.** 개수가 2의 거듭제곱이 아니면 한쪽 가지가 한 층 짧아진다. 그래도 내려갔다 올라오는 것과 순서가 뒤집히는 것은 달라지지 않는다.
`;
