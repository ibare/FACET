/**
 * selection-sort 완결형의 글.
 *
 * `{facet:selectionSort}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const selectionSortDescription = `
## 선택 정렬

한 바퀴를 다 훑어 남은 것 중 가장 작은 것을 찾고, 그것을 앞으로 데려온다. 훑는
동안에는 아무것도 움직이지 않는다. 움직이는 것은 "여기까지가 가장 작다" 는
**표식** 뿐이고, 값이 실제로 옮겨지는 것은 다 훑고 난 뒤 딱 한 번이다.

{facet:selectionSort}

## 견줌은 많고 이동은 적다

화면 아래 장부를 보라. 견줌은 바퀴마다 한 칸씩 짧아지는 계단으로 쌓이고,
이동은 오른쪽에 한 열로 선다. 두 그림의 길이가 이 알고리즘의 성격 그대로다 —
견줌 스물하나에 이동 다섯.

이동이 적은 것은 우연이 아니라 구조다. 한 바퀴는 자리 하나를 확정하고, 자리를
확정하는 데 드는 이동은 많아야 한 번이다. 그래서 이동 횟수가 **자리 수를 넘지
않는다.** 값을 옮기는 비용이 비싼 상황 — 덩치 큰 레코드를 다루거나 쓰기가 느린
매체에 올려 둔 경우 — 에서 선택 정렬이 거론되는 이유가 이것이다.

\`i=3\` 줄은 이동 자리가 비어 있다. 그 바퀴의 최솟값이 이미 제자리였기 때문이다.
코드가 맞바꿈을 \`if min_idx != i\` 로 감싼 것이 이 대목을 만든다. 감싸지 않아도
결과는 같지만, 자기 자신과 맞바꾸는 시늉이 이동으로 세어져 "이동은 적다" 가
흐려진다.

## 견줌 수는 입력이 정하지 않는다

계단은 6·5·4·3·2·1 이다. 첫 바퀴는 남은 여섯을 전부 보고, 마지막 바퀴는 하나만
본다. 합은 언제나 \`n(n-1)/2\` 다.

여기서 "언제나" 가 중요하다. 이미 정렬된 배열을 줘도, 거꾸로 뒤집힌 배열을 줘도
견줌은 스물한 번이다. 최솟값을 알아내려면 **남은 것을 하나도 빠뜨리지 않고**
봐야 하고, 그 사실은 배열이 어떻게 생겼든 달라지지 않기 때문이다. 운 좋은 입력에
빨라지는 정렬 — 삽입 정렬이나 거품 정렬 — 과 갈리는 지점이 여기다.

## 마지막 하나는 저절로 제자리다

바깥 반복이 \`n - 1\` 에서 멈춘다. 일곱 칸인데 여섯 바퀴만 도는 것이다.

앞의 여섯 자리에 각각 "남은 것 중 가장 작은 값" 이 앉았다면, 마지막 칸에 남은
것은 정의상 가장 큰 값 하나뿐이다. 그것을 위해 한 바퀴를 더 도는 것은 훑을
것이 없는 빈 바퀴다. 코드에서는 \`- 1\` 한 글자지만, 그 한 글자가 알고리즘이
무엇을 이미 알고 있는지를 말한다.
`;
