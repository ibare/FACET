export const subsetsumDescription = `## 부분집합 합 — 백트래킹

\`{3, 7, 1, 8, 4, 2}\`에서 합이 13이 되는 부분집합이 있을까?

각 원소에 대해 **포함/제외** 두 가지 선택. 모든 \`2^n\` 조합을 탐색하지만,
**가지치기(pruning)**로 합이 이미 \`target\`을 넘으면 더 깊이 들어가지 않는다.

{facet:subsetSum}

### 시간/공간 복잡도

- 최악 시간: **O(2^n)**
- 가지치기로 평균은 훨씬 빠름
- 공간: **O(n)** — 재귀 깊이

### 백트래킹 = DFS + 되돌리기

1. 선택한다.
2. 재귀 호출.
3. 안 되면 **되돌리고**(취소) 다른 선택.

\`include\` 후 실패 → \`exclude\`로 시도.

### 응용

- N-Queens, 미로, 스도쿠, 순열/조합 생성, **그래프 색칠**.
- DP가 적용 가능한 경우(예: subset-sum의 \`O(n × target)\` DP)는 더 빠르지만, 백트래킹은 **해를 직접 구성**하기 좋다.
`;
