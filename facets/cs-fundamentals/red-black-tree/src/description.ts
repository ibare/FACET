/**
 * red-black-tree 완제품의 글.
 *
 * `{facet:redBlackTree}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const redBlackTreeDescription = `
## 레드-블랙 트리

자리마다 빨강이나 검정을 적어 두고, 두 가지만 지킨다.

- **빨강 아래에 빨강이 오지 않는다.**
- **어느 길로 가도 지나는 검은 자리 수가 같다.**

이 둘만으로 나무가 한쪽으로 늘어지지 않는다. 가장 긴 길이 가장 짧은 길의 두
배를 넘을 수 없기 때문이다 — 검은 수가 같으니 길이 길어지려면 빨강을 끼워야
하는데, 빨강은 이어 붙일 수 없다.

{facet:redBlackTree}

새 자리는 늘 **빨강**으로 들어온다. 검정으로 넣으면 그 길만 검은 수가 하나 늘어
곧바로 둘째 규칙이 깨진다. 빨강으로 넣으면 첫째 규칙만 깨질 수 있고, 그것이
고치기 쉬운 쪽이다.

## 색칠은 밀고 회전은 끝낸다

넣은 뒤의 수선은 세 경우다.

- **옆자리도 빨강** — 색만 갈아 끼운다. 그런데 이것은 문제를 없앤 것이 아니라
  조부모로 **밀어 올린** 것이다. 거기서 다시 위반이 생길 수 있어 뿌리까지
  이어질 수 있다. 화면에서 "위로 밀렸다" 는 표시를 따라가 보면 보인다.
- **꺾였다** — 먼저 한 번 돌려 곧게 편다.
- **곧다** — 돌리고 색을 맞추면 **끝난다.**

지울 때는 네 경우인데 짜임이 같다. 색칠하는 경우는 문제를 위로 밀고, 회전하는
경우는 거기서 끝난다.

## AVL 과 왜 둘 다 있는가

두 화면의 메트릭을 견주면 답이 나온다. AVL 은 높이를 더 조이는 대신 회전을 더
하고, 레드-블랙은 회전을 아껴 넣고 빼기가 잦은 쪽에 맞는다. 같은 키를 같은
순서로 넣어 보고 회전 수와 높이를 견주어 보면 그 값의 차이가 눈에 든다.
`;
