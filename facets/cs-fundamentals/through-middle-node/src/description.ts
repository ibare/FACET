export const throughMiddleNodeDescription = `
## 가운데를 거치면 짧아지는가

두 점 사이의 길을 고칠 때 묻는 것은 하나다.

> 이 정점을 거치면 짧아지는가?

가운데에 정점 하나를 세워 두고, \`from → 가운데 → to\` 가 지금 아는 \`from → to\`
보다 짧은지 본다. 짧으면 곧던 길이 그 꺾인 길에게 자리를 내준다. 안 짧으면 그대로
둔다. 그것이 전부다.

{facet:throughMiddleNode}

## 왜 반복문이 세 겹인가

물음 하나는 이렇게 단순한데, **그 물음을 어디에 던져야 하는지는 미리 알 수 없다.**
그래서 가운데 후보를 하나씩 세워 보고(첫째 겹), 그 아래서 출발점을 훑고(둘째 겹),
그 아래서 도착점을 훑는다(셋째 겹). 화면 오른쪽 장부가 그 수를 남긴다 — 가운데
후보 넷 × 짝 여섯 = 스물넷을 묻고, 그중 손이 가는 것은 네 번뿐이다.

고쳐지는 순간만 보이면 그것은 운 좋은 지름길 몇 개로 읽힌다. 헛물켜는 스무 번이
함께 보여야 "왜 n³ 인가" 가 그림에서 나온다.

## 이 그림의 전제

- 길은 **한쪽으로만** 난다. \`A→B\` 가 있다고 \`B→A\` 가 있는 것은 아니다.
  둘 다 있어도 값은 따로다 — \`C→B\` 는 3 이고 \`B→C\` 는 7 이다.
- 가운데는 A · B · C · D 를 이 차례로 세운다. 순서를 바꿔도 마지막 표는 같지만,
  어느 물음이 언제 "그렇다" 가 되는지는 달라진다.
- 자기 자신이 끼는 물음(\`from\` 과 \`to\` 가 같거나 어느 한쪽이 가운데인 경우)은
  건너뛴다. 아무것도 짧게 만들지 못하는 자리다. 그래서 걸음이 4 × 4 × 4 = 64 가
  아니라 4 × 6 = 24 다.
- 고쳐지는 **자리**는 세 군데(\`A→B\` · \`C→D\` · \`A→D\`)지만 **손질**은 네 번이다.
  \`A→D\` 는 두 번 고쳐진다 — B 를 가운데 세울 때 없던 길이 10 으로 생기고, C 를
  세울 때 6 으로 다시 줄어든다. 세 변을 잇는 길(\`A→C→B→D\`)은 가운데를 두 번
  거쳐야 하므로 한 번에 나올 수 없다.
- 답은 **세 갈래**로 갈린다. 스물넷 가운데 열여덟은 **길이 끊겨** 재 볼 것도
  없이 지나가고(A 로 들어오는 길이 없어 A 를 세운 여섯 번은 나서자마자 막히고,
  D 에서 나가는 길이 없어 D 를 세운 여섯 번은 D 에 닿고서야 막힌다), 둘은
  **길이 다 있어 재 보았는데 더 멀어** 그냥 두며(\`A→C\` 를 B 로 돌면 9+7=16 인데
  이미 2 이고, \`B→D\` 를 C 로 돌면 7+4=11 인데 이미 1 이다), 넷만 **짧아진다**.
  가운데 갈래가 있어야 이 조각이 묻는 것이 "길이 있는가" 가 아니라 "짧아지는가"
  가 된다. 화면에서도 셋이 다르게 보인다 — 막히면 빗장에 부딪혀 튕기고, 재 보고
  물러날 때는 끝까지 갔다가 되돌아 나오며, 짧아질 때만 길이 휜다.
`;
