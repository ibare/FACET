/**
 * 노드 분할 학습 설명 — 단일 마크다운.
 *
 * `{facet:splitWhenFull}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const splitWhenFullDescription = `## 꽉 찬 자리에 하나가 더 들어오면

**노드가 담을 수 있는 키의 수에는 한도가 있다.** 그 한도를 채운 자리에 하나가 더 들어오면 그 자리에서 해결하지 않는다 — 가운데 키를 뽑아 위로 올리고, 남은 것을 둘로 갈라 옆으로 넓힌다.

{facet:splitWhenFull}

### 왜 가운데인가

가운데 키는 왼쪽보다 크고 오른쪽보다 작다. 그래서 부모로 올라가면 그 자체로 "이보다 작으면 왼쪽, 크면 오른쪽" 이라는 갈림 기준이 된다. 아무 값이나 뽑으면 그 기준이 무너진다.

### 층수가 늘지 않는다

자식이 넘칠 때 새로 생기는 자식은 형제로 들어간다 — 부모 밑에 층 하나를 더 쌓는 것이 아니다. 그래서 나무는 넣을 때마다 깊어지지 않고 옆으로만 넓어진다. 깊이가 늘어나는 경우는 **부모까지** 넘칠 때뿐이고, 그때는 분할이 한 층 위로 이어진다.

### 어디에 쓰이나

- **데이터베이스 인덱스 (B-트리/B+트리)** — 디스크에서 한 번 읽는 블록에 최대한 많은 키를 담고, 그 블록이 차면 이 방식으로 나눈다.
- **파일시스템** — 디렉터리 엔트리가 많아지면 같은 방식으로 나눠 탐색 깊이를 낮게 유지한다.
`;
