/**
 * one-more-round-drops 는 코드 패널을 두지 않는다.
 *
 * 조각은 한 주장만 그림으로 말하고 멈추는 물건이라 IR 이 없다 (S-piece).
 * 파일 구성은 S-facet 그대로 지키므로 빈 배열을 둔다.
 */

import type { IR } from '@ffacet/core/runtime';

export const oneMoreRoundDropsIRs: IR[] = [];
