/**
 * 병합 조각 학습 설명 — 단일 마크다운 (i18n 미적용).
 *
 * 본문의 `{facet:mergeTwoSorted}` 토큰은 호스트(Tiptap NodeView) 가 시각화
 * 노드로 치환해 인라인 마운트한다.
 *
 * 화면이 밝히지 않는 전제 — 축척 · 데이터 크기 · 같은 값일 때의 규칙 — 는
 * 여기서 말한다. 조각은 여러 글에 불려 다니므로 각주를 화면에 박지 않는다
 * (S-piece).
 */

export const mergeTwoSortedDescription = `## 정렬된 두 줄 합치기

이미 줄 서 있는 둘을 하나로 합칠 때, 사람들은 흔히 **둘을 쏟아 섞은 뒤 다시 정렬하는** 장면을 떠올린다. 합치기는 그런 일이 아니다.

{facet:mergeTwoSorted}

### 맨 앞 둘이면 충분하다

양쪽이 각각 오름차순이라면, **아직 나오지 않은 값 중 가장 작은 것은 반드시 두 줄 중 하나의 맨 앞** 이다. 다른 자리에 있을 수 없다 — 어떤 줄의 두 번째 값은 그 줄의 첫 값보다 크거나 같으니까.

그래서 매 걸음의 판단 재료는 딱 두 칸이다. 뒤쪽은 읽지 않는다. 읽을 필요가 없어서 읽지 않는 것이지, 아껴 두는 것이 아니다.

- **견준다** — 두 줄의 맨 앞끼리. 한 번에 한 번.
- **내려간다** — 작은 쪽이 결과줄의 다음 자리로. 자리는 언제나 왼쪽부터 차례로 찬다.
- **앞이 바뀐다** — 내려간 자리는 비고, 그 줄의 다음 값이 새 맨 앞이 된다.
- **따라 내려간다** — 한쪽이 바닥나면 남은 쪽은 견줌 없이 그대로. 견줄 상대가 없으므로 판단할 것도 없다.

### 되돌아가지 않는다

내려앉은 값은 다시 움직이지 않는다. 자리를 바꾸는 일도, 다시 견주는 일도 없다. 각 값은 정확히 한 번 내려가고, 한 번 내려간 뒤로는 확정이다.

그래서 합치기의 비용은 두 줄의 길이 합에 비례한다 — 다시 정렬하는 비용이 아니라 **한 번 훑는** 비용이다. 병합 정렬이 이 걸음을 층마다 되풀이해도 감당되는 이유가 여기 있다.

### 화면이 보이는 것에 대해

- 왼쪽 줄은 \`[1, 4, 7]\`, 오른쪽 줄은 \`[2, 3, 9]\` 다. 짧게 잡은 것은 걸음마다 무엇이 판단 재료였는지 눈으로 세어 볼 수 있게 하기 위해서다.
- 마지막 값 \`9\` 는 견줌 없이 내려온다. 그때 왼쪽 줄이 이미 비어 있기 때문이다. 그래서 여섯 개를 합치는 데 견줌은 다섯 번이다 — 값의 개수보다 언제나 적다.
- 두 맨 앞이 **같은 값** 이면 이 구현은 왼쪽을 먼저 내려보낸다. 같은 값끼리의 원래 순서가 보존된다는 뜻이고, 이것이 병합 정렬을 안정 정렬로 만든다. 화면의 데이터에는 같은 값이 없어 그 장면은 나오지 않는다.

### 어디서 쓰나

- **병합 정렬 (merge sort)** — 절반씩 나눠 정렬한 뒤 이 걸음으로 합친다. 나누는 일보다 합치는 일이 알고리즘의 본체다.
- **외부 정렬** — 메모리에 다 못 올리는 자료를 조각내 정렬한 뒤 순서대로 흘려 보내며 합친다. 맨 앞만 보면 되므로 나머지는 디스크에 두어도 된다.
- **정렬된 목록 여럿의 교집합 · 합집합** — 색인 검색의 게시 목록(posting list) 처리가 같은 걸음을 쓴다.
`;
