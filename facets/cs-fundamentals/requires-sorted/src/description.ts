/**
 * 정렬 전제 학습 설명 — 단일 마크다운.
 *
 * `{facet:requiresSorted}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 *
 * 조각의 전제는 화면이 아니라 이 글이 밝힌다 (S-piece). 여기 쓰인 값 일곱과
 * 짚는 순서는 화면이 실제로 셈해 보이는 것과 같다.
 */

export const requiresSortedDescription = `## 정렬은 권장 사항이 아니다

이진 탐색을 배우면 대개 "정렬된 배열에서 쓴다" 는 단서가 딸려 온다. 그 단서를 성능 조언으로 읽기 쉽다 — 정렬해 두면 빠르고, 안 해 두면 느리겠거니. **틀린 읽기다.** 정렬되지 않은 배열에 이진 탐색을 걸면 느려지지 않는다. 여전히 로그 시간에 끝나고, 다만 **틀린 답**을 낸다.

{facet:requiresSorted}

### 무엇이 어긋나는가

이진 탐색의 한 걸음은 이렇게 생겼다. 가운데를 짚고, 찾는 값이 그보다 작으면 오른쪽 절반을 통째로 버린다. 이 버림이 정당한 이유는 **오름차순이면 가운데보다 오른쪽의 모든 값이 가운데보다 크기 때문**이다. 그 전제가 없으면 "가운데보다 작다" 는 사실은 오른쪽에 대해 아무것도 말해 주지 않는다.

그런데 절차는 그 전제를 확인하지 않는다. 확인하려면 배열 전체를 훑어야 하고, 그러면 이진 탐색을 쓸 이유가 사라진다. 그래서 이진 탐색은 **전제를 믿고 버린다.** 위 그림에서 흐트러진 줄이 첫 걸음에 오른쪽 절반을 버릴 때, 그 절반 안에 찾던 3 이 들어 있다. 그 뒤로 무엇을 어떻게 짚어도 3 은 다시 후보가 되지 못한다.

### 조용히 틀린다는 것

더 나쁜 것은 절차 자신이 아무 이상도 느끼지 못한다는 점이다. 마지막에 구간이 비어 닫히면 "없다" 를 반환한다. 이것은 **성공적으로 끝난 실행**이다. 예외도 없고 경고도 없고 로그도 남지 않는다. 호출한 쪽은 반환값 하나만 받는다.

그래서 이 버그는 대개 아주 늦게 발견된다. 어떤 값은 제대로 찾히고 어떤 값은 안 찾힌다. 자료를 조금 바꾸면 증상이 옮겨 다닌다. 원인이 탐색 함수라는 것을 의심하기까지가 가장 길다 — 탐색 함수 자체는 교과서대로 정확히 구현되어 있기 때문이다.

### 전제는 호출부의 책임이다

정리하면 정렬은 이진 탐색의 **사전 조건(precondition)** 이다. 지키는 것은 부르는 쪽의 몫이고, 어기면 결과는 정의되지 않는다.

- 자료가 자주 바뀌고 탐색이 드물면, 그때그때 정렬하느니 처음부터 끝까지 훑는 편이 낫다.
- 자료가 고정이고 탐색이 잦으면 한 번 정렬해 두고 그 불변식을 깨지 않도록 삽입 경로를 막는다.
- 정렬 여부를 자신할 수 없는데 정확성이 중요하다면, 디버그 빌드에서만 인접 쌍을 검사해 전제를 지키는지 확인한다.

"정렬해 두는 게 좋다" 가 아니라 **"정렬되어 있지 않으면 이 함수를 부르면 안 된다"** 가 바른 문장이다.
`;
