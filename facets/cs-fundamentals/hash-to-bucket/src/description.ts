export const hashToBucketDescription = `
## 값을 자리 번호로 바꾼다

해시 테이블은 값을 **어디에 둘지 계산으로 정한다.** 찾을 때 처음부터 훑지 않는
이유가 이것이다 — 키를 보면 자리가 나온다.

{facet:hashToBucket}

그 계산은 두 번의 접힘이다.

1. **해시 함수가 키를 하나의 정수로 접는다.** 글자 수가 셋이든 여섯이든 결과는
   정수 하나다. 화면의 글자 칸들이 가운데로 모여 수 하나가 되는 대목이다.
2. **나머지 연산이 그 정수를 자리 수만큼으로 다시 접는다.** 자리 수를 넘으면
   처음으로 돌아오므로, 아무리 큰 수가 나와도 결과는 \`0\` 부터 \`자리 수 - 1\`
   사이에 갇힌다.

사이에 한 걸음이 더 있다. 해시 값은 **음수일 수 있다.** 자바의
\`String.hashCode\` 는 32비트 정수를 넘치도록 곱해 쌓기 때문에 "banana" 처럼
부호 비트가 켜진 값이 나온다. 음수를 그대로 나누면 자리 번호도 음수가 되므로,
\`& 0x7FFFFFFF\` 로 부호 비트를 떨어뜨린 뒤 나눈다. 화면에서 조각 하나가 칩에서
떨어져 나가는 것이 그 비트다.

### 이 그림의 전제

- 해시 함수는 자바의 \`String.hashCode\` (\`h = h * 31 + c\`) 다. 언어와 구현마다
  다른 값이 나오지만, 두 번 접힌다는 구조는 같다. 화면의 수는 모두 그 식으로
  실제 계산한 값이다.
- 자리를 여덟 개로 둔 것은 한눈에 보이는 크기여서다. 실제 해시 테이블은 훨씬
  많은 자리를 쓰고, 채워지는 정도에 따라 자리 수를 늘린다.
- 수가 자리 띠를 감아 도는 움직임은 나머지 연산이 하는 일을 보인 것이지 실제로
  감기는 횟수는 아니다. 실제 몫은 수천만에 이른다.

### 여기서 말하지 않는 것

서로 다른 키가 같은 자리로 접히는 일 — 충돌 — 은 이 그림의 이야기가 아니다.
자리 수가 정해져 있는 한 충돌은 반드시 생기며, 그것을 어떻게 감당하는지는
별도의 그림이 맡는다.
`;
