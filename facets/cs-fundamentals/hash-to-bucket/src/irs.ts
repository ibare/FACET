/**
 * hash-to-bucket 의 IR — 없다.
 *
 * 조각은 코드 패널을 두지 않는다. 화면에서 벌어지는 접힘 자체가 주장이고,
 * 그 옆에 소스를 세우면 조각이 아니라 작은 완결형이 된다 (S-piece).
 */

import type { IR } from '@ffacet/core/runtime';

export const hashToBucketIRs: IR[] = [];
