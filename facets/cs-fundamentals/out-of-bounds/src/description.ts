/**
 * 경계 밖 접근 학습 설명 — 단일 마크다운.
 *
 * `{facet:outOfBounds}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const outOfBoundsDescription = `## 번호가 끝을 넘으면

**\`arr[5]\` 는 오류가 아니다. 계산이다.** 배열의 번호는 주소를 구하는 재료일 뿐이고, 그 계산은 배열이 어디서 끝나는지 모른다. 기준 주소에 번호 곱하기 원소 크기를 더하면 자리가 나오고, 기계는 그 자리를 읽는다. 번호가 4든 5든 셈은 똑같이 성립한다.

{facet:outOfBounds}

### 거기에는 무엇이 있나

배열 바로 뒤에 무엇이 놓이는지는 아무도 약속하지 않았다. 다른 지역 변수일 수도, 함수의 복귀 주소일 수도, 아무 데도 매핑되지 않은 자리일 수도 있다. 그래서 경계 밖 접근의 결과는 **정해져 있지 않다**. 어제는 멀쩡히 돌던 코드가 컴파일러 설정 하나를 바꾸자 무너지는 일이 여기서 나온다.

읽기가 이 정도면 쓰기는 더하다. \`arr[5] = 0\` 은 남의 변수를 조용히 갈아 치운다. 망가진 쪽은 자기가 왜 망가졌는지 알 도리가 없으므로, 오류는 사고 지점이 아니라 한참 뒤에 엉뚱한 얼굴로 나타난다.

### 그래서 검사를 앞에 세운다

막는 방법은 하나뿐이다 — **자리를 구하기 전에 번호를 본다.** \`0 ≤ i < length\` 가 참일 때만 접근을 허락하면, 셈이 배열 밖으로 나가는 일 자체가 일어나지 않는다.

- **검사를 언어가 하는 쪽** — 자바 · 파이썬 · C# 등은 접근마다 길이를 본다. 어긋나면 그 자리에서 예외를 던지므로, 사고 지점과 증상이 같은 곳에 있다.
- **검사를 사람이 하는 쪽** — C · C++ 은 검사를 넣지 않는다. 대신 빠르고, 대신 위험하다. 경계는 프로그래머가 지켜야 하는 약속이 된다.

빠르기를 포기하지 않으면서 안전을 얻으려는 시도가 여기서 갈린다. 반복문 대신 범위 순회를 쓰거나, 길이를 함께 들고 다니는 자료형(슬라이스 · \`span\`)을 쓰는 것이 모두 같은 목적이다 — 번호가 끝을 넘을 기회를 애초에 주지 않는 것.
`;
