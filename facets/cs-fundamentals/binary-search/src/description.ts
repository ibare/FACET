/**
 * binary-search 완결형의 글.
 *
 * `{facet:binarySearch}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const binarySearchDescription = `
## 이진 탐색

값들이 이미 줄 서 있다면, 하나씩 훑을 이유가 없다. 가운데 하나를 보면 찾는 값이
그보다 앞인지 뒤인지 알 수 있고, 그 순간 **나머지 절반은 볼 필요가 없어진다.**
한 번 견줄 때마다 후보가 절반이 된다.

{facet:binarySearch}

## 후보가 12 → 6 → 3 → 1 로 준다

화면 아래의 계단이 그것이다. 71 을 찾는 동안 구간은 열둘에서 여섯, 셋, 하나로
줄고 네 번째 견줌에서 값을 만난다. 스물넷이었다면 다섯 번, 마흔여덟이었다면 여섯
번이다. 자료가 두 배가 되어도 견줌은 한 번만 늘어난다 — 하나씩 훑는 방법이
두 배로 늘어나는 것과 갈리는 자리다.

경계는 둘뿐이다. \`lo\` 가 "여기부터", \`hi\` 가 "여기까지" 를 가리키고, 가운데는
그 둘의 한가운데다. 가운데 값이 찾는 값보다 **작으면** 가운데를 포함해 왼쪽을
통째로 버리고 \`lo\` 를 \`mid + 1\` 로 옮긴다. **크면** 거울상으로 \`hi\` 를
\`mid - 1\` 로 옮긴다. 버리는 데 배열을 건드리지 않는다 — 경계 하나를 움직이는
것이 곧 절반을 버리는 것이다.

## 「없다」고 답하려면 구간이 비어야 한다

40 을 찾는 두 번째 시도가 그것을 보인다. 세 번 견주고 나면 \`lo\` 가 \`hi\` 를
지나쳐 구간에 아무것도 남지 않는다. 그때에야 없다고 말할 수 있다.

이것이 이진 탐색의 덜 눈에 띄는 절반이다. 찾는 것은 값을 만나면 끝나지만,
**없다는 답은 만날 것이 하나도 남지 않았다는 사실**로만 나온다. 그래서 반복문의
조건이 \`lo <= hi\` 이고, 그 조건이 깨진 뒤에 \`-1\` 이 있다. 코드 패널의 마지막
줄에 닿으려면 반드시 그 앞의 \`while\` 이 끝나야 한다.

\`lo <= hi\` 를 \`lo < hi\` 로 잘못 적으면 후보가 하나 남았을 때를 보지 않고
끝나 버린다. 71 은 마지막 한 칸에서 발견되므로, 그 부등호 하나가 있는 값을
없다고 말하게 만든다.

## 가운데를 셀 때

\`(lo + hi) // 2\` 는 **몫만 취하는 나눗셈**이다. 그냥 나누면 파이썬에서는 실수가
되어 자리 번호로 쓸 수 없다. 자바 · C++ · C# 은 정수끼리의 \`/\` 가 이미 몫만
남기므로 코드 패널에서 언어마다 표기가 갈리는 것을 볼 수 있다 — 같은 IR 이
각 언어의 관용으로 옮겨진 결과다.

## 전제를 잊지 말 것

이 모든 것은 값들이 줄 서 있어야 성립한다. 줄이 흐트러지면 "가운데보다 작으니
왼쪽은 볼 필요가 없다" 는 추론 자체가 무너지고, 알고리즘은 틀린 답을 자신 있게
내놓는다. 한 번 정렬해 두고 여러 번 찾는 것이 이진 탐색이 값을 내는 자리다.
`;
