/**
 * linear-search 완결형의 글.
 *
 * `{facet:linearSearch}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const linearSearchDescription = `
## 선형 탐색

앞에서부터 한 칸씩 보며 찾는 값인지 견준다. 맞으면 그 자리에서 멈추고, 끝까지
가도 없으면 없다고 답한다. 그것이 전부다.

시시해 보이지만 여기에는 조건이 하나 숨어 있다 — **줄이 서 있지 않다.** 값들이
크기순으로 놓여 있지 않으니 "여기까지 봤는데 없으니 뒤에도 없겠다" 고 말할 근거가
없다. 없다는 답을 하려면 끝까지 다 봐야 한다.

{facet:linearSearch}

## 찾을 때와 못 찾을 때의 값이 다르다

화면은 같은 줄에서 두 번 찾는다.

먼저 **55**. 자리 4 에 있으므로 다섯 칸을 보고 멈춘다. 뒤의 세 칸은 보지 않는다 —
이미 답을 알았기 때문이다.

그다음 **50**. 이 줄에 없다. 여덟 칸을 하나도 빠짐없이 보고 나서야 없다고 답한다.
줄이 서 있었다면 50 보다 큰 값을 만난 순간 그만둘 수 있었겠지만, 이 줄은 서 있지
않다. 42 다음에 17 이 오고 93 다음에 8 이 온다. 큰 값을 만났다는 것이 아무것도
말해 주지 않는다.

그래서 최악은 늘 칸 수만큼이고, **그 최악은 값이 없을 때 반드시 온다.**

## 코드 셋 줄이 여섯 언어로 갈리는 자리

이 알고리즘은 코드가 짧다. 반복 하나, 견줌 하나, 돌려주기 둘. 그런데 **짧아서
오히려 언어의 문법 자체가 주인공이 된다.**

코드 패널에서 언어를 바꿔 보면 한 줄이 눈에 띄게 갈린다 — 배열의 길이를 얻는
자리다. 같은 뜻인데 여섯이 다 다르게 적는다.

| 언어 | 길이를 얻는 표현 |
| --- | --- |
| Python | \`len(arr)\` |
| JavaScript | \`arr.length\` |
| TypeScript | \`arr.length\` |
| Java | \`arr.length\` |
| C++ | \`arr.size()\` |
| C# | \`arr.Length\` |

파이썬은 길이를 **바깥의 함수**로 묻고, 나머지 다섯은 **배열 자신에게** 묻는다.
그 다섯 중에서도 자바와 C# 은 필드 (괄호가 없다) 인데 대문자 여부가 갈리고,
C++ 은 메서드 (괄호가 있다) 다. 자바에서 문자열의 길이는 또 \`length()\` 로
괄호가 붙는다 — 배열과 문자열이 다른 물건이기 때문이다.

이 갈림은 알고리즘과 아무 상관이 없다. 다섯 줄짜리 IR 하나에서 여섯 소스가
나오는 것을 보면, 알고리즘이 무엇이고 언어가 무엇인지가 저절로 갈라져 보인다.
`;
