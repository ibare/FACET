/**
 * interpolation-search 완결형의 글.
 *
 * `{facet:interpolationSearch}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const interpolationSearchDescription = `
## 보간 탐색

사전에서 "ㅎ" 으로 시작하는 낱말을 찾을 때 우리는 책을 반으로 펴지 않는다. 뒤쪽을
편다. **찾는 값의 크기가 이미 자리를 말해 주기 때문이다.** 보간 탐색은 그것을 식
하나로 적은 것이다.

{facet:interpolationSearch}

## 이진 탐색과 딱 한 줄이 다르다

골격은 같다. \`while lo <= hi\` 로 살아 있는 구간을 잡고, 한 자리를 짚어 견주고,
아니면 한쪽을 버린다. 다른 것은 **자리를 정하는 줄** 하나다.

\`\`\`
mid = (lo + hi) // 2                                              반씩 접기
mid = lo + ((target - arr[lo]) * (hi - lo)) // (arr[hi] - arr[lo])  겨눔
\`\`\`

아래 줄이 하는 말은 이렇다. 찾는 값이 구간의 값 범위에서 **어느 지점에** 있는지를
비율로 재고 (\`(target - arr[lo])\` 를 \`(arr[hi] - arr[lo])\` 로 나눈 것), 그 비율만큼
자리 번호를 나아간다 (\`(hi - lo)\` 를 곱한 것). 화면의 자료에서 110 은 10 과 120
사이의 11 분의 10 지점이고, 자리 번호도 열한 칸 폭에서 열 칸을 나아간 곳이다.
겨눈 자리가 곧 답이라 **한 번에 닿는다.**

같은 자료를 반씩 접으면 자리 5 (60) → 자리 8 (90) → 자리 10 (110) 로 세 걸음이
걸린다. 화면의 두 트랙이 그 1 과 3 을 같은 가로줄에서 견준다.

## 곱셈이 나눗셈보다 먼저다

식에서 괄호의 자리가 결정적이다. \`//\` 는 몫만 남기는 **정수 나눗셈**이라
곱하고 나누느냐, 나누고 곱하느냐가 결과를 바꾼다.

\`\`\`
(100 * 11) // 110 = 1100 // 110 = 10      맞다
(100 // 110) * 11 =    0  * 11  =  0      틀리다
\`\`\`

뒤처럼 쓰면 겨눔이 언제나 구간의 왼쪽 끝에 떨어져 알고리즘이 한 칸씩만 나아간다.
코드 패널에서 여섯 언어를 넘겨 보면 파이썬의 \`//\` 가 자바·C++·C# 에서는 그냥
\`/\` 이고 자바스크립트에서는 \`Math.floor\` 인 것을 볼 수 있다. 표기는 갈려도
괄호가 정한 순서는 같다.

## 구간 밖 값을 막는 두 부등식

\`while\` 조건에 \`target >= arr[lo] && target <= arr[hi]\` 가 붙어 있다. 이진
탐색에는 없는 줄이고, **겨누기 때문에 생긴 줄**이다.

찾는 값이 구간의 값 범위 밖이면 비율이 0 보다 작거나 1 보다 커지고, 그러면 겨눈
자리가 배열 밖을 가리킨다. 반씩 접는 방식은 자리 번호만 가지고 셈하므로 이런
사고가 없다. 값으로 겨누는 대가다.

## 고르게 퍼져 있다는 전제

한 번에 닿는 것은 **자료가 고르게 퍼져 있어서**다. 화면의 열둘은 10 씩 일정하게
늘어나므로 비율과 자리가 정확히 맞아떨어진다.

값이 한쪽에 몰려 있으면 이야기가 달라진다. \`[1, 2, 3, 4, 5, 6, 7, 8, 9, 1000]\`
에서 9 를 찾으면 겨눔은 거의 언제나 맨 앞을 가리키고, 구간이 한 칸씩만 줄어
자리 수만큼 걸린다. 고르게 퍼진 자료에서 평균 \`log log n\` 이라는 좋은 값을
내지만, 치우친 자료에서는 \`n\` 까지 나빠진다. 이진 탐색이 자료의 생김새와
무관하게 늘 \`log n\` 인 것과 갈리는 지점이다.

겨눔은 **자료에 대한 짐작을 코드에 넣는 일**이다. 짐작이 맞으면 크게 벌고,
틀리면 반씩 접는 것만도 못하다.
`;
