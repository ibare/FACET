/**
 * find-root 학습 설명 — 단일 마크다운.
 *
 * `{facet:findRoot}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const findRootDescription = `## 자리가 아니라 이름이 무리를 정한다

**자리마다 가리키는 자리가 하나씩 있다.** 자기 자신을 가리키는 자리를 만날 때까지 그 화살표를 따라 올라가면, 마지막에 닿는 자리가 그 무리의 이름이다.

{facet:findRoot}

### 세 번 올라가 본다

3에서 올라가면 1을 거쳐 0에 닿는다. 6에서 올라가면 5를 거쳐 4에 닿는다. 2에서 올라가면 1을 거쳐 0에 닿는다. **3과 2는 같은 이름(0)에 닿았으니 한 무리고, 3과 6은 다른 이름에 닿았으니 남남이다.**

### 자리로는 알 수 없다

3과 2는 화면에서 나란히 있지도 않다. 둘이 같은 무리라는 사실은 자리를 아무리 들여다봐도 나오지 않는다 — 올라가서 같은 이름에 닿아 봐야 나온다. 이것이 union-find 의 find 연산이 하는 일이다.
`;
