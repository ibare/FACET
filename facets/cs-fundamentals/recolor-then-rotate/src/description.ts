/**
 * 재색칠과 회전 학습 설명 — 단일 마크다운.
 *
 * `{facet:recolorThenRotate}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const recolorThenRotateDescription = `## 옆자리의 색이 가른다

레드-블랙 트리는 규칙을 하나 지킨다 — **빨강 아래 빨강이 오면 안 된다.** 새 자리는 언제나 빨강으로 들어오니, 이 규칙은 자주 걸린다. 걸렸을 때 고치는 방법은 하나가 아니다. 무엇을 보고 고르는가?

{facet:recolorThenRotate}

### 삼촌의 색을 본다

위반이 걸리면 위반한 자리의 부모, 그리고 부모의 형제(흔히 "삼촌"이라 부른다)를 본다.

- **삼촌이 빨강이면** — 부모와 삼촌을 검정으로, 조부모를 빨강으로 칠하기만 하면 된다. 자리는 하나도 옮기지 않는다. 조부모가 빨강이 되면서 그 위에서 같은 문제가 다시 생길 수 있으니, 조부모를 기준으로 다시 검사한다 — 여기서는 조부모가 뿌리라 검정으로 되돌리는 것으로 끝난다.
- **삼촌이 검정이면(빈 자리도 검정이다)** — 색칠로는 답이 없다. 검정 노드 수를 세는 규칙(모든 길이 같은 수의 검정을 지나야 한다)이 이미 삼촌 쪽에서 꽉 차 있기 때문이다. 이때는 돌린다 — 회전으로 자리를 옮기고, 옮겨 온 두 노드의 색을 맞바꾼다.

### 왜 색칠만으론 항상 안 되는가

색칠은 삼촌 쪽 검정 수를 건드리지 않는다. 삼촌이 이미 빨강이면 삼촌을 검정으로 만들어도 그 갈래의 검정 수가 하나 늘 뿐이라 문제가 없다(정확히는 부모·자식 갈래 양쪽의 검정 수가 같아진다). 하지만 삼촌이 검정이면, 그 갈래는 이미 검정 수를 다 채운 상태다 — 색만 바꾸면 한쪽 갈래의 검정 수가 달라져 버린다. 그래서 자리를 옮겨 검정 수 자체를 재배치하는 회전이 필요하다.

### 어디에 쓰이나

- **정렬 컨테이너** — C++ \`std::map\`/\`std::set\`, Java \`TreeMap\`/\`TreeSet\` 이 내부에서 레드-블랙 트리를 쓴다.
- **리눅스 커널** — 프로세스 스케줄러(CFS)와 가상 메모리 영역 관리가 레드-블랙 트리로 되어 있다.
- **균형 유지 비용** — 삽입마다 회전은 최대 두 번뿐이라, AVL 트리보다 삽입이 싸면서도 높이는 로그 스케일로 유지된다.
`;
