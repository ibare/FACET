/**
 * 순차 탐색 학습 설명 — 단일 마크다운.
 *
 * 조각이 화면에 각주를 두지 않는 대신 전제를 여기서 밝힌다 (S-piece).
 * `{facet:scanUntilFound}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const scanUntilFoundDescription = `## 찾으면 멎고, 없으면 끝까지 간다

**탐색의 비용은 하나의 수가 아니다.** 같은 줄에서 같은 방식으로 찾아도, 찾는 값이 어디 있느냐에 따라 몇 번 들여다보는지가 달라진다. 그리고 값이 아예 없을 때는 — 언제나 최악이다.

{facet:scanUntilFound}

위 그림은 줄 서 있지 않은 다섯 칸을 두 번 훑는다. 한 번은 줄 안에 있는 값을, 한 번은 없는 값을 찾는다. 줄은 한 벌뿐이고 훑는 눈길도 같은 하나다. 다른 것은 지나간 길이뿐이다.

### 왜 중간에 포기할 수 없나

줄이 **서 있지 않기 때문이다.** 세 번째 칸에서 2 를 봤다고 해서, 뒤쪽에 9 가 없으리라고 말할 근거가 어디에도 없다. 값들 사이에 아무 약속이 없으므로 남은 칸 하나하나가 여전히 후보다.

그래서 "없다" 는 답은 **끝까지 다 보고 나서만** 할 수 있는 말이다. 찾는 값이 있을 때는 운이 좋으면 첫 칸에서 멎지만, 없을 때는 예외 없이 n 번을 다 본다.

- **최선** — 첫 칸이 답이다. 한 번.
- **평균** — 어디에 있는지 모르면 대략 절반. n/2 번.
- **최악** — 마지막 칸에 있거나, 아예 없다. n 번.

세 경우가 다 O(n) 으로 뭉뚱그려지지만, 없는 값을 찾는 일이 늘 최악이라는 것은 상수배 이상의 의미를 갖는다. "이 목록에 없음" 을 자주 확인해야 하는 코드라면 순차 탐색은 매번 전부를 훑는다.

### 줄을 세우면 달라지는 것

값들을 미리 정렬해 두면 "이 지점보다 크니 뒤는 볼 것도 없다" 는 판단이 생긴다. 그 판단 하나가 남은 절반을 통째로 지우고, 비용이 n 에서 log n 으로 내려간다. 해시로 자리를 정해 두면 아예 한 번에 간다.

바꿔 말하면 **순차 탐색의 비용은 자료에 아무 약속이 없을 때 치르는 값**이다. 미리 줄을 세우거나 색인을 만드는 수고는, 나중에 치를 이 값을 앞당겨 내는 일이다.

### 그림에 대한 전제

- 다섯 칸은 **정렬되어 있지 않다.** 정렬된 줄이었다면 두 번째 훑기가 중간에 멈출 수 있었다.
- 찾는 값 둘은 대비를 위해 고른 것이다. 하나는 넷째 칸에 있고 하나는 줄에 없다.
- 화면의 수 4 와 5 는 이 다섯 칸에서 나온 값이다. 줄이 길어지면 두 수의 차이도 함께 벌어진다.
`;
