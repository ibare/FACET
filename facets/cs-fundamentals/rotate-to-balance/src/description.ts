export const rotateToBalanceDescription = `
# 회전으로 기울기를 편다

{facet:rotateToBalance}

이진 탐색 트리는 값이 한쪽으로 몰려 들어오면 기울어진다. 기울어진 만큼
비교 횟수가 늘어나므로, 어떤 자료구조(AVL 트리 등)는 삽입할 때마다 균형
인수(왼쪽 높이 − 오른쪽 높이)를 검사하고, 그 값이 [-1, 1] 범위를 벗어나면
**회전** 한 번으로 되돌린다.

회전은 두 노드의 자리를 맞바꾸는 동작이다 — 축(피벗)이 한 칸 내려가고,
그 오른쪽 자식이 축이 있던 자리로 올라온다. 이때 올라온 노드가 원래 왼쪽에
달고 있던 가지 하나는 갈 곳을 잃는데, 그 가지는 손을 바꿔 내려간 축의
오른쪽에 다시 붙는다. 노드 하나, 간선 하나만 다시 잇는 셈이라 값 자체는
어느 것도 옮기거나 다시 정렬하지 않는다.

그래서 회전 전후로 **중위 순회 결과가 그대로**다 — 이 조각의 그림에서
노드의 가로 위치는 값의 정렬 순서로 고정되어 있고, 회전은 오직 세로(깊이)만
바꾼다. 가로가 움직이지 않는다는 사실 자체가 순서가 보존된다는 증거다.

여기 쓰인 수치는 6개 노드 트리 하나의 실제 계산 결과이며, 왼쪽으로
회전(오른쪽 자식이 축의 자리로 올라오는 경우) 한 번만 보여준다. 더 큰
트리에서는 회전이 여러 번 필요할 수 있고, 반대 방향(오른쪽 회전)이나
이중 회전(LR/RL)이 필요한 경우도 있다 — 이 조각은 그 중 가장 단순한
한 번의 사례만 다룬다.
`.trim();
