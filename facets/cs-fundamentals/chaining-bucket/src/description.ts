/**
 * 체이닝 학습 설명 — 단일 마크다운.
 *
 * `{facet:chainingBucket}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const chainingBucketDescription = `## 같은 자리에 둘이 오면

해시 표는 키를 자리 번호로 바꿔 한 번에 찾아간다. 그런데 자리는 유한하고 키는 그렇지 않으니 **서로 다른 키가 같은 번호를 받는 일은 반드시 생긴다.** 그때 나중 것을 어디에 둘 것인가.

체이닝의 답은 단순하다. **자리마다 사슬을 하나씩 두고, 뒤에 온 것을 그 사슬에 매단다.** 먼저 있던 것은 밀려나지도 옮겨지지도 않는다.

{facet:chainingBucket}

### 찾기가 두 걸음으로 갈린다

- **자리로 간다** — 해시값에서 번호를 계산하므로 자리가 몇 개든 한 번에 간다. 다른 자리는 쳐다보지 않는다.
- **그 사슬만 훑는다** — 자리에 걸린 것들을 앞에서부터 견준다. 그림에서 mango 를 찾는 데 세 번 견주는 것은 그 자리의 사슬 길이가 셋이기 때문이지, 표에 담긴 키가 다섯이기 때문이 아니다.

그래서 비용은 표의 크기가 아니라 **한 자리에 몇이 매달렸는가** 로 정해진다. 고르게 흩어지면 사슬은 짧게 유지되고, 한 자리로 몰리면 그 자리의 사슬이 길어져 결국 하나씩 훑는 일이 된다.

### 그림의 전제

- 해시값은 Java \`String.hashCode\` 의 실제 값이다. 자리는 \`(hash & 0x7FFFFFFF) % 8\` 로 정했다.
- 자리를 여덟으로 줄여 겹침이 한눈에 보이게 했다. 실제 표는 훨씬 크고 겹침도 그만큼 드물다.
- 사슬 끝에 붙이는 방식으로 그렸다. 앞에 끼워 넣는 구현도 흔하며, 그 경우 방금 넣은 것이 가장 빨리 찾힌다.

### 다른 답도 있다

같은 문제를 자리 안에서 푸는 방법(개방 주소법)도 있다. 사슬을 만들지 않고 **빈 자리를 찾아 옮겨 앉는** 쪽이다. 체이닝은 자리 밖으로 자라고, 개방 주소법은 자리 안에서 밀린다.
`;
