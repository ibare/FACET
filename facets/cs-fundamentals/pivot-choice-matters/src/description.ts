/**
 * pivot-choice-matters 학습 설명 — 단일 마크다운.
 *
 * `{facet:pivotChoiceMatters}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 * 화면이 밝히지 않는 전제 (입력이 이미 정렬되어 있다는 것, 한 번만 가른다는 것)
 * 는 여기서 말한다 — 조각은 각주를 두지 않는다 (S-piece).
 */

export const pivotChoiceMattersDescription = `## 기준 고르기

**퀵 정렬은 기준 하나를 골라 그보다 작은 것과 큰 것으로 가른다. 어느 값을 기준으로 삼느냐는 취향이 아니라, 가른 뒤 남는 일의 크기를 정하는 결정이다.**

{facet:pivotChoiceMatters}

### 무엇을 보고 있나

이미 줄이 선 일곱 값을 두 번 가른다. 값도, 가르는 방법도 같다. 다른 것은 기준으로 삼은 자리 하나뿐이다.

- **가운데 값 4** — 작은 쪽 셋, 큰 쪽 셋. 저울대가 수평으로 멎는다. 다음에 할 일은 셋짜리 두 덩이다.
- **맨 앞 값 1** — 1 보다 작은 값이 없으니 여섯이 모두 오른쪽으로 쏠린다. 왼팔은 맨 저울대로 남는다.

### 남는 일

가른 뒤에 남는 일은 **양쪽 중 큰 쪽**이다. 큰 쪽을 다시 갈라야 하기 때문이다.

- 기준 4 — 일곱이 셋이 되었다. 절반 아래로 줄었다.
- 기준 1 — 일곱이 여섯이 되었다. **기준 하나가 빠졌을 뿐이다.**

한 번 가르는 데 드는 품은 두 경우가 같다. 일곱 값을 전부 기준과 견주어야 하니까. 같은 품을 들이고 한쪽은 절반을 덜어 내고 다른 쪽은 하나를 덜어 낸다.

### 그래서 최악은 어디서 오나

절반씩 줄면 일곱 → 셋 → 하나로 세 번 만에 끝난다. 하나씩 줄면 일곱 → 여섯 → 다섯 → … 로 일곱 번을 반복한다. 값이 백만 개면 스무 번과 백만 번의 차이가 된다.

그리고 이 최악은 **드물게 나타나는 우연이 아니다.** 이미 정렬된 입력에 맨 앞 값을 기준으로 삼으면 매번 이 그림이 나온다. 정렬된 입력은 실제로 가장 흔한 입력 중 하나다 — 그래서 맨 앞 값을 기준으로 삼는 것이 최악의 선택이 된다.

### 전제

- 화면은 **한 번 가르는 장면만** 보여 준다. 갈린 덩이를 다시 가르는 과정은 그리지 않는다.
- 입력은 **이미 오름차순으로 정렬되어 있다.** 뒤섞인 입력이라면 맨 앞 값도 운이 좋으면 가운데쯤에서 갈린다. 여기서 말하는 것은 맨 앞을 고르는 것이 늘 나쁘다는 것이 아니라, 흔한 입력 한 종류에서 반드시 최악이 된다는 것이다.
- 저울대가 기우는 각도는 양쪽 개수의 차이에서 나온다. 무게라는 뜻이 아니라 **어느 쪽에 얼마나 몰렸는지**를 한눈에 보이려는 장치다.

### 그래서 실제로는

- **가운데 값** 을 기준으로 삼거나, 앞·가운데·뒤 셋의 중앙값 (median-of-three) 을 쓴다.
- **무작위 자리** 를 기준으로 삼는다. 어떤 입력에도 최악이 고정되지 않는다.
`;
