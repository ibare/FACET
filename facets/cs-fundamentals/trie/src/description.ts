/**
 * trie 완결형의 글.
 *
 * `{facet:trie}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const trieDescription = `
## Trie

낱말을 글자 단위로 나무에 담는다. 자리가 글자를 지는 것이 아니라 **자리를 잇는
선**이 글자를 진다 — 뿌리에서 내려온 글자를 이으면 그 자리까지의 접두사가 된다.

{facet:trie}

앞이 같은 낱말은 같은 길을 함께 쓴다. \`car\` 와 \`cart\` 와 \`cat\` 은 c-a 까지
한 길이고, 갈라지는 첫 글자에서만 가지가 돋는다. 담긴 것 아래 "낱말 / 자리" 를
보면 낱말보다 자리가 훨씬 덜 느는 것이 보인다.

## 자동완성

trie 를 쓰는 까닭이 여기 있다. 접두사 하나를 주면 그 자리까지 내려간 뒤 **그
아래만** 훑으면 된다 — 담긴 낱말이 백만 개여도 앞부분을 다시 견주지 않는다.
\`ca\` 를 넣고 자동완성을 눌러 보면 세 낱말이 한꺼번에 나온다.

## 지울 때 남는 것

\`car\` 를 지워도 c-a-r 은 남는다. \`cart\` 가 그 길을 쓰고 있기 때문이다.
끝 표시만 걷고, 자리는 아무도 쓰지 않게 된 것만 걷어낸다 — "접두사를 공유한다"
는 말의 결과가 지울 때 드러난다.

## 찾기의 세 결말

\`car\` 는 있다. \`ca\` 는 길이 있지만 낱말이 아니다. \`cap\` 은 p 가지가 없어
도중에 멈춘다. 뒤의 둘은 같은 "없다" 가 아니고, 자동완성은 그 차이 위에 선다.
`;
