export const indegreeZeroFirstDescription = `
## 진입 차수로 순서 정하기

{facet:indegreeZeroFirst}

일이 서로 앞뒤를 요구할 때, 무엇부터 해도 되는지는 한 가지로 판정된다 —
**나에게 들어오는 화살이 하나도 없으면 지금 해도 된다.** 들어오는 화살은
"이것이 먼저다" 라는 요구이고, 요구가 하나도 남지 않았다는 뜻이기 때문이다.

그래서 정점마다 들어오는 화살의 수부터 센다. 이 수를 진입 차수라 부른다.
그림의 정점 다섯은 \`a→c\`, \`b→c\`, \`c→d\`, \`c→e\`, \`b→e\` 다섯 화살로 묶여
있고, 세어 보면 \`a\` 와 \`b\` 만 0 이다. 지금 꺼낼 수 있는 것은 이 둘뿐이다.

여기서부터가 이 그림이 말하려는 대목이다. \`a\` 를 꺼내면 \`a\` 가 걸어 두었던
화살 \`a→c\` 가 함께 사라진다. 그 화살을 이고 있던 \`c\` 의 수가 2 에서 1 로
줄어든다. 아직 0 이 아니니 \`c\` 는 그대로 있다. 이어서 \`b\` 를 꺼내면
\`b→c\` 와 \`b→e\` 가 떨어지고, \`c\` 는 1 에서 0 이 되어 **뒤따라 떨어진다.**

꺼낼 수 있는 것이 둘 이상이면 이 그림은 알파벳 순으로 고른다. 그것은 이
방법의 규칙이 아니라 그림을 한 갈래로 보이기 위한 선택이다 — 실제로는 0 인
것 아무거나 골라도 되고, 그래서 위상 순서는 대개 여럿이다.

수가 눈에 보이는 것이 중요하다. 나오는 순서만 보면 왜 그 순서인지 알 수 없지만,
머리에 얹힌 수가 하나씩 내려앉는 것을 보면 순서가 어디서 나왔는지가 보인다.
끝까지 갔을 때 다섯이 모두 나왔다는 것은 이 화살들 사이에 순환이 없었다는
뜻이기도 하다. 순환이 있으면 그 안의 정점들은 서로를 기다리느라 영영 0 이
되지 않고, 줄에 자리가 남는다.
`.trim();
