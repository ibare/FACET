/**
 * insertion-sort 완결형의 글.
 *
 * `{facet:insertionSort}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const insertionSortDescription = `
## 삽입 정렬

손에 카드를 쥐고 정리할 때 하는 그 일이다. 왼쪽은 이미 줄이 서 있고, 오른쪽에서
카드를 한 장 가져와 그 줄의 **맞는 자리**에 끼워 넣는다. 끼워 넣고 나면 줄이 한
장만큼 길어진다. 오른쪽이 다 떨어지면 전부가 한 줄이다.

{facet:insertionSort}

## 맞바꿈이 아니라 밀기다

집어 든 값이 배열을 떠나면 그 자리가 **빈 자리**로 남는다. 뒤에서부터 왼쪽으로
걸어가며 자기보다 큰 것을 만나면 그것을 오른쪽으로 한 칸 비켜세우는데, 그러면
빈 자리가 왼쪽으로 한 칸 걸어온다. 큰 것들이 차례로 비켜서는 동안 빈 자리가
따라 걸어오고, 걸음이 멈춘 곳이 곧 들고 있던 값이 내려앉을 자리다.

코드로는 \`arr[j + 1] = arr[j]\` 한 줄이다. 오른쪽 칸을 덮어쓸 뿐 왼쪽 칸에서
값을 가져오지 않는다는 것이 요점이다. 선택 정렬이 두 자리를 **맞바꿔** 값 둘을
동시에 옮기는 것과 여기서 갈린다. 화면에서 빈 자리가 왼쪽으로 걸어가는 것이
보이면 그것이 밀기다 — 맞바꿈이라면 빈 자리가 생길 일이 없다.

## 이미 줄 서 있으면 거의 일하지 않는다

걸음은 자기보다 크지 않은 것을 만나는 **순간** 멈춘다. 왼쪽이 이미 줄 서 있으니
그 하나만 확인하면 그 왼쪽은 볼 필요가 없다. 그것이 이 알고리즘의 성격이다.

화면 아래의 기록을 보면 값마다 비켜선 횟수가 크게 다르다. \`11\` 을 넣을 때는 네
번이나 비켜세웠는데 \`90\` 을 넣을 때는 **한 번도** 비켜세우지 않는다. 90 은 그때
가장 큰 값이라 첫 견줌에서 바로 멈춘 것이다.

일곱 개를 다 넣는 데 견줌 열네 번, 비켜섬 열한 번이 든다. 만약 입력이 처음부터
줄 서 있었다면 값마다 견줌 한 번씩, 모두 여섯 번이면 끝난다. 비켜섬은 0 이다.
거의 정렬된 자료를 다룰 때 삽입 정렬을 고르는 이유가 이것이다.

## 견줌을 세려면 조건을 펼쳐야 한다

안쪽 걸음은 두 가지 이유로 멈춘다 — 왼쪽 끝을 지났거나(\`j >= 0\` 이 깨졌거나),
더 작은 것을 만났거나(\`arr[j] > key\` 가 깨졌거나). 흔히 이 둘을 \`and\` 로 묶어
한 줄에 적는다.

그런데 그러면 **견줌이 몇 번 일어났는지** 를 코드가 말하지 못한다. 자리 범위를
지키는 검사와 두 값을 실제로 견주는 일은 성격이 다르기 때문이다. 코드 패널의
IR 은 안쪽을 \`if arr[j] <= key: break\` 로 펼쳐 두었다. 그래서 견줌 한 번이 코드
한 줄과 정확히 맞물리고, 화면의 견줌 수가 그 줄이 실행된 횟수와 같아진다.
`;
