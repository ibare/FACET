/**
 * node-holds-many description — 학습 설명 마크다운.
 */

export const nodeHoldsManyDescription = `
## 다분기 노드

한 자리가 키를 하나가 아니라 여럿 담을 수 있다면, 자리 자체가 갈림길이 된다.
자리에 이르면 그 안의 키를 왼쪽에서 오른쪽으로 훑는다. 찾는 값이 어느 키보다
크면 다음 키로, 어느 키보다 작으면 그 앞의 틈으로 자식을 골라 내려간다.

{facet:nodeHoldsMany}

아래 그림은 키를 아홉 개 담은 나무다. 이진 트리였다면 자리도 아홉 개가
필요했을 것이다. 한 자리가 여럿을 담는 여기서는 자리가 넷뿐이다 — 담는
수를 늘리면 같은 키를 더 적은 층으로 훑는다.
`.trim();
