/**
 * dijkstra 완결형의 글.
 *
 * `{facet:dijkstra}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 *
 * 화면 각주로 밀어 넣지 않고 이 글이 지는 것 둘 — **무게가 모두 양수여야
 * 한다는 전제** 와 **우선순위 큐를 쓰면 더 빠르다는 사실** 이다. 앞의 것은
 * 그림에 그릴 수 있는 종류의 말이 아니고, 뒤의 것은 코드 패널이 보여 주는
 * 코드와 다른 코드의 이야기다.
 */

export const dijkstraDescription = `
## 다익스트라

지도에서 한 곳을 정하고 나머지 모든 곳까지의 가장 짧은 길을 한꺼번에 구한다.
길마다 걸리는 시간이 다르니 이음의 수를 세는 것으로는 안 된다 — 이음 셋을
밟는 길이 이음 둘을 밟는 길보다 짧을 수 있다.

다익스트라가 하는 일은 두 마디로 끝난다. **아직 확정되지 않은 것 중 가장 가까운
것을 굳히고, 그 이웃을 편다.** 하나를 굳힐 때마다 그 둘레의 잠정 거리가 갱신되고,
갱신된 값 중에서 다시 가장 가까운 것을 굳힌다.

{facet:dijkstra}

## 잠정과 확정

화면의 정점에 적힌 수는 **지금까지 알아낸 가장 짧은 길** 이지 최단 거리가 아니다.
테두리만 있는 빈 원이 그 잠정이고, 속이 찬 원이 확정이다.

정점 3 을 보라. 1 을 굳힌 뒤에는 22 였다 (0−1−3 = 7 + 15). 그런데 2 를 굳히자
20 으로 줄었다 (0−2−3 = 9 + 11). 잠정인 동안에는 이렇게 얼마든지 흔들린다.

오른쪽 기록에 한 줄이 적히는 순간이 확정이다. 굳은 뒤로는 재생이 끝날 때까지
그 값이 한 번도 다시 움직이지 않는다.

## 왜 굳혀도 되는가

굳히는 순간 그 정점의 값이 최단 거리라는 것은 증명이 필요하다. 지금 고른 정점을
\`u\` 라 하고, 그 값보다 더 짧은 길이 어딘가 있다고 해 보자. 그 길은 출발점에서
나와 언젠가는 **아직 굳지 않은 정점**을 처음 밟게 된다. 그 정점을 \`x\` 라 하자.

- \`x\` 는 미확정이므로, 우리가 \`u\` 를 고를 때 함께 견주어졌다. 그런데도 \`u\` 를
  골랐으니 \`x\` 까지의 잠정 거리는 \`u\` 의 값보다 작지 않다.
- 그 길은 \`x\` 를 지난 뒤에도 계속 가야 \`u\` 에 닿는다.

**무게가 모두 양수라면** 뒤에 남은 구간은 길이를 더하기만 한다. 그러니 그 길은
\`u\` 의 값보다 짧을 수 없다. 모순이다.

이 논증이 기대는 것은 딱 하나, **음수 무게가 없다**는 전제다. 무게 하나가 음수면
"뒤에 더 가면 더 짧아질 수 있다" 가 되어 굳힌 값이 나중에 뒤집힌다. 그런 그림에는
다익스트라를 쓰지 않는다 (벨만-포드가 그 자리를 맡는다).

## 이음이 많은 길이 더 짧을 수 있다

이 그림에서 4 까지의 최단 거리는 20 이고, 그 길은 **0−2−5−4** 다. 이음을 셋
밟는다. 이음을 둘만 밟는 0−2−3−4 는 9 + 11 + 6 = 26 이라 오히려 멀다.

그래서 "몇 번 갈아타는가" 를 세는 너비 우선 탐색으로는 이 답이 나오지 않는다.
무게가 있는 그림에서 셀 것은 이음의 수가 아니라 무게의 합이다.

## 고르는 일이 절반이다

코드 패널을 보면 우선순위 큐가 없다. 미확정 정점을 처음부터 끝까지 훑어 가장
작은 것을 찾는 반복문이 그대로 있다.

\`\`\`
for i in range(n):
    if done[i] == 0 and dist[i] < best:
        best = dist[i]
        u = i
\`\`\`

이 다섯 줄이 "아직 확정되지 않은 것 중 가장 가까운 것" 의 전부다. \`pq_pop_min()\`
한 줄로 감싸면 문장은 짧아지지만 이 알고리즘의 절반이 이름 뒤로 사라진다.

펼쳐 쓴 대가는 코드에 그대로 드러난다. 바깥 회차가 정점 수만큼, 그 안의 훑기가
다시 정점 수만큼이라 이 판은 **O(V²)** 다. 컨트롤바의 "훑기" 가 36 에서 멈추는데
정점이 여섯이니 6 × 6 이다.

우선순위 큐(이진 힙)를 쓰면 가장 작은 것을 매번 훑지 않고 꺼낼 수 있어
**O((V + E) log V)** 가 된다. 이음이 성긴 큰 그림에서는 그쪽이 훨씬 빠르다.
다만 그때도 굳히고 펴는 순서는 지금 화면에 보이는 것과 똑같다 — 바뀌는 것은
"가장 가까운 것을 어떻게 찾는가" 하나뿐이다.

## 펴는 일

굳힌 정점의 이웃마다 이 한 줄을 묻는다.

\`\`\`
if dist[u] + w < dist[v]:
    dist[v] = dist[u] + w
\`\`\`

"\`u\` 를 거쳐 가면 지금 알고 있는 것보다 짧은가." 짧으면 값을 줄이고, 그 정점의
부모를 \`u\` 로 바꾼다. 화면의 굵은 선이 그 부모 관계이며, 그것을 다 모으면
출발점에서 뻗은 **최단 경로 나무**가 된다. 잠정인 동안에는 점선이고, 굳으면
실선이 된다.

이 판에서는 간선을 열여덟 번 들여다보고 그중 일곱 번만 값이 줄었다. 나머지
열한 번은 "더 짧지 않다" 는 확인이다. 확인도 일이라 세어야 한다.
`;
