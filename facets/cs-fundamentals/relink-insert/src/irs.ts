/**
 * relink-insert IR — 비어 있다.
 *
 * 조각은 코드 패널을 두지 않는다. 한 질문에 답하고 멈추는 그림이라 옆에
 * 소스가 붙으면 그 질문이 둘이 된다 (S-piece). 파일 자체는 S-facet 의 6파일
 * 구성을 지키기 위해 남긴다 — 뒤에 코드 패널이 필요해지면 여기가 그 자리다.
 */

import type { IR } from '@ffacet/core/runtime';

export const relinkInsertIRs: IR[] = [];
