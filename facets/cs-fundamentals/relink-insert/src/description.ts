export const relinkInsertDescription = `# 재연결 — 고리를 끊고 다시 잇는다

연결 리스트에 값 하나를 끼워 넣을 때 무엇이 움직이는가.

배열이라면 답이 분명하다. 자리를 하나 비우려면 뒤에 있는 값이 전부 한 칸씩
밀려나야 한다. 넣는 값 하나 때문에 나머지 전부가 움직인다.

연결 리스트는 그렇지 않다. 상자는 어디에 있든 상관없다 — 순서를 아는 것은
상자가 아니라 상자가 들고 있는 화살표(next)뿐이기 때문이다. 그래서 삽입은
자리를 만드는 일이 아니라 **화살표를 고쳐 쓰는 일**이 된다.

{facet:relinkInsert}

두 걸음이 전부다.

1. 새 노드의 next 를 뒤 노드에 붙인다.
2. 앞 노드의 next 를 뒤 노드에서 떼어 새 노드에 붙인다.

순서를 바꾸면 안 된다. 1 을 먼저 해 두어야 2 에서 화살표를 떼는 순간에도
뒤쪽이 미아가 되지 않는다. 앞 노드의 화살표를 먼저 떼면 그것이 유일한 길이던
뒤쪽 전부를 잃는다.

끝나고 세어 보면 고쳐 쓴 화살표는 둘, 자리를 옮긴 노드는 하나도 없다.
리스트가 아무리 길어도 이 둘은 그대로다 — 삽입 지점을 이미 손에 쥐고 있다면
길이와 무관하게 일정하다. 배열의 삽입이 뒤쪽 길이에 비례해 무거워지는 것과
갈리는 지점이 여기다.

대신 잃는 것도 있다. 화살표를 따라가야만 다음을 알 수 있으므로, 세 번째
노드를 보려면 첫 노드부터 두 번 건너뛰어야 한다. 배열이 곧바로 짚는 그 일을
연결 리스트는 못 한다.
`;
