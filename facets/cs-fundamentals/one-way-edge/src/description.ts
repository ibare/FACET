/**
 * 조각과 함께 딸려 오는 글. 화면에 각주를 두지 않는 대신 전제를 여기서
 * 밝힌다 (S-piece).
 */

export const oneWayEdgeDescription = `
## 방향 간선

선을 하나 긋는 것과 화살을 하나 붙이는 것은 다르다. 무방향 그래프에서
\`u—v\` 는 두 가지를 한꺼번에 말한다 — u 에서 v 로 갈 수 있고, v 에서 u 로도
갈 수 있다. 화살을 붙이는 순간 그중 하나만 남고 나머지는 사라진다.

{facet:oneWayEdge}

그림의 다섯 정점은 고리로 이어져 있다. 화살이 없을 때 S 에서 걸어 보면
다섯 모두에 닿는다 — 고리는 어느 쪽으로도 돌 수 있기 때문이다.

같은 다섯 선에 방향을 붙이면 그림이 달라진다. \`S—C\` 에 붙은 화살은
\`C → S\` 라, S 에서 C 로 가던 길이 없어진다. \`C—T\` 도 \`C → T\` 라 T 에서
C 로 돌아갈 수 없다. C 에 닿아 있는 선은 둘 다 밖으로 나가는 화살이어서,
C 로 **들어오는** 화살은 하나도 없다. 그래서 S 에서 닿는 정점은 넷이고 C 가
빠진다.

정점의 개수도, 선의 개수도, 그림이 그려 놓은 모양도 그대로다. 달라진 것은
각 선이 몇 방향을 허락하는가뿐이다. 도달 가능성이 그것 하나에 달려 있다.

- C 에서는 여전히 S 와 T 로 갈 수 있다. "닿지 못한다" 는 것은 **S 에서 C 로**
  라는 뜻이지, C 가 고립되었다는 뜻이 아니다.
- 무방향 그래프의 연결성은 대칭이지만 (u 가 v 에 닿으면 v 도 u 에 닿는다),
  방향 그래프의 도달 가능성은 그렇지 않다. 이 조각이 보이는 것이 그 비대칭의
  가장 작은 예다.
`.trim();
