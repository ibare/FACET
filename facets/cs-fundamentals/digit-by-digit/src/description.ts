/**
 * digit-by-digit 학습 설명 — 단일 마크다운.
 *
 * 본문 안의 `{facet:digitByDigit}` 토큰은 호스트(Tiptap NodeView) 가 시각화
 * 노드로 치환해 인라인 마운트한다.
 *
 * 화면이 밝히지 않는 전제를 여기서 밝힌다 (S-piece) — 자리를 맞춰 보이려고 앞을
 * 0 으로 채워 그렸다는 것, 열 개의 통은 십진법을 골랐기 때문이라는 것, 그리고
 * 이 방식이 아무 자료에나 통하지는 않는다는 것.
 */

export const digitByDigitDescription = `## 자릿수 정렬

정렬은 값끼리 견주는 일이라고 배운다. 이 방식은 한 번도 견주지 않는다.

대신 **한 자리씩만** 본다. 낮은 자리부터 시작해, 그 자리의 숫자가 가리키는 통에
수를 넣고, 통을 0 부터 9 까지 읽어 다시 줄로 만든다. 그것을 자릿수만큼 되풀이하면
줄이 다 서 있다.

{facet:digitByDigit}

믿기 어려운 대목은 여기다. 1 의 자리로 줄을 세우면 그 순서는 10 의 자리를 다시
세우는 순간 부서질 것 같다. 부서지지 않는 이유는 두 가지가 함께 있어서다.

- **낮은 자리부터** 한다. 마지막에 본 자리가 가장 힘이 세고, 그 자리가 같을 때에만
  앞 라운드의 순서가 살아남는다. 그것이 바로 우리가 원하는 차례다.
- 라운드가 **앞 라운드의 순서를 뒤엎지 않는다**. 같은 통에 들어간 수들은 들어온
  차례 그대로 나온다 (안정). 그래서 10 의 자리가 같은 두 수는 1 의 자리로 매긴
  앞뒤를 그대로 지킨다.

둘 중 하나만 빠져도 무너진다. 안정하지 않은 방법으로 통을 비우면 앞 라운드가
지워지고, 높은 자리부터 세면 낮은 자리를 세울 때 다시 뒤엎인다.

### 그림이 말하지 않는 전제

- 자리를 맞춰 보이려고 45 를 \`045\` 로, 90 을 \`090\` 으로 앞에 0 을 채워 그렸다.
  값이 달라진 것이 아니라 없는 자리의 숫자가 0 이라는 뜻이다.
- 통이 열 개인 것은 십진법을 골랐기 때문이다. 밑을 256 으로 잡으면 통이 256 개가
  되는 대신 라운드가 줄어든다 — 통의 수와 라운드 수는 서로 맞바꾸는 관계다.
- 그림의 수는 자릿수가 셋 이하인 음이 아닌 정수다. 음수나 실수, 길이가 제각각인
  문자열은 자리를 어떻게 매길지부터 따로 정해야 한다.

### 값이 얼마나 드나

수가 n 개이고 자릿수가 k 라면 걸음은 대략 n × k 다. 견줌으로 정렬하는 방법이
피할 수 없는 n log n 벽 아래로 내려갈 수 있는 것은, 값을 견주는 대신 **값이 스스로
자기 자리를 말하게** 했기 때문이다. 대신 통을 담을 자리가 따로 든다.
`;
