/**
 * 이진 탐색 트리 — 넣는 순서가 모양을 정한다. 조각과 함께 딸려 오는 글.
 *
 * `{facet:bstDegenerate}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const bstDegenerateDescription = `## 넣는 순서가 모양을 정한다

이진 탐색 트리는 "작으면 왼쪽, 크면 오른쪽" 이라는 규칙 하나로 자리를 정한다. 그런데 같은 값을 담아도, **넣는 순서가 다르면 나오는 모양이 다르다.**

{facet:bstDegenerate}

### 오름차순으로 넣으면 줄이 된다

10, 20, 30, 40, 50, 60 을 이 순서 그대로 넣으면 어떻게 될까. 10 을 넣고 나면 20 은 10 보다 크니 오른쪽, 30 도 10·20 보다 크니 오른쪽, ... 매번 지금까지 넣은 값보다 큰 값이 들어오니 **언제나 오른쪽으로만 내려간다.** 결과는 나무가 아니라 사슬이다 — 높이가 노드 수만큼 깊어진다.

### 순서를 흔들면 옆으로 퍼진다

같은 여섯 값을 40, 20, 60, 10, 30, 50 순서로 넣으면 다르다. 40 이 뿌리에 앉고, 20 은 왼쪽, 60 은 오른쪽 — 값이 뿌리보다 작기도 하고 크기도 하니 **양쪽으로 갈린다.** 남은 넷도 마찬가지로 좌우에 골고루 붙어 훨씬 낮은 나무가 된다.

### 모양이 다르면 찾는 비용이 다르다

두 나무 모두 같은 여섯 값을 담고 있고, 훑어보는 순서(중위 순회)도 똑같이 오름차순이다. 그런데 같은 값 하나를 찾아보면 사슬 쪽은 비교를 훨씬 더 많이 해야 한다 — 트리의 높이가 곧 최악의 경우 비교 횟수이기 때문이다. **담긴 값이 같아도, 넣은 순서가 나쁘면 탐색은 배열을 순서대로 훑는 것과 다를 게 없어진다.**

### 그림이 보이는 것

- 두 나무 모두 노드 여섯 (10~60), 같은 값을 담는다
- 열(가로)은 값의 오름차순 순위로 고정된다 — 두 나무 모두 같은 값은 같은 열에 선다
- 행(세로)만 삽입 순서가 만든 실제 깊이를 따른다. 그래서 키 차이가 곧 모양 차이다
- 다 기른 뒤 같은 값을 찾아 실제 비교 횟수를 센다
`;
