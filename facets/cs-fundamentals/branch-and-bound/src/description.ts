/**
 * branch-and-bound 완결형의 글.
 *
 * `{facet:branchAndBound}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const branchAndBoundDescription = `
## 분기 한정

배낭에 넣을 물건을 고르는 문제다. 물건마다 값과 무게가 있고 배낭에는 한도가
있다. 답을 확실히 얻는 가장 정직한 방법은 **모든 조합을 다 보는 것**이다.
물건이 넷이면 조합은 열여섯이고, 스물이면 백만이 넘는다.

분기 한정은 그 열여섯을 다 보지 않는다. 물건 하나마다 "담는다 / 두고 간다" 로
갈래를 뻗되, 갈래에 들어설 때마다 **그 아래에서 최선을 다했을 때의 값**을 먼저
재어 본다. 그 값이 지금까지의 최고를 못 넘으면 아래를 통째로 접는다.

{facet:branchAndBound}

## 한계는 쪼갤 수 있다고 치고 잰다

접어도 되는지 판단하려면 "이 갈래가 아무리 잘해도 여기까지" 라는 수가 있어야
한다. 그 수를 **한계** 라 부른다. 한계는 실제 답보다 **낮으면 안 된다** —
낮게 잡으면 답이 들어 있는 갈래를 잘라 버린다.

그래서 한계를 잴 때는 규칙 하나를 느슨하게 푼다. **물건을 쪼갤 수 있다고 친다.**
남은 물건을 값/무게가 큰 순서로 담다가, 마지막 하나가 통째로는 안 들어가면
들어가는 만큼만 잘라 담는다. 실제로는 못 하는 일이니 이렇게 잰 값은 진짜 답보다
결코 작지 않고, 따라서 이 값으로 자르는 것이 안전하다.

화면의 **한계 자**에서 빗금 친 조각이 그 "쪼개 담은 몫" 이다. 코드에서는
\`bound\` 함수의 \`else\` 가지 한 줄이다.

\`\`\`
total = total + values[k] * room / weights[k]
\`\`\`

이 줄의 나눗셈은 **실수 나눗셈**이어야 한다. 정수 나눗셈으로 쓰면 남은 자리 3 을
무게 6 짜리 물건으로 나눈 몫이 0 이 되어 한계가 실제보다 낮게 나오고, 낮은
한계는 살아 있어야 할 갈래를 자른다. 문법은 멀쩡한 채로 답만 틀리는 종류의
잘못이다. 물건을 값/무게 순으로 미리 정렬해 두는 것도 이 셈을 위한 것이다 —
순서가 흐트러지면 "최선을 다했을 때" 가 최선이 아니게 된다.

## 자르는 조건은 같을 때도 자른다

\`bound <= best\` 다. 부등호에 등호가 붙어 있다. 한계가 지금까지의 최고와 **같기만**
해도 자른다 — 잘해야 비기는 갈래를 열어 봐야 새 최고가 나오지 않기 때문이다.
답 하나만 필요하다면 이 등호가 걸음을 꽤 줄여 준다.

## 표를 채우는 쪽과 견주면

같은 배낭을 동적 계획법으로 풀면 물건 넷 × 한도 열의 칸 마흔넷을 **하나도
빠짐없이** 채운 끝에 답에 닿는다. 여기서는 갈래 아홉을 열어 보고 같은 답에
닿는다. 나머지는 무게가 넘쳐서, 또는 한계가 최고를 못 넘어서 열리지 않았다.

대신 값을 치른다. 표를 채우는 쪽은 걸음 수가 물건 수와 한도로 **미리 정해져**
있지만, 분기 한정은 자료가 나쁘면 결국 전부를 볼 수도 있다. 한계가 얼마나
날카로운지에 성능이 통째로 달려 있는 것이다. 잘 드는 한계가 있으면 표를 채우는
쪽이 손도 못 대는 크기를 풀고, 무딘 한계밖에 없으면 아무것도 줄이지 못한다.
`;
