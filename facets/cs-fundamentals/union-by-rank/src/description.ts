/**
 * union-by-rank 조각의 글.
 *
 * `{facet:unionByRank}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const unionByRankDescription = `
## 랭크 기반 합집합

서로 다른 두 무리를 합칠 때, 어느 쪽 뿌리를 아래로 넣을지는 자유롭게 고를 수
있다. 이 그림은 자리 다섯(0~4)을 자리마다 랭크(대략의 키)를 매겨 가며 넷 번
합친다.

{facet:unionByRank}

- 첫 번째와 두 번째 합치기는 둘 다 랭크 0끼리 만나 하나가 다른 하나 밑으로
  들어간다. 랭크가 같을 때는 어느 쪽이 위로 가든 결과가 같으므로, 이긴 뿌리의
  키가 하나 는다 — 어쩔 수 없다.
- 세 번째 합치기도 랭크가 같다(1과 1). 다시 한 번 키가 는다.
- 네 번째가 요점이다. 랭크 0인 자리와 랭크 2인 자리를 합칠 때, 낮은 쪽을 높은
  쪽 밑에 넣으면 키가 그대로다. 반대로 넣었다면(높은 쪽을 낮은 쪽 밑에) 키가
  하나 더 늘었을 것이다.

고르기만 잘해도 나무가 불필요하게 길어지지 않는다. 그림에서 나무의 세로 폭이
곧 그 뿌리의 랭크이므로, 넷째 합치기에서 세로가 늘지 않는 것을 눈으로 바로
확인할 수 있다.
`;
