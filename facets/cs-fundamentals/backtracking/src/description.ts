/**
 * backtracking 완결형의 글.
 *
 * `{facet:backtracking}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const backtrackingDescription = `
## 백트래킹

되는 데까지 가 보고, 막히면 방금 한 선택을 **되돌린 뒤 다음 선택을 본다.**
이것이 전부다. 앞을 미리 내다보는 영리함이 없고, 대신 한 번 한 선택을 언제든
거둘 수 있게 해 두는 규율이 있다.

4×4 판에 퀸 넷을 놓아 본다. 퀸은 같은 행 · 같은 열 · 같은 대각선을 모두 친다.
위 행부터 한 줄에 하나씩 놓기로 정하면 행이 겹칠 일은 없으니, 남은 것은
**열과 대각선**뿐이다.

{facet:backtracking}

## 물러남은 실패가 아니다

화면의 자취 띠를 보라. 막대가 올라갔다 내려오기를 되풀이한다. 올라간 막대는
퀸을 놓은 것이고 빈 막대는 물린 것이다. 놓은 것이 열여섯, 물린 것도 열여섯 —
**놓은 것은 결국 다 물린다.**

그럴 수밖에 없다. 이 절차는 판 위에 답을 쌓아 두지 않는다. 해를 찾으면 그것을
따로 적어 두고, 판은 원래대로 되돌린 뒤 남은 가지를 마저 훑는다. 마지막에 판이
비는 것은 아무것도 못 찾아서가 아니라 **다 훑었다는 표시**다.

## 코드가 그것을 말한다

\`solve\` 는 bool 이 아니라 **찾은 해의 수**를 돌려준다. 안전한 열을 찾아도
\`return\` 으로 빠져나가지 않고 \`found\` 에 더하기만 하고 루프를 계속 돈다.
첫 해에서 멈추는 판본은 여기서 \`return 1\` 을 하는데, 그 한 줄 차이가
"하나만 찾기" 와 "전부 찾기" 를 가른다.

그리고 놓기와 물리기가 재귀를 사이에 두고 짝을 이룬다.

\`\`\`
cols[row] = col                        놓는다
found = found + solve(cols, row+1, n)  내려간다
cols[row] = -1                         물린다
\`\`\`

가운데 줄이 아무리 깊이 들어갔다 와도 세 번째 줄이 반드시 실행된다. 이 짝이
깨지면 다음 열을 시도할 때 판에 유령 퀸이 남는다.

## 판이 왜 막히는지

\`is_safe\` 는 앞선 행들을 하나씩 훑으며 둘을 본다.

\`\`\`
cols[r] == col                열이 같다
cols[r] - col == row - r      왼쪽 위 대각선
col - cols[r] == row - r      오른쪽 위 대각선
\`\`\`

대각선 판정을 \`abs\` 하나로 줄여 쓸 수도 있다. 그러면 짧아지지만 **대각선이
둘이라는 사실**이 그 이름 뒤로 사라진다. 행 차이와 열 차이가 같으면 대각선이고,
열 차이의 부호가 왼쪽인지 오른쪽인지를 가른다 — 나란히 적어 두면 화면에서 그은
붉은 선이 코드의 어느 줄인지가 바로 보인다.

훑기가 **첫 충돌에서 멈춘다**는 것도 그대로다. 한 군데라도 걸리면 나머지를 볼
이유가 없다. 그래서 판에 퀸이 많이 놓인 아래 행일수록 후보가 빨리 잘려 나간다.

## 4×4 의 해는 둘

\`[1, 3, 0, 2]\` 와 \`[2, 0, 3, 1]\` — 각 행에 놓인 열 번호다. 둘은 서로의 좌우
거울상이다. 첫 행을 0 열이나 3 열로 시작하면 어떻게 해도 넷을 다 놓을 수 없어
그 가지는 통째로 버려진다. 판을 조금만 키워도(8×8 은 92 개) 가지의 수가 폭발
하므로, 그때부터는 이 뼈대에 **가지치기**를 얹는 것이 다음 이야기가 된다.
`;
