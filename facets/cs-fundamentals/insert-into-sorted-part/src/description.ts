/**
 * 정렬부 삽입 학습 설명 — 단일 마크다운.
 *
 * `{facet:insertIntoSortedPart}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 * 화면이 밝히지 않는 전제 — 왼쪽이 이미 정렬되어 있다는 것, 이것이 삽입 정렬의
 * 안쪽 한 걸음이라는 것 — 는 여기서 말한다 (S-piece).
 */

export const insertIntoSortedPartDescription = `## 벌어지는 자리는 누군가 비켜서서 생긴다

"정렬된 곳에 값을 하나 끼워 넣는다" 는 말은 자리가 저절로 벌어지는 장면을 떠올리게 한다. 그런 자리는 없다. **넣을 자리는 옆의 값들이 비켜서야 생긴다.**

{facet:insertIntoSortedPart}

새 값은 먼저 줄에서 **뽑혀 나온다.** 들려 있는 동안 그 값은 줄에 없고, 있던 칸은 빈자리로 남는다. 그 빈자리가 이 장면의 주인공이다 — 값이 하나씩 오른쪽으로 비켜설 때마다 빈자리는 한 칸씩 왼쪽으로 옮겨 오고, 걸음이 멈추는 순간 그 자리가 곧 새 값의 자리다.

### 왜 뒤에서부터인가

비켜서는 일이 앞에서부터 번지면 첫 걸음에서 값을 잃는다. 아직 비키지 않은 값 위에 다른 값을 쓰게 되기 때문이다. 뒤에서부터 하면 옮겨 갈 곳이 언제나 **방금 빈 칸** 이라 아무것도 덮이지 않는다. 임시 저장소도, 배열을 하나 더 만드는 일도 필요 없다 — 순서를 뒤집는 것만으로 값이 보존된다.

### 왼쪽 끝까지 가지 않는다

왼쪽이 **이미 줄 서 있다는 전제** 가 여기서 값을 한다. 뒤에서부터 걸어오다 자기보다 크지 않은 값을 만나면, 그보다 더 왼쪽에 있는 것들은 볼 필요가 없다. 이미 오름차순이므로 전부 그보다 작거나 같기 때문이다. 걸음은 그 자리에서 멈춘다.

이것이 삽입 정렬의 안쪽 한 걸음이며, 거의 정렬된 입력에서 삽입 정렬이 빠른 이유이기도 하다. 새 값이 제자리에 가까울수록 걸음은 일찍 멈춘다. 이미 정렬된 입력이라면 매번 첫 견줌에서 멈추므로 전체가 **O(N)** 이 되고, 거꾸로 정렬된 입력이라면 매번 왼쪽 끝까지 걸어가 **O(N²)** 이 된다.

### 견줌과 비켜섬은 다른 일이다

견줌은 한 번 더 일어난다. 멈추게 한 그 값도 견주기는 했으나 비키지는 않았기 때문이다. 걸음마다 견줌은 반드시 있고 비켜섬은 있을 수도 없을 수도 있다 — 화면 끝의 두 숫자가 다른 것은 그래서다.
`;
