export const partitionAroundPivotDescription = `
# 피벗으로 가르기

기준값 하나를 정하고, 나머지를 그보다 **작은 쪽**과 **큰 쪽**으로 가른다.
퀵정렬이 매 단계 하는 일이 이것이다.

{facet:partitionAroundPivot}

## 이 화면이 말하는 것

가운데 세로선이 기준이다. 값들은 위쪽에 흩어져 있다가 하나씩 선 위로 내려와
**기준 하나와만** 견준다. 값끼리는 한 번도 견주지 않는다 — 견줌의 한쪽 끝이 늘
같은 값이라는 것이 분할의 뼈대다. 값 다섯이면 견줌도 다섯 번이다.

견주고 나면 선을 넘어 좌 또는 우의 방으로 내려가 자리를 잡는다. 기준 자신은
넘어가지 않고 두 방 사이에 남는다.

## 가르는 것과 정렬하는 것

한 번 가르고 나면 줄이 선 것처럼 보이지만 아니다. 가른 결과가 말하는 것은
"기준보다 작다" 와 "기준보다 크다" 두 가지뿐이다. **방 안에서의 자리는 건넌
순서가 정할 뿐 값의 대소가 정하지 않는다.** 오른쪽 방의 7, 9, 8 이 그 증거다.

대신 하나가 확정된다. 자기보다 작은 것이 전부 왼쪽에, 큰 것이 전부 오른쪽에
있으므로 **기준의 최종 자리는 이 한 번으로 정해진다.** 나머지 정렬은 두 방
안에서 같은 일을 되풀이해 끝낸다.

## 화면이 줄인 것

실제 구현(Lomuto·Hoare 분할)은 새 자리를 만들지 않고 원래 배열 안에서 값을
맞바꿔 가른다. 그래서 가른 뒤 각 쪽의 값 순서는 구현마다 다르다. 이 화면은
건넌 순서대로 기준선 쪽부터 바깥으로 쌓아 "안쪽은 정렬되지 않았다" 만 보인다 —
어느 쪽으로 갔는지가 이 조각의 논점이고, 그 안에서 몇 번째냐는 논점이 아니다.
`;
