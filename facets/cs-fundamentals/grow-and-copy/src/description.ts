/**
 * 재할당 학습 설명 — 단일 마크다운.
 *
 * `{facet:growAndCopy}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const growAndCopyDescription = `## 칸이 차면 새 자리로 옮긴다

배열은 이어진 한 덩어리다. 한 덩어리이기 때문에 몇 번째 칸이든 곱셈 한 번으로 찾아가지만, 같은 이유로 **끝에 칸 하나를 덧붙일 수 없다.** 그 자리는 이미 다른 것이 쓰고 있을지도 모른다.

{facet:growAndCopy}

### 옮기는 값은 복사본이다

새 자리를 얻는 것만으로는 아무 일도 일어나지 않는다. 옛 자리의 값은 저절로 따라오지 않으므로 **하나씩 읽어 하나씩 쓴다.** 값이 n 개면 복사도 n 번이다. 그래서 한 번의 삽입이 갑자기 n 번의 일을 하는 순간이 생긴다.

복사가 끝나면 옛 자리는 쓸모가 없어져 돌려준다. 그 순간 **배열의 주소가 바뀐다.**

### 두 배로 키우는 이유

한 칸씩만 늘리면 넣을 때마다 매번 전부를 옮기게 된다. 두 배로 키우면 다음 재할당까지 넣을 수 있는 횟수도 두 배가 되어, 옮긴 비용을 그 사이의 삽입들이 나눠 진다. 길게 보면 삽입 한 번의 평균 비용이 상수로 눌린다 — 이것을 분할 상환이라 부른다.

### 주소가 바뀐다는 것의 값

바뀐 것은 값이 아니라 **자리**다. 그래서 옛 자리를 가리키고 있던 것들이 한꺼번에 헛짚게 된다.

- **C++** — \`std::vector\` 가 자라면 그 전에 얻어 둔 포인터·참조·반복자가 전부 무효가 된다.
- **Java / C#** — \`ArrayList\` / \`List<T>\` 는 참조를 통해 접근하므로 사용자는 이동을 보지 못하지만, 늘어나는 순간의 복사 비용은 그대로 치른다.
- **Go** — \`append\` 가 용량을 넘기면 새 배열이 잡히고, 옛 슬라이스와 새 슬라이스가 서로 다른 곳을 가리키게 된다.

용량을 미리 잡아 두는 수단 (\`reserve\` / \`make([]T, 0, n)\`) 이 있는 것은 이 이동을 피하기 위해서다.
`;
