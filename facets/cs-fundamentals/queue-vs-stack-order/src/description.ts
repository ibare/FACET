/**
 * 탐색이 쓰는 그릇 학습 설명 — 단일 마크다운.
 *
 * `{facet:queueVsStackOrder}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 *
 * 조각이 화면에 각주를 두지 않으므로 (S-piece) 전제를 밝히는 것은 이 글의 일이다:
 * 화면이 두 탐색을 나란히 돌리는 것은 견주기 위한 연출이라는 것, 그리고 이웃을
 * 번호 오름차순으로 본다는 것.
 */

export const queueVsStackOrderDescription = `## 어디로 갈지는 그릇이 정한다

**그래프 탐색의 뼈대는 짧다.** 아직 안 가 본 정점을 그릇에 담아 두었다가, 하나 꺼내 방문하고, 그 정점의 이웃 중 처음 보는 것을 다시 그릇에 담는다. 그릇이 빌 때까지 이것만 되풀이한다.

이 뼈대에는 "다음에 어디로 갈지" 를 정하는 대목이 없다. 그 결정은 통째로 그릇에 맡겨져 있다 — 담아 둔 것 중 **어느 것을 먼저 내주느냐** 가 곧 방문 순서이기 때문이다.

{facet:queueVsStackOrder}

### 같게 둔 것과 다르게 둔 것

위 그림에서 그래프는 하나다. 출발점도 하나이고, 이웃을 보는 순서도 양쪽 모두 번호가 작은 것부터로 같다. 알고리즘의 글자도 같다. 다르게 둔 것은 그릇에서 꺼내는 자리 하나뿐이다.

- 먼저 넣은 것을 **앞에서** 꺼내면 (큐) 방문 순서는 1 → 2 → 3 → 4 → 5 → 6 이다.
- 나중에 넣은 것을 **위에서** 꺼내면 (스택) 방문 순서는 1 → 3 → 6 → 2 → 5 → 4 가 된다.

앞의 것은 출발점에서 가까운 것부터 한 겹씩 훑는 너비 우선 탐색(BFS)이고, 뒤의 것은 한 줄기를 끝까지 따라 내려갔다가 되돌아오는 깊이 우선 탐색(DFS)이다. 두 이름은 서로 다른 두 알고리즘의 이름이 아니라, **같은 뼈대에 서로 다른 그릇을 끼운 결과**의 이름이다.

### 나란히 돌린 것은 견주기 위해서다

실제 탐색은 한 번에 하나만 돈다. 화면이 두 갈래를 같은 걸음에 맞춰 함께 돌리는 것은 갈리는 순간을 한눈에 보이기 위한 연출이다. 두 줄기는 둘째 칸에서 이미 어긋나고, 그 뒤로 다시 만나지 않는다.

한 가지 더. 스택 쪽은 이웃을 번호 오름차순으로 넣지만 꺼낼 때는 나중에 넣은 것부터이므로, 결과적으로 **한 정점의 이웃을 번호가 큰 쪽부터** 따라간다. 1 의 이웃 2 와 3 을 그 순서로 넣었는데 3 이 먼저 나오는 것이 그것이다. 재귀로 쓴 깊이 우선 탐색이 1 → 2 → 4 → 5 → 3 → 6 으로 도는 것과 순서가 다른 이유도 여기에 있다 — 재귀는 호출 순서가 곧 방문 순서라 뒤집히지 않는다. 둘 다 깊이 우선이지만, 스택을 손으로 쓰면 형제 사이의 순서가 뒤집힌다.

### 그래서 무엇을 고르는가

찾는 것이 출발점 근처에 있을 법하거나 최단 거리가 필요하면 큐를 쓴다 — 가까운 것을 먼저 훑으므로, 처음 닿은 경로가 곧 간선 수 기준의 최단 경로다. 끝까지 파고들어야 알 수 있는 것 (사이클이 있는지, 위상 순서가 무엇인지, 미로에 출구가 있는지) 이라면 스택을 쓴다. 그릇을 바꾸는 것이 알고리즘을 바꾸는 일이다.
`;
