/**
 * HashTable 학습 설명 — 단일 마크다운.
 *
 * `{facet:hashTable}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const hashTableDescription = `## 해시 테이블 (Hash Table — 분리 체이닝)

**키를 결정적 규칙으로 슬롯 배열의 한 자리에 던져 넣어 평균 한 번의 점프로 값을 찾도록 만든 자료구조.** 키 공간이 슬롯 공간보다 크므로 두 키가 같은 자리로 떨어지는 충돌은 정상 작동의 일부이며, 그 자리에 사슬을 늘여 함께 둠으로써 흡수한다. 표가 차오를수록 충돌이 잦아지므로 적재율이 임계를 넘으면 표를 키우고 모든 키를 새 함수로 다시 던지는 리해싱이 일어난다.

{facet:hashTable}

### 핵심 아이디어

해시 테이블의 본질은 **결정적 매핑** 과 **충돌 흡수** 다.

- 사람이 키만 건네면 함수 \`h(k)\` 가 슬롯 인덱스를 정한다. 같은 키는 항상 같은 자리로 떨어진다.
- 키 공간 ≫ 슬롯 공간이므로 두 키가 같은 자리에 떨어지는 일은 막을 수 없다 — 그 자리에 작은 **사슬** 을 늘여 함께 둔다.
- 검색은 두 단계다 — \`h(k)\` 로 한 자리를 정하고, 그 자리의 사슬 안에서 키를 비교한다. 사슬이 짧으면 평균 **O(1)**.
- 표가 답답해지면 (적재율 α = N/M 이 임계를 넘으면) 표 자체를 키우고 모든 키를 새 함수로 다시 던진다.

### 결정적 순간 — 같은 자리에 둘이 떨어진다

값 14, 7, 25, 11, 5, 19, 6 의 일곱 키를 차례로 흘리면, h(k) = k mod 11 의 결정적 매핑은 14 와 25 를 모두 인덱스 3 으로 보낸다.

1. **14 도착**: 빈 셀 3 에 첫 키가 안착. 셀이 진한 테두리로 점유 상태가 된다.
2. **25 도착**: 함수 박스가 다시 인덱스 3 을 출력하고, 그 셀 테두리가 충돌 색으로 한 번 강하게 깜빡인다. 셀 아래로 두 번째 노드가 한 칸 늘어진다.
3. **나머지 다섯 키**: 7, 11, 5, 19, 6 은 각각 다른 인덱스로 흩어진다. 분포는 "대부분 자리는 사슬 1, 한 자리만 2" 의 모양으로 정착한다.

**다른 칸은 그대로 있다 — 변한 것은 충돌 자리의 사슬 한 칸뿐.** 이 한 컷이 "같은 자리에 둘이 떨어지는 일이 정상 작동의 일부" 라는 약속을 시각으로 운반한다.

### 검색의 두 단계

| 단계 | 운동 | 비용 |
| --- | --- | --- |
| 1. 점프 | 함수 박스가 한 자리를 가리킨다 | O(1) |
| 2. 비교 | 그 자리의 사슬을 위→아래로 짚는다 | O(사슬 길이) |

평균 사슬 길이 ≈ α 이므로 평균 검색 비용은 **O(1 + α)** — α 가 작게 유지되면 사실상 한 번의 점프와 거의 같다.

### 적재율과 리해싱

- α = N / M. 표가 채워질수록 충돌 확률이 올라간다.
- α < 0.5 — 안전 구간 (안정 색).
- 0.5 ≤ α < 0.75 — 주의 구간 (주의 색). 사슬이 자라기 시작한다.
- α ≥ 0.75 — 경고 구간. 다음 삽입에서 **리해싱** 이 발화한다.
- 리해싱: 표 크기 M 을 (보통 다음 소수로) 키우고, 모든 키를 새 함수로 다시 던진다. 같은 키도 새 인덱스를 받는다 — 해시는 표 크기에 의존하기 때문이다.

### 다른 자료구조와의 구별

- **배열** — 사람이 인덱스를 직접 지정한다. 화살표가 어느 셀에든 자유롭게 꽂힌다. 해시 테이블은 항상 함수 박스를 통과해서만 셀에 닿는다.
- **이진 탐색 트리** — 키를 비교하며 좌·우 가지로 깊이 내려간다. 해시 테이블은 한 번의 점프로 자리에 도착하고 짧은 사슬만 살핀다 — 트리는 세로로 깊고, 해시 테이블은 가로로 평평하다.
- **연결 리스트** — 머리에서 꼬리까지 한 줄. 해시 테이블은 머리 자리가 M 개이고 각 자리에서 사슬이 짧게 늘어난다 — "긴 한 줄" 과 "짧은 줄 여러 개" 의 모양 차이가 평균 접근 속도의 차이를 운반한다.

### 분리 체이닝 vs 개방 주소법 (선형 탐사)

같은 충돌은 다른 메모리 형태로도 흡수된다. 화면 우측 보조 영역이 같은 입력을 두 모양으로 동시에 보여준다.

- **분리 체이닝** — 충돌 자리에서 **아래로 사슬이 늘어진다**. 셀의 가로 위치는 인덱스, 세로 위치는 사슬 안 순서.
- **선형 탐사** — 사슬 없이 같은 가로 줄에서 **옆으로 비집고 전진** 한다. 다음 빈 칸이 그 키의 자리.

본 시각의 베이스는 분리 체이닝이다. 우측 보조 도식은 형상의 대비만 운반한다.

### 언제 쓰나

- **키로 값을 자주 호명할 때** — 평균 O(1) 의 약속이 그대로 활용된다.
- **순서가 중요하지 않을 때** — 해시 테이블은 키의 정렬을 보존하지 않는다.
- **메모리에 여유가 있을 때** — 적재율을 낮게 유지할수록 평균 비용이 줄어든다.

반대로, 정렬·범위 질의가 잦거나 최악 비용을 보장해야 한다면 균형 잡힌 트리 (예: 적흑 트리) 가 더 어울린다.
`;
