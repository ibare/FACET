/**
 * 접두사 공유 학습 설명 — 단일 마크다운.
 *
 * `{facet:sharePrefixPath}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const sharePrefixPathDescription = `## 앞글자가 같으면 길도 같다

**낱말을 한 글자씩 넣을 자리를 만들다가, 이미 난 자리와 글자가 같으면 새로 만들지 않고 그 자리를 그대로 쓴다.** 글자가 갈라지는 첫 자리에서만 새 가지가 돋는다. 이 구조를 트라이(trie)라고 부른다.

{facet:sharePrefixPath}

### 넷을 넣고 아홉만 든다

car · cart · cat · dog 넉 자를 순서대로 넣는다. car 를 넣을 때는 c·a·r 세 자리가 전부 새로 난다. cart 를 넣을 때는 이미 난 c·a·r 를 그대로 타고 t 하나만 새로 낸다. cat 은 c·a 를 타고 t 하나만 새로 낸다. dog 는 car 와 앞글자가 겹치지 않으니 d·o·g 세 자리가 새로 난다.

넉 자의 글자 수를 그냥 더하면 3+4+3+3 = 열셋이다. 하지만 실제로 만들어진 자리는 뿌리를 포함해 아홉뿐이다 — 넷을 아꼈다. 낱말이 쌓일수록 새로 나는 자리는 점점 줄어든다.

### 어디에 쓰이나

- **자동완성** — 사용자가 입력한 글자로 시작하는 낱말을 트라이 한 번의 하강으로 전부 찾는다.
- **라우팅 테이블** — IP 접두사(prefix)를 트라이에 넣어 가장 긴 일치를 빠르게 찾는다.
- **맞춤법 검사** — 사전에 있는 낱말인지 글자 하나씩 확인하며 없는 자리에서 바로 멈춘다.
`;
