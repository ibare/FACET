/**
 * 조각은 코드 패널을 두지 않는다 — 한 주장만 말하고 멈추는 물건이라 여섯 언어로
 * 갈라 보일 산출물이 없다 (S-piece). 파일은 S-facet 의 6파일 구성을 지키기 위해
 * 자리만 지킨다.
 */

import type { IR } from '@ffacet/core/runtime';

export const relaxShorterPathIRs: IR[] = [];
