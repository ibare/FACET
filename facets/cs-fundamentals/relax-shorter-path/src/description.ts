/**
 * 완화(relaxation) 조각의 학습 설명 — 단일 마크다운 (i18n 미적용).
 *
 * 본문 안의 `{facet:relaxShorterPath}` 토큰은 호스트(Tiptap NodeView) 가
 * 시각화 노드로 치환해 인라인 마운트한다.
 *
 * 화면에 각주를 두지 않는 대신, 그림이 깔고 있는 전제를 여기서 밝힌다 (S-piece).
 */

export const relaxShorterPathDescription = `## 완화 (relaxation)

최단 경로를 찾는 알고리즘은 정점마다 **"지금까지 알아낸 가장 짧은 거리"** 를 하나씩 적어 두고 다닌다. 이 수는 답이 아니라 **잠정치** 다. 더 짧은 길이 나타나면 적어 둔 수를 지우고 낮은 수로 다시 적는데, 이 한 번의 갈아 끼우기를 **완화(relaxation)** 라고 부른다.

{facet:relaxShorterPath}

### 한 번도 올라가지 않는다

완화는 \`d[v] > d[u] + w(u,v)\` 일 때만 일어난다. 조건이 부등호 한쪽 방향이므로 적힌 수는 **내려가기만 한다.** 위 그림에서 A 는 7 로 적혔다가 5 로, C 는 8 로 적혔다가 6 으로 내려간다. 지운 자리에 남은 취소선과 그 아래로 난 자국이 전부 아래를 향하는 것이 이 성질이다.

- **처음 적는 일** — 아직 아무 수도 없던 정점에 값이 생긴다. 견줄 상대가 없으니 비교가 아니다.
- **내려가는 일** — 이미 적힌 수보다 짧은 길이 나타나 갈아 끼운다. 이것만이 완화다.
- **그대로 두는 일** — 후보가 더 짧지 않으면 아무것도 지우지 않는다. 그림에서 B 를 거쳐 C 로 가는 길(2 + 9 = 11)이 그렇다. 버려진 후보 11 은 적힌 수 8 보다 **위**에 취소선이 그어진 채 남는다.

### 전제

- 간선의 무게는 **음이 아니다.** 음의 간선이 있으면 "가장 작은 수를 이고 있는 정점부터 편다" 는 순서가 무너지고, 한 번 확정한 수가 나중에 또 내려갈 수 있다.
- 그림이 정점을 고르는 순서는 **지금까지 적힌 수가 가장 작은 것부터** 다 — 다익스트라가 쓰는 순서다. 완화 자체는 순서와 무관한 연산이며, 벨만-포드는 같은 완화를 모든 간선에 대해 되풀이한다.
- 세로 자는 거리에 **비례하는 눈금** 이다. 패가 붙은 높이가 곧 그 정점에 적힌 수이며, 자 위쪽 ∞ 칸은 아직 아무 수도 적히지 않았다는 뜻이다.
- 정점 넷과 간선 여섯은 한 번의 순회에서 세 사건이 모두 나오도록 골라 놓은 작은 예다 — 처음 적히고, 내려가고, 더 짧지 않아 그대로 두는 것. 실제 그래프에서는 한 정점의 수가 여러 번 내려갈 수 있다.
`;
