/**
 * 인접 리스트 vs 인접 행렬 학습 설명 — 단일 마크다운.
 *
 * `{facet:adjacencyListVsMatrix}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const adjacencyListVsMatrixDescription = `## 이웃을 목록으로 두는가, 표로 두는가

그래프를 코드로 옮기는 방법은 크게 둘이다. **인접 리스트**는 정점마다 이웃의
목록을 들고, **인접 행렬**은 정점 × 정점 크기의 표를 만들어 이웃이면 1, 아니면
0 을 채운다. 둘 다 같은 그래프를 담지만, 담는 방식이 다르면 자리를 쓰는 방식도
달라진다.

{facet:adjacencyListVsMatrix}

### 자라는 자리와 잡아 둔 자리

간선이 하나 생길 때마다 인접 리스트는 목록에 칸을 하나 붙여 **자란다** — 든
만큼만 자리를 쓴다. 인접 행렬은 정점 수의 제곱만큼 칸을 **처음부터 다 잡아
둔다** — 간선이 하나도 없어도 표는 이미 그 크기다. 정점이 다섯이면 스물다섯
칸을 예약해 두고, 다섯 개의 간선만 1로 채운다.

### 같은 물음이 다른 값을 낸다

"A 와 E 는 이웃인가?" — 인접 리스트는 A 의 목록을 처음부터 훑어야 답할 수
있다. 인접 행렬은 \`matrix[A][E]\` 한 칸만 보면 끝난다. 이 물음에서는 행렬이
싸다.

"A 의 이웃을 모두 대라" — 인접 리스트는 이미 A 의 이웃만 모아 둔 목록을 그대로
읽으면 된다. 인접 행렬은 A 행 전체(정점 수만큼)를 다 훑어야 누가 이웃인지
알 수 있다. 이 물음에서는 리스트가 싸다.

### 그래서 고르는 기준

어느 쪽이 더 나은 자료구조라는 뜻이 아니다. **무엇을 자주 묻느냐**가 고른다.
그래프가 성기고(간선이 적고) 이웃을 나열하는 질문이 잦으면 인접 리스트가
유리하고, 두 정점의 연결 여부를 반복해서 확인해야 하면 인접 행렬이 유리하다.
`;
