/**
 * depth-doubles-count 학습 설명 — 단일 마크다운 (i18n 미적용).
 *
 * 본문 안의 `{facet:depthDoublesCount}` 토큰은 호스트(Tiptap NodeView) 가
 * 시각화 노드로 치환해 인라인 마운트한다.
 */

export const depthDoublesCountDescription = `## 깊이가 하나 늘면 자리는 두 배

이진 트리에서 한 층 내려가는 일은 자리를 **하나 더** 얻는 일이 아니라 **두 배로**
얻는 일이다. 위 층의 자리마다 아래로 둘씩 달리기 때문이다. 층은 하나씩 늘어나는데
자리는 곱으로 늘어난다.

{facet:depthDoublesCount}

### 두 개의 수

깊이 \`d\` 인 층의 자리는 \`2^d\` 이고, 0층부터 \`d\` 층까지의 합은 \`2^(d+1) - 1\` 이다.

| 층 | 그 층의 자리 | 그때까지의 합 |
| --- | --- | --- |
| 0 | 1 | 1 |
| 1 | 2 | 3 |
| 2 | 4 | 7 |
| 3 | 8 | 15 |
| 4 | 16 | 31 |

### 뒤집어 읽기

이 표를 아래에서 위로 읽는 것이 더 쓸모 있다. 자리가 많아도 **층은 얼마 안 된다.**
자리 1023개를 담아도 층은 9층까지다 (\`2^10 - 1 = 1023\`). 균형 잡힌 트리를 훑는 일이
싼 까닭이 여기 있다 — 훑는 것은 자리가 아니라 층이다.

### 그림이 말하지 않는 것

그림은 자리의 크기와 간격을 층마다 똑같이 두고 축척을 줄이지 않는다. 그래서 4층
(16자리) 까지는 줄이 화면에 온전히 들어오지만 5층부터는 양옆으로 넘쳐 나간다.
잘려 보이는 것이 사실이다 — 자리 수는 오른쪽 눈금이 계속 말해 준다.

또 그림은 **가득 찬** 트리를 보인다. 실제 트리는 층이 차다 말 수 있고, 한쪽으로만
자란 트리는 자리 \`n\` 개에 층이 \`n-1\` 이 된다. 층이 적다는 이득은 균형이 지켜질
때만 얻는다.

### 언제 쓰면 안 되는가

가지가 둘이 아닌 트리에는 그대로 적용되지 않는다. 가지가 \`k\` 개면 층마다 \`k\` 배가
되고, 그때 층은 \`log_k n\` 으로 더 얕아진다. "두 배" 는 이진일 때의 수다.
`;
