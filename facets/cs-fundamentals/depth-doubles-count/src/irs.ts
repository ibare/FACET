/**
 * depth-doubles-count IR.
 *
 * 조각은 코드 패널을 두지 않는다 — 답하는 것은 "깊이 하나에 자리 두 배" 라는
 * 사실 하나이지 절차가 아니다. 그래서 IR 은 비어 있다.
 */

import type { IR } from '@ffacet/core/runtime';

export const depthDoublesCountIRs: IR[] = [];
