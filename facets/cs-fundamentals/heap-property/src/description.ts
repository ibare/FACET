export const heapPropertyDescription = `
## 힙 성질

{facet:heapProperty}

힙이 지키는 약속은 하나뿐이다 — **부모가 자식보다 앞선다.** 나무 전체를 한 번에
줄 세우라는 말이 아니다. 부모-자식으로 이어진 짝 하나하나만 그 순서를 지키면
된다.

이 그림의 나무는 \`3, 5, 8, 9, 6, 12, 10\` 일곱 값을 담은 최소 힙이다. 부모-자식
짝은 여섯 — (3,5) (3,8) (5,9) (5,6) (8,12) (8,10) — 이고 전부 부모가 작다.

그런데 형제끼리는 한 번도 견주지 않는다. 9 와 6 은 둘 다 5 의 자식인데
9 > 6 이다. 12 와 10 도 둘 다 8 의 자식인데 12 > 10 이다. 배열을 정렬해 두었다면
있을 수 없는 배치인데, 힙에서는 아무 문제가 아니다 — 애초에 그 둘을 견줄
약속이 없었기 때문이다.

그래서 이 나무를 왼쪽부터 훑으면 \`9, 5, 6, 3, 12, 8, 10\` 이 나온다. 정렬과는
전혀 다른 순서다. 힙이 보장하는 것은 "가장 작은 값이 꼭대기에 있다" 는 사실
하나뿐이고, 나머지 값들이 어디에 있는지는 정해지지 않는다.
`.trim();
