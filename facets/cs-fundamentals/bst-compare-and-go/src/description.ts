/**
 * bst-compare-and-go 학습 설명 — 단일 마크다운.
 *
 * `{facet:bstCompareAndGo}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const bstCompareAndGoDescription = `## 비교 한 번, 가지 하나

**이진 탐색 트리는 값을 찾을 때 전체를 훑지 않는다.** 노드 하나와 값을 견주면 어느 쪽에 답이 있는지 바로 정해지고, 반대쪽 가지는 통째로 후보에서 빠진다. 그래서 노드가 일곱이어도 몇 걸음 안에 찾는다.

{facet:bstCompareAndGo}

### 버린 쪽은 지워지지 않는다

가지를 버린다는 것은 화면에서 지운다는 뜻이 아니다. 옅어진 채로 자리에 남아 "더 이상 후보가 아니다"만 표시한다. 커서는 실제로 그 노드에서 다음 노드로 내려간다 — 한 층씩, 비교가 끝날 때마다.

### 왜 빠른가

각 비교가 남은 후보를 대략 절반으로 줄인다. 노드 천 개짜리 트리라도 균형이 잡혀 있으면 열 걸음 안팎이면 찾는다. 배열을 처음부터 훑는 것과 다른 점이 이것이다 — 훑지 않고, 매번 절반을 버린다.
`;
