/**
 * 배열 기반 트리 학습 설명 — 단일 마크다운.
 *
 * `{facet:arrayAsTree}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const arrayAsTreeDescription = `## 한 줄과 한 그루, 같은 것의 두 모습

**나무를 그리는 데 나무가 필요하지 않다.** 노드를 만들고 왼쪽·오른쪽 자식을 가리키는 줄을 잇는 대신, 값을 한 줄로 늘어세운 배열 하나면 충분하다. 자리 번호가 곧 그 값이 나무의 어디에 있는지를 말해 주기 때문이다.

{facet:arrayAsTree}

### 번호가 자리를 셈한다

규칙은 셋뿐이다. 자리 \`i\` 의 왼쪽 자식은 \`2i+1\`, 오른쪽 자식은 \`2i+2\`, 부모는 \`⌊(i−1)/2⌋\`. 뿌리(자리 0)에서 시작해 이 셈만 반복하면 나무의 어느 자리든 오갈 수 있다 — 자식 쪽으로 곱하고 더하며 내려가고, 부모 쪽으로 빼고 나누며 올라간다.

칸을 하나 짚으면 그 값이 나무의 어느 자리에 있는지가 곧바로 드러나는 것도 이 셈 덕분이다. 배열 자리와 나무 자리는 서로 다른 두 개의 데이터가 아니라, 같은 자리를 보는 두 가지 방식일 뿐이다.

### 아낀 것

칸이 일곱이면 값도 일곱 개다. 잇는 줄(포인터)은 하나도 없다. 같은 나무를 노드로 만들어 이었다면, 자리마다 왼쪽·오른쪽 두 줄이 필요해 열넷이 들었을 것이다. 번호로 셈해 건너갈 수 있다면, 애초에 줄을 저장해 둘 이유가 없다.

이 대가로 나무는 촘촘해야 한다 — 중간에 빈 자리가 많으면 그 자리도 칸을 차지하므로 배열이 헐렁해진다. 힙(heap)이 이 방식을 쓰는 이유가 여기 있다. 힙은 언제나 빈틈없이 채워진 완전 이진 트리이므로, 낭비 없이 이 셈만으로 전부를 오갈 수 있다.
`;
