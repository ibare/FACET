/**
 * 가중 그래프의 경로 길이 학습 설명 — 단일 마크다운.
 *
 * `{facet:fewerHopsNotShorter}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const fewerHopsNotShorterDescription = `## 적게 거친다고 짧은 길은 아니다

**간선을 몇 개 밟느냐와 얼마나 먼가는 다른 물음이다.** 지도를 눈으로 훑을 때 우리는 갈아타는 횟수가 적은 쪽을 짧은 길로 읽는다. 간선마다 무게가 붙는 순간 그 짐작은 무너진다.

{facet:fewerHopsNotShorter}

그림은 두 길을 같은 출발선에 눕히고, **가로 길이가 곧 재고 있는 값**이 되게 한다. 처음에는 간선 하나가 모두 같은 길이라 간선을 둘만 밟는 길이 먼저 끝난다. 그 다음 간선마다 제 무게만큼 늘이고 줄이면 도착점이 자리를 바꾼다.

도착 정점은 원 둘로 그려져 있지만 같은 정점이다. 길마다 끝나는 자리가 달라야 길이를 견줄 수 있어 둘로 그렸다. 출발 정점을 원이 아니라 세로 막대로 둔 것도 같은 까닭이다 — 두 길이 정확히 같은 자리에서 출발해야 비교가 거짓이 되지 않는다.

### 그래서 무엇이 달라지나

- **너비 우선 탐색**은 간선 수를 재는 알고리즘이다. 무게가 모두 같을 때만 최단 경로를 준다.
- **다익스트라**가 따로 있는 이유가 이것이다. 무게가 붙으면 "적게 거친 길" 을 먼저 확정할 수 없고, 지금까지의 무게 합이 가장 작은 정점부터 확정해 나가야 한다.
- 무게가 음수면 그 확정마저 흔들린다. 이미 확정한 정점이 나중에 더 싸질 수 있어 다익스트라의 전제가 깨진다.

### 무게는 거리만이 아니다

간선의 무게는 킬로미터일 수도, 요금일 수도, 지연 시간일 수도 있다. 무엇을 무게로 삼느냐가 곧 무엇을 짧다고 부를 것이냐다. 갈아타는 횟수를 줄이고 싶다면 무게를 전부 1로 두면 되고, 그때는 간선 수가 다시 정답이 된다.
`;
