/**
 * LruCache 학습 설명 — 단일 마크다운.
 *
 * `{facet:lruCache}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const lruCacheDescription = `## LRU 캐시 (Least Recently Used Cache)

**용량 제한이 있는 키-값 저장소 위에 "가장 최근에 사용된 순서" 라는 추상을 새겨, 모든 호출이 해당 노드를 MRU 끝으로 끌어올리고 capacity 가 꽉 찬 상태에서 새 키가 들어오면 LRU 끝의 노드를 두 영역에서 동기 소멸시키는 자료구조.** 위쪽 hash map 은 키 → 노드 포인터의 빠른 조회를, 아래쪽 doubly linked list 는 사용 순서를 담당하며, **두 영역은 같은 노드를 공유** 한다. 이 결합 덕분에 get / put 모두 평균 **O(1)** 에 끝난다.

{facet:lruCache}

### 핵심 아이디어

LRU 캐시의 본질은 **두 좌표계의 결합** 과 **get 의 쓰기성 부수효과** 다.

- 위쪽 영역 — **hash map**: 키 → 노드 포인터. \`O(1)\` 조회로 노드 참조를 즉시 얻는다.
- 아래쪽 영역 — **doubly linked list**: 왼쪽 끝이 LRU, 오른쪽 끝이 MRU. 노드의 prev / next 포인터로 임의 노드를 \`O(1)\` 에 unlink / 재삽입할 수 있다.
- 두 영역은 **같은 노드를 공유** 한다. hash 의 값이 곧 list 노드 포인터라는 invariant 가 화면 내내 유지된다.
- **get 도 단순 조회가 아니다** — 노드를 MRU 끝으로 끌어올리는 쓰기성 행위다. "읽기인데 쓰기" 라는 부수효과가 LRU 의 정체성을 만든다.

### 결정적 순간 A — promotion (hit 또는 기존 키 갱신)

호출이 들어오면 해당 노드가 list 위 자기 자리에서 풀려나 MRU 끝으로 끌려 올라간다.

1. **hash 슬롯 점등**: 키 슬롯이 점등되며 list 노드를 가리키는 lookup 화살표가 한 번 깜빡인다. 두 좌표계가 같은 노드를 호명하는 순간이다.
2. **unlink + 떠오름**: 노드가 양쪽 이웃과의 연결을 끊고 살짝 떠오른다.
3. **promotion 호**: 부드러운 호 (arc) 를 그리며 MRU 끝으로 이동해 재삽입된다. 위쪽 hash 연결선이 함께 따라가며 끊기지 않는다.

**get 과 put 의 promotion 단계는 동일 시각 언어로 처리된다** — "읽기인데 쓰기" 의 부수효과가 한눈에 잡힌다.

### 결정적 순간 B — eviction (capacity 초과 put)

capacity 게이지가 꽉 찬 상태에서 새 키 put 이 들어오면, **LRU 끝 노드가 list 와 hash 양쪽에서 동시에 사라진다.**

1. **capacity 초과 시도**: 게이지가 "초과 시도" 로 한 박자 멈춘다. 여유 → 꽉 참 → 초과 시도 의 세 단계 임계가 색으로 분리된다.
2. **동기 소멸**: LRU 끝 노드가 아래로 떨어져 사라지고 같은 순간 위쪽 hash 슬롯도 비워진다. 두 영역의 동기 소멸이 한 프레임으로 압축된다.
3. **새 노드 삽입**: 새 노드가 MRU 끝에 삽입되며, 비워진 hash 슬롯 자리에 새 키 슬롯이 점등된다.

### 다른 캐시 정책과의 구별

- **FIFO 캐시** — 노드는 들어온 순서로만 정렬되고 promotion 호가 없다. get 호출이 list 를 흔들지 않는다. LRU 의 결정적 순간 A (get 의 부수효과로 인한 노드 이동) 가 FIFO 에는 없다.
- **LFU 캐시** — 정렬축이 "최근성" 이 아니라 "빈도" 다. 노드 옆에 빈도 카운터가 붙고 promotion 은 카운트 증가에 따른 재정렬이다. LRU 의 가로축 일관성 (왼쪽 LRU → 오른쪽 MRU) 이 LFU 에서는 다중 버킷 그림으로 분해된다.
- **일반 doubly linked list** — hash map 영역 자체가 없다. 위쪽 좌표계가 부재하므로 두 좌표계 결합이라는 본질이 사라진다.
- **일반 hash map** — 아래쪽 list 영역이 없다. "최근성" 축이 부재하므로 promotion / eviction 의 시각 언어 자체가 정의되지 않는다.

### 시간 / 공간 복잡도

| 연산 | 평균 | 메모리 |
| --- | --- | --- |
| get | O(1) | hash 조회 + list unlink/재삽입 |
| put (기존 키) | O(1) | 값 갱신 + promotion |
| put (새 키, 여유) | O(1) | hash 삽입 + list 추가 |
| put (새 키, 꽉 참) | O(1) | hash 삭제·삽입 + list 양 끝 갱신 |

전체 공간 **O(capacity)** — 두 영역이 같은 노드를 공유하므로 노드 수만큼만 메모리를 쓴다.

### 언제 쓰나

- **최근성 가설** 이 강할 때 — 한 번 본 키가 곧 다시 호출될 가능성이 높은 워크로드 (예: 페이지 캐시, CDN edge cache).
- **용량이 한정된 in-memory 저장소** — 무한 성장이 불가능하고 어떤 키를 버릴지 정책으로 결정해야 할 때.
- **삽입·조회·삭제 모두 평균 O(1)** 이 필요한 곳.

반대로, 빈도가 더 중요한 분포 (자주 호출되는 키가 따로 있는 경우) 라면 LFU 가 더 어울린다.
`;
