/**
 * 주소 계산 학습 설명 — 단일 마크다운.
 *
 * `{facet:indexAddressCalc}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const indexAddressCalcDescription = `## 번호로 자리를 셈한다

**arr[3] 을 꺼낼 때 컴퓨터는 칸을 세며 가지 않는다.** 번호에 원소 크기를 곱하고 기준 주소를 더하면 그 자리가 바로 나온다. 곱셈 한 번, 덧셈 한 번이 전부다.

{facet:indexAddressCalc}

int32 배열이 0x1000 에 놓였다면 원소 하나는 4바이트를 차지하므로 자리는 이렇게 정해진다.

\`\`\`
addr(i) = 0x1000 + i × 4
\`\`\`

arr[3] 은 0x1000 + 12 = 0x100C, arr[5] 는 0x1000 + 20 = 0x1014 다. 번호가 커져도 셈의 횟수는 늘지 않는다 — 사이의 칸을 들르지 않기 때문이다. 배열의 임의 접근이 O(1) 이라는 말은 이 한 줄의 다른 이름이다.

세 가지가 갖춰져야 이 셈이 성립한다.

- **기준 주소** — 배열이 시작하는 자리. 할당이 정한다.
- **원소 크기** — 자료형이 정한다. int32 는 4바이트, double 은 8바이트다.
- **같은 크기로 붙어 있음** — 칸마다 크기가 다르거나 사이가 벌어져 있으면 곱셈이 성립하지 않는다.

연결 리스트가 임의 접근을 못 하는 이유도 여기 있다. 노드는 흩어져 있고 크기도 위치도 규칙이 없으므로, 셋째 노드에 닿으려면 첫째부터 이어진 참조를 따라가는 수밖에 없다. 배열의 빠름은 자료를 잘 정리해서가 아니라 **자리를 셈으로 알아낼 수 있게 놓았기 때문**이다.
`;
