/**
 * LinkedList 학습 설명 — 단일 마크다운.
 *
 * `{facet:linkedList}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const linkedListDescription = `## 연결 리스트 (Linked List)

**노드 각각이 자기 다음 한 명만 가리키는 단 하나의 손가락을 갖는 자료구조.** 어떤 위치에 닿으려면 머리(head)에서 출발해 손가락을 한 칸씩 따라가야 하고, 어떤 자리에 새 원소를 끼우거나 빼는 일은 노드를 옮기는 게 아니라 두세 개의 손가락을 끊고 다시 잇는 일이다. 끝은 NULL 이라는 명시적 표식으로 닫힌다.

{facet:linkedList}

### 핵심 아이디어

연결 리스트의 본질은 **지역 인식** 과 **재배선** 이다.

- 각 노드는 값과 next 한 칸만 안다. **어디에 있는지** 가 아니라 **다음이 누구인지** 만 안다.
- 그래서 \`insert(i, v)\` / \`remove(i)\` 는 **카드를 옮기지 않고** 화살표 두세 개만 끊고 다시 잇는다. **O(1)** — 단 그 자리에 닿은 다음에는.
- 반대로 \`access(i)\` 는 head 에서 출발해 손가락을 i 번 따라가야 한다. **O(N)**.

### 결정적 순간 — 두 화살표가 동시에 변하지 않는다

값 10, 20, 30 의 세 카드가 head → 10 → 20 → 30 → NULL 로 이어진 상태에서, 인덱스 2 자리에 새 값 25 를 끼우는 두 단계 운동.

1. **단계 1 (350ms)**: 새 카드 25 의 next 손가락이 호를 그리며 30 의 본체로 이동한다. 이 새 화살표만 강조색으로 떠오른다 (25 → 30 → NULL 의 임시 곁가지).
2. **휴지기 (200ms)**: 학습자가 "지금 1단계가 끝났다" 를 인식하는 정지.
3. **단계 2 (350ms)**: 20 의 next 손가락이 30 에서 떨어져 25 로 옮겨 끼워진다. 옛 화살표 (20 → 30) 는 점선으로 흐려지고, 새 화살표 (20 → 25) 가 강조색으로 떠오른다.

**카드 다섯 장은 모두 처음 그 자리에 그대로 있다 — 변한 것은 두 화살표뿐.** 표준 레퍼런스가 가장 약하게 다루는 본질, 즉 "재배선의 안전 순서" 가 이 두 단계 시간차로 강제된다.

### 주요 연산과 비용

- \`insert(i, v)\` / \`remove(i)\` — **위치 도달 O(N) + 재배선 O(1)**. 가운데 끼우기는 손가락 두 개를 차례로 갈아 끼우는 일이지, 카드를 옮기는 일이 아니다.
- \`insert-head(v)\` / \`remove-head()\` — **O(1)**. head 라벨이 한 칸 활주.
- \`access(i)\` / \`search(v)\` — **O(N)**. head 부터 한 칸씩.

### head 와 NULL — 입구와 끝

리스트로 들어가는 길은 head 하나뿐이다. 첫 카드 위에는 항상 굵은 head 라벨과 아래로 향한 화살표가 떠 있고, 마지막 카드의 next 칸 너머에는 빨간 ✕ 와 작은 NULL 글자가 붙는다. 빈 리스트일 때 head 라벨은 회색으로 흐려지고 화살표 끝에 ✕ 가 붙어 "닿을 곳이 없다" 가 시각적으로 표시된다.

### 다른 자료구조와의 구별

- **배열** — 같은 자리에 25 를 끼울 때 30 카드가 한 칸 우측으로 밀린다. 배열의 운동 주체는 카드, 연결 리스트의 운동 주체는 화살표.
- **이중 연결 리스트** — 두 줄짜리 손가락. 같은 두 카드 사이에 화살표가 위·아래 두 줄로 마주 본다. 단일 연결 리스트의 한 줄짜리 손가락은 "거꾸로 갈 수 없다" 의 일방향성으로 시각화된다.
- **원형 연결 리스트** — 마지막 next 가 NULL 대신 첫 카드를 향해 큰 호를 그린다. 단일 연결 리스트는 끝이 반드시 ✕ NULL 로 닫힌다.

### 언제 쓰나

- **삽입/삭제 위치를 이미 손에 쥐고 있을 때** — 재배선 O(1).
- **크기를 미리 알 수 없을 때** — 노드 단위 동적 할당.
- **무작위 접근이 드물 때** — head 부터 따라가는 비용이 문제되지 않는 경우.

반대로, 인덱스 호명이 잦거나 메모리 지역성이 중요하면 배열 / 동적 배열이 더 어울린다.
`;
