/**
 * 이진 트리 — 한 자리에서 아래로 둘. 조각과 함께 딸려 오는 글.
 *
 * `{facet:parentTwoChildren}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const parentTwoChildrenDescription = `## 한 자리에서 아래로 둘

**이진 트리는 한 자리에서 아래로 최대 둘까지만 뻗는 트리다.** 셋은 없다. 그리고 그 둘은 그냥 둘이 아니라 **왼쪽과 오른쪽**이라는 서로 다른 이름을 가진 자리다.

{facet:parentTwoChildren}

### 자식이 하나여도 자리는 정해진다

그림의 C 는 자식이 F 하나뿐이다. 그런데도 F 는 "C 의 자식" 이 아니라 **"C 의 오른쪽 자식"** 이다. 왼쪽 자리는 비어 있는 채로 그대로 남아 있고, 그 빈자리는 없는 것이 아니라 **비어 있는 것**이다.

그래서 F 를 왼쪽으로 옮기면 같은 트리가 아니라 **다른 트리**가 된다. 노드 여섯이 그대로여도 자리가 달라졌기 때문이다. 이것이 이진 트리가 그냥 "자식이 둘 이하인 트리" 와 갈리는 지점이다 — 일반 트리에서 자식은 순서 없는 묶음이지만, 이진 트리에서 자식은 **이름 붙은 두 자리**다.

### 그림이 보이는 것

- 노드 여섯 (A~F), 가지 다섯, 잎 셋 (D · E · F)
- 높이 둘 — 뿌리 A 를 0 으로 세었을 때 가장 깊은 잎까지의 걸음 수
- 자리마다 열리는 두 칸 중 채워지지 않은 쪽은 점선으로 남는다

### 왜 자리를 고정하는가

자리가 이름을 가지면 **위치가 곧 정보**가 된다. 탐색 이진 트리는 "작으면 왼쪽, 크면 오른쪽" 이라는 약속으로 그 자리에 뜻을 싣고, 그 덕분에 한 번 비교할 때마다 한쪽 전체를 보지 않고 넘어간다. 자리를 바꿀 수 있었다면 그 약속이 성립하지 않는다.
`;
