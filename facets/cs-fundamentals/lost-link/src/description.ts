/**
 * 연결 유실 학습 설명 — 단일 마크다운.
 *
 * `{facet:lostLink}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const lostLinkDescription = `## 고리를 놓치면 뒤가 사라진다

연결 리스트에서 **한 노드로 가는 길은 그 노드를 가리키는 화살표뿐이다.** 배열이라면 몇 번째인지만 알면 언제든 짚을 수 있지만, 연결 리스트에는 그런 지름길이 없다. 앞에서부터 화살표를 따라가는 것 말고는 방법이 없으니, 화살표 하나가 옮겨 가는 순간 그 뒤쪽 전부와의 연이 끊긴다.

{facet:lostLink}

### 노드는 사라지지 않는다

떨어져 나간 노드들은 여전히 메모리에 있고, 저희끼리는 멀쩡히 이어져 있다. 다만 바깥에서 들어갈 문이 없을 뿐이다. 이 상태가 언어마다 다르게 끝난다 — 가비지 컬렉터가 있는 언어에서는 조용히 회수되고, 직접 해제해야 하는 언어에서는 **해제할 주소조차 잃어버린 채** 그대로 새는 메모리가 된다. 어느 쪽이든 데이터는 되찾을 수 없다.

### 그래서 차례가 전부다

삽입은 화살표 두 개를 다루는 일이고, 둘의 순서가 결과를 가른다.

- **새 노드부터 뒤에 잇는다** — 이 동안 원래 화살표는 그대로라 뒤쪽은 여전히 붙들려 있다.
- **그 다음에 앞의 화살표를 옮긴다** — 옮기는 순간 이미 다른 화살표가 뒤를 붙들고 있으므로 끊기는 구간이 없다.

거꾸로 하면 앞 화살표를 옮긴 그 한순간에 뒤쪽을 붙드는 것이 아무것도 없다. **끊긴 시간이 아무리 짧아도 되돌릴 방법이 없다는 것이 요점이다.** 잃어버린 뒤에 찾으려 해도 그 노드를 가리키는 것이 하나도 남아 있지 않다.

같은 이유로 삭제도 순서를 탄다. 지울 노드의 앞을 먼저 뒤에 이어 붙이고 나서 지워야 하며, 지우고 나서 이으려 하면 이을 곳을 이미 잊은 뒤다.

### 임시 변수 하나면 된다

실무 코드가 삽입 전에 \`const next = node.next\` 같은 줄을 먼저 쓰는 이유가 이것이다. **화살표를 옮기기 전에 뒤쪽 주소를 손에 쥐어 두는 것** — 잃어버릴 길을 미리 하나 더 만들어 두는 셈이다. 리스트를 뒤집는 코드가 포인터 세 개(\`prev\`, \`curr\`, \`next\`)를 들고 도는 것도 같은 대비다.
`;
