export const maxFlowDescription = `# 최대 유량 — 에드몬즈-카프

관이 얽힌 망이 있다. 관마다 굵기(용량)가 정해져 있고, 들어오는 곳에서 나가는
곳으로 한 번에 얼마까지 보낼 수 있는지를 묻는다.

{facet:maxFlow}

## 욕심만으로는 닿지 않는다

빈 관을 아무 길이나 골라 가득 채우기를 되풀이하면 어떻게 될까. 어떤 길을 먼저
골랐느냐에 따라 뒤에 남는 길이 막힌다. 앞서 흘린 것이 잘못 놓인 것이었다면
그것을 무를 방법이 있어야 하는데, "이미 흘린 것" 을 되돌린다는 개념이 없으면
거기서 끝이다.

## 되돌릴 폭

풀이의 전부가 이 한 줄이다.

\`\`\`
cap[u][v] = cap[u][v] - f
cap[v][u] = cap[v][u] + f     ← 되돌릴 폭이 생기는 자리
\`\`\`

\`u\` 에서 \`v\` 로 \`f\` 만큼 흘리면 그 방향 여유가 \`f\` 줄고, **반대 방향에 \`f\`
만큼의 폭이 생긴다.** 나중에 찾은 길이 \`v → u\` 로 지나가면 그것은 새 물을
보내는 것이 아니라 앞서 \`u → v\` 로 흘린 것을 그만큼 밀어내는 일이다. 무르기가
길 찾기와 같은 문법으로 표현되는 것이다.

그래서 화면의 관은 하나의 띠이고 그 안이 두 갈래로 나뉘어 있다. 위쪽은 이쪽
방향으로 남은 여유, 아래쪽은 저쪽 방향으로 남은 여유. 두 갈래의 합은 절대
변하지 않는다 — 빼는 만큼 더하기 때문이다. **흘린다는 것은 경계선이 미끄러지는
것**이고, 강조색으로 칠한 몫이 되돌릴 수 있는 폭이다.

## 왜 깊이 우선이 아니라 너비 우선인가

여유가 남은 길이면 아무거나 잡아도 답에는 닿는다. 다만 몇 번 만에 닿느냐가
용량의 크기에 매인다 — 용량이 클수록 오래 걸릴 수 있다. 에드몬즈-카프는 **언제나
관을 가장 적게 지나는 길** 을 고른다. 그러면 되풀이 횟수가 용량과 무관해지고
정점·간선 수만으로 묶인다.

코드 패널에 큐가 배열 하나와 색인 둘로 펼쳐져 있는 것은 그 때문이다.
\`head\` 가 앞에서 꺼내고 \`tail\` 이 뒤에 붙이므로 먼저 발견한 것이 먼저 나온다.
\`head\` 를 \`tail - 1\` 로 바꾸면 그대로 깊이 우선이 되고, 그 한 글자가 보증을
잃게 만든다.

## 언제 멈추는가

여유가 남은 길을 하나도 못 찾으면 멈춘다. 그때 닿은 값이 최대라는 것은
**최대 유량 최소 절단** 정리가 보증한다 — 마지막 탐색에서 닿은 정점 무리와 못
닿은 무리 사이를 자르면, 그 면을 건너는 관들이 모두 가득 차 있다. 그 면의 용량
합보다 많이 보낼 길은 애초에 없으므로 지금 값이 상한이고, 동시에 달성된 값이니
최대다.

이 망에서는 그 면이 \`{0, 1, 2, 4}\` 와 \`{3, 5}\` 사이이고, 건너는 관은
\`1 → 3\` (12) · \`4 → 3\` (7) · \`4 → 5\` (4) 셋이다. 합이 **23** 이고 알고리즘이
셈한 값과 같다.

## 이 망에서는 되돌림이 한 번도 쓰이지 않는다

솔직히 적어 둔다. 이 망에 에드몬즈-카프를 돌리면 세 길
(\`0→1→3→5\` 12 · \`0→2→4→5\` 4 · \`0→2→4→3→5\` 7) 을 지나며 되돌릴 폭이 계속
쌓이지만, 그 폭을 **타고 되돌아가는 길은 한 번도 나오지 않는다.** 위 화면에서
강조색이 늘기만 하고 줄지 않는 것이 그것이다.

까닭은 너비 우선이다. 관을 가장 적게 지나는 길부터 고르므로, 뒤에 갈 길을 막는
자리에 먼저 들어차는 일이 이 망에서는 생기지 않는다. 이웃을 훑는 차례를 여섯
정점의 어떤 순서로 바꿔 봐도 마찬가지다 — 720가지 중 되돌릴 폭을 쓰는 경우가
하나도 없다.

## 그런데도 그 두 줄이 있어야 한다

길을 고르는 차례가 바뀌면 곧바로 필요해지기 때문이다. 짧은 길부터가 아니라 아무
길이나 잡는 순간, 가운데를 가로지르는 길에 먼저 들어차서 남은 길이 막히는 경우가
나온다. 그때 되돌릴 폭이 없으면 거기서 멈추고 **답이 모자란 채로 끝난다.** 그
장면만 떼어 놓은 것이 아래다.

{facet:undoByBackEdge}

앞으로 난 화살만 따라가면 넷에서 막히고, 가운데로 흘린 것을 되돌리는 폭을 타야
여섯에 닿는다. 위 화면이 되돌릴 폭이 **생기는** 것까지 보인다면, 저 조각은 그것이
**쓰이는** 것을 보인다.

## 답이 맞아 버려서 살아남는 실수

한 가지 더 적어 둔다. 이 망에서는 \`cap[v][u] = cap[v][u] + f\` 를 아예 지워도
결과가 **23 으로 똑같이 나온다.** 되돌릴 폭을 한 번도 안 쓰니 없어도 답이 같다.

이런 종류의 실수가 제일 오래 산다. 검사가 값만 보면 통과하고, 통과했으니 아무도
다시 안 보고, 망이 바뀐 어느 날 조용히 모자란 답을 낸다. **답이 맞는 것과 코드가
맞는 것은 다르다.** 그래서 이 facet 은 값이 아니라 코드를 보인다 — 아래 패널에서
\`- f\` 와 \`+ f\` 가 나란히 서 있는 것을 눈으로 확인하는 편이, 23 이라는 숫자를
확인하는 것보다 많은 것을 보증한다.
`;
