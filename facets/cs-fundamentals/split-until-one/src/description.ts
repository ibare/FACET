/**
 * splitUntilOne 설명 — 조각과 함께 딸려 오는 글.
 *
 * 화면에 각주를 두는 대신 전제를 여기서 밝힌다 (S-piece).
 */

export const splitUntilOneDescription = `
## 가르는 동안에는 아무것도 풀리지 않는다

"분할 정복" 이라는 말을 처음 들으면 분할이 무언가를 푸는 일처럼 들린다.
반으로 나누면 문제의 절반이 해결될 것 같다. 그렇지 않다.

{facet:splitUntilOne}

값 넷이 한 묶음에서 시작해 낱개 넷이 될 때까지 세 층을 내려간다. 그동안
일어난 일을 하나씩 짚어 보자.

- **견줌이 없다.** 6 과 2 를 비교한 적이 없고, 8 이 4 보다 큰지 확인한 적도 없다.
  가르는 자리는 값이 아니라 **자리 수**가 정한다 — 구간의 가운데다.
- **값이 움직이지 않는다.** 화면의 세로줄은 처음부터 끝까지 같은 자리에 있다.
  왼쪽에서 오른쪽으로 읽으면 6 · 2 · 8 · 4 이고, 마지막 층에서도 6 · 2 · 8 · 4 다.
- **바뀌는 것은 경계뿐이다.** 한 묶음이 둘이 되고 둘이 넷이 된다. 늘어난 것은
  묶음의 개수이지 정렬된 정도가 아니다.

그래서 분할이 끝난 시점의 배열은 시작할 때의 배열과 **완전히 같다.** 실제로
값을 옮기는 일은 그다음, 합치는 단계에서 일어난다.

## 왜 낱개에서 멈추는가

더 가를 수 없어서가 아니라 — 물론 그것도 맞지만 — **가를 이유가 없어서**다.

값이 하나뿐인 묶음은 그 자체로 이미 줄이 서 있다. 크기 1 짜리 배열은
정렬 여부를 물을 것도 없이 정렬되어 있다. 분할 정복이 재귀를 멈추는 바닥은
"가장 작은 조각" 이 아니라 "답을 이미 아는 조각" 이고, 정렬에서는 그 둘이
마침 같다.

화면에서 마지막 층의 상자마다 점이 하나씩만 남는 것이 그 뜻이다. 점이 둘이면
그 사이에 가를 자리가 있지만, 점 하나에는 없다.

## 전제

- 길이가 4 라 층이 셋(1 → 2 → 4)으로 딱 떨어진다. 길이가 2 의 거듭제곱이
  아니면 한쪽이 한 칸 더 큰 묶음이 생기고, 층 수는 \`ceil(log2 n) + 1\` 이 된다.
  가르는 방식 자체는 달라지지 않는다.
- 화면의 점은 "항목이 여기 하나 있다" 는 표시일 뿐 값의 크기가 아니다. 이 조각은
  크기를 말하지 않으므로 크기를 그리지 않는다.
`.trim();
