/**
 * splitUntilOne 의 IR — 없다.
 *
 * 조각은 코드 패널을 두지 않는다. 한 주장만 말하고 멈추는 화면에 소스 코드가
 * 붙으면 읽을 것이 둘이 된다 (S-piece). 파일 자체는 S-facet 의 6파일 구성을
 * 지키기 위해 남긴다.
 */

import type { IR } from '@ffacet/core/runtime';

export const splitUntilOneIRs: IR[] = [];
