/**
 * negativeEdgeBreaks 학습 설명 — 단일 마크다운 (i18n 미적용).
 *
 * 본문 안의 `{facet:negativeEdgeBreaks}` 토큰은 호스트(Tiptap NodeView) 가
 * 시각화 노드로 치환해 인라인 마운트한다.
 */

export const negativeEdgeBreaksDescription = `## 음수 간선이 확정을 깨뜨린다

가장 가까운 것부터 굳히는 최단 경로 방법(다익스트라)은 한 가지 약속 위에 서 있다. **한 정점을 굳히는 순간, 그 거리는 다시는 줄지 않는다.** 그 약속이 성립하려면 앞으로 더할 간선이 거리를 늘리기만 해야 한다. 간선 무게가 모두 0 이상일 때는 참이다. 음수 간선이 하나라도 있으면 거짓이 된다.

{facet:negativeEdgeBreaks}

### 화면에서 일어나는 일

정점 넷과 방향 간선 넷이다. S→A 는 3, S→B 는 4, B→A 는 −2, A→T 는 1. 출발은 S, 도착은 T.

1. S 를 0 으로 굳히고 이웃을 편다. A 는 3, B 는 4.
2. 아직 굳지 않은 것 중 가장 가까운 A(3) 를 굳힌다. A→T 를 펴서 T 는 4.
3. B(4) 를 굳히고 B→A 를 편다. 4 + (−2) = 2 — A 에 붙어 있는 3 보다 짧다. **그런데 A 는 이미 굳었다.** 굳은 자리는 값을 고치지 않기로 한 자리이므로 2 를 받지 않는다.
4. 받지 않았으니 A 밖으로도 나가지 못한다. 2 가 A 에 들어갔다면 A→T 를 타고 3 이 되어 T 를 고쳤을 텐데, 그 소식은 A 에서 막힌다. T 는 4 로 남는다.
5. 마지막으로 T 를 4 로 굳히고 끝난다.

참 최단은 S→B→A→T 로 **3** 이다. 화면에 남은 4 는 틀린 답이고, 이 조각이 보이려는 것이 바로 그 남은 수다. 되돌아가 고칠 기회가 없다는 것이 확정의 값이자 대가다.

### 왜 이렇게 되는가

굳히는 순서는 "지금까지 알려진 거리" 로 정한다. 음수 간선은 **나중에 도착한 길이 앞선 길보다 짧아질 수 있게** 만들어, 그 순서를 믿을 수 없게 한다. 잘못은 음수 그 자체가 아니라 **순서를 믿고 확정한 것** 에 있다.

### 그럼 무엇을 쓰는가

음수 간선이 있으면 확정을 미루는 방법을 쓴다. 벨만-포드는 모든 간선을 정점 수만큼 되풀이해 펴므로, 뒤늦게 짧아진 길도 다음 바퀴에 반영된다. 대신 한 번에 굳히지 않으므로 더 느리다. 음수 **순환** 이 있으면 최단 거리 자체가 정의되지 않는데, 벨만-포드는 그 사실도 검출한다.

### 전제

- 이 그래프에는 음수 순환이 없다. 순환이 있으면 참 최단이 아예 존재하지 않아 견줄 대상이 사라진다.
- 화면의 "참 최단" 은 같은 데이터를 벨만-포드로 다시 세어 얻은 값이다. 미리 적어 둔 정답이 아니다.
- 굳히는 차례가 같은 값으로 겹치면 정점을 적어 둔 차례를 따른다. 어느 쪽을 먼저 굳혀도 이 그래프의 결말은 4 로 같다.
`;
