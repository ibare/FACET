/**
 * 조각은 코드 패널이 없다. 한 주장만 말하고 멈추는 그림이라 IR 을 두지 않는다
 * (S-piece). 빈 배열이지만 파일은 6파일 구성을 지키기 위해 남긴다 (S-facet).
 */

import type { IR } from '@ffacet/core/runtime';

export const negativeEdgeBreaksIRs: IR[] = [];
