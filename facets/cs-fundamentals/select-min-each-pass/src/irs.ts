/**
 * selectMinEachPass IR 목록.
 *
 * 조각은 코드 패널을 두지 않는다 — 한 주장만 말하고 멈추는 그림이므로
 * 곁들일 소스가 없다. 그래서 빈 배열이다 (S-piece · S-facet 6파일 구성).
 */

import type { IR } from '@ffacet/core/runtime';

export const selectMinEachPassIRs: IR[] = [];
