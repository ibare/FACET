/**
 * selectMinEachPass 학습 설명 — 단일 마크다운 (i18n 미적용).
 *
 * 본문의 `{facet:selectMinEachPass}` 토큰은 호스트(Tiptap NodeView) 가
 * 시각화 노드로 치환해 인라인 마운트한다.
 */

export const selectMinEachPassDescription = `## 한 바퀴를 다 돌고 나서야 집는다

앞의 정렬 방식들을 보고 나면 "견줄 때마다 뭔가 일어난다" 는 인상이 남는다.
인접한 둘을 견주고 어긋나 있으면 그 자리에서 바꾸니까, 견줌 하나가 곧 이동
하나처럼 느껴진다.

최솟값 선택은 그렇지 않다. 훑는 눈길과 별개로 **"지금까지 가장 작았던 자리"
표식** 이 따로 있고, 견줌은 그 표식을 옮길지 말지만 정한다. 값은 건드리지
않는다. 한 바퀴를 다 훑은 뒤에야 비로소 한 번 옮긴다.

{facet:selectMinEachPass}

칸 넷을 훑는 동안 견줌은 세 번 일어나지만 표식은 한 번만 건너가고, 값이
자리를 바꾸는 것은 마지막의 한 번뿐이다. 훑는 켜와 기억하는 켜와 값이 있는
켜가 따로 그려져 있는 것은 그래서다 — 위쪽 레일에 남은 자국의 수와 아래에서
일어난 일의 수가 다르다는 것이 이 그림이 하는 말 전부다.

### 그래서 무엇이 달라지나

- **견줌은 n² 에 비례하고, 이동은 n 에 비례한다.** 한 바퀴마다 이동은 최대 한 번뿐이다.
- 값을 옮기는 비용이 견주는 비용보다 훨씬 클 때 — 원소가 큰 덩어리일 때 — 이 맞바꿈이 이득이 된다.
- 반대로 이미 거의 정렬된 입력이어도 견줌 횟수는 줄지 않는다. 조기 종료가 없다.

여기 그려진 것은 **한 바퀴**다. 남은 칸으로 같은 일을 되풀이하면 전체가 정렬된다.
`;
