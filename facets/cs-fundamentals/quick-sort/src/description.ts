/**
 * quick-sort 완결형의 글.
 *
 * `{facet:quickSort}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const quickSortDescription = `
## 퀵 정렬

기준 하나를 정하고, 그보다 작은 것을 왼쪽으로 큰 것을 오른쪽으로 몬다. 그러고
나면 기준은 **자기 최종 자리**에 앉아 있다. 아직 양쪽은 뒤죽박죽이지만 그 한
칸만은 끝난 것이다. 남은 두 덩이를 같은 방법으로 다시 가르면 된다.

{facet:quickSort}

## 가르기는 맞바꿈만으로 된다

여벌 배열을 두고 작은 것과 큰 것을 나눠 담으면 쉽다. 그런데 그러면 자리를 배열
하나만큼 더 쓴다. Lomuto 방식은 **경계 하나**를 끌고 다니는 것으로 그 비용을
없앤다.

경계 \`i\` 는 "여기까지가 작은 쪽" 이라는 표시다. 훑는 자리 \`j\` 가 기준보다
크지 않은 값을 만나면 경계를 한 칸 밀고, 경계가 새로 가리킨 칸과 \`j\` 를
맞바꾼다. 큰 값을 만나면 아무것도 하지 않고 지나간다 — 그 값은 이미 경계
바깥, 즉 큰 쪽에 있기 때문이다. 다 훑고 나면 경계 바로 다음 칸과 맨 뒤의
기준을 맞바꾼다. 그것이 기준의 자리다.

경계가 훑는 자리와 같을 때는 맞바꾸는 시늉만 난다 — 자기 자신과 바꾸는 것이라
값이 움직이지 않는다. 코드에는 그 줄이 그대로 있지만 화면의 맞바꿈 수는
실제로 옮겨진 것만 센다.

## 기준을 어떻게 고르느냐가 값을 가른다

여기서는 늘 **맨 뒤 칸**을 기준으로 삼는다. 가장 단순하고, 경계 하나로 펼쳐
쓰기에도 좋다. 대신 값이다.

화면의 기록을 보면 오른쪽 구간이 \`[3..6]\` → \`[3..5]\` → \`[3..4]\` 로 **한
칸씩만** 줄어드는 대목이 나온다. 절반씩 갈라지리라는 기대와 다르다. 기준이
그 구간에서 가장 큰 값이면 왼쪽이 전부가 되고 오른쪽이 비기 때문이다. 이런 일이
계속되면 가르는 횟수가 자리 수만큼 늘어난다.

가운데 값을 고르거나 셋 중 가운데를 고르는 방법들이 이 쏠림을 줄이려는 것이다.
알고리즘의 뼈대는 그대로 두고 **기준을 고르는 한 줄만** 바꾸면 된다.
`;
