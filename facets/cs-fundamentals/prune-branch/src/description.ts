/**
 * 가지치기 학습 설명 — 단일 마크다운.
 *
 * `{facet:pruneBranch}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const pruneBranchDescription = `## 가 보지 않는다

되짚어 가며 답을 찾는 탐색은 갈림길마다 두 갈래로 갈라진다. 갈림길이 넷이면 끝자리가 열여섯, 거쳐 가는 자리를 다 세면 서른하나다. 갈림길이 스물이면 백만이 넘는다 — 다 밟아 보는 것은 곧 불가능해진다.

**가지치기는 그 나무의 일부를 잘라 내는 일이다.** 어떤 자리에 서서, 그 아래를 하나도 보지 않고 발길을 돌린다.

{facet:pruneBranch}

### 왜 잃는 것이 없나

여기가 이 개념의 전부다. 가지치기는 "대충 건너뛰기" 나 "빨리 끝내는 요령" 이 아니다. 접기 전에 **그 아래에 답이 없다는 것이 증명되어 있어야** 한다.

위 그림의 판정은 셈 하나다 — \`지금까지의 합 > 목표\`. 이것이 참이면 그 아래에서 무엇을 더 고르든 합은 커지기만 한다. 따라서 목표에 다시 닿을 길이 **없다**. 없는 것이 확인된 곳을 안 보는 것이므로, 접힌 뒤에도 답의 집합은 접기 전과 정확히 같다.

접는 조건이 이렇게 단단해야 하는 이유는 반대 경우를 생각해 보면 분명하다. 만약 고를 수에 음수가 섞여 있다면 합이 목표를 넘었다가도 다시 내려올 수 있다. 그러면 \`합 > 목표\` 는 더 이상 증명이 아니라 짐작이 되고, 접는 순간 답을 잃는다. **그림이 값을 전부 양수로 둔 것은 이 전제를 지키기 위해서다.**

### 조건 위반과 셈의 차이

접는 근거는 크게 둘로 갈린다.

- **이미 어겼다** — 위 그림처럼 지금 상태가 규칙을 벌써 깨뜨렸다. 여왕들이 서로 잡는 자리, 이미 지나온 도시, 넘겨 버린 예산. 판정이 값싸고 확실하다.
- **가 봐야 손해다** — 아직 아무것도 어기지 않았지만, 그 아래에서 얻을 수 있는 **가장 좋은 결과를 내다본 값**이 지금 손에 든 최선보다 못하다. 분기한정(branch and bound) 이 이쪽이며, 내다보는 셈(한계값) 이 얼마나 날카로운지가 성능을 가른다.

둘 다 "답을 잃지 않는다" 는 조건 위에서만 성립한다. 내다본 값이 실제보다 나쁘게 잡히면 살아 있는 답을 잘라 버리게 되므로, 한계값은 **낙관적인 쪽으로 틀려야** 한다.

### 얼마나 줄어드나

가지치기는 최악의 경우를 바꾸지 못한다. 아무 가지도 접히지 않는 입력이 언제나 존재하므로 상한은 여전히 지수다. 바뀌는 것은 **실제로 만나는 입력에서의 크기**다. 위 그림에서는 서른하나 중 열여섯이 사라졌지만, 갈림길이 깊어질수록 한 번 접을 때 사라지는 양이 곱절로 커진다 — 위에서 접을수록 이득이 크다.

그래서 실전에서는 접히기 쉬운 순서로 후보를 정렬해 두는 일이 함께 간다. 큰 수부터 시도하면 합이 일찍 목표를 넘어 위쪽에서 가지가 닫히고, 그만큼 잘려 나가는 나무가 커진다.
`;
