/**
 * 조각과 함께 딸려 가는 글. 화면이 말하지 않는 전제는 여기서 밝힌다 (S-piece).
 */

export const countThenPlaceDescription = `# 세어서 놓기

정렬은 흔히 "견주는 일" 로 배운다. 두 값을 나란히 놓고 어느 쪽이 큰지 물어
자리를 바꾸는 일 말이다. 그래서 견줌으로 정렬하는 방식은 아무리 잘 짜도
n log n 번의 견줌 아래로는 내려갈 수 없다는 하한이 따라붙는다.

그런데 값의 범위가 좁으면 견줌 없이도 자리가 정해진다.

{facet:countThenPlace}

화면은 세 걸음으로 말한다.

1. **센다.** 왼쪽부터 값을 하나씩 읽어, 그 값의 열에 눈금을 하나 쌓는다. 읽는
   것은 값 하나뿐이고 다른 값은 보지 않는다.
2. **굳는다.** 다 세고 나면 눈금 더미를 앞에서부터 더한다. 앞선 값들이 차지할
   자리 수가 곧 그 값의 시작 자리 번호다. 세로로 쌓였던 더미가 그대로 누워
   결과 배열의 구역이 된다 — 눈금 하나가 자리 하나다.
3. **놓는다.** 다시 왼쪽부터 값을 집어 제 번호로 곧장 보낸다. 하나 앉을 때마다
   그 값의 번호가 하나씩 오른다.

세 걸음 어디에도 값끼리 견주는 연산이 없다. 그래서 견줌 횟수의 하한이 이
방식에는 적용되지 않는다. 대신 다른 값을 치른다 — **값의 종류만큼 자리를 미리
잡아 두어야 한다.** 값이 0·1·2 셋뿐이라 열이 셋으로 끝났지만, 값이 0부터
10억까지라면 열도 그만큼 필요하다. 세어서 놓기가 값싼 것은 범위가 좁을 때뿐이다.

그림은 여섯 개의 값과 세 종류로 보이고 있다. 값을 읽는 순서가 정해져 있고 같은
값끼리는 읽은 순서 그대로 앞자리에 앉으므로, 이 방식은 같은 값의 앞뒤 순서를
흐트러뜨리지 않는다.
`;
