/**
 * 적재율과 재해싱 학습 설명 — 단일 마크다운.
 *
 * `{facet:loadFactorRehash}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 * 조각은 화면에 각주를 두지 않으므로, 전제(어떤 해시를 썼고 자리를 어떻게
 * 셈했는지)는 여기서 밝힌다 (S-piece).
 */

export const loadFactorRehashDescription = `## 판이 차면 넓힌다

해시 테이블은 칸이 정해진 판이다. 담기는 것이 늘수록 한 칸에 둘 이상이 몰릴 확률이 오르고, 몰리기 시작하면 "한 번에 찾는다" 는 성질이 무너진다. 그래서 **얼마나 찼는지**를 재는 수를 하나 둔다.

\`\`\`
적재율 = 담긴 수 / 버킷 수
\`\`\`

이 값이 임계를 넘으면 판을 넓힌다. 자바의 \`HashMap\` 은 임계가 0.75 이고, 넘으면 버킷 수를 두 배로 늘린다.

## 넓히는 것으로 끝이 아니다

여기가 걸려 넘어지는 대목이다. 판을 넓혔으면 담긴 것들을 새 판으로 옮겨야 하는데, **옛 자리를 그대로 베껴 오는 것이 아니다.** 자리는 나누기의 나머지로 정해지고, 나누는 수가 바뀌었으니 나머지도 다시 셈해야 한다.

{facet:loadFactorRehash}

여섯 중 셋은 자리가 바뀌고 셋은 그대로다. 그대로 남은 것들이 옮겨지지 않아서 그런 것이 아니라, **다시 셈했더니 마침 같은 수가 나왔기** 때문이다.

## 왜 절반쯤은 그대로인가

버킷 수를 두 배로 늘리는 방식에서는 그 비율이 우연이 아니다. 16 으로 나눈 나머지는 8 로 나눈 나머지에 비트가 하나 더 붙은 값이다.

\`\`\`
h % 16  =  (한 비트)  +  h % 8
\`\`\`

새로 붙는 그 비트가 0 이면 자리는 그대로고, 1 이면 정확히 여덟 칸 뒤로 간다. 해시가 고르게 퍼져 있다면 그 비트도 반반이라, 대략 절반은 남고 절반은 옮겨간다. 위 그림에서 판을 여덟 칸씩 두 줄로 접어 그린 것이 이 때문이다 — 열은 옛 나머지고, 줄은 새로 붙은 비트다.

## 값이 싸지 않다

재해싱은 담긴 것 전부를 다시 셈하는 일이라 O(n) 이다. 넣기 한 번이 어쩌다 아주 비싸진다는 뜻이고, 그래서 해시 테이블의 삽입은 **평균** O(1) 이라고 말한다. 담을 개수를 대강 안다면 처음부터 그만한 판을 잡아 두는 편이 낫다.

### 그림의 전제

- 해시는 자바 \`String.hashCode()\` 의 실측값이다 (\`kiwi\` = 3292336, \`banana\` = -1396355227 등).
- 자리는 \`(hashCode & 0x7FFFFFFF) % 버킷수\` 로 셈했다. 최상위 비트를 떨어뜨려 음수를 없애는 것이며, 화면의 계산식에 보이는 수는 그 마스킹을 거친 값이다.
- 한 칸에 여럿이 몰리는 경우(충돌)는 다루지 않는다. 여섯 키가 겹치지 않도록 고른 예다.
`;
