/**
 * memoWriteOnce 학습 설명 — 단일 마크다운 (i18n 미적용).
 *
 * 본문 안의 `{facet:memoWriteOnce}` 토큰은 호스트(Tiptap NodeView) 가
 * 시각화 노드로 치환해 인라인 마운트한다.
 *
 * 조각이 전제하는 것 — 어떤 항을 어떤 순서로 부르는지, 표에 적지 않았다면
 * 몇 번을 불렀을지 — 은 화면이 아니라 이 글이 밝힌다 (S-piece).
 */

export const memoWriteOnceDescription = `## 한 번만 적고, 그 뒤로는 읽는다

\`f(n) = f(n-1) + f(n-2)\` 를 정의 그대로 부르면 같은 항을 몇 번이고 다시 푼다.
메모이제이션은 거기에 표 하나를 놓는다. 답을 얻은 항은 표의 제 칸에 적어 두고,
같은 항을 다시 만나면 풀지 않고 읽는다.

{facet:memoWriteOnce}

화면에서 값은 두 방향으로 움직인다. 답이 나오면 그 값의 복제본이 **오른쪽으로**
건너가 표에 앉고, 이미 적힌 항을 만나면 값이 **왼쪽으로** 되돌아 나와 그 자리를
채운다. 원본은 늘 제자리에 남는다.

### 아끼는 것은 시간 한 조각이 아니다

값을 읽는 순간 그 아래로 뻗을 뻔한 가지가 통째로 사라진다. \`f(3)\` 을 표에서
읽으면 \`f(2)\`, \`f(1)\`, \`f(0)\` 으로 이어졌을 부분 나무 하나가 그대로 없어진다.
화면에서 표를 읽은 자리 아래가 텅 빈 것은 그림을 생략한 것이 아니라, 정말로
아무것도 부르지 않았기 때문이다.

\`f(5)\` 기준으로 실제로 푼 항은 여섯(0~5), 표에서 읽은 것은 세 번, 방문은 모두
아홉이다. 표를 두지 않았다면 같은 \`f(5)\` 가 호출 열다섯 번이다.

### 값을 치를 것

표가 차지하는 공간이 든다. 항이 \`n\` 개면 칸도 \`n\` 개다. 그리고 같은 항이
되풀이되지 않는 문제 — 부분 문제가 겹치지 않는 분할 정복 — 에서는 표가 한 번도
읽히지 않고 비용만 남는다. 메모이제이션이 값어치를 하려면 **부분 문제가 겹쳐야**
한다.
`;
