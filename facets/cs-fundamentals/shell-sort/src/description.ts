/**
 * shell-sort 완결형의 글.
 *
 * `{facet:shellSort}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const shellSortDescription = `
## 셸 정렬

삽입 정렬은 값을 하나 집어 들고 왼쪽으로 밀어 넣는다. 왼쪽 것이 더 크면 그것을
한 칸 오른쪽으로 비켜서게 하고, 그렇게 생긴 구멍을 향해 계속 왼쪽으로 간다.
자리가 나면 집은 값을 내려놓는다. 단순하고 대개 빠르지만 약점이 하나 있다 —
**한 걸음이 한 칸이라, 멀리서 온 값은 그 거리만큼 비켜섬을 만든다.**

셸 정렬은 그 걸음을 넓힌다. 처음에는 멀리 떨어진 것끼리 견주어 큰 어긋남을 몇
번의 이동으로 걷어 내고, 걸음을 좁혀 가다 마지막에 간격 1 — 곧 보통의 삽입
정렬 — 로 마무리한다. 그때는 이미 거의 정렬돼 있어 할 일이 적다.

{facet:shellSort}

## 코드는 삽입 정렬과 같다

코드 패널을 열어 보면 안쪽이 삽입 정렬 그대로다. 다른 것은 두 자리뿐이다.

- \`arr[j - 1]\` 이 \`arr[j - gap]\` 으로
- \`j = j - 1\` 이 \`j = j - gap\` 으로

\`gap\` 을 \`1\` 로 바꿔 읽으면 글자 하나 남김없이 삽입 정렬이다. 셸 정렬은 새
알고리즘이 아니라 **삽입 정렬의 상수 하나를 변수로 바꾼 것**이다. 바깥에
\`while gap > 0\` 과 \`gap = gap // 2\` 두 줄이 더 붙었을 뿐이다.

간격은 \`len(arr) // 2\` 에서 시작해 반씩 접는다. 값이 일곱이면 3 다음 1 이다.
정수 나눗셈이어야 하는 것에 주의한다 — \`gap\` 이 실수가 되면 \`arr[j - gap]\`
이 자리 번호가 아니게 된다.

## 한 라운드는 사슬 여럿을 각각 정렬한 것이다

간격 3 으로 도는 라운드가 실제로 하는 일은, 자리 0·3·6 과 1·4 와 2·5 라는 서로
떨어진 **세 사슬**을 각각 삽입 정렬하는 것이다. 사슬끼리는 서로를 건드리지
않는다. 화면에서 칸 아래 색 띠와 활 모양 선이 그 사슬을 묶고 있고, 지금 정렬
중인 사슬만 진하게 남는다.

사슬이 짧으니 각각의 일도 적다. 그런데 그 짧은 일들이 끝나면 줄 전체가 "대충
맞는" 상태가 된다 — 큰 값은 뒤쪽에, 작은 값은 앞쪽에. 그것이 마지막 라운드에
넘겨 주는 것이다.

## 앞 라운드는 낭비가 아니다

화면 아래 두 줄이 이 완제품의 주장이다. 위 줄은 간격 3 과 1 을 거쳐 온 이동
횟수이고, 아래 줄은 **같은 입력**을 처음부터 간격 1 만으로 했을 때의 이동
횟수다. 견줌은 양쪽 다 열넷으로 같은데 이동이 열하나에서 일곱으로 준다.

견줌이 같은 것이 오히려 요점이다. 간격 3 라운드가 쓴 견줌 다섯 번은 그냥
더해진 비용이 아니라, 마지막 라운드가 치렀어야 할 몫을 미리 가져간 것이다.
값이 클수록, 그리고 멀리서 온 값이 많을수록 이 차이가 벌어진다.

### 전제

- 여기 쓴 간격 수열은 절반씩 접는 가장 단순한 것이다. 어떤 수열을 쓰느냐로
  셸 정렬의 성능이 갈리며, 더 나은 수열들이 알려져 있다.
- 값이 일곱이라 차이가 작아 보인다. 같은 성질이 큰 배열에서 훨씬 크게 나타난다.
- 견줌 수가 양쪽에서 같은 것은 이 입력에서 그렇게 나온 것이지 일반적인 성질이
  아니다. 셸 정렬이 이기는 자리는 대개 이동 쪽이다.
`;
