/**
 * b-tree 완결형의 글.
 *
 * `{facet:bTree}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const bTreeDescription = `
## B-트리

한 자리에 키를 여럿 담고, 그 **키와 키 사이의 틈**마다 아래로 갈 길이 하나씩
열린다. 그래서 자리 하나가 갈림길 여럿을 품고, 가지가 넓은 만큼 나무의 키가
낮다. 자리를 한 번 밟는 것이 디스크를 한 번 읽는 일이라면, 낮은 키가 곧
적은 읽기다.

{facet:bTree}

넣을 때는 **내려가며 미리 쪼갠다.** 꽉 찬 자리를 만나면 지나가기 전에 가운데
키를 위로 올리고 나머지를 둘로 가른다. 그러면 되돌아 올라오며 고칠 일이 없다.
층이 느는 것은 뿌리를 쪼갤 때뿐이다.

## 줄어드는 쪽

지울 때가 더 재미있다. 자리에 키가 최소보다 적어지면 안 되므로, 내려가기 전에
그 자식에 여유를 만들어 둔다.

- **차용** — 옆 형제에게 여유가 있으면 하나 빌려 온다. 다만 형제에게서 곧장
  오는 것이 아니라 **부모를 거쳐** 온다. 부모의 키가 자식으로 내려가고, 형제의
  키가 그 빈 부모 자리로 올라간다.
- **병합** — 옆도 빠듯하면 부모의 키를 끌어내려 둘을 합친다. 부모의 키가 하나
  줄고 자식도 하나 준다.

병합이 이어지다 **뿌리가 비면** 그 하나뿐인 자식이 새 뿌리가 되고 나무의 키가
하나 준다. B-트리에서 층이 줄어드는 길은 이것 하나뿐이다.
`;
