/**
 * union-find 의 IR — 없다.
 *
 * 코드 패널을 두지 않았다. 이 완제품의 논증은 코드 한 벌이 아니라 **같은 조작을
 * 되풀이했을 때 값이 달라지는 것**이라, 코드 패널이 있으면 시선이 그쪽으로 새고
 * 화면의 두 HUD 가 하는 말이 묻힌다.
 *
 * find/union 의 코드 자체가 궁금하면 조각 셋(`findRoot` · `unionByRank` ·
 * `pathCompression`)이 각 걸음을 따로 보인다.
 */

import type { IR } from '@ffacet/core';

export const unionFindIRs: IR[] = [];
