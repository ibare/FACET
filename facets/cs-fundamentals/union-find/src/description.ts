/**
 * union-find 완결형의 글.
 *
 * `{facet:unionFind}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const unionFindDescription = `
## 유니온-파인드

자리마다 "내가 가리키는 곳" 하나만 적어 두면 무리를 나눌 수 있다. 자기가
자기를 가리키면 뿌리고, 그 뿌리가 곧 무리의 이름이다. 두 자리에서 각각 올라가
같은 이름에 닿으면 한 무리다.

{facet:unionFind}

합칠 때 어느 뿌리를 아래로 넣을지는 고를 수 있다. 아무렇게나 고르면 나무가
줄처럼 길어지고, 그러면 물을 때마다 그 줄을 끝까지 올라야 한다. **랭크**는
낮은 나무를 높은 나무 밑에 넣어 키가 늘지 않게 한다.

## 두 번 물으면 두 번째는 거의 공짜

**경로 압축**의 값어치는 한 번의 압축에 있지 않다. 뿌리를 찾아 올라간 김에
지나온 자리를 전부 뿌리에 곧장 붙여 두면, 그 길 위에 있던 자리 **전부가**
다음번에 한 칸이 된다. 물어본 자리 하나만이 아니다.

그래서 이 화면의 논증은 "모두 찾기" 를 **두 번 누르는 것**이다. 처음에는
평균 오름이 여러 칸이고, 두 번째에는 1 에 붙는다. 규칙 슬라이더를 "그냥" 으로
내리면 그 차이가 어디서 오는지 보인다 — 같은 짝을 같은 순서로 합쳤는데
나무의 키가 달라진다.
`;
