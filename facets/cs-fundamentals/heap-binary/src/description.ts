/**
 * heap-binary 완결형의 글.
 *
 * `{facet:heapBinary}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const heapBinaryDescription = `
## 이진 힙

힙은 **나무처럼 생각하고 배열에 담는** 자료구조다. 정해진 것은 하나뿐이다 —
부모가 자식보다 앞선다. 형제끼리는 아무 약속이 없어서, 왼쪽 잎이 오른쪽보다
클 수도 있다. 그래서 힙은 정렬이 아니고, **가장 앞선 값이 어디 있는지만**
안다.

{facet:heapBinary}

값을 넣으면 끝자리에 앉은 뒤 부모와 견주며 오른다. 앞서지 못하는 부모를
만나면 거기가 제 자리다 — 꼭대기까지 가는 일은 드물다. 꼭대기를 빼면 맨 끝
값이 그 자리에 올라와, 이번에는 두 자식 중 앞선 쪽과 견주며 내려간다.

## 한 번에 힙으로

값을 하나씩 넣어도 힙이 되지만, 이미 배열에 다 담겨 있다면 더 싼 길이 있다.
잎은 그 자체로 이미 힙이므로 건드릴 필요가 없고, **마지막 부모부터 거꾸로**
내려보내면 된다. 견줌 수를 보면 차이가 드러난다.

## 정렬이 딸려 나온다

꼭대기를 계속 빼내면 앞선 것부터 차례로 나온다. 뺀 값을 배열 뒤쪽의 빈자리에
그대로 두면 자리를 더 쓰지 않는다 — 힙이 앞에서 줄어드는 만큼 정렬된 꼬리가
뒤에서 자란다. 화면에서 그 경계가 움직이는 것을 볼 수 있다.
`;
