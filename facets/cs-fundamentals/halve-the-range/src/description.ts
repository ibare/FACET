/**
 * 구간 반분 학습 설명 — 단일 마크다운.
 *
 * `{facet:halveTheRange}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const halveTheRangeDescription = `## 절반을 버린다는 말의 셈

**"절반을 버린다" 는 비유가 아니다.** 줄이 선 값에서 가운데 하나를 찾는 값과 견주면, 그 한 번의 견줌만으로 나머지 절반은 볼 필요가 없어진다. 찾는 값이 가운데보다 크면 가운데와 그 왼쪽은 전부 작으므로 답이 될 수 없고, 작으면 가운데와 그 오른쪽이 전부 탈락한다. 어느 쪽이든 후보의 폭이 한 번에 반으로 준다.

{facet:halveTheRange}

### 몇이 한 번에 빠지는가

일곱에서 시작해 보자. 가운데는 넷째 자리다. 그것이 찾는 값보다 작으면 넷째와 그 왼쪽 셋, 그러니까 **네 개가 한꺼번에** 후보에서 빠진다. 남는 것은 셋이다. 다시 가운데를 짚어 그것이 답이면 나머지 둘도 함께 빠져 하나만 남는다.

7 → 3 → 1. 견줌은 두 번이었다. 하나씩 훑었다면 열한을 찾기까지 여섯 번을 견주어야 했다. 견줌 한 번이 지우는 것이 하나가 아니라 **남은 것의 절반**이라는 데서 차이가 생긴다.

### 왜 로그인가

폭이 매번 반으로 줄어드니, 폭이 1이 될 때까지 필요한 견줌의 수는 n 을 몇 번 반으로 접어야 1이 되는가와 같다. 그것이 log₂n 이다. 원소가 천 개면 열 번, 백만 개면 스무 번이다. 자료가 천 배로 늘어도 견줌은 열 번 더 늘 뿐이다.

### 이 셈이 기대는 전제

반분은 공짜가 아니다. **값이 줄을 서 있어야** 성립한다. 가운데 하나를 보고 한쪽을 통째로 버릴 수 있는 것은, 그 한쪽이 전부 가운데보다 작다(또는 크다)는 것을 순서가 보증해 주기 때문이다. 순서가 없으면 가운데를 본 결과는 그 자리 하나에 대해서만 말해 주고, 버릴 수 있는 것도 그 하나뿐이다.

또 하나 — 가운데 자리를 **단번에 짚을 수 있어야** 한다. 배열처럼 번호로 자리를 계산하는 자료라면 즉시 짚지만, 연결 리스트처럼 앞에서부터 따라가야 하는 자료라면 가운데를 찾는 데 이미 절반의 걸음을 쓴다. 그때는 반분의 이득이 사라진다.

이 그림은 찾는 값이 실제로 있는 경우를 보인다. 없는 경우에도 폭이 주는 방식은 같고, 폭이 0이 되는 순간 "없다" 가 답이 된다.
`;
