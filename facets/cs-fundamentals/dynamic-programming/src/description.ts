/**
 * dynamic-programming 완결형의 글.
 *
 * `{facet:dynamicProgramming}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const dynamicProgrammingDescription = `
## 동적 계획법

배낭에 담을 수 있는 무게가 정해져 있고 물건마다 무게와 값어치가 다르다. 어떤
조합이 가장 값진가. 물건이 넷이면 조합은 열여섯 가지라 다 세어 봐도 되지만,
서른이면 십억 가지가 넘는다.

동적 계획법은 세는 대신 **표를 채운다**. 칸 하나는 "물건 몇 개까지 쓸 수 있고
한도가 얼마일 때의 최적" 이고, 그 칸을 채우는 데는 **바로 윗줄의 두 칸**만 있으면
된다.

{facet:dynamicProgramming}

## 칸 하나는 윗줄 두 칸만 본다

지금 칸이 "물건 \`i\` 까지 쓰고 한도가 \`w\`" 라고 하자. 물건 \`i\` 를 놓고 두 갈래만
있다.

- **안 넣는다.** 그러면 물건 \`i-1\` 까지 쓴 것과 똑같다 — 윗줄 같은 자리 값 그대로다.
- **넣는다.** 그 무게만큼 자리를 비워야 하니 한도가 \`w - weight[i-1]\` 로 줄고,
  그 자리의 값에 이 물건의 값어치를 더한다 — 윗줄 왼쪽 어딘가의 값 + \`value[i-1]\`.

둘 중 큰 쪽이 이 칸의 답이다. 화면에서 곧은 화살이 앞의 것, 굽은 화살이 뒤의
것이다. 물건의 무게가 한도보다 크면 뒤쪽 갈래는 아예 없어서 화살이 하나만 온다.

## 되돌아가지 않는다

이 알고리즘에 재귀가 없다. 위에서 아래로, 왼쪽에서 오른쪽으로 한 번 훑으면
끝이고, 한 번 적은 칸은 다시 계산하지 않는다. 화면의 "채운 칸" 은 44 에서 멈추는데
그것이 표의 크기 그대로다 — 표를 다 채우는 데 표 크기만큼의 일만 든다.

같은 문제를 재귀로 풀면 같은 물음을 몇 번이고 되풀이하게 된다. 표가 하는 일은
**그 되풀이를 한 번으로 줄이는 것**이고, 그것이 이 방법의 전부다.

## 값은 알겠는데 무엇을 골랐는가

코드가 돌려주는 것은 마지막 칸의 수 하나다. 여기서는 90 이다. 어떤 물건을
골랐는지는 표에 이미 적혀 있다 — 각 칸이 **곧은 화살로 왔는지 굽은 화살로 왔는지**를
자국으로 남겨 두었으므로, 마지막 칸에서 자국을 따라 거슬러 오르면 된다.

90 에서 거슬러 오르면 물건 4 와 물건 2 가 나온다. 무게 3 + 4 = 7 이라 한도 10 에
여유가 셋 남는데도 그것이 최적이다. 남은 셋에 넣을 수 있는 물건이 없기 때문이다.

## 3 행이 말하는 것

셋째 물건은 무게 6 에 값 30 이다. 표의 3 행을 보면 2 행과 거의 같다 — 마지막
칸 하나만 70 으로 바뀌고 나머지는 그대로다. 무거우면서 값이 낮은 물건은 웬만한
한도에서 늘 밀린다. 그런 물건도 표는 빼놓지 않고 한 줄을 다 세는데, 그렇게
세어 봐야만 "밀린다" 는 것이 확인되기 때문이다.
`;
