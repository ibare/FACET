/**
 * BlackHeightEqual 학습 설명 — 단일 마크다운.
 *
 * `{facet:blackHeightEqual}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const blackHeightEqualDescription = `## 어느 길로 가도 검은 수는 같다

레드-블랙 트리는 "빨강 아래에 빨강이 없다"는 규칙 말고도 하나를 더 지킨다. **뿌리에서 어떤 잎(nil)까지 내려가도, 지나는 검은 자리 수가 같다.** 이 값을 흑색 높이(black-height)라 부른다.

{facet:blackHeightEqual}

### 길이는 달라도 셈은 같다

이 트리에는 네 갈래 길이 있다. 20→10→5→nil 과 20→40→30→nil 처럼 자리 셋을 지나는 길도 있고, 20→10→nil 처럼 둘만 지나는 길도 있다. **길이는 다른데 검은 수는 넷 다 2다.** 빨강(10 아래 5, 20 아래 40)은 셈에 들어가지 않기 때문이다 — 길이를 늘려도 검은 셈은 그대로다.

### 왜 이게 균형을 보장하나

빨강이 검정 사이사이 끼어들 수는 있어도 연달아 둘일 수는 없다("빨강 아래 빨강 없음"). 그러니 어떤 길도 검은 자리의 최대 두 배를 넘게 늘어질 수 없다 — 가장 긴 길이 가장 짧은 길의 두 배를 넘지 않는다. 흑색 높이가 어디서나 같다는 것이 바로 그 상한을 고정한다. 트리가 아무리 커져도 높이는 log 배로만 자라고, 삽입·삭제가 회전 몇 번으로 끝나는 이유가 여기 있다.
`;
