/**
 * 삭제 이동 학습 설명 — 단일 마크다운.
 *
 * `{facet:shiftOnRemove}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const shiftOnRemoveDescription = `## 가운데를 빼면 뒤가 당겨진다

**배열은 값을 빈틈없이 한 줄로 둔다.** i 번 값이 어디 있는지 계산 한 번으로 알아내는 것이 배열의 재주인데, 중간에 구멍이 있으면 그 계산이 어긋난다. 그래서 가운데 값을 빼면 자리를 비워 둘 수 없고, 뒤의 값들이 한 칸씩 왼쪽으로 옮겨 와 구멍을 메운다.

{facet:shiftOnRemove}

### 앞에서부터 당긴다

빈 칸 바로 뒤의 값부터 옮긴다. 뒤에서 시작하면 아직 옮기지 않은 값 위에 덮어쓰게 되고, 덮인 값은 되찾을 수 없다. 옮기는 방향이 왼쪽이니 훑는 방향도 왼쪽에서 오른쪽 — 방향이 같아야 앞선 걸음이 뒤의 걸음을 망치지 않는다.

### 값 하나를 빼는 값이 얼마인가

뺀 자리가 앞일수록 옮길 것이 많다. 인덱스 i 를 빼면 옮김은 (길이 - i - 1)번이다.

- **맨 뒤를 빼면** 옮길 것이 없다. 한 번도 움직이지 않는다.
- **가운데를 빼면** 절반쯤이 움직인다.
- **맨 앞을 빼면** 거의 전부가 움직인다.

최악이 길이에 비례하므로 배열의 삭제를 O(n) 이라 부른다. 값을 찾아가는 일은 한 번에 되는데 지우는 일은 그렇지 않다는 것이 배열의 성격이다.

### 마지막 칸은 사라지지 않는다

당김이 끝나면 길이는 4가 되지만 칸이 넷으로 줄어든 것은 아니다. 배열이 차지한 자리는 그대로고, **더 이상 읽지 않기로 한 칸** 이 끝에 하나 남는다. 줄어든 것은 칸이 아니라 길이다.

실제 코드의 당김은 옮기기가 아니라 베끼기(\`a[i] = a[i + 1]\`)라서, 그 칸에는 베끼기 전의 값이 그대로 남아 있기도 하다. 참조를 붙들면 놓아주지 않는 언어에서는 마지막 칸을 따로 비워 그 값을 풀어 준다.

값을 넣는 일도 같은 이야기의 반대편이다. 가운데에 자리를 만들려면 뒤의 값들을 오른쪽으로 밀어야 하고, 이번에는 **뒤에서부터** 밀어야 덮이지 않는다.
`;
