/**
 * 조각의 학습 설명. 화면 위젯이 아니라 호스트가 렌더하는 문서라 C10 의
 * 대상이 아니다 (C10 Exception).
 */

export const nodePointsNextDescription = `
# 노드가 다음을 가리킨다

배열은 순서를 **자리**로 만든다. 0번 다음이 1번인 이유는 메모리에서 바로 옆에
붙어 있기 때문이다. 그래서 배열은 자리를 미리 이어서 잡아 두어야 한다.

연결 리스트는 순서를 자리로 만들지 않는다. 노드는 메모리 어디에 있어도 되고,
실제로 흩어져 있다. 대신 노드마다 값 옆에 하나를 더 들고 있다 — **다음 노드의
주소**다.

{facet:nodePointsNext}

위 그림의 세 노드를 주소 순으로 보면 \`0x0100\` · \`0x0180\` · \`0x0240\` 이고,
값은 12 · 8 · 5 다. 그런데 쥐고 있는 주소를 따라가면 12 → 5 → 8 이다. 놓인
차례와 읽는 차례가 다르다. 순서를 만드는 것이 자리가 아니라 손에 쥔 주소라는
말은 이런 뜻이다.

- 노드 하나는 여기서 8바이트다 — int32 값 4바이트 + 주소 4바이트. 값 말고도
  자리를 내주어야 하는 것, 그것이 이 방식의 값이다.
- 첫 노드를 가리키는 주소는 리스트 밖에 따로 둔다. 그것이 \`head\` 다. head 를
  잃어버리면 나머지 노드는 메모리에 멀쩡히 있어도 닿을 길이 없다.
- 마지막 노드가 쥔 것은 \`null\` 이다. 갈 곳이 없다는 것도 하나의 주소다.

이 그림이 말하지 않는 것도 있다. 몇 번째 노드를 바로 집는 일은 여기서 할 수
없다 — 세 번째를 보려면 head 부터 두 번 따라가야 한다. 그 대가에 관한 이야기는
다른 자리에서 한다.
`;
