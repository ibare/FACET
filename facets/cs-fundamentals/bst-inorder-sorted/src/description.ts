/**
 * 중위 순회는 정렬되어 나온다 — 학습 설명 마크다운.
 *
 * `{facet:bstInorderSorted}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const bstInorderSortedDescription = `## 중위로 걸으면 왜 정렬되어 나오는가

이진 탐색 트리는 모든 노드에서 **왼쪽 < 자기 < 오른쪽**을 지킨다. 중위 순회는
이 규칙을 그대로 따라 걷는다 — 한 자리에 서면 먼저 왼쪽을 다 비우고, 그런
다음 자기를 내놓고, 오른쪽으로 넘어간다. 그 규칙이 모든 노드에서 성립하니
결과도 항상 오름차순이다.

{facet:bstInorderSorted}

### 뿌리 자리와 나온 순서는 무관하다

8, 3, 10, 1, 6, 14, 4, 7, 13 을 이 순서로 넣어 만든 트리다. 뿌리 8 은 화면
가운데 자리를 차지하지만, 중위로 걸으면 아홉 중 여섯째로 나온다. 트리 위의
자리는 삽입 순서가 만든 우연이고, 나온 줄의 자리는 규칙이 만든 필연이다 —
그 둘이 다르다는 것 자체가 이 그림의 논지다.

### 순회 순서 조각과의 차이

같은 재료(이진 트리)를 다르게 쓴다. 순회 순서 조각은 "같은 나무를 세 가지
차례로 훑으면 무엇이 달라지는가" 를 보여 주고, 이 조각은 그중 중위 하나만
골라 "그 결과가 왜 항상 오름차순인가" 를 보여 준다.

### 어디에 쓰이나

- **정렬된 순회가 필요할 때** — 범위 질의, "다음으로 큰 값" 탐색은 중위
  순서를 그대로 답으로 쓴다.
- **트리 정렬** — 값을 이진 탐색 트리에 하나씩 넣고 중위로 걸으면 그 자체로
  정렬된 목록이 나온다.
`;
