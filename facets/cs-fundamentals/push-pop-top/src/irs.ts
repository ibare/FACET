/**
 * push-pop-top IR — 없다.
 *
 * 조각은 코드 패널을 두지 않는다. 한 주장만 말하고 멈추는 그림이라 소스 코드와
 * 나란히 놓을 자리가 없다. 파일 구성은 S-facet 을 그대로 따르므로 빈 배열로
 * 자리만 지킨다.
 */

import type { IR } from '@ffacet/core/runtime';

export const pushPopTopIRs: IR[] = [];
