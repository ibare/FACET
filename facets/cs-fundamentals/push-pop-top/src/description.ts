/**
 * push-pop-top 학습 설명 — 단일 마크다운 (i18n 미적용).
 *
 * 본문 안의 `{facet:pushPopTop}` 토큰은 호스트(Tiptap NodeView) 가 시각화
 * 노드로 치환해 인라인 마운트한다.
 */

export const pushPopTopDescription = `## 넣기 · 빼기 · 꼭대기 (LIFO)

스택의 규칙은 자료구조가 아니라 **구멍의 개수** 에서 나온다. 넣는 문과 빼는 문이 같은 하나뿐이면, 나머지는 전부 따라 나온다 — 마지막에 들어온 것이 먼저 나오고, 아래에 깔린 것은 위가 다 걷힐 때까지 기다린다.

{facet:pushPopTop}

### 문이 하나라는 것의 귀결

- \`push(v)\` — 꼭대기에 얹는다. **O(1)**.
- \`pop()\` — 꼭대기를 걷는다. **O(1)**.
- \`top\` — 지금 손이 닿는 자리. 넣으면 오르고 빼면 내린다.

가운데를 건드리는 연산이 아예 없다. 그림에서 \`7\` 을 꺼내려는 손이 \`1\` 에 막히는 것은 구현이 게을러서가 아니라, 문이 하나인 통에서는 그 동작을 정의할 자리가 없기 때문이다.

### 왜 이 제약을 받아들이는가

되돌리기, 함수 호출, 괄호 짝 맞추기, 깊이 우선 탐색 — 모두 **가장 최근의 일부터 정리해야 하는** 문제다. 이런 문제에서 "마지막 것" 을 찾는 일은 공짜여야 하고, 스택은 그 하나만 공짜로 만드는 대신 나머지를 전부 포기한 거래다.

### 언제 쓰면 안 되는가

기다린 순서대로 처리해야 한다면 스택이 아니다 — 그때는 양끝이 갈라진 큐를 쓴다. 가운데 값을 자주 꺼내야 한다면 애초에 한쪽 끝만 여는 통을 고를 이유가 없다.
`;
