/**
 * bottomUpTable 의 IR — 없다.
 *
 * 조각은 코드 패널을 두지 않는다. 표준 6파일 구성을 지키기 위해 빈 배열로
 * 자리만 보존한다 (S-facet / S-piece).
 */

import type { IR } from '@ffacet/core';

export const bottomUpTableIRs: IR[] = [];
