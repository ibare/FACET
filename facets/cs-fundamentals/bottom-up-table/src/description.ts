/**
 * 상향식 표 채우기 조각의 학습 설명.
 *
 * 조각이 전제하는 것 — 무엇을 줄여 그렸고 무엇을 그리지 않았는지 — 은 화면
 * 각주가 아니라 이 글이 밝힌다 (S-piece).
 *
 * `{facet:bottomUpTable}` 토큰은 호스트가 인라인 시각화로 치환한다.
 */

export const bottomUpTableDescription = `## 상향식 표 채우기 (Bottom-Up Table)

**작은 것부터 표를 채워 올라가면 재귀가 어디론가 사라진다.** 없애는 것이 아니라, 애초에 부를 일이 생기지 않는다. 칸을 채울 때 필요한 값이 이미 왼쪽에 놓여 있기 때문이다.

{facet:bottomUpTable}

### 방향을 뒤집는다

재귀는 위에서 아래로 간다. \`f(5)\` 를 풀려고 \`f(4)\` 와 \`f(3)\` 을 부르고, 그것들이 다시 아래를 부르고, 바닥에 닿아야 값이 되돌아 오른다. 부르고 — 내려가고 — 되올라오는 세 동작이 필요하다.

상향식은 그 방향을 뒤집는다.

- \`T[0] = 0\`, \`T[1] = 1\` — 정의가 그냥 주는 두 칸을 먼저 적어 둔다.
- \`i\` 를 2 부터 하나씩 올리며 \`T[i] = T[i-1] + T[i-2]\` 를 적는다.

\`i\` 번째를 적을 차례가 되면 \`T[i-1]\` 과 \`T[i-2]\` 는 **이미 적혀 있다.** 순서를 그렇게 잡았기 때문이다. 그러니 부를 필요가 없고, 내려갈 곳도 없고, 되올라올 것도 없다. 남는 동작은 "왼쪽 둘을 더해 오른쪽에 적는다" 하나뿐이다.

### 이 조각의 화살은 한 방향뿐이다

조각을 끝까지 보면 화살 여덟이 남는데, **전부 왼쪽에서 오른쪽을 가리킨다.** 되돌아가는 화살이 하나도 없다는 것이 요점이다. 재귀 그림에서는 가지가 아래로 뻗고 값이 되올라오지만, 여기에는 가지도 없고 층도 없다. 나무가 아니라 한 줄짜리 표다.

\`T[5]\` 하나를 얻는 데 든 것은 덧셈 네 번이고, 재귀 호출은 **0 번**이다. 같은 항을 두 번 푸는 일도 일어나지 않는다 — 각 칸은 딱 한 번 적히고, 적힌 뒤에는 읽히기만 한다.

### 그래서 표도 필요 없어진다

한 칸이 보는 것은 **바로 앞 둘**뿐이다. \`T[5]\` 를 적을 때 \`T[0]\` 이나 \`T[1]\` 은 쳐다보지도 않는다. 마지막 창이 마지막 두 칸에만 앉는 것이 그 뜻이다.

그렇다면 표 전체를 들고 있을 이유가 없다. 변수 둘이면 된다.

\`\`\`
prev2 = 0; prev1 = 1
for i in 2..n:
    cur = prev1 + prev2
    prev2, prev1 = prev1, cur
\`\`\`

메모리가 \`O(n)\` 에서 \`O(1)\` 로 줄어든다. 상향식이 주는 두 번째 이득이며, 재귀로는 얻을 수 없다 — 재귀는 호출 스택 자체가 \`O(n)\` 이기 때문이다.

### 이 조각이 보여 주는 것 · 보여 주지 않는 것

- 화면은 **채우는 순서**만 보인다. 값의 크기는 그리지 않는다 — 여기서 중요한 것은 대소가 아니라 "필요한 값이 이미 왼쪽에 있는가" 이므로 막대 높이로 그릴 이유가 없다.
- 피보나치를 예로 든 것은 한 칸이 앞의 **두** 칸만 보기 때문이다. 동적 계획법 문제 일반에서 한 칸이 보는 자리는 더 많을 수도 있고 (배낭 문제는 표가 2차원이다), 그때는 "변수 둘이면 된다" 는 마지막 대목이 성립하지 않는다.
- 화면에는 재귀가 아예 등장하지 않는다. 재귀가 같은 항을 몇 번씩 다시 푸는 장면은 다른 조각의 몫이고, 여기서는 **그 낭비가 생길 자리 자체가 없다**는 것만 말한다.
`;
