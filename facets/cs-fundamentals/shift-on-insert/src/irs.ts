/**
 * shift-on-insert facet IR — 코드 패널을 두지 않으므로 빈 배열.
 *
 * 조각은 한 주장만 말하고 멈춘다. 코드 패널은 그 주장 옆에 둘 두 번째 화면이라
 * 조각의 범위 밖이다 (S-piece).
 */

import type { IR } from '@ffacet/core';

export const shiftOnInsertIRs: IR[] = [];
