/**
 * 삽입 이동 학습 설명 — 단일 마크다운.
 *
 * `{facet:shiftOnInsert}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const shiftOnInsertDescription = `## 가운데에 넣으면 뒤가 밀린다

**배열의 칸은 어디로도 가지 않는다. 옮겨 가는 것은 값이다.** 가운데 자리에 값을 하나 넣으려면 그 자리부터 뒤에 있는 값들을 전부 오른쪽으로 한 칸씩 옮겨야 하고, 그렇게 비운 자리에야 새 값이 들어간다.

{facet:shiftOnInsert}

### 왜 뒤에서부터인가

앞에서부터 옮기면 첫 걸음에서 이미 값 하나를 잃는다. 2번의 값을 3번에 쓰는 순간 3번에 있던 값이 사라지기 때문이다. 뒤에서부터 옮기면 목적지가 언제나 **방금 비었거나 원래 비어 있던 칸** 이라 아무것도 덮이지 않는다.

옮기는 순서를 뒤집는 것만으로 값을 잃지 않는다 — 임시 저장소도, 배열 하나를 더 만드는 일도 필요 없다.

### 값이 세 번 옮겨 다녔다

값 하나를 넣었는데 이동은 세 번 일어났다. 넣는 자리가 앞쪽일수록 뒤에 남은 값이 많으니 옮길 것도 많아진다. 맨 앞에 넣으면 전부 옮겨야 하고, 맨 뒤에 붙이면 한 번도 옮기지 않는다. 그래서 배열 가운데 삽입은 **O(N)** 이고, 끝에 붙이는 것은 **O(1)** 이다.

읽기는 반대다. 몇 번 칸이든 번호만 알면 한 번에 닿는다. 배열은 **읽기를 싸게 하는 대신 가운데 삽입을 비싸게** 치르는 자료구조다.

### 밀 자리가 없으면

여기서는 마지막 칸이 비어 있어 밀 자리가 있었다. 칸이 다 찼다면 미는 것부터 불가능하고, 더 큰 배열을 새로 잡아 전부 옮겨 담은 뒤에야 삽입을 시작할 수 있다.
`;
