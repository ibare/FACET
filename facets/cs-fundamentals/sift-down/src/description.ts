export const siftDownDescription = `
## 하향 재배치 (sift-down)

최소 힙에서 꼭대기를 뺀 자리는 비워 둘 수 없다. 관례는 **맨 끝 값을 그 자리로
올리는 것**이다 — 배열을 통째로 밀어 올리지 않아도 되는 가장 싼 방법이지만,
올라온 값이 그 자리에 맞는 값이라는 보장은 없다.

그래서 내려보낸다. 지금 자리의 **두 자식 중 앞선(더 작은) 쪽**과 견주어, 자신이
그보다 크면 그 자식과 자리를 맞바꾼다. 아무 자식과나 바꾸면 안 된다 — 큰 쪽과
바꾸면 작은 자식이 부모보다 큰 값 아래 깔려 힙의 성질(부모 ≤ 자식)이 깨진다.
앞선 쪽과 바꿔야 그 조건이 다시 성립한다.

이 과정은 두 자식보다 이미 앞서거나, 자식이 없는 자리에 이르면 멈춘다.

{facet:siftDown}

아래 그림은 일곱 칸짜리 최소 힙에서 꼭대기를 한 번 빼내는 과정 전부를 보인다.
자동으로 한 번 재생되며, 다시 보기와 한 걸음씩 짚어보기로 되짚을 수 있다.
`.trim();
