/**
 * mark-visited-or-loop 조각의 글.
 *
 * `{facet:markVisitedOrLoop}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 * 화면이 말하지 않는 전제 — 걸음 규칙이 무엇이고 왜 열두 걸음에서 끊는지 — 는
 * 여기서 밝힌다. 조각은 여러 글에 불려 가므로 각주를 화면에 두지 않는다 (S-piece).
 */

export const markVisitedOrLoopDescription = `
## 표시를 남기지 않으면

그래프를 걷는 규칙은 단순하다. 지금 자리의 이웃을 앞에서부터 보고, 갈 수 있는
첫 이웃으로 옮긴다. 이 규칙만으로는 어디도 빠뜨리지 않을 것 같지만, 자리마다
길이 여러 갈래로 갈리고 그 길들이 서로 이어져 있으면 사정이 달라진다.

{facet:markVisitedOrLoop}

## 같은 세 자리

A · B · C 가 서로 손을 잡아 고리를 이루고, D 는 C 에만 매달려 있다. C 의 이웃
목록은 \`[A, D]\` 이므로 C 에 설 때마다 첫 칸인 A 가 먼저 걸린다. 그래서 걸음은
A → B → C → A 로 되돌아오고, 그 다음도, 그 다음도 같다. 열두 걸음을 걸어도
밟은 자리는 셋뿐이고 **D 는 한 번도 걸리지 않는다.**

열둘은 그저 화면을 끊는 자리다. 스물이든 백이든 결과는 같다 — 규칙에 끝나는
조건이 없기 때문이다. 실제 코드였다면 이 자리에서 프로그램이 멈추지 않는다.

## 표시 한 칸이 하는 일

두 번째 회차는 자리를 밟을 때마다 그 자리에 표시를 남긴다. 노란 테가 그것이다.
바뀐 것은 걸음 규칙이 아니라 **"갈 수 있는"** 의 뜻뿐이다 — 표시가 있는 자리는
이제 갈 수 있는 자리가 아니다.

C 에 서면 첫 칸의 A 는 이미 테를 두르고 있어 지나친다. 목록의 다음 칸은 D 다.
고리가 끊기는 것이 아니라, **고리를 다시 돌 이유가 없어진 것**이다. 그래서
세 걸음 만에 네 자리를 모두 밟고 걸음이 끝난다.

## 자국을 보라

첫 회차가 끝났을 때 왼쪽 세 선은 굵게 패어 있고 오른쪽으로 난 길은 그은
그대로다. 되풀이한 만큼 굵어지도록 그렸으니, 그 굵기가 곧 헛걸음의 양이다.
표시를 켜면 네 선이 모두 한 번씩만 밟힌다.

이것이 깊이 우선 탐색이든 너비 우선 탐색이든 시작이 언제나 \`visited\` 집합인
이유다. 순서를 고르는 방법은 알고리즘마다 다르지만, **다녀간 자리를 기억하지
않으면 어떤 순서로도 끝나지 않는다.** 표시는 최적화가 아니라 종료 조건이다.

## 트리에는 왜 없었나

트리를 도는 코드에는 \`visited\` 가 없다. 부모로 되돌아가는 간선이 없어서 —
정확히는 자식으로만 내려가서 — 이미 본 자리로 돌아올 길이 애초에 없기 때문이다.
그래프에서 그 안전장치가 사라진다. 사이클이 하나라도 있으면 표시가 필요하고,
사이클이 있는지 미리 알 수 없다면 언제나 필요하다.
`;
