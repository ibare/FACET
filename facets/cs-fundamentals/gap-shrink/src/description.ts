export const gapShrinkDescription = `
## 간격 축소

정렬을 옆칸끼리만 견주면, 멀리 있는 값 하나가 제자리를 찾아가는 데 한 칸씩
여러 번 밀려야 한다. 큰 값이 맨 앞에 있으면 그 값 하나 때문에 뒤의 모든 칸이
한 번씩 움직인다.

간격 축소는 순서를 뒤집는다. 먼저 **멀리 떨어진 짝**부터 견준다. 세 칸 건너
짝을 견주고 어긋나 있으면 맞바꾸면, 값이 한 번에 세 칸을 건너뛴다. 큰 어긋남이
몇 번의 이동으로 걷힌다. 그다음 보폭을 좁혀 옆칸끼리 견주는데, 이때 줄은 이미
"거의 정렬된" 상태라 옆칸 라운드가 할 일이 얼마 남지 않는다.

{facet:gapShrink}

화면의 장부 두 줄이 이 조각의 근거다. 위 줄은 보폭 3 라운드와 보폭 1 라운드를
차례로 거친 셈이고, 아래 줄은 **같은 입력**을 처음부터 옆칸만으로 정렬한 셈이다.
견줌 횟수는 10 대 13 으로 조금 갈리지만, 이동 횟수는 5 대 9 로 갈린다. 앞
라운드의 견줌 세 번이 낭비가 아니라 뒤의 일을 덜어 낸 값이라는 뜻이다.

### 전제

- 여기서 쓰는 간격은 3 과 1 둘뿐이고 값은 여섯 개다. 실제 셸 정렬은 배열
  크기에 따라 간격 수열을 여러 단계로 잡으며, 어떤 수열을 쓰느냐로 성능이
  갈린다.
- 한 라운드는 그 간격의 삽입 정렬이다. 화면은 그것을 "짝을 견주고 어긋나면
  맞바꾼다" 는 형태로 편다 — 견줌과 이동을 눈으로 셀 수 있게 하기 위해서다.
  값을 밀어 두고 마지막에 한 번 놓는 구현과 견줌·이동 횟수는 같다.
- 이 입력에서는 보폭을 줄인 쪽이 이겼지만, 이미 거의 정렬된 입력이라면 옆칸
  라운드 하나만으로도 이동이 거의 없다. 간격 축소가 값어치를 갖는 것은 멀리
  있는 어긋남이 있을 때다.
`;
