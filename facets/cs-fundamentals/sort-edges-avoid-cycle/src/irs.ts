/**
 * 조각(piece) 은 코드 패널을 두지 않는다 (S-piece). 등록할 IR 이 없다.
 * 파일 자체는 S-facet 의 6파일 구성이라 비어 있어도 남긴다.
 */

import type { IR } from '@ffacet/core/runtime';

export const sortEdgesAvoidCycleIRs: IR[] = [];
