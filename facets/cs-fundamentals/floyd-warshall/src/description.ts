export const floydWarshallDescription = `# 플로이드-워셜 — 모든 쌍 최단 경로

한 곳에서 모든 곳으로 가는 길을 재는 알고리즘은 여럿이다. 플로이드-워셜은
질문이 다르다. **모든 곳에서 모든 곳으로** 가는 길을 한꺼번에 잰다. 그래서
답도 하나의 수가 아니라 표 한 장이다.

{facet:floydWarshall}

## 가운데를 세운다

표의 칸 \`d[i][j]\` 는 "i 에서 j 로 가는, 지금까지 알아낸 가장 짧은 거리" 다.
처음에는 아는 것이 거의 없다 — 자기 자신까지는 0, 직접 잇는 간선이 있으면 그
무게, 나머지는 무한이다.

여기서부터 알고리즘이 하는 일은 한 가지뿐이다. 정점 하나를 **가운데 세우고**,
모든 쌍에 대해 이렇게 묻는다.

> i 에서 곧장 j 로 가는 것보다, 가운데를 거쳐 가는 편이 짧은가?

가운데를 거쳐 가는 길이는 표에서 두 칸을 꺼내 더하면 나온다 —
\`d[i][k] + d[k][j]\`. 그것이 지금 칸보다 작으면 칸을 **고쳐 적는다.**
화면에서 옛 수가 위로 밀려 나가고 새 수가 아래에서 올라오는 것이 그 순간이다.

이 그래프에서는 물음이 백 번이고, 그중 스무 번만 고쳐 적는다. 나머지 여든 번은
"이미 그만하면 짧다" 는 확인이다. 알고리즘의 대부분은 아무 일도 일어나지 않는
시간이고, 그 확인이 있어야 남은 수를 믿을 수 있다.

## 왜 가운데가 바깥 반복문인가

세 겹 반복문에서 \`k\` 가 가장 바깥에 있는 것이 이 알고리즘의 전부다. 순서를
바꿔 \`i\` 를 바깥에 두면 답이 틀린다.

까닭은 이렇다. \`k\` 를 한 바퀴 다 돌고 났을 때 표가 보장하는 것은
**"0번부터 k번까지만 가운데로 써서 갈 수 있는 가장 짧은 거리"** 다. 다음 바퀴는
그 표 위에서 \`k+1\` 을 하나 더 얹는다. 즉 바깥 반복문 한 바퀴가 "쓸 수 있는
가운데 정점을 하나 늘리는 일" 이고, 안쪽 두 반복문은 그 늘어난 하나를 모든 쌍에
적용하는 일이다.

그래서 \`d[i][k] + d[k][j]\` 를 읽을 때 두 칸이 이미 여러 번 고쳐진 값이어도
괜찮다. 그 값들은 "k 이전의 정점만 가운데로 써서" 얻은 것이고, 그것이 정확히
지금 필요한 것이다. 이 그래프에서 0 에서 4 로 가는 길이 처음엔 무한이었다가
10 이 되는 것도 그 덕이다 — 0 → 1 → 2 → 3 → 4 는 가운데를 세 개나 거치는데,
가운데를 하나씩 늘려 가는 동안 조각조각 고쳐져 마지막에 이어 붙는다.

## 무한을 어떻게 두었는가

닿지 않는 자리에는 무한을 두어야 하는데, 컴퓨터에는 무한이 없다. 여기서는
**큰 수 하나(10억)** 로 두었고, 더하기 전에 "둘 다 무한이 아닌지" 를 묻는 줄은
두지 않았다. 코드 패널의 조건이 한 줄로 서 있는 것은 그 선택 때문이다.

그래도 되는 까닭은 두 가지다.

- 무한 둘을 더해도 20억이라 32비트 정수 상한(약 21억 4천만)을 넘지 않는다.
  넘쳤다면 음수가 되어 "가장 짧은 길" 로 둔갑했을 것이다.
- 어떤 칸도 10억보다 커지지 않으므로 \`무한 + 무엇 < 지금 칸\` 은 결코 참이
  되지 않는다. 헛되이 고쳐 적는 일이 없다.

무한을 \`2^31 - 1\` 처럼 상한에 바짝 붙여 두면 이야기가 달라진다. 그때는
더하기 전 검사가 군더더기가 아니라 정직한 한 줄이 된다. **무한을 어떻게 두느냐가
검사 한 줄의 필요를 정한다** — 이 알고리즘에서 그 둘은 따로 떼어 정할 수 없다.

## 자기 자신 칸은 왜 건너뛰는가

\`d[i][i]\` 는 0 이고, 이 그래프에는 음수 무게가 없어 0 보다 짧아질 수 없다.
물어도 답이 정해져 있으므로 코드가 \`continue\` 로 건너뛴다. 덕분에 물음이
125 번에서 100 번으로 준다.

거꾸로 말하면, 음수 무게가 있는 그래프에서 \`d[i][i]\` 가 0 아래로 내려가는
순간이 **음수 순환을 찾아낸 순간**이다. 그 쓰임이 필요하면 이 \`continue\` 를
빼고 대각선도 함께 물으면 된다.

## 셈의 크기

정점이 n 개면 반복이 n × n × n 이다. 정점 100 개면 백만 번, 1000 개면 십억
번이다. 간선이 적은 큰 그래프라면 시작점마다 다익스트라를 돌리는 편이 낫다.
플로이드-워셜이 값을 하는 자리는 **정점이 많지 않고, 모든 쌍이 정말로 필요한**
경우다. 표 한 장을 한 번 만들어 두고 그 뒤로는 칸만 읽으면 되기 때문이다.
`;
