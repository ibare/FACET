/**
 * Stack (LIFO) 학습 설명 — 단일 마크다운.
 *
 * `{facet:stack}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const stackDescription = `## 스택 (Stack — LIFO)

**모든 변화가 단 하나의 꼭대기 자리에서만 일어나도록 강제하는 자료구조.** 새 원소를 쌓는 곳도, 떼어내는 곳도 같은 한 점이다. 그래서 **마지막에 들어온 것이 가장 먼저 나온다** (Last In, First Out).

{facet:stack}

### 핵심 아이디어

스택의 본질은 **한 점 제약** 이다.

- 진입과 퇴출이 **같은 자리** 에서만 일어난다 — 그 자리를 **꼭대기 (top)** 라 부른다.
- 꼭대기 아래의 박스들은 잠겨 있다. 직접 만질 수 없고, 위에 있는 박스가 떼어져야 비로소 꼭대기가 된다.
- 한 점 제약 덕분에 **입력 순서가 자연스럽게 뒤집혀** 출력된다 — 1, 2, 3 으로 쌓으면 3, 2, 1 로 빠진다.

### 주요 연산과 비용

- \`push(v)\` — 꼭대기에 새 원소 얹기. **O(1)**.
- \`pop()\` — 꼭대기 원소 떼어내기. **O(1)**.
- \`peek()\` — 꼭대기 원소 조회 (구조 변화 없음). **O(1)**.

모두 **상수 시간**. 꼭대기만 만지므로 인덱스 탐색이 필요 없다.

### 결정적 순간 — 순서 역전

세 박스 1, 2, 3 을 차례로 쌓은 직후 떼기를 누르면 가장 마지막에 올린 3 이 먼저 사라지고, 다음에 2, 그 다음에 1 이 사라진다. **입력 시퀀스 1, 2, 3 의 거울상이 출력 시퀀스 3, 2, 1 로 한 화면 안에서 보존된다.**

### 변형과 경계

- **bounded stack** — 최대 높이가 있는 스택. 가득 찬 상태에서 \`push\` 하면 **overflow**. 빈 상태에서 \`pop\` 하면 **underflow**.
- **deque** — 양끝에서 모두 넣고 뺀다. 스택의 한 점 제약이 양 끝으로 풀린 형태.
- **call stack** — 함수 호출의 활성 레코드 (지역 변수 + 복귀 주소) 가 push 되고 \`return\` 시 pop 되는, 언어 런타임이 직접 운용하는 스택.

### 언제 쓰나

- **함수 호출** — 거의 모든 언어 런타임이 호출 스택으로 흐름을 관리한다. 재귀가 자연스러운 이유.
- **DFS** — 한 가지를 끝까지 파고들었다가 되돌아오는 탐색. \`pop\` 이 곧 "되돌아옴".
- **표현식 평가 / 괄호 짝 검사** — 여는 괄호를 push, 닫는 괄호에서 pop 해 짝을 맞춘다.
- **실행 취소 (undo)** — 가장 최근 행동을 가장 먼저 되돌린다.

### 큐 (FIFO) 와의 대비

큐는 활동 지점이 **두 곳** 으로 공간 분리되어 있다 (앞으로 빠지고 뒤로 들어옴). 스택은 활동 지점이 **한 곳** 에 집중된다 (위로만 자라고 위로만 줄어듦). 정지 프레임 하나로도 "위가 뚫린 용기" 는 "양끝이 뚫린 벨트" 와 즉각 구별된다.
`;
