/**
 * merge-sort 완결형의 글.
 *
 * `{facet:mergeSort}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 * 조각 셋(`splitUntilOne` · `mergeTwoSorted` · `inPlaceVsExtra`) 은 각자
 * 한 주장만 말하므로, 여기서는 그 셋이 한 알고리즘으로 맞물리는 자리를 적는다.
 */

export const mergeSortDescription = `
## 머지 정렬

머지 정렬은 두 걸음으로만 되어 있다. **끝까지 쪼개고, 되짚어 오르며 합친다.**
쪼개는 동안에는 아무것도 정렬되지 않는다 — 구간을 반으로 가르기만 할 뿐이라,
값은 제자리에 그대로 있다. 정렬은 오직 되짚어 오르는 길에서만 일어난다.

{facet:mergeSort}

화면의 세로는 재귀의 깊이다. 한 줄 아래로 내려갈 때마다 구간이 둘로 갈라지고,
낱개에 닿으면 더 갈 곳이 없다 — 홀로 남은 하나는 이미 정렬된 줄이기 때문이다.
거기서부터 위로 되돌아오며 두 줄을 한 줄로 합친다.

## 합침이 쪼갬의 역순이다

쪼갬은 위에서 아래로 퍼지지만, 합침은 아래에서 위로 모인다. 그래서 **맨 위의
합침이 맨 마지막**이다. 화면에서 가장 넓은 덩이가 채워지는 것은 언제나 끝이고,
그때까지 배열 전체는 정렬돼 있지 않다. 재생을 한 걸음씩 밟으면 코드 패널의
\`merge_sort(arr, lo, mid)\` 줄이 몇 번이나 다시 짚히는지 보인다 — 그 줄 하나가
스스로를 부르며 아래로 내려가는 자리다.

## 맨 앞 둘만 본다

합칠 때 보는 것은 양쪽 줄의 **맨 앞 한 칸씩, 모두 둘뿐**이다. 두 줄이 이미
정렬돼 있으므로 각 줄에서 가장 앞선 값은 반드시 맨 앞에 있고, 그러니 둘 중
앞선 것이 전체에서 앞선 것이다. 뒤쪽은 볼 필요가 없다.

한쪽이 먼저 비면 견줄 상대가 없어진다. 남은 쪽은 이미 정렬된 줄이므로 그대로
옮기면 된다 — 코드의 뒤쪽 두 while 이 그 일을 한다.

## 자리를 빌린다

합치려면 두 줄을 옆에 베껴 두어야 한다. 제자리에서 밀어 넣으려 들면 아직 옮기지
않은 값을 덮어쓰기 때문이다. 그래서 머지 정렬은 입력만큼의 자리를 더 쓴다.
견줌과 옮김의 횟수는 입력이 어떻게 생겼든 거의 변하지 않는다 — 최악과 평균이
갈리지 않는 것이 이 정렬의 성질이다.
`;
