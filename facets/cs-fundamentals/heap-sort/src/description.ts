/**
 * heap-sort 완결형의 글.
 *
 * `{facet:heapSort}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const heapSortDescription = `
## 힙 정렬

배열 한 줄을 놓고, 자리 번호만으로 그것을 트리로 읽는다. 자리 \`i\` 의 자식은
\`2i+1\` 과 \`2i+2\` 다. 포인터도 없고 자리를 더 쓰지도 않는다 — 셈이 곧 구조다.

그 트리를 **최대 힙**으로 만들면 꼭대기가 가장 큰 값이다. 꼭대기를 꺼내 뒤에
쌓기를 되풀이하면 정렬이 끝난다. 꺼낸 값이 앉을 자리는 따로 마련하지 않는다 —
힙이 한 칸 줄면서 마침 그 칸이 비기 때문이다.

{facet:heapSort}

## 힙부터 만든다

이미 힙인 배열을 받는 것이 아니라 뒤죽박죽인 배열을 받는다. 그래서 먼저 힙으로
만들어야 하고, 그 방법이 **뒤에서부터 내리기**다.

잎(자식이 없는 자리)은 그 자체로 이미 힙이다. 그러니 손댈 곳은 자식을 가진
자리뿐이고, 그 마지막 자리가 \`n // 2 - 1\` 이다. 거기서부터 0 번까지 거꾸로
올라오며 각 자리의 값을 아래로 내린다. 아래쪽이 이미 힙이므로 위에서 하나만
내리면 그 부분트리 전체가 힙이 된다.

화면 위쪽 띠가 힙의 층이다. 한 층이 다음 층에서 두 배가 되므로 층은 칸 수의
로그만큼만 쌓이고, 한 번 내리는 일이 그 층 수를 넘지 않는다.

## 꺼내기는 맞바꿈 한 번이다

꼭대기를 "꺼낸다" 고 하지만 실제로 하는 일은 **꼭대기와 힙의 마지막 칸을
맞바꾸는 것**이다. 그러면 가장 큰 값이 힙의 끝 칸에 앉고, 힙의 크기를 하나 줄이면
그 칸은 힙 바깥이 된다. 바깥으로 나간 칸은 다시 움직이지 않는다.

줄 아래에 걸린 활이 부모와 자식을 잇는 선인데, 힙 바깥으로 나간 칸에는 활이
걸리지 않는다. 오른쪽부터 활이 하나씩 사라지는 것이 곧 정렬된 꼬리가 자라는
것이다. 배열은 하나뿐이고, 트리와 정렬 결과가 그 한 줄을 나눠 쓴다.

맞바꾼 뒤 꼭대기에는 원래 끝에 있던 작은 값이 올라와 있으므로 힙 성질이
깨진다. 그것을 다시 아래로 내리면 새 꼭대기가 정해진다. 이 되풀이가 칸 수만큼
일어나고, 한 번마다 층 수만큼 내려가므로 전체가 \`n log n\` 이다.

## 견줌과 맞바꿈

내리는 한 걸음은 견줌 둘로 이뤄진다. **두 자식 중 큰 쪽을 고르는 견줌** 하나와,
**부모가 이미 그보다 크거나 같은가** 하는 견줌 하나다. 두 번째에서 답이 "그렇다"
면 거기서 멈춘다 — 더 내려갈 이유가 없다.

오른쪽 자식이 없는 자리에서는 첫 번째 견줌을 건너뛴다. 화면의 견줌 수가 층
수의 정확히 두 배가 아닌 것은 그 때문이다.
`;
