/**
 * 벨만-포드 학습 설명. 호스트가 렌더하는 마크다운 한 벌.
 *
 * `{facet:bellmanFord}` 토큰은 `facet.ts::id` 와 같아야 한다 (C4).
 */

export const bellmanFordDescription = `# 벨만-포드

음수 간선이 섞인 그래프에서 한 출발점부터의 최단 거리를 구한다.

{facet:bellmanFord}

## 한 줄로

**간선 전부를 정점 수보다 한 번 적게 되풀이해 편다.** 그러면 거리가 확정된다.

"편다(relax)" 는 한 줄이다.

\`\`\`
dist[u] + w < dist[v] 이면 dist[v] = dist[u] + w
\`\`\`

이 간선을 타고 가는 편이 지금 아는 것보다 짧으면, 아는 값을 그것으로 갈아 끼운다.
그뿐이다. 어려운 것은 이 한 줄이 아니라 **몇 번 되풀이해야 끝나는가** 다.

## 왜 n-1 번인가

한 바퀴란 간선 아홉을 정해진 차례로 한 번씩 훑는 것이다. 한 바퀴를 돌면 무슨 일이
일어나는가 — **출발점에서 간선 하나로 닿는 곳은 반드시 확정된다.** 두 바퀴를 돌면
간선 둘로 닿는 곳까지 확정된다. 값이 한 겹씩 더 깊이 스며드는 것이다.

최단 경로에 간선이 몇 개까지 들어갈 수 있을까. 정점을 두 번 밟는 경로는 고리를
품는데, 음수 고리가 없다면 고리를 빼는 편이 언제나 짧거나 같다. 그러니 최단 경로는
정점을 두 번 밟지 않고, 정점이 n 개면 간선은 많아야 **n-1 개** 다. 바퀴 수도 거기서
나온다.

화면 오른쪽 원장은 바퀴마다 한 줄씩이고, 그 바퀴에 값이 줄어든 칸만 물들어 있다.
오른쪽 끝의 ↓ 는 그 바퀴에 줄어든 칸 수다.

## 차례가 결과를 바꾸지 않는다 — 속도를 바꾼다

이 그림의 간선 차례는 이렇다.

\`\`\`
0→1(6)  0→2(7)  1→2(8)  1→3(5)  1→4(−4)  2→3(−3)  2→4(9)  3→1(−2)  4→3(7)
\`\`\`

첫 바퀴에 여섯 칸이 한꺼번에 줄어든다. 앞에서 갓 줄어든 값이 같은 바퀴 안에서 뒤
간선에 곧바로 쓰이기 때문이다 — 0→2 로 정점 2 가 7 이 되고, 곧이어 2→3 이 정점 3
을 4 로 끌어내리고, 다시 3→1 이 정점 1 을 6 에서 2 로 끌어내린다. 세 겹이 한 바퀴에
스민 셈이다.

둘째 바퀴에는 딱 한 칸(정점 4 가 2 에서 −2 로)만 줄고, 셋째와 넷째 바퀴에는 한
칸도 줄지 않는다. 차례를 거꾸로 뒤집으면 같은 답에 이르되 바퀴가 더 걸린다.
**차례는 답을 바꾸지 않고 스미는 속도만 바꾼다.** 그래서 최악을 각오하고 n-1 번을
도는 것이다.

## 한 바퀴 더 — 이것이 없으면 벨만-포드가 아니다

n-1 바퀴가 끝난 뒤 **한 바퀴를 더** 돈다. 조건은 앞과 글자 하나 다르지 않다. 다만
줄이는 대신 알린다.

여기서도 줄어드는 간선이 하나라도 있으면, 최단 경로가 간선 n-1 개를 넘었다는 뜻이고,
그것은 음수 고리를 돌고 있다는 뜻이다. 음수 고리가 있으면 돌 때마다 거리가 줄어드니
"최단" 이라는 말 자체가 성립하지 않는다. 그럴 때 이 알고리즘은 답 대신 **답이 없다는
사실** 을 낸다.

이 그림의 자료에는 음수 고리가 없다. 원장의 마지막 줄이 온통 흐린 채로 남고 ↓ 가 0
인 것 — 그것이 "아무것도 줄지 않았다" 이고, 앞서 구한 0 · 2 · 7 · 4 · −2 가 최종
답이라는 증거다.

이 검사를 빼면 남는 것은 그냥 느린 완화 반복문이다. 다익스트라보다 느린 값을
치르고 얻는 것이 바로 이 판정이므로, 빼면 치른 값이 헛것이 된다.

## 무한대를 어떻게 두었나

아직 닿지 않은 정점의 거리는 \`INF = 1000000000\` 으로 두었다. 언어마다 무한대 표기가
갈리고(\`float('inf')\` · \`Integer.MAX_VALUE\` · \`INT_MAX\`) 정수 표에 실수를 섞으면
타입이 흐려지므로, 32비트 정수에 넉넉히 드는 큰 수 하나로 통일했다. 화면에는 이 수를
그대로 띄우지 않고 ∞ 로 그린다.

그리고 완화 조건 앞에 \`dist[u] != INF\` 를 세웠다. **아직 닿지 않은 정점에서는 펴지
않는다.** 이것을 빠뜨리면 \`INF + (−4)\` 가 \`INF\` 보다 작으므로, 닿지도 않은 곳에서
거리가 새어 나와 표 전체가 조용히 틀어진다. 음수 간선이 있는 알고리즘에서만 생기는
함정이다 — 무게가 모두 양수라면 저 덧셈이 \`INF\` 보다 작아질 일이 없다.

## 다익스트라와 무엇이 다른가

다익스트라는 "지금 가장 가까운 정점" 을 하나 골라 확정하고 다시 보지 않는다. 그
확정이 옳으려면 앞으로 갈 길이 거리를 줄일 수 없어야 하는데, 음수 간선이 있으면 그
전제가 깨진다.

벨만-포드는 아무것도 미리 확정하지 않는다. 무엇을 먼저 볼지 고르지 않고 그냥 전부를
n-1 번 편다. 고르지 않는 대신 느리다 — 간선 m 개를 n-1 번이니 O(n·m) 이다. 그 값을
치르고 음수 간선을 얻고, 덤으로 음수 고리 판정을 얻는다.

## 코드에서 볼 것

- 간선을 정점별 이웃 목록이 아니라 **세 배열**(\`edge_from\` · \`edge_to\` · \`weight\`)로
  두었다. 그래서 안쪽 반복문이 그냥 간선 번호를 훑는다 — 이 알고리즘에는 "어느 정점의
  이웃" 이라는 개념이 필요 없다. 전부를 훑을 뿐이다.
- 거리표 \`dist\` 는 밖에서 받는다. 그 길이가 곧 정점 수다.
- 반복문 두 개의 \`if\` 조건이 완전히 같다. 위의 것은 줄이고 아래의 것은 알린다.
`;
