export const walkPerCharacterDescription = `
트라이(trie)에서 말을 찾는 일은 배열을 훑는 것과 다르다. 찾는 말의 글자를
앞에서부터 하나씩 집어, 그 글자가 붙은 가지를 따라 한 칸씩 내려갈 뿐이다.
내려간 칸 수가 곧 지금까지 확인한 글자 수이고, 담긴 말이 다섯 개든 오백만
개든 이 과정 자체는 달라지지 않는다.

그런데 이 "내려가다 멈추는" 결말은 한 가지가 아니다.

{facet:walkPerCharacter}

- 글자를 다 쓰고 닿은 자리에 ● 표시가 있으면 그 말은 **있다**.
- 글자를 다 썼지만 닿은 자리에 ● 표시가 없으면, 그 말은 다른 말의
  접두사일 뿐 **말로는 없다**.
- 글자를 다 쓰기도 전에 필요한 가지가 없으면, 거기서 더 내려갈 수 없어
  **없다**.

뒤의 둘은 겉보기엔 똑같이 "없다" 지만 원인이 다르다. 하나는 길을 끝까지
걸었는데 그 자리가 말의 끝이 아니었던 것이고, 다른 하나는 애초에 그 방향
으로 가는 길이 없었던 것이다.
`;
