/**
 * radix-sort 완결형의 글.
 *
 * `{facet:radixSort}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const radixSortDescription = `
## 기수 정렬

값을 견주지 않는다. 한 자리만 보고 줄을 세우고, 그것을 자리를 옮겨 가며
되풀이한다. 낮은 자리부터 하고 **매 라운드가 앞 라운드의 순서를 뒤엎지
않으면**, 마지막 자리를 마쳤을 때 전체가 줄 서 있다.

{facet:radixSort}

## 자리 숫자 하나를 뽑는 한 줄

\`(arr[i] // exp) % 10\` 이 이 알고리즘의 전부다. \`exp\` 로 나누어 관심 있는
자리를 맨 아래로 내리고, 10 으로 나눈 나머지로 그 한 자리만 남긴다. 자리를
옮기는 일은 \`exp\` 를 열 배 하는 것뿐이다.

이 나눗셈은 **정수 나눗셈**이라야 한다. 코드 패널에서 언어를 바꿔 보면
파이썬은 \`//\`, 자바·C++·C# 은 정수끼리의 \`/\`, 자바스크립트·타입스크립트는
\`Math.floor(...)\` 로 갈린다. 같은 한 줄이 언어마다 다르게 적히는 자리다.

## 누적합이 자리를 정한다

한 자리로 줄을 세우는 안쪽 절차가 이 화면의 요점이다. 세 걸음으로 간다.

1. **센다** — 자리 숫자가 0 인 것 몇 개, 1 인 것 몇 개 … 통 열에 적는다.
2. **누적한다** — 왼쪽 통을 차례로 더한다. 그러면 \`count[d]\` 는 개수가 아니라
   **자리 번호**가 된다. "자리 숫자가 \`d\` 이하인 수가 모두 \`count[d]\` 개" 이므로,
   \`d\` 인 것들이 앉을 마지막 칸의 바로 다음이 그 수다.
3. **놓는다** — 값을 하나 놓을 때마다 \`count[d]\` 를 하나 줄이고, 줄어든 그 수를
   칸 번호로 쓴다. 화면에서 통과 \`output\` 칸을 잇는 선이 그것이다.

통에 값을 담아 두었다가 쏟아붓는 것이 아니다. **개수만 세어 자리를 계산한다.**
그래서 여벌 배열 하나와 통 열 개면 된다.

## 뒤에서부터 놓는 이유

놓는 걸음은 배열을 **뒤에서부터** 훑는다. 이 방향이 안정성의 근거다.

자리 숫자가 같은 값이 둘 있다고 하자. 뒤엣것을 먼저 만나 놓으면 그것이 큰
칸 번호를 가져가고, 앞엣것은 그다음에 하나 줄어든 칸을 받는다. 원래의 앞뒤가
그대로 보존된다. 앞에서부터 훑으면 이 순서가 뒤집히고, 그러면 앞 라운드가
만들어 둔 순서가 무너져 알고리즘 전체가 성립하지 않는다.

이 자료에서는 10의 자리 라운드에 그 대목이 나온다. \`802\` 와 \`2\` 는 10의
자리가 둘 다 \`0\` 이라 이 라운드가 둘 사이를 가르지 못한다. 그런데 앞 라운드가
1의 자리로 \`802\` 를 \`2\` 보다 앞에 두었으므로, 그 순서를 지켜 주기만 하면
된다. 실제로 지켜진다.

## 견줌 0회

화면의 견줌 수는 끝까지 0 이다. 값끼리 크고 작음을 물어본 적이 없기 때문이다.
비교 기반 정렬이 \`n log n\` 아래로 내려갈 수 없다는 한계는 **견주는 것**에
붙은 한계이므로, 견주지 않는 이 방법은 거기 매이지 않는다.

대신 다른 것을 가정한다. 값이 자릿수로 쪼개지는 정수여야 하고, 가장 큰 수의
자릿수만큼 라운드를 돈다. 그리고 라운드마다 배열 하나와 통 열 개를 더 쓴다.
공짜가 아니라 **다른 값을 치른 것**이다.
`;
