/**
 * 힙 추출 학습 설명 — 단일 마크다운.
 *
 * `{facet:heapSortExtract}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const heapSortExtractDescription = `## 꺼낸 것을 어디에 쌓는가

힙은 꼭대기가 늘 가장 큰 값이다. 그러니 **꼭대기를 꺼내기를 되풀이하면 큰 것부터 차례로 나온다.** 그렇게 나온 값들을 어딘가에 순서대로 놓으면 정렬이 끝난다.

여기서 대부분이 한 번 멈춘다 — 꺼낸 것을 담을 배열이 하나 더 필요해 보이기 때문이다. 원소가 백만 개면 백만 칸을 더 잡아야 하나?

{facet:heapSortExtract}

### 비는 자리가 마침 쌓을 자리다

꼭대기를 꺼내면 힙의 크기가 하나 줄어든다. 크기가 줄었으니 **줄의 맨 끝 한 칸이 힙 밖으로 나온다.** 그 칸은 이제 힙이 쓰지 않는 칸이고, 꺼낸 값을 놓기에 딱 맞는 자리다.

그래서 한 줄이 둘로 갈린다. 왼쪽은 아직 다룰 힙, 오른쪽은 이미 끝난 것. 꺼낼 때마다 그 경계가 한 칸씩 왼쪽으로 밀려가고, 왼쪽이 줄어드는 만큼 오른쪽이 자란다. 자리를 새로 얻는 일은 한 번도 없다.

큰 것부터 나온 값을 뒤에서부터 채우니 끝나고 보면 줄이 오름차순이 되어 있다.

### 이 그림이 다루지 않는 것

꼭대기를 꺼낸 뒤 남은 것이 다시 힙 모양을 되찾는 과정은 여기서 걸음으로 보이지 않는다. 화면은 **새 꼭대기가 무엇인지**만 보여 준다. 그 되찾는 절차 자체는 따로 다룬다.

또 하나 — 여기서는 시작할 때 이미 최대 힙인 줄을 놓고 시작한다. 아무렇게나 놓인 줄을 힙으로 만드는 일은 이 그림의 앞에 있다.

### 그래서

이 성질 때문에 힙 정렬은 **제자리 정렬**이다. 입력 배열 하나면 되고 덧붙는 메모리가 상수다. 병합 정렬이 같은 O(n log n) 이면서도 n 칸을 더 빌려야 하는 것과 갈리는 대목이 여기다.
`;
