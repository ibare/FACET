/**
 * Array 학습 설명 — 단일 마크다운.
 *
 * `{facet:array}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const arrayDescription = `## 배열 (Array)

**같은 너비의 칸을 옆자리끼리 빈틈 없이 붙여 놓고 0 부터 매긴 번호로 호명하게 만든 자료구조.** 번호만 알면 옆 칸을 거치지 않고 한 번에 도달하지만, 가운데에 한 칸을 끼우거나 빼면 그 뒤의 모든 칸이 한 자리씩 밀려난다. 칸이 다 차면 두 배 큰 새 띠로 모든 항목을 한꺼번에 옮긴다.

{facet:array}

### 핵심 아이디어

배열의 본질은 **연속 (contiguity)** 과 **0-기반 호명** 이다.

- 칸이 메모리 위에서 옆자리끼리 빈틈 없이 붙어 있어, **시작 주소 + i** 라는 단 한 번의 산술로 i 번 칸의 자리를 계산할 수 있다.
- 그래서 \`get(i)\` / \`write(i, v)\` 는 옆 칸을 건드리지 않는 **단 한 걸음**. **O(1)**.
- 반대로 가운데에 끼우거나 빼는 사건은 그 뒤 모든 칸의 자리가 한 칸씩 동시에 밀려나는 **도미노**. **O(N)**.

### 주요 연산과 비용

- \`get(i)\` / \`write(i, v)\` — **O(1)**. 한 번의 산술 + 한 번의 메모리 접근.
- \`insert(i, v)\` / \`remove(i)\` — **O(N)**. i 이후 모든 칸이 한 자리씩 시프트.
- \`push(v)\` (뒤에 추가) — **amortized O(1)**. 평소엔 끝 자리에 얹기만 하지만, capacity 가 다 차는 순간 두 배 길이 새 띠로의 이사가 끼어든다.
- \`search(v)\` — **O(N)**. 0 번부터 한 칸씩 살핀다 (정렬되지 않은 경우).

### 결정적 순간 — 한 걸음 vs 도미노

같은 띠 위에서 두 사건이 잇달아 일어난다.

1. 인덱스 4 호명: 화살표가 0, 1, 2, 3 칸을 건드리지 않고 곧장 4 번 칸으로 한 호 곡선에 점프한다 (200ms). 띠 아래에 "시작 + 4" 라는 산술식이 잠시 떠 있다 사라진다.
2. 인덱스 1 에 삽입: 1 번 자리에 새 칸이 솟아 내려오고, 그 뒤 2~5 번 자리에 있던 칸들이 한 프레임 시차로 우측으로 한 자리씩 미끄러진다 (400ms). 띠 옆 누적 막대가 "밀린 칸 수" 만큼 자란다.

**같은 띠 위, 같은 배율, 다른 운동.** 한쪽은 한 걸음, 다른 쪽은 도미노 파동. 운동의 길이가 곧 비용이다.

### size 와 capacity — 채색 칸 / 회색 예비 칸

띠는 둘로 색이 갈린다. 채워진 앞쪽 N 칸 (\`size\`) 은 채색, 그 뒤의 예비 칸 (\`capacity - size\`) 은 회색 격자 무늬. 한 화면에 **지금 채워진 자리** 와 **미래에 자랄 자리** 가 함께 보인다. 회색 칸이 다 차는 \`push\` 순간, 두 배 길이의 새 띠가 등장하고 모든 칸이 한 묶음으로 새 띠로 이사한다 (resize).

### 다른 자료구조와의 구별

- **연결 리스트** — 칸들이 옆자리에 붙어 있지 않다. 인덱스 4 호명이 0→1→2→3→4 의 네 단계로 진행. 가운데 삽입은 화살표 두 개만 다시 묶일 뿐 도미노가 없다.
- **해시 테이블** — 칸과 인덱스가 있지만 호명 전에 키를 해시 함수로 변환하는 한 단계가 끼어든다. 배열은 인덱스 그 자체가 자리다.
- **정적 배열** — 회색 예비 영역이 결코 늘어나지 않는다. 동적 배열만이 두 배 더블링을 한다.

### 언제 쓰나

- **랜덤 액세스가 잦은 경우** — 인덱스로 한 번에 닿는다.
- **앞/뒤가 아닌 끝 추가가 많은 경우** — amortized O(1).
- **메모리 지역성이 중요한 경우** — 옆자리끼리 붙어 있으니 캐시 친화적.

반대로, 가운데 삽입/삭제가 잦거나 크기 예측이 안 되는 경우엔 연결 리스트 / 덱 / 해시맵 같은 대안이 더 어울린다.
`;
