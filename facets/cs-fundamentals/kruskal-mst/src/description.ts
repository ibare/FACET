export const kruskalMstDescription = `
# 크루스칼 — 가벼운 간선부터 집되, 고리가 되면 버린다

그래프의 모든 정점을 잇되 쓰는 길의 무게 합을 가장 적게 하려면 어떤 간선을
골라야 할까. 크루스칼의 답은 놀랄 만큼 단순하다. **간선을 무게 순으로 줄 세우고,
위에서부터 하나씩 집는다.** 집어 든 간선의 양 끝이 이미 같은 무리에 있으면
버리고, 다른 무리면 잇는다. 그것이 전부다.

{facet:kruskalMst}

## 무엇을 보고 있나

왼쪽 줄이 먼저 움직인다. 간선은 적힌 차례대로 서 있다가 무게 순으로 자리를
바꾼다. 이웃끼리만 맞바꾸므로 무게가 같은 둘은 적힌 차례를 그대로 지킨다 —
5 짜리 둘 중 2–4 가 0–3 보다 앞에, 8 짜리 둘 중 1–2 가 4–5 보다 앞에 남는다.
같은 무게 중 무엇을 먼저 집느냐로 트리의 모양이 갈리므로, 이것은 취향이 아니라
정의의 일부다.

줄이 서면 커서가 맨 위에서 아래로만 내려간다. 오른쪽 그래프의 정점은 저마다
다른 색으로 시작한다. 그 색이 곧 **무리**다. 간선을 집을 때마다 양 끝에서
부모를 타고 올라가 뿌리를 찾고,

- 뿌리가 **다르면** 잇는다. 두 색이 하나로 번지고 무게가 합에 더해진다.
- 뿌리가 **같으면** 버린다. 이미 이어진 두 점을 또 이으면 고리가 생길 뿐이고,
  고리 안의 가장 무거운 간선은 없어도 되는 간선이기 때문이다.

일곱 정점이 한 색이 되면 끝난다. 열한 간선 중 여섯을 잇고 다섯을 버린다.
잇는 간선의 수는 언제나 **정점 수 - 1** 이다 — 그보다 하나라도 더 이으면 고리가
생기고, 하나라도 덜 이으면 갈라진 무리가 남는다.

## 왜 이 욕심이 통하는가

크루스칼은 앞을 내다보지 않는다. 지금 남은 것 중 가장 가벼운 것을 집을 뿐이다.
그런데도 최적이 나온다. 까닭은 이렇다 — 어떤 간선 \`e\` 를 버렸다면, 그 양 끝은
이미 \`e\` 보다 가벼운 간선들만으로 이어져 있다. 그러니 \`e\` 를 쓰는 어떤 트리도
그 가벼운 간선들로 바꿔치기해 무게를 줄일 수 있다. **버린 것은 언제나 버려도
되는 것이었다.**

## 무리는 어떻게 세는가

"이미 같은 무리인가" 를 묻는 것이 이 알고리즘의 유일한 어려움이다. 코드 패널의
\`find\` 가 그 답이다. 정점마다 부모를 하나씩 적어 두고, 뿌리에 닿을 때까지
올라간다. 뿌리가 같으면 같은 무리다. \`unite\` 는 한 뿌리를 다른 뿌리에 매다는
한 줄이다 — 무리를 합치는 일이 화살표 하나라는 것이 유니온 파인드의 요점이다.

여기서는 경로를 압축하지 않는다. 올라간 자리를 뿌리에 곧바로 매달면 다음
\`find\` 가 빨라지지만, 그 두 줄이 코드 패널의 주인공을 바꿔 버린다. 압축 자체를
말하는 자리는 따로 있다.

## 프림과 나란히 놓으면

같은 그래프를 프림으로 풀어도 무게 합은 **39** 로 같다. 그러나 고르는 차례는
전혀 다르다. 프림은 한 정점에서 시작해 **자라난다** — 지금까지 자란 덩어리에
닿아 있는 간선 중 가장 가벼운 것만 본다. 크루스칼은 자라지 않는다. 그래프
전체에 흩어진 조각들을 아무 데서나 집어 **합쳐 나간다**. 그래서 크루스칼의
화면에서는 한동안 여러 무리가 따로 굴러다니고, 그것들이 뒤늦게 하나로 만난다.

둘 다 답이 같은 것은 우연이 아니다. 최소 신장 트리를 만드는 규칙(어떤 컷을
가로지르는 가장 가벼운 간선은 언제나 어떤 최소 신장 트리에 들어간다)이 하나이고,
두 알고리즘은 그 규칙을 **어느 컷에 적용하느냐**만 다르다.

## 셈의 값

간선을 줄 세우는 데 드는 것이 지배적이다 — 간선이 \`E\` 개면 \`O(E log E)\`.
그다음 훑는 동안 하는 일은 \`find\` 와 \`unite\` 뿐이고, 이 둘은 (압축과 랭크를
얹으면) 사실상 상수에 가깝다. 그래서 크루스칼은 **간선이 적은 성긴 그래프**에서
특히 잘 맞는다.
`;
