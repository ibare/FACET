/**
 * t-SNE 완제품의 글.
 *
 * 화면이 깔고 있는 전제와, 화면이 낸 수를 어떻게 읽어야 하는지를 여기서 밝힌다.
 */

export const tsneDescription = `## t-SNE — 편 그림에서 무엇을 읽어도 되는가

평면에 점 예순이 있다. 무리 셋이고 저마다 스물이다. 실제로 재 보면 A 와 B 의
가운데 사이는 3.45, B 와 C 사이는 7.52 다. **B–C 가 A–B 의 2.18 배**다.

이것을 t-SNE 로 다시 편다. 원래 이미 평면이므로 차원이 줄어드는 것은 없다 —
줄어드는 것은 차원이 아니라 **무엇을 지킬지**다. t-SNE 는 가까운 것끼리의 관계만
지키고 나머지는 놓아준다.

{facet:tsne}

## 손잡이 하나가 답을 가른다

퍼플렉시티는 "점 하나가 이웃을 몇쯤 본다고 칠 것인가" 다. 그 수에 맞춰 점마다
가우시안의 폭 σ 를 이진 탐색으로 정하고, 그 폭으로 잰 이웃 관계를 평면에서
되살리도록 점을 끌고 민다.

| 퍼플렉시티 | 갈림 | 무리 사이 비 | 무슨 일이 일어났나 |
| --- | --- | --- | --- |
| (원래 자리) | 5.30 | 1 : 2.18 | 잰 그대로다 |
| 5 | 0.26 | 1 : 0.13 | 이웃을 너무 적게 보아 무리 C 가 흩어졌다 |
| 15 | 19.17 | 1 : 1.05 | 셋이 깨끗이 갈렸다 |
| 30 | 2.95 | 1 : 1.56 | 무리는 촘촘한데 경계가 맞닿았다 |

갈림은 **무리 사이 최단 거리 ÷ 무리 안 최대 퍼짐**이다. 5 를 넘으면 눈으로도
갈렸다고 볼 만하다. 퍼플렉시티 5 에서 이 값이 0.26 인 것은 무리 하나가 화면
전체로 흩어져 무리 안 퍼짐이 무리 사이 거리보다 커졌다는 뜻이다.

같은 자료, 같은 알고리즘, 같은 씨앗인데 손잡이 하나로 "무리가 셋이다" 가
"무리를 못 세겠다" 가 된다. **t-SNE 그림에서 무리의 수를 셀 때는 퍼플렉시티를
여럿 돌려 봐야 한다.**

## 깨끗이 갈렸다고 거리를 읽으면 안 된다

퍼플렉시티 15 의 그림은 셋이 또렷하다. 그런데 그 또렷한 그림에서 무리 사이 비는
**1 : 1.05** 다. 원래 두 배가 넘게 멀던 B–C 가 A–B 와 거의 같은 폭이 되었다.

t-SNE 가 틀린 것이 아니다. t-SNE 가 지키기로 한 것은 이웃 관계뿐이고, 무리 사이
거리는 처음부터 지키기로 한 목록에 없었다. 무리가 서로 충분히 멀면 그 사이의
p(j|i) 는 전부 바닥값 가까이로 눌리므로, 세 무리는 **얼마나 먼지에 대한 정보 없이**
서로 밀어내기만 한다. 그 결과 아무 데나 고르게 흩어져 앉는다.

그러니 이렇게 나눠 읽는다.

| 무엇을 | 읽어도 되는가 |
| --- | --- |
| 한 무리 안의 이웃 관계 | 읽어도 된다 |
| 무리가 몇 덩이인가 | 퍼플렉시티를 여럿 돌려 본 뒤에만 |
| 무리 사이의 멀고 가까움 | **읽으면 안 된다** |
| 무리의 크기(퍼진 폭) | **읽으면 안 된다** — 퍼플렉시티에 따라 통째로 바뀐다 |

## 그림이 깔고 있는 전제

- 왼쪽 판과 오른쪽 판의 **자는 단위가 다르다.** t-SNE 의 좌표에는 단위가 없어
  눈금을 그리지 않았다. 두 판에서 견줄 수 있는 것은 A–B 를 1 로 놓은 **몫**뿐이라,
  가운데를 잇는 줄 위에 그 몫만 적는다.
- 점선 동그라미는 무리가 퍼진 폭(가운데에서의 제곱평균 거리)이다. 퍼플렉시티 5
  에서 한 동그라미가 판을 넘어가는 것이 곧 "부서졌다" 는 말이다.
- 점 예순은 mulberry32 씨앗 4242 의 결정적 지터로 놓았고, 저차원 초기값은 씨앗
  27 로 뽑은 **표준편차 1e-4 의 정규분포**다. **자리 자체는 씨앗에 매인다** — 씨앗을 바꾸면
  세 무리가 화면의 다른 곳에 앉는다. 바뀌지 않는 것은 위 표의 결이다.
- 하강은 걸음 1000, η = 200, 초반 100 걸음에 과장 4, 관성은 20 걸음에서
  0.5 → 0.8 로 바뀐다. 걸음마다 무게중심을 0 으로 옮긴다.

## 곁에 두고 볼 조각

- **가까운 것만 가깝게** — 고리 하나를 줄로 펴 보이며, 이웃을 지키려면 어딘가는
  반드시 끊어야 한다는 것을 말한다.
- **전역과 지역** — 같은 자리를 두 방법으로 펴 견준다. 이웃만 지킨 그림에서
  무리 사이 간격이 어떻게 고르게 눌리는지가 거기 있다.

두 조각은 **손으로 편 결과**를 보인다. 여기서는 실제로 t-SNE 를 돌려 그 둘이
따로 말한 것을 한 화면에서 잇는다.
`;
