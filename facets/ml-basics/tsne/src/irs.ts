/**
 * t-SNE facet IR — **비워 둔다.** 이 완제품에는 코드 패널이 없다.
 *
 * ── 왜 비었는가
 *
 * t-SNE 를 IR 로 펴면 코드 패널이 말하는 것이 "t-SNE 가 무엇인가" 가 아니라
 * "이 구현이 어떻게 도는가" 가 된다. 셈의 대부분이 알고리즘의 주장이 아니라
 * 구현의 살림이기 때문이다.
 *
 *   - σ 를 정하는 **퍼플렉시티 이진 탐색** 이 스무 줄이다. β 를 1 에서 시작해
 *     엔트로피가 log(perplexity) 에 닿을 때까지 위아래로 조여 가는 되풀이인데,
 *     이 되풀이 자체는 t-SNE 의 주장이 아니라 "가우시안의 폭을 어떻게 고르나"
 *     라는 수치해석의 잡일이다.
 *   - **기울기와 하강** 이 또 스무 줄이다. gains 를 0.2 씩 올리고 0.8 씩 줄이는
 *     것, 관성을 0.5 에서 0.8 로 바꾸는 것, 초반 100 걸음만 과장을 거는 것은
 *     전부 "이 최적화가 터지지 않게 하는 요령" 이지 t-SNE 가 세상에 대해
 *     말하는 바가 아니다.
 *
 * 그 마흔 줄을 여섯 언어로 갈라 보여 주면 독자는 알고리즘 대신 살림을 읽는다.
 * `svm` 에서 SMO 를 버린 것과 같은 판단이고, `linearRegression` 이 `irs.ts` 를
 * 빈 배열로 두고도 완제품인 선례가 있다.
 *
 * ── 그러면 이 완제품은 무엇으로 완제품인가
 *
 * **손잡이가 논증을 진다.** 퍼플렉시티를 5 · 15 · 30 으로 옮기는 것 자체가
 * 주장이다 — 작으면 무리가 부서지고, 크면 흐려지고, 맞으면 깨끗이 갈린다.
 * 그리고 깨끗이 갈린 그 그림에서조차 무리 사이 비는 원래의 1 : 2.18 이 아니라
 * 1 : 1.05 가 된다. 조각 `keepNeighborsClose` 와 `globalAndLocal` 이 손으로 편
 * 결과로 따로 말한 것을, 여기서는 실제로 t-SNE 를 돌려 한 화면에서 잇는다.
 *
 * ── 뒤따르는 규약
 *
 * IR 이 없으므로 `facet.ts` 에 `code-view` 블록을 두지 않고, `algorithm.ts` 도
 * `phase` 이벤트를 발신하지 않는다. C3 은 all-or-none 이라 한쪽만 두면 안 된다.
 */

import type { IR } from '@ffacet/core';

export const tsneIRs: IR[] = [];
