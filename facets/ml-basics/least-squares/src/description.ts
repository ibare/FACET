/**
 * 최소제곱 조각의 글.
 *
 * 조각이 화면에 각주를 두지 않는 대신, 전제를 밝히는 것은 이 글의 몫이다
 * (S-piece) — 견줄 직선 셋은 미리 정해 둔 것이고 이 조각은 직선을 학습하지
 * 않는다는 것, 그리고 오른쪽 탑의 높이가 곧 정사각형들의 넓이 합이라는 것.
 */

export const leastSquaresDescription = `## 벗어남을 그냥 더하면 왜 안 되는가

직선 하나를 놓고 "이 직선이 점들에서 얼마나 벗어나는가" 를 재려 한다. 가장 먼저
떠오르는 방법은 벗어난 만큼을 다 더해 보는 것이다. 그런데 그 셈은 아무것도
가려내지 못한다.

{facet:leastSquares}

점 넷 \`(1,2) (2,5) (3,4) (4,9)\` 에 직선 셋을 대 본다. 셋은 미리 정해 둔 것이고
이 조각은 직선을 학습하지 않는다 — 견주기 위해 세워 둔 것이다.

- 평평한 것 \`y = 5\` — 벗어남 \`−3, 0, −1, +4\`
- 가파른 것 \`y = 3x − 2.5\` — 벗어남 \`+1.5, +1.5, −2.5, −0.5\`
- 들어맞는 것 \`y = 2x\` — 벗어남 \`0, +1, −2, +1\`

셋의 벗어남을 그냥 더하면 **모두 0** 이다. 눈으로 보기에 하나는 엉터리고 하나는
꽤 좋은데도 셈은 같은 답을 낸다. 위로 벗어난 만큼과 아래로 벗어난 만큼이 만나
서로를 지우기 때문이다. 부호를 지닌 채 쌓으면 내려간 만큼 다시 올라와 기준선으로
돌아온다.

그래서 벗어남을 **제곱한다.** 제곱은 그 길이를 한 변으로 하는 정사각형의 넓이이고,
넓이에는 음수가 없다. 지워질 방향이 없으니 쌓이기만 한다. 화면 오른쪽에서
정사각형이 레인 폭에 맞춰 눌리며 위로 늘어나는데, 눌러도 넓이는 그대로라 **탑의
높이가 곧 정사각형들의 넓이 합**이다.

- 평평한 것 \`26\`
- 가파른 것 \`11\`
- 들어맞는 것 \`6\`

이제야 셋이 갈린다. 이 넷을 지나는 직선 가운데 제곱합을 가장 작게 만드는 것이
실제로 \`y = 2x\` 이고, 그 값 \`6\` 보다 작은 직선은 없다. 최소제곱이란 이 셈을
가장 작게 만드는 직선을 고르는 일이다.

절댓값을 더해도 셋이 갈리기는 한다. 제곱을 쓰는 까닭은 따로 있다 — 크게 벗어난
하나를 더 무겁게 벌하고, 미분이 되어 답을 식으로 풀 수 있게 해 준다. 그 이야기는
이 조각의 몫이 아니다.
`;
