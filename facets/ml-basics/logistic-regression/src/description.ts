/**
 * 로지스틱 회귀 facet 설명 — 학습자에게 노출되는 본문.
 *
 * placeholder `{facet:logisticRegression}` 은 host 가 stage 임베드 자리를
 * 차지하도록 치환한다. 본문 한국어 markdown.
 */

export const logisticRegressionDescription = `## 로지스틱 회귀

로지스틱 회귀는 "이쪽이냐 저쪽이냐" 를 **수 하나로 답하지 않고 확률로 답하는**
분류기다. 두 축의 값에 무게를 곱해 더한 것(\`z = w₀x₁ + w₁x₂ + b\`)은 −∞ 에서
+∞ 까지 끝없이 뻗는 수인데, 시그모이드 \`1 / (1 + e^{−z})\` 가 그 무한한 축을
0 과 1 사이의 띠로 접는다. 접힌 뒤의 값이 "이 점이 위쪽 무리일 확률" 이다.

학습은 그 확률이 실제 이름표에 가까워지도록 무게를 미는 일이다. 어긋난 만큼
\`p − y\` 에 그 축의 값을 곱해 모으면 기울기가 되고, 무게를 그 반대쪽으로
학습률만큼 옮긴다. 이것을 되풀이하면 확률의 지도가 점점 또렷해진다.

{facet:logisticRegression}

### 배우는 것과 고르는 것

여기가 이 모형의 요점이다. **무게는 학습이 정하지만 문턱은 학습이 정하지
않는다.** 확률 0.62 를 두고 "이쪽" 이라 할지 "저쪽" 이라 할지는 데이터가 아니라
쓰는 사람이 정한다.

문턱을 0.3 · 0.5 · 0.8 로 옮겨 보면 그 맞바꿈이 그대로 드러난다. 문턱을 낮추면
확률이 조금만 높아도 "이쪽" 이라 하니 **놓치는 것이 줄고 헛짚는 것이 는다.**
올리면 그 반대다. 열아홉 점 가운데 두 점이 서로의 영역에 발을 걸치고 있어서,
어떤 문턱을 골라도 모두 맞힐 수는 없다 — 무엇을 더 두려워하는지를 고를 수
있을 뿐이다.

### 겹침이 있어야 하는 까닭

두 무리가 깨끗이 갈라지면 무게를 키울수록 손실이 계속 줄어들어 학습이 멈추지
않는다. 확률이 전부 0 과 1 에 달라붙으니 문턱을 어디에 두어도 판정이 같아지고,
문턱 조작이 뜻을 잃는다. 겹치는 두 점이 이 그림을 살아 있게 한다.

### 선형 회귀와의 구별

선형 회귀는 점들 사이를 **지나는** 직선을 찾는다. 로지스틱 회귀의 직선은 점들
사이를 **가르는** 자리다 — 확률이 문턱을 넘나드는 등고선 한 줄이다. 그래서
문턱을 옮기면 직선이 평행하게 미끄러진다. 기울기는 무게가 정하고, 위치는
문턱이 정한다.
`;
