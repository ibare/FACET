export const denseNeighborhoodDescription = `
## 무리를 가운데로 정하지 않는 방법

무리를 나누는 가장 흔한 방법은 가운데를 몇 개 찍어 두고 **각 점이 어느 가운데에
더 가까운가**로 가르는 것이다. 그 방법은 무리가 덩이일 때 잘 듣는다. 덩이가
아니면 듣지 않는다.

여기 점 스무 개가 있다. 바깥에 고리로 늘어선 열넷과, 그 한가운데 뭉친 여섯이다.
두 무리의 가운데가 **거의 같은 자리**다 — 고리 열넷의 평균은 (4.00, 4.00) 이고
덩이 여섯의 평균은 (4.07, 3.90) 이다. 가운데로부터의 거리로 가르는 방법은 여기서
할 말이 없다. 무엇을 가운데로 잡든 고리를 반으로 잘라 한쪽은 이쪽, 한쪽은
저쪽으로 보낸다.

## 번짐으로 정한다

가운데를 아예 두지 않는 길이 있다. 자를 하나 정해 두고 — 여기서는 \`eps = 1.60\` —
**그 안에 든 것끼리 이어 붙이는 것**이다.

한 점의 \`eps\` 안에 이웃이 셋 이상이면 그 점은 무리의 속이다. 속인 점의 이웃은
같은 무리가 되고, 그 이웃이 또 속이면 거기서 다시 번진다. 불이 옮아붙는 것과
같다. 옮아붙기를 멈추는 자리까지가 한 무리다.

{facet:denseNeighborhood}

## 두 수의 차이가 전부다

오른쪽 눈금이 이 그림의 논증이다. 번짐이 재 본 거리가 전부 그 축에 떨어지는데,
떨어지는 자리가 두 무더기로 갈린다.

- 고리에서 이웃한 두 점 사이는 **1.42 남짓**. \`eps\` 아래라 이어진다.
- 고리와 가운데 덩이 사이의 가장 짧은 거리는 **2.53**. \`eps\` 위라 안 이어진다.

그 사이에 \`1.60\` 이 놓여 있다. 무리가 둘로 갈리는 까닭은 그것뿐이다. 고리가
둥글다는 것도, 덩이가 뭉쳐 있다는 것도 알고리즘은 모른다 — 잰 것은 거리 하나이고,
넘느냐 못 넘느냐가 전부다.

## 그래서 무리가 둥글 필요가 없다

번짐은 이어진 자리를 따라간다. 고리를 따라 열넷을 한 바퀴 돌아 제자리로 오는
동안, 한 번도 고리 안쪽으로 질러가지 않는다. 이어진 이웃이 그쪽에 없기 때문이다.
그래서 이 방법은 초승달이든 나선이든 이어져만 있으면 한 무리로 본다.

대신 값이 하나 더 든다. \`eps\` 와 최소 이웃 수를 사람이 정해야 하고, 그 둘이
바뀌면 답도 바뀐다. \`eps\` 를 \`2.6\` 으로 키웠다면 이 그림의 스무 점은 전부
한 무리가 됐을 것이다.

## 이 그림이 말하지 않는 것

이 자료에는 어느 무리에도 못 드는 점 — 잡음 — 이 하나도 없다. 스무 점이 전부
속이라 경계와 잡음을 가르는 대목은 여기서 보이지 않는다. 밀도로 나누는 방법이
실제로 값을 하는 자리 중 하나가 그 대목인데, 그것은 다른 그림의 몫이다.
`;
