/**
 * dbscan 완결형의 글.
 *
 * `{facet:dbscan}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const dbscanDescription = `
## DBSCAN

무리를 나눌 때 대개는 "가운데를 몇 개 둘 것인가" 부터 정한다. DBSCAN 은 그것을
묻지 않는다. 대신 손잡이 둘을 준다 — **eps** 는 "얼마나 가까워야 이웃인가",
**minPts** 는 "이웃이 몇이어야 속이 찬 것인가" 다. 무리의 수는 정하는 것이
아니라 이 둘에서 **나오는** 값이다.

그래서 배우는 사람이 곧바로 막히는 자리가 여기다. 손잡이가 둘인데 나오는 것은
수 하나이니, 어느 손잡이를 돌려야 하는지 알 길이 없다. 아래에서 둘을 따로
돌려 보면 그 둘이 서로 다른 것을 만진다는 것이 보인다.

{facet:dbscan}

## eps 는 잇는다

eps 는 두 점이 이웃인지 가르는 자다. 올리면 이웃의 범위가 넓어지고, 넓어진
만큼 **떨어져 있던 덩이가 서로 닿는다.** 닿으면 둘은 한 무리다.

minPts 를 3 에 두고 eps 만 올려 보라. 무리 수가 3 → 4 → 3 → 2 → 1 로 간다.
1.0 에서 3 인 것은 오른쪽 끝의 성긴 덩이가 아직 자기들끼리도 못 닿아서고,
1.3 에서 4 가 되며 그 덩이가 잡히고, 그 뒤로는 계속 **합쳐지기만** 한다.
3.0 에서는 넷이 통째로 하나다.

잡음 수는 이 사이에 거의 움직이지 않는다 — 1.3 부터 3.0 까지 내내 2 다.
eps 를 올려도 밖에 남은 외톨이 둘은 그대로다. eps 가 만지는 것은 **무리 사이의
경계**이지 어느 점이 남는가가 아니다.

## minPts 는 무너뜨린다

minPts 는 "이 점 둘레 eps 안에 몇이 있어야 속점인가" 다. 속점이 없으면 번짐이
시작되지 않으므로, minPts 를 올리면 무리가 합쳐지는 것이 아니라 **속부터 빈다.**

eps 를 1.9 에 두고 minPts 만 올려 보라. 무리는 3 → 3 → 2 → 1 인데 잡음이
2 → 2 → 5 → 9 로 뛴다. 무리 수만 보면 eps 를 올린 것과 비슷해 보이지만
속내가 다르다 — eps 는 덩이를 **붙여서** 수를 줄이고, minPts 는 덩이를
**지워서** 수를 줄인다. 지워진 점들은 잡음으로 화면에 ×로 남는다.

작은 덩이부터 무너진다. 셋뿐인 덩이는 minPts 가 4 가 되는 순간 아무도 속점이
될 수 없다 — 자기를 넣어 세어도 셋이기 때문이다.

## 같은 무리 수, 다른 속내

대조표에서 무리 2 인 칸을 찾아보면 여럿이다. eps 2.4 · minPts 2 도 2 이고
eps 1.9 · minPts 4 도 2 다. 그런데 앞의 것은 잡음이 2 이고 뒤의 것은 5 다.
무리 수 하나만 보고 손잡이를 고르면 이 차이가 보이지 않는다.

거꾸로 무리가 1 로 떨어지는 길도 둘이다. eps 3.0 은 **다 붙어서** 1 이고
(잡음 2), minPts 5 는 **다 무너져서** 1 이다 (eps 1.0·1.3 에서 잡음 13).
같은 숫자가 정반대의 사정을 가리킨다.

## 번짐과 스택

화면 아래 띠가 스택이다. 코드에서 스택은 자료구조 이름이 아니라 **배열 하나와
꼭대기 색인 하나**로 되어 있다 — \`stack[top] = j; top = top + 1\` 로 넣고
\`top = top - 1; q = stack[top]\` 로 꺼낸다.

이렇게 펴 놓으면 알고리즘이 하는 일이 그대로 보인다. 꺼낸 점의 이웃을 다시
밀어 넣는 것, 그것이 "이웃의 이웃으로 옮아붙는다" 의 정확한 뜻이다. 꼭대기가
0 으로 내려오면 그 무리는 더 번질 데가 없다는 뜻이고, 그때 바깥 고리가 다음
씨앗을 찾으러 간다.

꼭대기 위쪽 칸의 숫자를 지우지 않은 것은 실제로 그렇기 때문이다. 배열로 만든
스택은 꺼낼 때 값을 비우지 않는다. 색인 하나가 내려갈 뿐이다.

## 잡음이 가장자리가 되는 순간

한 번 "잡음" 이라고 적은 점이 나중에 무리에 드는 갈래가 코드에 있다.
\`if label[j] == -1: label[j] = clusters\` 가 그것이다. 자기 둘레에는 이웃이
모자랐지만, 어떤 속점의 eps 안에 들어 있으면 그 무리의 **가장자리**가 된다.

가장자리는 무리에 들어도 스택에 올리지 않는다. 속점이 아니므로 그 점에서
번짐이 이어지지 않기 때문이다. 화면에서 속점은 속이 찬 동그라미, 가장자리는
테두리만 있는 동그라미다.

이 한 갈래가 없으면 무리마다 테두리가 통째로 잡음이 되어 잡음 수가 크게
어긋난다. 바깥 고리가 점을 어느 차례로 보느냐에 따라 어떤 무리의 가장자리가
되는지는 달라질 수 있지만, 무리 수와 잡음 수는 달라지지 않는다.

## 거리를 몇 번 쟀는가

계기의 "잰 거리" 는 손잡이를 옮길 때마다 늘어난다. 점 열아홉으로 한 번 셈하는
데 손잡이에 따라 사백에서 팔백 번을 잰다. 씨앗을 볼 때 한 번, 스택에서 꺼내
속인지 볼 때 한 번, 이웃에게 옮아붙일 때 또 한 번 훑기 때문이다. 점이 n 개면
대략 n² 에 비례한다.

이것이 DBSCAN 의 값이다. 무리의 수를 미리 정하지 않아도 되는 대신, 이웃을
찾는 일을 점마다 되풀이한다. 실제 구현에서 격자나 나무로 이웃 질의를 빠르게
만드는 것도 여기를 줄이려는 것이다.
`;
