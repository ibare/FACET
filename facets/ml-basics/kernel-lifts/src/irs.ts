/**
 * 커널 트릭 IR — 비어 있다.
 *
 * 조각은 코드 패널을 두지 않는다. 한 주장만 말하고 멈추는 물건이라 여섯 언어로
 * 갈리는 소스를 곁들일 자리가 없다 (S-piece). 파일은 6파일 구성을 지키기 위해
 * 남긴다 (S-facet).
 */

import type { IR } from '@ffacet/core/runtime';

export const kernelLiftsIRs: IR[] = [];
