/**
 * knn 완제품의 글.
 *
 * `{facet:knn}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const knnDescription = `
## k-최근접 이웃

새로운 자리 하나를 두고 "이것은 A 인가 B 인가" 를 물어야 한다. 가장 손쉬운
대답은 **가까운 것에게 물어보는 것**이다. 이미 이름표가 붙은 점들 가운데
가장 가까운 몇을 불러 세우고, 그들이 던진 표를 세어 많은 쪽으로 정한다.

여기서 정할 것이 딱 하나 남는다. **몇에게 물을 것인가.** 그 수가 k 다.

{facet:knn}

## 학습이 없다

이 알고리즘에는 훈련 단계가 없다. 계수도 없고 미리 셈해 둔 표도 없다. 자료를
그대로 두었다가 물음이 올 때마다 처음부터 끝까지 훑는다 — 화면 아래 "잰 거리"
가 만 단위로 뛰는 것이 그 값이다. 평면의 자리 576 곳을 물으면 열여덟 개
거리를 576 번 잰다. k 를 옮기면 그 일을 통째로 다시 한다.

코드 패널이 보이는 것도 그것이다. \`classify\` 하나가 전부이고, 그 안은 세 토막이다.

- **잰다.** \`dist[i]\` 에 제곱 거리를 담는다. \`sqrt\` 를 부르지 않는데,
  제곱근을 씌워도 **가까운 차례가 바뀌지 않기 때문**이다. 여기서 거리는
  "얼마나 먼가" 가 아니라 "누가 더 가까운가" 로만 쓰인다.
- **고른다.** 정렬 함수를 부르지 않는다. 선택 정렬처럼 k 번 돌며 매번 아직
  안 뽑힌 것 중 가장 가까운 것을 집는다. k 가 커질수록 훑는 횟수가 그만큼
  늘어난다는 사실이 이 이중 루프에 그대로 있다.
- **센다.** \`votesA\` 와 \`votesB\` 두 칸에 표를 쌓고 큰 쪽을 답으로 낸다.

## k 를 옮기면 한 점이 아니라 평면이 바뀐다

슬라이더를 1 · 3 · 7 · 15 로 옮겨 보라. 바뀌는 것은 지금 보고 있는 물음점의
답만이 아니다. **평면 전체의 색이 다시 칠해진다.** 판정이 뒤집힌 칸에는 테두리를
둘러 두었다.

- k = 1 에서 3 으로 갈 때 뒤집히는 칸이 가장 많다. 1 에서 15 까지 가면 576 중
  일흔여덟 칸, 곧 여덟에 하나가 다른 답을 낸다.
- k 가 커질수록 경계가 매끄러워지고, 외딴 점이 만들어 두었던 작은 섬이 주변에
  삼켜진다.

## k = 1 은 자료를 외운다

자료 열여덟 가운데 둘이 남의 무리 안에 있다 — \`A (5.2, 4.6)\` 은 B 들 사이에,
\`B (4.2, 3.2)\` 는 A 들 사이에 있다.

k = 1 로 두면 이 둘도 제 이름표대로 판정된다. 당연하다. **자기 자신이 자기의
가장 가까운 이웃**이라 거리가 0 이고, 표는 언제나 제 이름표에 던져진다. 그래서
k = 1 에서는 "자기 이름표와 다르게 판정된 점" 이 0 이다. 자료를 하나도 틀리지
않는데, 그것은 잘 맞힌 것이 아니라 **통째로 외운 것**이다.

k 를 3 으로 올리면 그 둘은 곧바로 주변에 삼켜진다. 화면의 붉은 고리가 그
점들이다. 틀린 점 수가 0 에서 늘어난 그 순간이, 자료를 외우는 일과 이웃에게
묻는 일이 갈리는 자리다.
`;
