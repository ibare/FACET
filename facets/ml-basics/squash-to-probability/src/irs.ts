/**
 * 조각은 코드 패널을 두지 않는다 (S-piece). 답하는 것이 질문 하나뿐이라
 * 보일 소스가 없고, `blocks` 에도 code-view 가 없다. 빈 배열로 둔다.
 */

import type { IR } from '@ffacet/core/runtime';

export const squashToProbabilityIRs: IR[] = [];
