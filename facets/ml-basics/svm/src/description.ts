export const svmDescription = `
## 틈 안에 점이 들어와 버리면

두 무리를 가르는 직선은 무수히 많다. 그중 하나를 고르는 기준으로 **가장 넓은
틈** 을 쓰기로 했다 — 선을 한가운데 두고 띠를 부풀리다가 어느 점에 닿으면
멈추고, 그 두께가 가장 두꺼워지는 선을 고른다. 닿은 점만이 답을 정하고 나머지는
멀리 있을 뿐이다.

여기까지는 두 무리가 **직선 하나로 완전히 갈린다** 는 전제 위에 서 있다. 그
전제를 깨면 어떻게 되는가. 점 하나를 반대 무리 한복판으로 옮겨 보자. 이제 어떤
직선을 그어도 한쪽에는 두 이름표가 섞여 남는다. 띠는 어느 두께에서도 멈추지
못한다.

{facet:svm}

### 완벽을 포기하는 값

답은 차원을 올리는 것이 아니다. **얼마나 틀려도 되는지를 값으로 정하는 것**이다.

여백을 못 지킨 정도를 수로 재고, 그 수만큼 벌을 매긴다. 점 하나가 얼마나
못 지켰는지는

    max(0, 1 - y(w·x + b))

로 잰다. \`y\` 는 이름표이고 −1 또는 +1 이다. 제 쪽에 넉넉히 서 있으면
\`y(w·x+b)\` 가 1 을 넘어 괄호 안이 음수가 되고, \`max\` 가 그것을 0 으로
깎는다. **여백을 지킨 점은 벌점이 0 이다.** 틈 안으로 들어오면 0 과 1 사이,
선을 넘어가면 1 을 넘는다. 이것을 힌지(경첩) 손실이라 부른다 — 어느 지점부터
꺾여 올라가는 모양이 그렇다.

전체는 이렇게 된다.

    L = (1/2)|w|² + C · Σ max(0, 1 - y(w·x + b))

앞은 틈을 넓히려는 힘이고 (\`|w|\` 가 작을수록 틈 \`2/|w|\` 가 넓다), 뒤는 틀리지
않으려는 힘이다. **C 가 그 둘의 환율이다.**

### C 를 옮겨 보라

- **C = 0.1** — 몇을 포기하더라도 넓게 간다. 틈 안에 점이 여럿 들어와도 개의치
  않는다.
- **C = 10** — 한 점도 틀리지 않으려 한다. 틈이 좁아진다.

겹침을 켜든 끄든 이 방향은 같다. 겹침이 있을 때 달라지는 것은 **셋 다 그 한
점을 포기한다는 사실이 아니라, 얼마나 좁히면서 포기하는가**다. 오른쪽 기록장에
C 마다의 폭이 쌓이므로 셋을 나란히 놓고 견줄 수 있다.

### 코드에서 볼 것은 조건문 하나다

코드 패널이 보이는 것은 훈련 한 걸음이다. SVM 을 실제로 푸는 방법으로 SMO 가
널리 쓰이지만 그것을 펼치면 화면이 "SVM 이 무엇인가" 대신 "SMO 가 어떻게 도는가"
를 말하게 된다. 선형 SVM 은 이 한 걸음을 되풀이해 실제로 훈련한다.

그 안에서 가장 중요한 줄은 이것이다.

    if m < 1:

여백을 이미 지킨 점은 이 안으로 들어오지 못하고, 따라서 기울기 \`g\` 를 조금도
건드리지 못한다. **선을 미는 것은 틈 안에 들어온 점과 선을 넘어간 점뿐이다.**
서포트 벡터가 무엇인가가 이 조건문 하나에 들어 있다. 마지막 한 걸음에서 점을
하나씩 짚어 가는 것은 그 장면을 보이기 위해서다 — 멀리 있는 점 차례가 되면
\`accumulate\` 줄이 그냥 지나간다.

### 전제

- 훈련은 매번 \`w = 0\`, \`b = 0\` 에서 다시 시작한다. 학습률 0.05 로 4000 걸음.
  손잡이를 옮길 때마다 처음부터 다시 푸는 것은, 그래야 \`C\` 만 다른 두 답을
  견줄 수 있기 때문이다.
- 4000 걸음을 다 보이지는 않는다. 앞은 촘촘히 뒤는 성기게 열여섯 자리를 짚는다 —
  경사하강은 빠르게 자리를 잡고 천천히 다듬기 때문이다.
- 가로세로 축척을 같게 두었다. 마진 폭은 선에 수직인 거리라 눈금이 어긋나면
  두께를 눈으로 견줄 수 없다.
- 커널은 쓰지 않는다. 굽은 경계로 가르는 이야기는 따로 있고, 여기서 하는 말은
  **곧은 선을 그대로 두고 잘못을 값으로 사들이는 쪽** 이다.
`;
