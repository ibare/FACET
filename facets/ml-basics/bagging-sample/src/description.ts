export const baggingSampleDescription = `## 자료가 한 벌뿐인데 나무를 여럿 기르는 법

여러 나무를 길러 표를 모으려면 나무마다 다른 것을 배워야 한다. 그런데 손에 든
자료는 한 벌뿐이다. 같은 자료로 같은 방식으로 기르면 똑같은 나무가 여럿 나올
뿐이다.

배깅은 자료를 새로 구하는 대신 **가지고 있는 것에서 다시 뽑는다.** 여덟 개짜리
자료에서 하나를 뽑아 새 벌에 담고, 뽑은 것을 **도로 넣는다.** 그렇게 여덟 번
뽑으면 크기 여덟짜리 새 벌이 하나 생긴다.

{facet:baggingSample}

## 되돌리는 그 동작이 전부다

도로 넣지 않는다면 여덟 번 뽑아 여덟 개를 다 가져가게 되고, 세 벌이 결국 원본과
똑같아진다. 되돌리기 때문에 같은 것이 두 번 세 번 뽑히고, 그래서 자리가 모자라
**한 번도 안 뽑히는 것**이 생긴다. 벌마다 두 번 뽑힌 것이 다르고, 그래서 벌마다
빠지는 것도 다르다. 나무가 서로 달라지는 까닭이 여기 있다.

## 남은 것은 버리는 것이 아니다

그 벌을 기를 때 한 번도 쓰이지 않은 자료는, 그 나무가 **한 번도 본 적 없는**
자료다. 그래서 그 나무를 시험하는 데 그대로 쓸 수 있다. 따로 시험용을 떼어 두지
않고도 검증 자료가 공짜로 생기는 셈이고, 이것을 OOB(out-of-bag) 라 부른다.

## 얼마나 남는가

n 개에서 n 번 뽑을 때 특정 하나가 한 번도 안 뽑힐 확률은 매 뽑기가 서로
독립이므로 \`(1 − 1/n)^n\` 이다. n 이 8 이면 34.4%, n 을 키우면 \`1/e\` ≈ 36.8%
로 다가간다. 대체로 자료의 3분의 1 남짓이 매번 남는다.

그림의 세 벌은 뽑은 순서를 고정해 둔 것이라 실제 값은 25% · 37.5% · 37.5% 로
갈린다. 여덟 번뿐인 뽑기라 흔들림이 큰 것이고, 자료가 커지면 그 흔들림이 줄어
이론값 가까이 모인다.
`;
