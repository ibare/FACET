/**
 * 잔차 조각의 글. 화면이 밝히지 않는 전제 — 직선이 고정이라는 것 — 은 여기서 말한다
 * (S-piece: 전제는 글이 밝힌다).
 */

export const residualDistanceDescription = `## 잔차 — 선에서 벗어난 만큼

직선 하나를 그어 놓고 점 하나를 본다. 이 점에서 그 직선은 얼마나 틀렸는가.

잴 방법이 둘로 갈린다. 점에서 직선까지 **가장 짧은 거리** 는 직선에 직각으로 닿는
자리, 곧 수선의 길이다. 그런데 회귀가 쓰는 것은 그것이 아니라 **세로 거리** 다.

{facet:residualDistance}

까닭은 우리가 무엇을 맞히려 하는지에 있다. 직선은 x 를 받아 y 를 내놓는 장치이고,
틀렸다는 말은 **내놓은 y 가 실제 y 와 다르다** 는 뜻이다. 그러니 재는 것도 y 방향의
어긋남이어야 한다. x 는 주어진 값이지 맞힐 값이 아니다.

이 세로 어긋남이 잔차다.

    잔차 = 실제 y − 예측 y

부호가 함께 남는다. 점이 선 위에 있으면 잔차는 양수 — 예측보다 남는다. 아래에 있으면
음수 — 모자란다. 길이는 얼마나 틀렸는지를, 부호는 어느 쪽으로 틀렸는지를 말한다.

그림의 직선은 고정이다. 계수를 학습하지 않는다 — 잰다는 일 하나만 보이려는 것이라,
선을 옮겨 잔차를 줄이는 이야기는 여기 없다.
`;
