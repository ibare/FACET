export const kmeansDescription = `
## 가운데를 모르는 채로 무리를 찾는다

점이 흩어져 있다. 몇 덩이로 뭉쳐 있다는 것까지는 보이는데 **각 덩이의 가운데가
어디인지는 모른다.** 가운데를 알면 어느 점이 어느 덩이인지 알 수 있고, 어느 점이
어느 덩이인지 알면 가운데를 셈할 수 있다. 둘이 서로를 필요로 한다.

k-평균은 이 고리를 자르지 않고 **번갈아 답한다.** 가운데를 안다고 치고 점을
붙이고(할당), 무리를 안다고 치고 가운데를 옮긴다(갱신). 붙고 옮기고, 다시 붙고
다시 옮긴다. 아무도 안 움직이는 회가 오면 멎는다.

{facet:kmeans}

## 무엇을 보장하는가

**멎는다는 것은 보장한다.** 아무도 안 움직였다는 것은 곧 붙는 자리가 그대로였다는
뜻이고, 붙는 자리가 그대로면 평균도 그대로이니 다음 회도 똑같다. 그러니 더 돌
까닭이 없다. 흩어짐 — 각 점에서 제 중심까지 거리의 **제곱**을 다 더한 값 — 은
두 몸짓 어느 쪽에서도 늘어나지 않으므로 언젠가는 반드시 멎는다.

멎은 자리는 **더 옮길 데가 없는 자리**다. 점 하나를 다른 중심으로 옮겨도, 중심
하나를 다른 데로 밀어도 흩어짐이 줄지 않는다.

## 무엇을 못 하는가

### 시작이 다르면 답이 다르다

화면의 **시작 다시 뽑기** 를 눌러 보라. k 는 3 그대로인데 시작 중심만 바뀐다.

- 시작 A (점 0 · 1 · 2) → 무리 3/3/6, 흩어짐 8.80
- 시작 B (점 0 · 6 · 9) → 무리 6/3/3, 흩어짐 18.45
- 시작 C (점 0 · 6 · 7) → 무리 4/2/6, 흩어짐 10.31
- 시작 D (점 2 · 6 · 10) → 무리 6/3/3, 흩어짐 18.45

**서로 다른 답이 셋.** 그리고 셋 다 "더 옮길 데가 없는" 답이다 — 어느 것도 실패한
것이 아니고, 어느 것도 도중에 멈춘 것이 아니다. k-평균이 닿는 곳은 가장 좋은
답이 아니라 **제 자리에서 더 나아질 데가 없는 답**이다.

흩어짐 차이는 9.65 다. 시작을 어디에 찍었느냐만으로 그만큼이 갈린다.

### 흩어짐이 가장 작은 답이 옳은 무리가 아니다

이 자료를 사람이 보면 무엇이 보이는가. 왼쪽 아래에 길게 늘어선 **띠 여섯**,
오른쪽 위에 떨어져 있는 **작은 덩이 둘**. 6/3/3 이다.

그런데 흩어짐만 보면 그 답은 18.45 로 꼴찌에 가깝다. 가장 촘촘한 답은 8.80 인
3/3/6 — **띠를 가운데서 쪼개고, 떨어져 있던 두 덩이를 하나로 합친** 나눔이다.
두 배 넘게 낫다.

띠는 길다. 길게 늘어선 것을 한 무리로 두면 각 점에서 가운데까지가 멀어 흩어짐이
커진다. 반대로 오른쪽 두 덩이는 서로 가까워, 합쳐도 흩어짐이 별로 안 는다.
흩어짐이라는 잣대는 **뭉친 정도**를 재지 **떨어진 정도**를 재지 않는다. 그래서
"길지만 하나인 것" 을 싫어하고 "가깝지만 둘인 것" 을 좋아한다.

장부에서 노란 막대가 붙은 줄이 가장 촘촘한 답이고, 동그라미가 붙은 줄이 사람이
보는 무리다. **둘이 같은 줄이 아니라는 것**이 이 화면이 말하는 두 번째다.

## k 는 누가 정하는가

**k 슬라이더**를 옮겨 보라. 2 · 3 · 4 · 5 어느 것을 주어도 알고리즘은 성실하게
답을 낸다. 몇 무리인지는 알고리즘이 찾아 주는 것이 아니라 **받아서 쓰는 것**이다.

k = 2 만은 시작을 무엇으로 잡아도 6/6 하나에 멎는다. 위아래 두 덩이가 워낙 멀리
떨어져 있어 다른 자리에 멎을 길이 없기 때문이다. 시작이 답을 가르는 것은 **자료가
여러 답을 품고 있을 때**뿐이다.

## 코드가 말하는 것

코드 패널의 진입점은 한 판이 아니라 **한 걸음**이다 — 할당 한 번, 갱신 한 번.
돌려주는 값은 중심이 옮긴 거리의 합이고, 그것이 0 이면 멎은 것이다. 호출부가
0 이 나올 때까지 되부르는 것이 곧 k-평균이다.

거리를 재는 안쪽 루프에 **제곱근이 없다.** 제곱근은 음 아닌 수에서 단조라
\`a < b\` 와 \`sqrt(a) < sqrt(b)\` 가 같은 답을 낸다 — 가장 가까운 것이 바뀌지
않으므로 씌울 까닭이 없다. 그래서 변수 이름도 \`bestsq\` · \`dsq\` 로 "제곱" 임을
달고 있다.

\`sqrt\` 는 딱 한 번, 중심이 옮긴 거리를 더하는 자리에만 나온다. 거기서는 순서가
아니라 **크기 자체**가 답이기 때문이다.

## 이 그림이 말하지 않는 것

- 시작 중심을 **점 위에서만** 고른다. 실제로는 k-means++ 처럼 서로 먼 점을
  고르도록 확률을 기울이는 방법이 있고, 그것이 나쁜 시작에 걸릴 확률을 크게 줄인다.
  줄일 뿐 없애지는 못한다.
- 여러 시작으로 여러 번 돌려 흩어짐이 가장 작은 것을 고르는 것이 실제 관행이다.
  그러면 시작 문제는 상당히 눌린다 — 다만 **흩어짐이 가장 작은 것을 고른다** 는
  그 관행이 위에서 본 둘째 문제를 오히려 키운다.
- 무리가 둥글고 크기가 비슷하다는 전제가 깔려 있다. 길쭉하거나 크기가 크게 다른
  무리에는 이 잣대가 맞지 않는다.
`;
