/**
 * 조각은 코드 패널을 두지 않는다 (S-piece). IR 은 빈 배열로 남긴다 —
 * `index.ts` 의 등록 순서를 다른 facet 과 같게 두기 위해 파일만 유지한다.
 */

import type { IR } from '@ffacet/core/runtime';

export const widestMarginIRs: IR[] = [];
