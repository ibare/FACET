export const hierarchicalDescription = `
## 두 무리 사이의 거리는 무엇인가

점 둘 사이의 거리는 물을 것이 없다. 자를 대면 된다. 그런데 **무리 둘 사이의
거리**는 그렇지 않다. 한쪽에 점이 셋, 다른 쪽에 점이 둘이면 점쌍은 여섯이고,
그 여섯 개의 수에서 하나를 뽑아야 한다.

세 가지 답이 있다.

- **단일 연결** — 여섯 중 **가장 작은** 것. 두 무리에서 가장 가까운 한 쌍만 본다.
- **완전 연결** — 여섯 중 **가장 큰** 것. 가장 먼 한 쌍이 두 무리를 대표한다.
- **평균 연결** — 여섯의 **평균**.

셋 다 말이 된다. 그리고 셋은 서로 **다른 나무**를 짓는다.

{facet:hierarchical}

### 다리를 놓아 보면

화면의 점 여덟은 두 덩이다. 왼쪽 아래에 a · b · c 가 모여 있고 오른쪽 위에
f · g · h 가 모여 있다. 그 사이에 **d 와 e** 가 징검다리처럼 놓여 있는데, 이
둘이 이 자료의 함정이다.

단일 연결은 "가장 가까운 한 쌍" 만 보므로, c → d → e → f 로 이어지는 징검다리를
차례로 밟아 두 덩이를 결국 하나로 만든다. 이것을 **체이닝(chaining)** 이라 한다.
단일 연결의 병합 높이를 보라 — 0.919 · 1.006 · 1.012 · 1.051 · 1.098 · 1.645 ·
1.981 로, 마지막 병합까지도 2 를 넘지 않는다.

완전 연결은 "가장 먼 한 쌍" 을 보므로 다리를 밟지 못한다. a 와 h 는 멀리
떨어져 있고, 그 거리가 두 덩이를 합치는 문턱이 된다. 마지막 병합이 6.521 이다.
평균 연결은 그 사이 어디쯤에서 4.490 이다.

### 높이 2 에서 자르면

나무를 어느 높이에서 가로로 자르면, 그 선 아래에서 이어진 것들이 한 무리다.
높이 **2** 에서 자른 답을 셋 다 놓고 보면

| 높이 | 단일 | 완전 | 평균 |
| --- | --- | --- | --- |
| 1.0 | 7 | 7 | 7 |
| 1.5 | 3 | 3 | 3 |
| **2.0** | **1** | **3** | **3** |
| 2.5 | 1 | 3 | 3 |
| 3.5 | 1 | 3 | 2 |
| 5.0 | 1 | 2 | 1 |

**같은 점, 같은 높이인데 답이 1 과 3 으로 갈린다.** 자료가 바뀐 것도 아니고
자르는 자리가 바뀐 것도 아니다. "두 무리 사이의 거리" 를 무엇으로 쟀는가만
달랐다.

높이 1.0 과 1.5 에서는 셋이 같은 답을 낸다. 연결 방식의 차이는 무리가 커진
뒤에야 벌어지기 때문이다 — 점 하나와 점 하나 사이에서는 최솟값도 최댓값도
평균도 모두 같은 수다.

### 코드에서 볼 것은 세 줄이다

코드 패널이 보이는 것은 **병합 한 걸음**이다. 아직 살아 있는 무리를 둘씩 짝지어
훑고, 각 짝에 대해 점쌍 거리를 전부 재고, 가장 가까운 짝을 합친다. 그 스무 줄
남짓 가운데 연결 방식에 따라 갈리는 것은 안쪽 루프의 **세 줄**뿐이다.

    if mode == 0 and (cnt == 0 or d < acc): acc = d     # 단일 — 최솟값
    if mode == 1 and (cnt == 0 or d > acc): acc = d     # 완전 — 최댓값
    if mode == 2: acc = acc + d                         # 평균 — 합을 세었다 나눈다

손잡이를 옮기면 코드 패널의 강조가 그 세 줄 사이를 오간다. 나무 전체가 바뀌는
까닭이 저 한 줄이라는 것이 이 화면이 말하려는 것이다.

거리는 \`sqrt\` 로 잰다. 제곱 거리로 줄이면 셈은 빨라지지만 **나무의 높이가 뜻을
잃는다** — 화면에서 높이 2 로 자른다는 말이 곧 "두 무리가 2 만큼 떨어져 있다" 는
말이어야 하기 때문이다.

### 어느 것을 골라야 하는가

정답은 없다. 다만 성질은 있다.

- **단일 연결**은 길쭉하거나 구불구불한 모양을 잘 찾는다. 대신 잡음 몇 점이
  다리를 놓으면 무관한 두 무리를 이어 붙인다 (지금 본 그것이다).
- **완전 연결**은 지름이 작은 동글동글한 무리를 만든다. 대신 길쭉한 무리를
  둘로 쪼갠다.
- **평균 연결**은 둘 사이에서 대체로 무난하다.

무엇을 고를지는 자료가 어떤 모양이라고 믿는지에 달렸다. **군집화에는 정답
이름표가 없으므로**, 그 믿음을 검증해 줄 것도 없다. 고르는 것 자체가 가정이다.
`;
