/**
 * 결정 경계 조각의 글.
 *
 * `{facet:decisionBoundary}` 토큰이 그림이 들어갈 자리다. 빠뜨리면 글만
 * 렌더링되고 그림이 뜨지 않는다.
 *
 * 전제를 밝히는 것은 화면이 아니라 이 글의 일이다 (S-piece) — 무게와 치우침이
 * 주어진 값이라는 것, 축척을 정사각으로 잡았다는 것이 여기 적혀 있다.
 */

export const decisionBoundaryDescription = `## 결정 경계

분류 모델은 "이쪽" 이나 "저쪽" 을 곧바로 내놓지 않는다. 먼저 **점수** 를 내고 그것을
확률로 눌러 담는다.

    z = x + y − 5.5
    p = 1 / (1 + e^(−z))

z 는 얼마든지 크거나 작을 수 있지만 p 는 0 과 1 사이에 갇힌다. 그래서 모델이 실제로
하는 말은 "왼쪽" 이 아니라 "0.881" 이다.

**결정 경계는 그 확률이 반이 되는 자리다.** 두 무리의 한가운데가 아니다. 우연히
가운데처럼 보일 때가 있지만 그것은 이 데이터가 대칭이어서이지 경계의 정의가
그래서가 아니다. 무게나 치우침을 조금 흔들면 선은 곧장 한쪽으로 밀려난다.

{facet:decisionBoundary}

### 순서가 곧 논증이다

그림은 선을 마지막에만 그린다. 먼저 여덟 점에 하나씩 확률을 묻고, 그 값들이 오른쪽
확률자에 꽂히는 것을 본다. 여덟이 다 꽂히고 나면 두 뭉치 사이가 텅 비어 있다 —
0.5 근처에 앉은 점이 하나도 없으므로, **여덟 점만으로는 경계가 어디인지 짚을 수
없다.**

그래서 평면의 모든 자리에 같은 것을 묻는다. 격자가 확률의 농도로 물들면 경계 부근은
저절로 옅어져 흰 이음매가 생긴다. 확률이 반을 넘나드는 칸에 불을 켜고, 그제야 그
칸들을 잇는다. 선은 미리 그어 둔 것이 아니라 **드러난 것이다.**

### 전제

- 무게 (1, 1) 과 치우침 −5.5 는 **주어진 값**이다. 이 조각은 학습을 보이지 않는다.
  경사하강으로 그 값이 어떻게 정해지는지는 다른 이야기다.
- 두 축의 축척을 같게 잡았다. 그래서 x + y = 5.5 가 45° 대각선으로 보인다. 축척이
  다르면 같은 선이 다른 기울기로 보이지만 선인 것은 변하지 않는다.
- 격자는 유한하다. 불이 켜진 칸의 띠는 선 자체가 아니라 선이 지나는 칸들이다. 더
  촘촘히 물을수록 그 띠가 선에 가까워진다.

### 이웃한 물음과의 경계

- **왜 하필 0.5 인가** — 대칭인 두 진영을 똑같이 대할 때의 기본값이다. 한쪽을
  놓치는 값이 더 비싸면 문턱을 0.3 이나 0.7 로 옮기고, 그러면 같은 모델에서 다른
  경계가 나온다. 모델은 그대로인데 선만 평행하게 밀린다.
- **직선이 아닌 경계** — 여기서는 z 가 x 와 y 의 일차식이라 경계가 직선이다. z 에
  x·y 나 x² 이 들어가면 같은 방법(자리마다 확률을 재고 반이 되는 자리를 잇는 것)
  으로 굽은 경계가 나온다. 방법은 같고 z 만 다르다.
- **경계와 여유(margin)** — 이 그림은 경계가 어디인지만 말한다. 경계에서 얼마나
  멀리 떨어져 있어야 안심인가는 다른 물음이고, 서포트 벡터 머신이 그것을 정면으로
  다룬다.
`;
