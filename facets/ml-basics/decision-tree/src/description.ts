/**
 * decision-tree 완결형의 글.
 *
 * `{facet:decisionTree}` 토큰은 호스트가 인라인 시각화 노드로 치환한다 (C4).
 */

export const decisionTreeDescription = `
## 의사결정 트리

점 열여덟이 판 위에 놓여 있다. 아홉은 A, 아홉은 B다. 이 점들을 갈라 놓는
방법을 하나 만들려 한다. 다만 쓸 수 있는 물음이 아주 단순하다 — **"x 가 얼마보다
작은가", "y 가 얼마보다 작은가"** 둘뿐이다. 비스듬한 선도, 동그라미도 없다.

그런 물음 하나로는 어림도 없다. 그래서 하나 묻고, 갈라진 양쪽에서 **또 묻는다.**
그것이 의사결정 트리다.

{facet:decisionTree}

## 어떤 물음이 좋은 물음인가

좋은 물음은 **섞임을 줄이는** 물음이다. 섞임은 지니로 잰다.

\`\`\`
gini = 1 − (A비율)² − (B비율)²
\`\`\`

반반이면 0.5, 한쪽만 남으면 0이다. 자름 하나를 놓으면 무리가 둘로 갈리는데,
그 둘의 지니를 **크기로 가중해** 평균 낸 것이 그 자름의 값이다. 가장 작은 것이
가장 좋은 자름이다.

후보는 몇 개나 될까. 각 축에서 **이웃한 두 값의 한가운데** 전부다. 열여덟 점이면
축마다 열 몇 개, 둘 합쳐 스무 개 남짓. 그것을 전부 시험해 보고 가장 좋은 하나를
고른다 — 화면의 점선이 미끄러지고 서 있다 누워 있다 하는 것이 그 훑기다.

뿌리에서는 \`x < 5\` 가 이긴다. 지니가 0.5 에서 0.357 로 떨어진다.

## 그리고 다시, 그 자리에서

여기서부터가 이 알고리즘의 전부다. 자름 하나를 고르고 나면 **양쪽 각각에 같은
일을 다시 한다.** 새 알고리즘이 아니라 **자기 자신**이다. 코드 패널의 마지막 두
줄이 그것이다 — \`growTree\` 가 \`growTree\` 를 두 번 부른다.

나무를 담는 자료구조도 따로 없다. 배열 넷이면 된다.

- \`feature[i]\` — 이 노드가 보는 축
- \`threshold[i]\` — 자름값
- \`left[i]\` / \`right[i]\` — 자식의 자리 번호

노드 번호는 전위 순서로 매기므로 왼쪽 자식은 언제나 \`i + 1\` 이다. 오른쪽 자식의
번호는 왼쪽 부분나무를 다 기르고 나서야 알 수 있어서, 함수가 **다음 빈 자리**를
돌려주는 것으로 해결한다. 잎은 \`left[i] < 0\` 으로 표시하고 그 음수 자리에
이름표를 접어 넣는다.

## 언제 멈추는가

되풀이는 스스로 멈추지 않는다. 점 하나만 남을 때까지 계속 물으면 열여덟 개를
다 맞히는 나무를 언제나 만들 수 있다. 그래서 **깊이 상한**을 둔다.

슬라이더를 움직여 보라.

| 깊이 상한 | 맞힌 수 |
| --- | --- |
| 1 | 13 / 18 |
| 2 | 15 / 18 |
| 3 | 17 / 18 |
| 4 | 17 / 18 |
| 5 | 18 / 18 |

3 까지는 물을수록 는다. **4 에서는 한 걸음 더 물었는데 하나도 늘지 않는다.**
그리고 5 에서 마침내 열여덟을 다 맞히는데 — 그 마지막 두 질문(\`x < 1.45\`,
\`x < 1.2\`)이 무엇을 하는지 판을 보라. A 무리 깊숙이 앉은 B 하나를 위해
손톱만 한 칸을 오려 낸다.

이 점은 잡음이다. 자료를 만들 때 일부러 섞어 넣은 하나이고, 다음에 새 점이
오면 그 칸에 들어올 리가 없다. 나무는 규칙을 배운 것이 아니라 **이 점 하나를
외운 것이다.**

## 그것이 멈추는 까닭

"맞힌 수" 만 보면 깊을수록 좋다. 언제나 그렇다 — 가진 자료에 대해서는. 그래서
멈추는 조건은 맞힌 수가 아니라 **더 물어서 얻는 것이 있는가**로 정해야 한다.
4 에서 얻은 것이 없었고, 5 에서 얻은 것은 잡음 하나였다.

여기서는 깊이 상한 하나로 멈춰 세웠지만, 실제로는 잎의 최소 크기, 줄어드는 폭의
하한, 다 기른 뒤 가지를 쳐 내는 것 같은 방법을 함께 쓴다. 이름은 달라도 묻는 것은
같다 — **이 물음은 규칙을 말하는가, 이 자료를 외우는가.**
`;
