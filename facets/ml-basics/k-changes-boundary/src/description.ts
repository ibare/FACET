/**
 * k 값의 영향 조각의 설명 — 그림과 함께 딸려 오는 글.
 *
 * 자리 표시자 {facet:kChangesBoundary} 를 호스트가 그림으로 바꾼다.
 * 그림이 밝히지 않는 전제(거리 재는 법, 데이터가 반반이라는 사실)는 여기서 말한다.
 */

export const kChangesBoundaryDescription = `## k 를 바꾸면 경계가 바뀐다

k-최근접 이웃은 새로 들어온 점의 이름표를 스스로 정하지 않는다. 가장 가까운 이웃
k 개에게 묻고 표가 많은 쪽을 따를 뿐이다. 그래서 이 방법에는 학습이랄 것이 거의
없고, 대신 **몇을 물을 것인가** 하나가 결과를 통째로 좌우한다.

{facet:kChangesBoundary}

### 뒤집히는 순간

가장 가까운 하나는 A 다. 그러나 그 바로 뒤에 B 가 둘 붙어 있어서, 셋만 물어도
표는 1 대 2 가 되고 답은 B 로 넘어간다. 다섯을 물어도 일곱을 물어도 B 다. 점도
데이터도 거리 재는 법도 그대로인데 답이 둘인 것은, 물어본 범위가 달라졌기
때문이다.

"그럼 전체에게 물으면 되지 않는가" 라는 도피처도 없다. 이 데이터는 A 다섯, B
다섯으로 정확히 반반이라 전체의 다수라는 것이 아예 존재하지 않는다.

### k 를 키우면 무엇이 달라지는가

k 가 작으면 경계는 점 하나하나를 감싸며 들쭉날쭉해진다. 가장 가까운 이웃이
우연히 잘못 붙은 이름표거나 튀는 값이면 그 오류가 그대로 답이 된다. k 를 키우면
경계가 완만해지는 대신 가까운 곳의 사정이 먼 곳의 다수에 묻힌다. 한쪽 끝은
잡음을 외우고 다른 쪽 끝은 전체 평균으로 수렴하니, k 는 이 둘 사이를 고르는
손잡이다.

표가 갈리지 않도록 두 무리를 가릴 때는 k 를 홀수로 두는 것이 보통이다. 그림이
1 · 3 · 5 · 7 만 보이는 것도 그래서다.

### 전제

거리는 두 좌표축을 그대로 쓴 유클리드 거리이고, 축의 눈금은 이미 서로 맞춰져
있다고 본다. 실제 데이터에서 축의 단위가 다르면 — 한쪽은 미터, 한쪽은 킬로그램
이면 — 무엇이 "가까운" 이웃인지 자체가 눈금에 따라 달라진다. 그때는 k 를 고르기
전에 축부터 표준화해야 한다.
`;
