/**
 * 키 방향의 역전 학습 설명 — 단일 마크다운.
 *
 * `{facet:signatureKeyDirection}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const signatureKeyDirectionDescription = `## 두 키의 자리가 바뀐다

**암호화와 서명은 같은 키 한 쌍을 쓰지만 방향이 반대다.** 암호화에서는 누구나 공개키로 잠그고 주인만 개인키로 열지만, 서명에서는 주인만 개인키로 만들고 누구나 공개키로 확인한다.

{facet:signatureKeyDirection}

### 왜 반대인가

무엇을 하나로 줄이려는지가 다르기 때문이다.

- **암호화** — 읽을 수 있는 사람을 하나로 줄인다. 그래서 개인키가 **받는 쪽**에 선다.
- **서명** — 만들 수 있는 사람을 하나로 줄인다. 그래서 개인키가 **보내는 쪽**에 선다.

개인키는 언제나 "하나로 줄이는 자리" 에 있다. 그 자리가 흐름의 끝인지 앞인지에 따라 방향이 갈릴 뿐이다.

### 흔한 오해

서명을 "개인키로 암호화하는 것" 이라고 설명하는 글이 많다. RSA 에서는 수식이 비슷해 보여 그렇게 말해도 통하는 것처럼 느껴지지만, 실제 서명 방식(RSA-PSS, ECDSA, Ed25519)은 암호화와 다른 절차이고 ECDSA 처럼 암호화가 아예 정의되지 않은 것도 있다. **방향이 반대라는 것**이 핵심이지 암호화를 거꾸로 돌리는 것이 아니다.
`;
