/**
 * 머클 트리 학습 설명 — 단일 마크다운.
 *
 * `{facet:merkleTree}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const merkleTreeDescription = `## 무엇이 바뀌었는지 찾기

**해시를 둘씩 묶어 위로 접으면 꼭대기에 값 하나가 남는다.** 그 값 하나만 견주면 아래 전체가 그대로인지 알 수 있고, 갈라진 쪽만 따라 내려가면 어디가 바뀌었는지도 몇 걸음에 찾는다.

{facet:merkleTree}

### 한 줄만 바뀐다

잎 하나가 바뀌면 그 잎의 해시가 바뀌고, 그 잎이 속한 묶음이 바뀌고, 꼭대기가 바뀐다. **그게 전부다.** 옆 가지는 손대지 않은 그대로 남는다. 파일이 천 개여도 잎에서 꼭대기까지는 열 걸음쯤이니, 어디가 달라졌는지 찾는 데 천 번을 비교할 필요가 없다.

### 해시 사슬과의 차이

같은 재료로 다른 모양을 만든 것이다. 사슬은 한 칸을 고치면 그 뒤가 전부 무너지고, 트리는 한 줄만 갈린다. 사슬은 **순서**를 지키고 트리는 **묶음**을 지킨다.

### 어디에 쓰이나

- **Git** — 커밋이 트리의 꼭대기 값을 가리킨다. 파일 하나가 바뀌면 그 경로의 객체만 새로 생긴다.
- **분산 저장** — 서로 다른 곳에 있는 복제본이 같은지 꼭대기 값 하나로 확인한다.
- **블록체인** — 블록 하나가 담은 거래 전부를 꼭대기 값 하나로 요약한다.
`;
