/**
 * hash-avalanche facet IR — 코드 패널을 두지 않으므로 빈 배열.
 *
 * 이 facet 은 한 문장을 말하는 조각이라 코드 패널이 들어갈 자리가 없다.
 * 해시 함수의 내부를 여는 것은 다른 조각의 몫이다.
 */

import type { IR } from '@ffacet/core';

export const hashAvalancheIRs: IR[] = [];
