/**
 * 해시 눈사태 효과 학습 설명 — 단일 마크다운.
 *
 * `{facet:hashAvalanche}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const hashAvalancheDescription = `## 한 글자가 전부를 바꾼다

**암호학적 해시는 입력이 아무리 조금 달라져도 출력이 통째로 달라진다.** 비슷한 입력이 비슷한 출력을 내지 않는다는 이 성질을 눈사태 효과라 부른다.

{facet:hashAvalanche}

### 숫자로 보면

\`hello\` 와 \`hellp\` 는 마지막 글자 하나만 다르다. 비트로 펼치면 40비트 중 **5비트** 차이 — 12.5% 다. 그런데 SHA-256 을 통과한 두 출력은 256비트 중 **131비트**가 다르다. 51.2%, 절반이 넘는다.

입력의 작은 차이가 출력의 절반으로 증폭되는 이 비대칭이 눈사태 효과의 전부다. "많이 달라진다" 가 아니라 "출력의 절반이 뒤집힌다" 가 정확한 서술이다.

### 왜 중요한가

- **변조를 숨길 수 없다** — 문서를 조금만 고쳐도 해시가 알아볼 수 없게 달라진다. 원본과 비슷한 해시를 남기며 고치는 것이 불가능하다.
- **출력에서 입력을 추측할 수 없다** — 출력이 조금 비슷하다고 입력이 조금 비슷한 것이 아니므로, 가까운 값을 찾아가는 탐색이 통하지 않는다.
`;
