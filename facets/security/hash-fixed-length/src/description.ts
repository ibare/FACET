/**
 * 고정 길이 출력 학습 설명 — 단일 마크다운.
 *
 * `{facet:hashFixedLength}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const hashFixedLengthDescription = `## 무엇을 넣든 같은 길이

**해시 함수는 입력이 얼마나 길든 언제나 같은 길이의 값을 낸다.** 빈 문자열을 넣어도, 한 글자를 넣어도, 수 메가바이트짜리 파일을 넣어도 SHA-256 은 256비트 — hex 로 64자를 돌려준다.

{facet:hashFixedLength}

### 이것이 뜻하는 것

같은 길이로 접힌다는 말은 **정보가 버려진다**는 말이다. 3.7MB 파일을 32바이트로 줄이면서 원본을 그대로 담을 방법은 없다. 해시는 압축이 아니다 — 압축은 되돌릴 수 있지만 해시는 되돌릴 수 없고, 되돌릴 수 없는 이유가 바로 버려진 정보에 있다.

그리고 여기서 다음 사실이 따라 나온다. 들어오는 것은 얼마든지 길어질 수 있는데 나가는 것이 늘 같은 길이라면, **서로 다른 두 입력이 같은 값을 내는 일은 반드시 생긴다.**

### 왜 쓸모 있는가

- **자리를 미리 잡을 수 있다** — 데이터베이스 칼럼도 인덱스도 32바이트로 고정된다. 원본이 얼마나 클지 몰라도 상관없다.
- **크기와 무관하게 비교할 수 있다** — 영상 파일 두 개가 같은지 보려면 64자 두 줄만 견주면 된다.
`;
