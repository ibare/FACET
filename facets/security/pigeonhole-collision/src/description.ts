/**
 * 비둘기집 충돌 학습 설명 — 단일 마크다운.
 *
 * `{facet:pigeonholeCollision}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const pigeonholeCollisionDescription = `## 충돌은 반드시 있다

**해시 충돌은 일어날 수도 있는 사고가 아니라 반드시 존재하는 사실이다.** 입력은 얼마든지 만들 수 있는데 출력이 앉을 자리는 정해진 수뿐이기 때문이다.

{facet:pigeonholeCollision}

### 확률이 아니라 셈이다

자리가 16개인데 입력이 17개면, 어떻게 나눠 넣든 한 자리에는 둘이 앉는다. 운이 나빠서가 아니라 셈이 그렇다. 이것을 비둘기집 원리라 부른다.

SHA-256 의 자리는 2²⁵⁶ 개다. 상상하기 어려운 수지만 유한하다는 점은 같고, 입력은 그보다 많이 만들 수 있으므로 겹치는 쌍은 반드시 존재한다.

### 그런데 왜 안전한가

**존재하는 것과 찾을 수 있는 것은 다르다.** 겹치는 쌍이 있다는 것은 셈으로 알지만, 그중 하나를 실제로 집어내려면 감당할 수 없는 계산이 필요하다. 좋은 해시 함수의 안전성은 "충돌이 없다" 가 아니라 "있는 줄 알아도 못 찾는다" 에 있다.
`;
