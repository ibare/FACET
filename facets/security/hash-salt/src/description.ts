/**
 * 소금 치기 학습 설명 — 단일 마크다운.
 *
 * `{facet:hashSalt}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const hashSaltDescription = `## 같은 비밀번호, 다른 저장값

**비밀번호를 그대로 해싱해 저장하면, 같은 비밀번호를 쓴 사람들의 저장값도 똑같아진다.** 하나가 풀리면 나머지도 함께 풀린다는 뜻이다. 계정마다 다른 값을 앞에 붙여 해싱하면 그 연결이 끊긴다. 그 값을 소금이라 부른다.

{facet:hashSalt}

### 소금은 비밀이 아니다

소금은 해시 바로 옆에 감추지 않고 저장한다. 감추는 것이 목적이 아니기 때문이다. 소금이 하는 일은 **저장값을 저마다 다르게 만드는 것**이고, 그것만으로 두 가지가 막힌다.

- **미리 계산해 둔 표가 통하지 않는다** — 흔한 비밀번호의 해시를 아무리 많이 모아 놔도, 소금이 다르면 그 표에 없는 값이 나온다.
- **한 번의 계산으로 여러 계정을 노릴 수 없다** — 계정마다 소금이 다르니 공격도 계정마다 따로 해야 한다.

### 이것으로 충분하지는 않다

소금은 같은 비밀번호가 같게 저장되는 것을 막을 뿐, 한 계정을 노린 공격을 느리게 만들지는 못한다. 그 몫은 일부러 느리게 만든 해시(bcrypt, scrypt, Argon2)가 맡는다.
`;
