/**
 * 무결성 대조 학습 설명 — 단일 마크다운.
 *
 * `{facet:hashIntegrityCheck}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const hashIntegrityCheckDescription = `## 받은 것이 원본인지 확인하기

**파일과 해시가 서로 다른 경로로 오면, 받은 파일이 손댔는지 알 수 있다.** 파일은 아무 데서나 받아도 되고 해시만 믿을 수 있는 곳에서 받으면 된다. 파일을 고친 쪽은 해시까지 함께 고칠 수 없으므로 어긋남이 드러난다.

{facet:hashIntegrityCheck}

### 왜 되는가

해시는 입력이 조금만 달라져도 출력이 통째로 달라진다. 그래서 "비슷하게 고치면서 해시는 그대로 두기" 가 불가능하다. 한 글자를 고치는 순간 해시는 알아볼 수 없게 갈린다.

### 경로가 하나면 성립하지 않는다

파일과 해시를 같은 서버에서 받는다면 공격자는 둘 다 바꿔치기하면 그만이다. 이 방법이 지키는 것은 "파일이 안 바뀌었다" 가 아니라 **"해시가 온 경로를 믿는 만큼 파일도 믿을 수 있다"** 이다.

그래서 배포처들은 해시를 다른 경로에 둔다 — 다른 도메인, 문서 본문, 메일링 리스트, 혹은 아예 서명을 붙여 해시 자체의 출처를 보증한다. 신뢰를 없애는 것이 아니라 **작은 것 하나로 옮기는 것**이 요점이다.
`;
