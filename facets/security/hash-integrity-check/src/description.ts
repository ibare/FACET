/**
 * 무결성 대조 학습 설명 — 단일 마크다운.
 *
 * `{facet:hashIntegrityCheck}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const hashIntegrityCheckDescription = `## 받은 것이 원본인지 확인하기

**해시 한 줄이면 받은 파일이 손댔는지 알 수 있다.** 원본을 만든 쪽이 자기 해시를 함께 내걸면, 받은 사람은 자기가 받은 것을 해싱해 그 한 줄과 견주기만 하면 된다.

{facet:hashIntegrityCheck}

### 왜 되는가

해시는 입력이 조금만 달라져도 출력이 통째로 달라진다. 그래서 "비슷하게 고치면서 해시는 그대로 두기" 가 불가능하다. 한 글자를 고치는 순간 해시는 알아볼 수 없게 갈린다.

### 무엇을 믿어야 하는가

이 방법이 서는 것은 **해시를 믿을 수 있는 경로로 받았을 때**뿐이다. 파일과 해시를 같은 서버에서 받는다면 공격자는 둘 다 바꿔치기하면 그만이다. 그래서 배포처들은 해시를 다른 경로에 두거나, 아예 서명을 붙여 해시 자체의 출처를 보증한다.
`;
