/**
 * 해시 사슬 학습 설명 — 단일 마크다운.
 *
 * `{facet:hashChain}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const hashChainDescription = `## 지난 기록을 고치면 드러난다

**기록마다 앞 기록의 해시를 품게 하면, 지난 것을 조용히 고칠 수 없다.** 한 칸을 고치는 순간 그 칸의 해시가 바뀌고, 다음 칸이 쥐고 있던 값과 어긋나며, 그 어긋남이 끝까지 번진다.

{facet:hashChain}

### 고친 자리만 손봐서는 안 된다

사슬이 없다면 기록 하나를 고치는 일은 그 줄만 바꾸면 끝난다. 사슬이 있으면 그 뒤의 모든 칸을 다시 계산해야 한다. 기록이 길수록 되돌릴 수 없는 일이 된다.

### 그래도 다시 계산하면 되지 않나

된다. 뒤를 전부 다시 쓰면 사슬 자체는 다시 맞는다. 그래서 사슬 하나만으로는 부족하고, 보통 두 가지 중 하나를 덧댄다.

- **마지막 해시를 밖에 남긴다** — 누군가 그 값을 따로 갖고 있으면, 전부 다시 계산한 사슬도 그 값과는 어긋난다.
- **다시 계산을 비싸게 만든다** — 작업 증명처럼 한 칸을 잇는 데 큰 비용이 들면 뒤를 전부 다시 쓰는 일이 현실적으로 불가능해진다.

사슬이 막는 것은 **조용한 수정**이지 수정 자체가 아니다.
`;
