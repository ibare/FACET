/**
 * 해시에 서명하기 학습 설명 — 단일 마크다운.
 *
 * `{facet:signatureOnHash}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const signatureOnHashDescription = `## 서명은 해시에 한다

**디지털 서명은 문서 전체가 아니라 그 해시에 한다.** 문서는 얼마든지 커질 수 있지만 해시는 언제나 32바이트이고, 그것에 서명한 결과도 64바이트로 끝난다.

{facet:signatureOnHash}

### 크기가 고정된다는 것

서명의 크기와 서명에 드는 계산량이 **문서 크기와 무관해진다.** 3.7MB 파일이든 3.7GB 파일이든 서명은 64바이트이고, 서명을 만드는 시간도 거의 같다. 해싱은 빠르고 파일 크기에 비례하지만, 공개키 연산은 느린 대신 32바이트에만 적용된다.

### 크기 문제는 가능 여부이기도 하다

비용만의 문제가 아니다. RSA 는 **키보다 큰 것을 직접 서명할 수 없다.** RSA-2048 이 한 번에 다룰 수 있는 양은 256바이트뿐이라, 해시로 줄이지 않으면 애초에 서명이라는 연산이 성립하지 않는다.

### 무엇을 서명한 것이 되는가

32바이트에 서명했지만 **문서 전체에 서명한 것과 같다.** 그 해시를 내는 다른 문서를 찾아낼 수 없기 때문이다. 그래서 서명의 안전성은 쓰인 해시 함수의 충돌 저항성에 기댄다 — MD5 나 SHA-1 로 만든 옛 서명이 위험한 이유가 여기 있다.
`;
