/**
 * 2D 행렬 변환 학습 설명 — 단일 마크다운.
 *
 * `{facet:matrixTransform2d}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const matrixTransform2dDescription = `## 2D 행렬 변환 (2D Matrix Transform)

**2×2 행렬은 두 기저 벡터 i-hat·j-hat 의 도착지이며, 평면의 모든 점은 자신을 두 기저의 선형 결합으로 적은 뒤 같은 (u, v) 계수로 새 기저 결합 자리로 따라 이동하는 운동이다.** 행렬의 첫째 열은 i-hat 이 어디로 가는지, 둘째 열은 j-hat 이 어디로 가는지를 그대로 적은 좌표이고, 평면의 모든 점은 자신을 두 기저의 결합으로 적은 뒤 그 결합을 도착지에서 반복하는 것만으로 따라간다. 변환은 직선을 직선으로 보내고 원점을 그 자리에 박아 두며, 단위 정사각형이 변환 후 갖는 면적이 곧 행렬식의 절댓값이고, 부호가 음수면 평면이 거울에 비친 것처럼 뒤집힌다.

{facet:matrixTransform2d}

### 핵심 아이디어

행렬 변환의 본질은 **두 기저의 도착지 + 직선·원점 보존 + (u, v) 계수의 동기 운동** 세 결합이다.

- **두 열은 두 기저의 도착지** — 행렬의 첫째 열 [a; c] 는 i-hat 이 옮겨 가는 좌표, 둘째 열 [b; d] 는 j-hat 이 옮겨 가는 좌표다. 셀 한 칸을 잡고 흔들면 정확히 한 기저 화살표의 한 좌표만 움직인다 — 셀과 화살표의 색이 1:1 로 묶여 그 미시 동기가 시각으로 강제된다.
- **직선은 직선, 원점은 그 자리** — 어떤 행렬 운동에서도 격자선은 직선을 유지하며 등간격이 깨지지 않는다. 곡선이 되거나 원점이 옮겨 가는 일은 없다. 평면 위에 옅은 항등 격자와 진한 변환 격자가 두 겹으로 동시에 그려져 "어디에서 어디로" 가 한 컷에 보존된다.
- **모든 점은 같은 (u, v) 계수로 따라간다** — 평면 위 한 점이 항등 좌표 (u, v) 자리에 있다면, 변환 후에도 그 점은 새 기저 결합 u·i-hat' + v·j-hat' 자리에 있다. 점의 (u, v) 라벨은 변환 동안 변하지 않고 위치 라벨만 새 기저 결합 자리로 갱신된다 — "계수는 같고 자리만 따라간다" 가 두 라벨의 비대칭으로 운반된다.
- **단위 정사각형 → 평행사변형, 면적 = |det|** — 두 기저 화살표가 이루는 단위 정사각형이 변환 후 평행사변형으로 휘어지고, 그 면적이 정확히 행렬식의 절댓값이다. det 가 0 에 닿는 순간 평행사변형이 한 직선으로 무너지고, 음수로 진입하는 순간 평행사변형이 거울에 비치며 채움 색이 반전된다.
- **회전·스케일·전단·반사는 모두 같은 행렬 형식** — 네 표준 변환은 모두 2×2 행렬의 특수 사례일 뿐이다. 토글 한 번으로 셀 네 칸이 해당 패턴 — 회전 [[cosθ, -sinθ], [sinθ, cosθ]], 스케일 [[s, 0], [0, t]], 전단 [[1, k], [0, 1]], 반사 [[1, 0], [0, -1]] (x축) — 으로 자동 채워지며, 슬라이더를 흔드는 동안 셀 어디에 어떤 모양의 숫자가 들어가는지의 패턴이 직접 학습된다.

### 결정적 순간 — 한 셀 = 한 좌표의 미시 동기

학습자가 셀 a 한 칸을 잡고 슬라이더로 1 → 2 로 끄는 그 한 컷이다. 셀 a 의 숫자가 실시간으로 변하는 동안, 평면 위 i-hat 화살표의 끝점이 가로 방향으로만 미끄러진다 — j-hat 은 그 자리에 그대로 있다. 진한 변환 격자가 가로 방향으로 늘어나며 옅은 항등 격자 위에 겹쳐 두 겹의 차이가 시각화된다. 보조 점 (1.5, 0.7) 이 같은 (u, v) 계수로 새 기저 결합 자리로 즉시 옮겨 가고, 점 옆의 라벨 "(1.5, 0.7)" 은 변하지 않는다 — 좌표는 같고 자리만 따라간다. 단위 평행사변형의 면적이 1.0 → 2.0 으로 차오르며 |det| 게이지가 동기 갱신된다. 직선은 직선, 원점은 그 자리.

1. **셀 한 칸의 운동** — 셀 a 의 테두리가 강조 펄스로 한 번 빛나며 숫자가 실시간으로 흘러간다.
2. **한 화살표의 한 좌표** — i-hat 끝점이 가로로만 미끄러진다. j-hat 은 정지. 셀 색과 화살표 색의 1:1 동기가 위치 운동으로 보인다.
3. **격자 21 줄의 동시 재계산** — 가로·세로 격자선이 모두 새 기저 방향으로 휘어지지만 직선과 등간격을 유지한다. 옅은 항등 격자 위에 진한 변환 격자가 겹쳐 "어디에서 어디로" 가 보존된다.
4. **단위 평행사변형의 면적 차오름** — 변환 격자 위 단위 정사각형이 평행사변형으로 늘어나고 옆 라벨 "면적 = …" 이 동기 갱신.
5. **|det| 게이지의 양방향 미끄러짐** — 0 중심 양방향 막대가 새 부호 영역으로 미끄러지고 숫자 라벨이 함께 갱신된다.
6. **보조 점의 동기 운동** — 점이 새 기저 결합 자리로 즉시 이동하고 위치 라벨만 갱신, (u, v) 라벨은 그대로.

### 다른 대상과의 구별

- **CSS 2D transform (\`matrix(a, b, c, d, e, f)\`)** — 같은 가설이 같은 격자 운동을 그리지만 결정의 단위가 다르다. CSS 의 \`matrix()\` 는 마지막 두 파라미터 e, f 로 이동(translation) 까지 포함하는 어파인 변환이며, 6 개 파라미터로 정의된다. 시각적으로는 변환 후에도 원점이 자기 자리에 있을 필요가 없어 격자 전체가 좌·우 또는 상·하로 통째로 미끄러지는 운동이 함께 발화한다. 본 시각화는 2×2 의 4 개 파라미터만 다루며 원점은 고정된 점으로 항상 그 자리에 박혀 있다.
- **비선형 변환 (자유 워프 / 곡선 변환)** — 같은 평면을 같은 평면으로 보내지만 격자선이 곡선이 된다. 본 시각화는 어떤 셀 조합을 흔들어도 격자선이 직선을 유지하며 등간격이 깨지지 않는다. "직선은 직선, 간격은 균등" 이라는 선형성의 시각 조건이 평면 운동의 한계로 강제된다.
- **어파인 변환 (이동 포함, 3×3 동차좌표)** — 회전·스케일·전단·반사에 더해 평행이동까지 한 행렬로 표현하는 일반화. 시각적으로는 격자 전체가 변환 후에도 직선·등간격을 유지하지만 원점이 다른 자리로 옮겨 갈 수 있다. 본 시각화의 4 종 변환은 모두 원점을 고정시키므로, "원점은 그 자리" 가 어파인과 본 변환을 가르는 시각 사실이다.

### 시각 한계 사건

- **det 가 0 에 닿는 순간** — 변환 격자의 모든 가로·세로선이 한 직선 위로 무너지고 단위 평행사변형이 한 직선으로 압축된다. |det| 게이지가 중심에 멈추고 색이 회색으로 변한다. 캡션에 "면적이 0 이다 — 평면이 직선으로 무너졌다" 가 잠시 뜬다.
- **det 가 음수로 진입** — 평행사변형 채움이 보라 → 청록 빗금 패턴으로 전환되고 빗금 방향이 뒤집힌다. 게이지 막대가 0 좌측(주황 영역) 으로 이동. 캡션에 "방향이 뒤집혔다 — 평면이 거울에 비쳤다" 가 잠시 뜬다.
- **반사 프리셋 활성** — x축 / y축 / 원점 반사 토글 즉시 평행사변형이 뒤집히는 사건이 발화한다.

### 언제 보이나

- **선형 대수의 도입** — "행렬 = 두 열 = 두 기저의 도착지" 의 시각 정전. 추상 숫자 묶음으로서의 행렬이 평면 운동의 좌표로 이해된다.
- **컴퓨터 그래픽 변환 파이프라인** — 회전·스케일·전단·반사가 모두 같은 행렬 형식의 특수 사례임이 토글로 학습된다. 어파인·투영의 일반화로 가는 디딤돌.
- **선형 사상의 직관 형성** — "직선은 직선, 원점은 그 자리, 모든 점은 같은 계수" 라는 선형성의 세 시각 조건이 한 컷의 운동으로 동시에 보인다.

반대로, 격자선이 곡선이 되거나 원점이 옮겨 가야 한다면 그 운동은 선형이 아니다 — 본 시각화의 평면 운동은 그 한계 안에서만 발화한다.
`;
