/**
 * Tokenization 학습 설명 — 단일 마크다운.
 *
 * `{facet:tokenization}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const tokenizationDescription = `## 토큰화 (어휘 분석, lexing/scanning)

**원시 소스 문자열을 좌에서 우로 한 글자씩 응시하며 의미 있는 최소 단위로 끊어 라벨이 붙은 토큰의 줄로 바꾸는 첫 변환 단계다.** 위쪽 띠 위로 응시 표식이 한 글자씩 전진하면서 같은 종류 글자들이 한 색의 구간 박스로 점점 길게 묶이고, 더는 묶이지 않는 순간 그 구간이 통째로 한 장의 토큰 카드가 되어 아래쪽 출력 행 끝에 안착한다. 공백과 주석은 사라지지 않고 회색으로 가라앉아 흔적으로 남는다.

{facet:tokenization}

### 핵심 아이디어

- **앞으로만 전진하는 응시** — 한 번 지나간 글자에는 되돌아가지 않는다. 응시는 결코 뒤로 가지 않는다.
- **자라다 닫히는 한 박자** — 같은 종류로 묶이는 동안 구간이 길어지고, 다음 글자가 그 종류로 못 묶이는 순간 통째로 한 토큰으로 닫힌다.
- **종류 라벨 + 원문 값** — 카드는 두 가지를 동시에 새긴다. 상단 띠 색이 종류, 중앙 글자열이 원문이다.
- **최장 일치의 망설임** — \`>=\` 처럼 한 글자에서 끊을 뻔하다 한 칸 더 들여다본 뒤 두 글자를 모두 묶는 결정 박자가 점선 → 실선의 한 컷으로 보인다.
- **식별자 → 키워드 두 단계 변신** — 닫힌 카드가 우선 식별자로 표지되고, 그 직후 사전 대조로 라벨이 키워드로 갈아끼워진다.
- **공백·주석 흔적** — 토큰이 되지 못한 글자도 사라지지 않고 회색으로 띠 위에 남는다.
- **오류는 빨간 카드** — 인식 불가 글자도 출력 행에 빨간 카드로 박힌다. 응시는 멈추지 않는다.

### 결정적 순간 — 한 토큰이 닫히는 그 한 박자

응시가 어떤 글자 위에 있고, 직전 몇 글자가 같은 색의 구간으로 자라나 있는 상태에서, 다음 글자가 같은 종류로 못 묶인다고 판정되는 순간 — 자라던 구간의 테두리가 한 번 굳고, 색이 진해지며, 띠 → 카드 사이에 한 줄기 낙하 궤적이 그어진다. 그 구간이 통째로 한 장의 카드가 되어 출력 행 끝에 안착하고, 응시는 새 글자 위에서 길이 0 짜리 새 구간을 다시 키우기 시작한다.

### 다른 대상과의 구별

- **구문 분석 (파싱)** — 토큰 줄을 받아 트리 구조를 세운다. 출력은 트리이며 시각은 가지치기로 펼쳐진다. 토큰화의 출력은 평평한 한 줄의 카드 시퀀스다.
- **정규식 매칭** — 하나의 패턴이 입력의 어디에 매치되는지를 색칠한다. 토큰화는 여러 종류 패턴이 입력 전체를 빠짐없이 분배해 라벨 붙은 카드 줄로 떨어뜨린다.
- **NLP 단어 분리 (형광펜 색칠)** — 입력 띠 자체에 색만 입히고 끝낸다. 토큰화는 띠의 색칠과 별개로 아래쪽에 토큰 카드 행이 분리되어 쌓인다.
- **문자열 split** — 한 가지 구분자만 보고 잘라 동질의 조각을 만든다. 토큰화는 끊는 위치 자체가 종류마다 다른 패턴으로 결정되며 카드의 색이 종류마다 다르다.

### 인터랙션

- **다음 예제** — \`var x = 42;\`, \`if x >= 10  // ok\\n  return x;\`, \`// note\\n  return n\` 세 예제를 순환. 본질의 다른 면(키워드 승격, 최장 일치, 공백·주석 삼킴) 을 강조한다.
- **다시 재생** — 현재 예제를 처음부터 한 번 더 자동 스캔.
- **속도** — 한 박자 길이를 조절. 느릴수록 망설임 박자가 또렷해진다.
- **초기화** — 첫 예제로 돌아간다.

### 참고 자료

- Crafting Interpreters — Scanning (\`start\`/\`current\` 두 인덱스 루프의 정전).
- Hugging Face — Tokenizer Playground (인플레이스 색 띠 시각 어휘의 표준).
- Ruslan's Blog — Let's Build A Simple Interpreter Part 1 (\`3+5\` 가 (type, value) 카드로 분해되는 입문 다이어그램).
- regex101 (패턴 ↔ 입력 양방향 색상 매칭으로 본 "규칙 → 매치").
`;
