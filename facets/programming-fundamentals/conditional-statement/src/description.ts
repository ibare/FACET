/**
 * Conditional Statement 학습 설명 — 단일 마크다운.
 *
 * `{facet:conditionalStatement}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const conditionalStatementDescription = `## 조건문 (if / else if / else)

**흐르던 길이 분기점에 도착해 조건의 참/거짓을 위에서부터 차례로 평가하다 처음 참이 된 한 가지에서 흐름이 확정되고, 나머지 가지는 어두워진 채 닫힌 뒤 다시 한 줄로 합쳐지는 약속이다.** 화면 위 한 줄의 흐름선이 다이아몬드 모양 평가 마름모로 빨려 들어가고, 그 자리에서 식이 환하게 빛나며 참 또는 거짓 한 글자로 응결되면, 응결된 결과를 따라 한 가지선이 굵고 또렷한 색으로 켜지고 나머지 가지는 회색 반투명에 빗장이 그어진다. 갈래는 합류 노드에서 다시 한 줄로 봉합된다.

{facet:conditionalStatement}

### 핵심 아이디어

- **분기점은 한 자리** — 모든 평가는 마름모 안 한 박자 안에서 끝난다. 마름모를 떠나는 순간 흐름은 이미 한 길로 정해져 있다.
- **첫 매칭의 순서감** — else if 사슬은 위에서 아래로 평가된다. 처음 참이 나타나는 마름모에서 시퀀스가 멈추고, 아래 마름모들은 차례 자체가 박탈된 듯 처음의 흐릿함으로 굳는다.
- **닫힌 가지의 능동적 어둠** — 비활성 가지는 단순히 회색이 아니라 빗장 무늬가 겹쳐져 "이번에는 닫힘" 이 형태로 박힌다.
- **합류의 봉합** — 어느 가지를 탔든 갈래가 끝나면 한 점에서 다시 모인다. 합류 노드의 짧은 봉합 애니메이션이 "한 줄로 합쳐졌다" 를 시각으로 박는다.

### 결정적 순간 — 마름모의 한 박자

학습자가 슬라이더를 흔드는 사이, 입력값이 마름모에 닿는 한 박자 안에 식이 환하게 빛나며 참/거짓 한 글자로 응결되고, 그 결과를 따라 한 가지선이 굵게 켜지며 나머지 가지가 빗장과 회색으로 가라앉는 한 컷이다. "같은 코드, 다른 길" 이 한 흐름 안에서 보이는 자리가 바로 이 박자다.

### 다른 제어 흐름과의 구별

- **반복문(for/while)** — 같은 한 가지를 여러 번 되돌아 흐른다. 조건문은 되돌아오지 않고, 한 번의 통과 안에서 갈래가 갈라졌다 합쳐질 뿐이다.
- **switch / case** — 단일 값과 여러 라벨의 일대다 동시 매칭. 조건문은 마름모 하나에 임의의 식이 들어가고, 위에서부터 차례로 평가된다는 순서감이 형태로 드러난다.
- **함수의 입출력 분기** — 함수는 호출 단위로 들어갔다 나오는 경계가 있다. 조건문은 같은 흐름선 위에서 갈래만 잠깐 갈라졌다 다시 한 줄이 된다.
- **결정 트리** — 한 번 갈라진 가지는 다시 만나지 않는다. 조건문은 합류 노드가 분명히 존재한다는 점이 결정적 차이다.

### 인터랙션

- **상단 슬라이더** — 0~100 정수. 드래그하면 마름모 안 식이 다시 평가되고, 활성 가지가 즉시 다른 가지로 옮겨가며 빗장과 강조가 자리를 바꾼다.
- **갈래 2/3 토글** — 같은 도식 안에서 if/else 변형과 if/else if/else 변형을 오간다. 마름모 사슬이 한 단 늘어나고 줄어든다.
- **자동 시연** — 모든 가지가 한 번씩 켜졌다 다시 초기 가지로 돌아오는 짧은 시퀀스. 학습자가 손을 대지 않아도 "값에 따라 길이 바뀐다" 가 한 번에 보인다.
- **참고 자료** — 도식 하단에 표준 플로차트 / Scratch 블록 / Python Tutor / ko.javascript.info 의 참고 칩 4개.
`;
