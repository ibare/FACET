/**
 * MessagingPubsub 학습 설명 — 단일 마크다운.
 *
 * `{facet:messagingPubsub}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const messagingPubsubDescription = `## Pub/Sub (Publish/Subscribe) 메시징

**발행자와 구독자가 서로의 신원을 모른 채 가운데 자리한 broker 의 토픽 라벨만 매개로, 한 발행이 모든 구독자에게 사본으로 펼쳐지는 다대다 비동기 메시징을 성립시키는 시스템 행동.** 좌측에 늘어선 발행자, 가운데 1급 박스로 자리한 broker, 우측에 늘어선 구독자가 서로 직접 화살표로 닿는 일이 한 줄도 없이 — 모든 흐름이 broker 의 토픽 라벨 위 한 점에서 한 번 끊어지고 다시 시작한다. 이 결합 덕분에 시간 디커플링·동적 토폴로지·확장성·이벤트 기반 아키텍처가 같은 약속 위에서 자란다.

{facet:messagingPubsub}

### 핵심 아이디어

Pub/Sub 의 본질은 **broker 가 사이를 막는다** 와 **한 발행이 N 사본으로 갈라진다** 는 두 결합이다.

- 좌측 영역 — **발행자**: 자기 색의 메시지 칩을 broker 의 토픽으로 던진다. 받는 자가 누구인지 모른다.
- 가운데 영역 — **broker (1급 박스)**: 모든 화살표가 통과하는 라이프라인. 도착한 칩을 그 자리에서 사본 N 개로 갈라 부채꼴로 펼친다.
- 우측 영역 — **구독자**: 토픽 이름만 신청한다. 보낸 자가 누구인지 모른다.
- 좌·우 영역 사이의 **분리 띠** — 어떤 화살표도 띠를 직접 건너 좌→우로 이어지지 못한다. "서로를 모른다" 가 위치만으로 강제된다.

### 결정적 순간 A — 한 발행, N 사본, 다른 도착 시각 (팬아웃)

P1 이 events 토픽으로 메시지 #7 을 던지는 한 컷이다.

1. **발행 → broker 도착**: P1 라이프라인의 한 시점에서 칩 #7 (P1 색) 이 솟아 broker 라이프라인 위 한 점에 도착한다. 도착 화살표 머리 옆에 "→ events" 가 잠시 뜬다.
2. **broker 정지 구간**: 도착 점에서 짧은 갈라짐 펄스가 퍼지고 broker 박스 안에 칩이 잠시 머문다 — "broker 를 거쳐갔다" 가 시간 위의 정지 길이로 보인다.
3. **부채꼴 N 사본**: 같은 색·같은 번호 라벨의 사본이 broker 라이프라인 위 약간씩 다른 시점에서 차례로 우측 구독자 라이프라인을 향해 출발한다. 도착 자리마다 짧은 펄스가 한 번씩 퍼진다.
4. **사선 배열의 보존**: 도달 시각이 서로 다르므로 사본들은 시간축 위에 비스듬한 사선으로 안착한다 — "한 발행, 다른 수신 시각" 이 한 컷의 정지 화면에 보존된다.

### 결정적 순간 B — 동적 합류·이탈 (라이프라인 위의 매듭)

새 구독자 S5 가 events 에 합류하는 순간, S5 박스가 우측 영역에 페이드인되며 라이프라인이 그어지고 broker 토픽 위에 매듭이 안착한다. 매듭 시점 **이후의** 모든 부채꼴에 S5 가닥이 추가되고, 매듭 **이전의** 부채꼴에는 그 가닥이 없다 — "합류 이전 메시지는 받지 않는다" 가 라이프라인의 빈 구간으로 보존된다.

이탈은 반대 방향: 라이프라인 위 한 시점에서 끊어짐 매듭이 찍히고, 그 시점 이후의 부채꼴에서 그 가닥이 사라진다. "이탈 이후 메시지는 가지 않는다" 가 가닥의 부재로 보존된다.

### 다른 메시징 패턴과의 구별

- **Observer 패턴**: Subject 가 Observer 목록을 직접 알고 차례로 update() 를 호출하는 직접 호출 모델. 시퀀스 도식 위에서 Subject → Observer 화살표가 직접 이어지고, 가운데에 broker 박스가 없다. Pub/Sub 은 발행자 → 구독자 직접 화살표가 0 개이며, 모든 화살표가 broker 라이프라인 위 한 점에서 끊어졌다 다시 시작한다. "서로를 안다 vs 토픽 라벨만 안다" 의 차이가 가운데 박스의 유무로 강제 구별된다.
- **메시지 큐 (Point-to-Point)**: 같은 가운데 박스 모양을 가지지만, 한 메시지가 단 한 컨슈머에게만 도달한다. broker 분기점에서 화살표가 한 줄로만 뻗고, 다음 메시지는 다음 컨슈머로 라운드로빈 분배된다. Pub/Sub 은 broker 분기점에서 한 번에 N 가닥의 부채꼴이 펼쳐지고 모든 구독자에게 같은 색·같은 번호의 사본이 도달한다.
- **단일 Publisher → 다중 Subscriber 팬아웃**: 발행자가 한 명뿐인 도식은 Pub/Sub 의 절반만 그린다. 본 시각화는 좌측에 발행자 두세 명을 함께 두어 서로 다른 색의 칩이 같은 broker 라이프라인 위로 섞여 들어가 다시 N 가닥으로 갈라지는 다대다 운동을 정면으로 그린다.

### 토픽 격리

같은 broker 박스 안이라도 토픽이 다르면 만나지 않는다. 본 시각의 events 라이프라인 위에서는 events 메시지만 분기되고, 보조 영역의 alerts 라이프라인 위에서는 alerts 메시지만 분기된다. events 토픽 구독자들의 라이프라인 위에는 alerts 메시지의 부채꼴이 도착하지 않는 빈 자리가 명시된다 — "토픽이 다르면 같은 broker 안에서도 만나지 않는다."

### 시간 디커플링

발행 시각과 수신 시각은 같지 않다. broker 박스 내부의 정지 구간 (시간 디커플링의 시각화) 길이가 메시지마다 약간씩 다르고, 같은 발행에서 갈라진 사본들도 도달 시점이 모두 다르다. 발행자와 구독자는 서로의 가용성에 영향받지 않는다 — broker 가 시간 축 위의 완충재 역할을 한다.

### 언제 쓰나

- **이벤트 기반 아키텍처** — 한 이벤트가 여러 다운스트림 컴포넌트에 동시에 영향을 줄 때 (감사 로그·캐시 무효화·검색 색인 갱신 등이 한 발행으로 연쇄).
- **확장성이 필요한 통신** — 발행자와 구독자의 수가 독립적으로 늘어나야 할 때. 양쪽이 서로의 신원을 모르므로 인스턴스 추가가 토폴로지 변경 없이 이뤄진다.
- **시간 디커플링이 필요한 통신** — 발행자가 보낸 시점과 구독자가 받는 시점이 분리되어도 무방한 워크로드 (백그라운드 처리·지연 허용 알림).

반대로, 한 메시지를 단 한 명에게만 보내야 한다면 메시지 큐 (Point-to-Point) 가 어울리고, 호출자가 응답을 기다려야 한다면 RPC 가 어울린다.
`;
