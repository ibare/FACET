/**
 * CachingCdn 학습 설명 — 단일 마크다운.
 *
 * `{facet:cachingCdn}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const cachingCdnDescription = `## CDN (Content Delivery Network)

**전 세계 도시마다 흩어 둔 엣지 캐시들이 가까운 클라이언트의 요청을 각자 받아, 가진 답이면 짧게 돌려주고 없으면 위 계층까지 거슬러 올라가 가져와 채워두는 시스템 차원의 행위.** 단순화된 세계지도 위에 6~8 개의 엣지 PoP 가 분포해 있고, 각 클라이언트는 자기 자리에서 가장 가까운 엣지에 점선 인근선으로 묶인다. 같은 콘텐츠 요청이라도 어디에서 발행됐는가에 따라 짧은 호 한 줄로 끝나기도, 오리진까지 거슬러 올라가 위 계층을 모두 채우며 내려오기도 한다 — 그 차이의 합이 사용자 체감 지연과 오리진 부하를 결정한다.

{facet:cachingCdn}

### 핵심 아이디어

CDN 의 본질은 **분산** 과 **계층** 두 결합이다.

- **분산된 엣지점** — 한 자리가 아니라 여러 도시에 동시 존재. 클라이언트가 어디서 발행했는가가 어느 엣지가 받는가를 결정한다.
- **클라이언트 ↔ 가까운 엣지** — 점선 인근선이 매 요청마다 강제하는 짝지음. "가까운 곳" 이 위치만으로 보인다.
- **두 결말의 호** — 같은 형태의 요청이 엣지에 닿은 순간 두 결말로 분기된다. 히트는 클라이언트→엣지 짧은 왕복으로 즉답, 미스는 엣지→지역→오리진까지 위 계층을 거슬러 올라가는 긴 여정.
- **계층 캐스케이드와 채움** — 미스가 끝나면 엣지에 콘텐츠가 남아 다음 같은 자리의 요청은 짧게 답받는다.

### 결정적 순간 — 첫 미스의 길이, 두 번째 히트의 짧음

서울·도쿄·프랑크푸르트 세 엣지가 모두 회색 (빈 캐시) 인 초기 상태에서, 같은 콘텐츠 A 가 잇따라 요청되는 짧은 시퀀스다.

1. **서울 미스** — 클라이언트→서울 엣지 짧은 호가 그어졌다 회색 그대로이므로 서울→지역→오리진까지 긴 호가 한 단씩 위로 이어진다. 답이 잡힌 오리진에서 콘텐츠 타일이 출발해 지역, 서울 엣지를 차례로 채우며 내려온다. 운동 총 길이가 길다.
2. **도쿄 미스** — 거의 같은 운동이 반복되며 도쿄 엣지가 A 색으로 채워진다.
3. **프랑크푸르트 미스** — 또 한 번 길게 다녀와 프랑크푸르트 엣지도 A 색으로 채워진다. 이 시점 세 엣지가 모두 A 색으로 빛난다.
4. **세 도시 동시 히트** — 서울·도쿄·프랑크푸르트에서 두 번째 클라이언트가 같은 A 를 요청한다. 클라이언트→엣지 짧은 호 세 줄이 거의 동시에 점멸하며 사라진다 — 오리진 펄스는 그려지지 않는다.

### 다른 캐싱과의 구별

- **단일 캐시 서버** — 모든 클라이언트의 짧은 호가 같은 한 점으로 모인다. CDN 은 클라이언트마다 자기 옆 엣지로 호가 갈리는 분포가 본질.
- **로드 밸런서 + 서버 팜** — 갈라짐의 자리가 데이터센터 한 곳 안. CDN 은 갈라짐의 자리가 지구 전체이며, 갈라지는 기준이 부하가 아니라 클라이언트와의 지리적 가까움.
- **CDN 없이 오리진 직접 접근** — 클라이언트들이 모두 자기 자리에서 오리진까지 긴 호를 직접 그어 다발로 모인다. 오리진의 호흡이 매 요청마다 격하게 출렁인다.

### 오리진 부하의 호흡

오리진 노드 둘레의 동심원 펄스 빈도가 곧 부하의 호흡이다. 캐시가 채워진 직후에는 펄스가 눈에 띄게 잦아들고, 같은 시점 엣지점들의 카운터는 빠르게 올라가는 비대칭 운동이 보인다 — 엣지가 일을 잘할수록 오리진은 한가해진다.

### 언제 쓰나

- **전 세계 사용자 대상의 정적 자산** (이미지·비디오·JS 번들) — 짧은 왕복으로 끝나는 히트 비율이 사용자 체감 지연을 결정.
- **트래픽이 크게 출렁이는 콘텐츠** (라이브 이벤트·뉴스 피크) — 엣지가 받아낸 만큼 오리진의 호흡이 잦아들어 오리진 부하 폭발을 막는다.
- **지연에 민감한 API** — 응답을 일정 TTL 동안 엣지에 캐싱해 같은 답을 다시 만들지 않는다.

반대로, 매 요청이 사용자별 동적 응답이고 캐싱 가능 비율이 0 에 가깝다면 CDN 이 줄여 줄 부하가 없다. CDN 의 가속은 "엣지가 답을 가질 수 있는가" 의 비율 위에서만 자란다.
`;
