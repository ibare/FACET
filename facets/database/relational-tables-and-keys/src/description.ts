/**
 * RelationalTablesAndKeys 학습 설명 — 단일 마크다운.
 *
 * `{facet:relationalTablesAndKeys}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const relationalTablesAndKeysDescription = `## 테이블과 키 (Tables and Keys)

**테이블은 같은 형태의 행을 모은 이름 붙은 격자이고, 그 안의 기본키가 한 행을 유일하게 식별하며 다른 격자의 외래키가 그 기본키 값을 가리켜 두 격자를 한 구조로 엮는 관계모델의 골격.** 화면 위 두 카드형 격자가 나란히 놓이고, 좌측 회원 테이블의 기본키 컬럼 끝점에서 출발한 베지어 곡선 한 줄이 우측 주문 테이블의 외래키 컬럼 끝점에 닿는다. 곡선 양 끝에는 Crow's Foot 의 카디널리티 기호가 붙어 1:N 관계의 비대칭이 위치만으로 강제되고, 셀 안의 같은 값을 가진 두 칸은 옅은 배경 강조색으로 짝을 보여준다.

{facet:relationalTablesAndKeys}

### 핵심 아이디어

- **이름 붙은 격자** — 컬럼 헤더와 본문 행이 살아 있는 카드형 테이블이 두 개 동시 존재한다. 한 격자만 그리면 "두 테이블 사이의 참조" 라는 본질이 사라진다.
- **기본키의 시각적 무게** — 기본키 컬럼은 헤더 굵기 + 밑줄 + 좌측 채움 열쇠로 다른 컬럼보다 한 단 떠 있어 "이 컬럼의 값들이 행을 정의한다" 가 시각 무게로 박힌다.
- **외래키의 비대칭** — 같은 키 어휘를 유지하되 좌측 슬롯은 윤곽선만 그어진 비어 있는 열쇠다. "정의하는 자리" 와 "참조하는 자리" 가 채움 vs 윤곽으로 즉시 갈린다.
- **참조 무결성의 셀 단위 짝짓기** — 외래키 셀의 값과 참조 대상 기본키 셀의 값이 같은 값임이 텍스트가 아니라 두 셀의 같은 옅은 배경으로 보존된다.

### 결정적 순간 — 짝지어진 두 셀 + 들이밀린 거부 행

학습자가 두 격자 사이 베지어 곡선을 따라 시선을 옮기다, 우측 외래키 셀의 값과 좌측 기본키 셀의 값이 정확히 같은 글자임을 확인하고 그 한 쌍이 같은 옅은 배경으로 동시에 빛나는 한 컷이다. 그 옆에는 본 격자보다 한 단 가라앉은 거부 인서트가 두 줄 떠 있다 — 회원 테이블 아래에는 같은 학번 값을 가진 후보 행이 빨간 X 와 함께 막혀 있고, 주문 테이블 아래에는 가리킬 회원이 없는 외래키 값이 같은 빨간 어휘로 거부되어 있다. "키가 행을 유일하게 만든다" 와 "외래키 값은 어딘가에 실재해야 한다" 두 제약이 같은 화면 같은 시점에 공존한다.

### 다른 자료구조와의 구별

- **단일 표 / 스프레드시트** — 화면 위에 한 격자만 있고 어떤 컬럼도 다른 컬럼보다 떠 있지 않다. 같은 값이 두 행에 들어와도 충돌 표식이 일어나지 않는다.
- **JSON / 문서형 자료구조** — 중첩된 중괄호 트리에 부모-자식이 위계로 표현되며, 같은 값이 여러 자리에 나타나도 그 자체가 사본이지 참조가 아니다. 테이블과 키는 격자 둘이 옆에 놓이고 한 값이 두 자리에 동시에 사는데, 한쪽이 정의 / 다른 쪽이 참조다.
- **ERD 의 Chen 표기** — 사각·마름모·타원이 스키마 골격만 담고 행 자체는 그리지 않는다. 테이블과 키는 같은 격자 안에 컬럼 헤더 + 실제 행이 살아 있어 키 표기가 컬럼 단위에서 인스턴스 단위까지 일관되게 보인다.

### 후보키와 기본키 선택의 분기

한 테이블에 행을 유일하게 식별할 수 있는 컬럼이 둘 이상 있을 때, 그중 하나가 기본키로 선택되고 나머지가 대체키 자리로 물러난다. 회원 테이블의 학번과 이메일 두 후보키는 동등한 자격으로 함께 떠 있다가, 기본키 토글 칩으로 자리를 바꾸면 채움 열쇠 + 굵은 밑줄과 alt 라벨이 서로 자리바꿈하며 두 테이블 사이의 곡선 출발점도 새 기본키 컬럼으로 미끄러지듯 옮겨 붙는다.

### 인터랙션

- **외래키 셀 호버** — 그 셀과 참조 대상 기본키 셀이 동시에 같은 옅은 배경으로 빛나고, 두 셀 사이에 가는 점선이 잠시 떠오른다. 캡션에 "외래키 값 1001 은 회원의 학번 1001 을 가리킨다" 가 잠시 뜬다.
- **기본키 셀 호버** — 그 셀을 가리키는 모든 외래키 셀들이 같은 옅은 배경으로 동시에 빛난다. 한 행이 N 건의 주문에 의해 참조되는 1:N 분포가 셀 단위로 즉시 보인다.
- **기본키 토글 칩** — 회원 테이블의 두 후보키 학번 / 이메일 사이를 토글한다. 두 컬럼의 강조가 자리를 바꾸고 곡선 출발점이 새 기본키 컬럼으로 옮겨 붙는다.
- **거부 인서트 보기 토글** — 두 격자 아래의 회색 후보 행 두 줄을 켜고 끌 수 있다. "정상" 과 "거부" 를 비교해 본다.
`;
