/**
 * IpRouting 학습 설명 — 단일 마크다운.
 *
 * `{facet:ipRouting}` 토큰은 호스트가 인라인 시각화 노드로 치환한다.
 */

export const ipRoutingDescription = `## IP 라우팅 (IP Routing)

**한 패킷이 출발지 호스트에서 목적지 호스트에 닿기까지, 매 hop 마다 그 라우터의 표 한 장만으로 next-hop 을 정하고 TTL 을 한 칸 줄여 한 걸음씩만 전진하는 hop-by-hop 분산 결정의 시스템 행동.** 어느 라우터도 전역 토폴로지를 알지 못한다. 길은 미리 깔리지 않고, 매 hop 마다 그 라우터의 결정으로 사후에 형성된다.

{facet:ipRouting}

### 핵심 아이디어

IP 라우팅의 본질은 **결정의 지역성** 과 **가장 긴 일치가 이긴다 (LPM)** 두 약속이다.

- 좌측 끝점 — **호스트 A**: 패킷의 src/dst/TTL 세 필드를 정해 첫 라우터로 떠나보낸다. 이후 hop 들에 관여하지 않는다.
- 가운데 — **라우터 사슬 R1·R2·R3 + 분기 R4**: 각자 자기 표만 안다. 한 시점에 단 한 라우터의 표만 펼쳐지고, 다른 라우터의 표는 안개로 가려진다.
- 우측 끝점 — **호스트 B**: 도착한 패킷을 받는다. 또는 도중 어느 라우터에서 TTL 폐기·외부 진출이 일어난다.

### 결정적 순간 — 한 라우터의 한 컷

R1 에 패킷이 도착하는 한 컷이다. R1 옆 표가 펼쳐지고, 패킷 dst (예: 203.0.113.42) 의 비트 표시줄이 표 상단에 떠오른다. 두 행 (203.0.113.0/24, 203.0.0.0/16) 이 모두 매치하지만 한 행이 더 길게 일치하며, 그 행만 굵은 강조 테두리를 얻는다. 채택 행에서 next-hop (R2) 으로 짧은 곡선 화살표가 그어지고, 카드가 떠나는 직전 TTL 칸이 64 → 63 으로 카운트다운한다. 표는 다시 접히고 R1 위에 안개가 다시 깔린다.

### 9 단 hop 시퀀스

매 hop 은 (도착 → 안개 걷힘 → 표 펼침 → 비트 비교 → 채택 행 → 시선 화살표 → 떠남 + TTL 카운트다운 → 표 접힘 → 다음 링크) 의 9 단 시퀀스로 1:1 대응된다. 학습자는 이 한 호흡을 라우터마다 같은 모양으로 본다.

### 다른 통신 모델과의 구별

- **L2 스위칭 (이더넷)**: 한 노드에서 N 가닥의 부채꼴 운동이 발화한다 (모르는 MAC 은 모든 포트로 플러딩). IP 라우팅은 어떤 라우터도 패킷을 N 가닥으로 복사하지 않는다 — 한 hop 마다 단 한 줄의 화살표만 next-hop 으로 뻗는다.
- **회선교환**: 발신 직전 출발지부터 목적지까지의 한 줄이 굵은 띠로 빛나며 예약된다. IP 라우팅에서는 어떤 시점에도 출발지에서 목적지까지의 길이 미리 그어지지 않는다 — 길은 매 hop 마다 사후에 형성된다.
- **"라우터 = 우체국, 세계 지도를 본다" 비유**: 본 시각화는 다른 라우터 영역을 안개로 가리는 위상으로 그 비유를 차단한다. 라우터는 자기 표만 보고 다음 한 걸음만 정한다.

### TTL 의 사건성

매 hop 카드는 TTL 한 칸을 잃는다. TTL ≤ 4 에서 게이지가 주황으로 바뀌고, TTL=0 에 닿는 순간 카드가 회색으로 페이드되며 X 표지와 함께 정지한다 — 다음 링크로 떠나지 않는다. 자동 시연의 마지막 발신 (TTL=2) 이 도중 폐기 사건을 명시한다.

### longest prefix match 의 가치

R1 의 표에 두 행 (203.0.0.0/16 → R4, 203.0.113.0/24 → R2) 이 dst 203.0.113.42 와 모두 매치하지만, 더 긴 일치 (24 비트) 를 가진 행이 채택되어 패킷은 R2 로 간다. 분기 R4 가 토폴로지에 있는 이유가 이 운동에서 명시된다 — 더 짧은 일치만 가졌다면 R4 로 가지만, 더 긴 일치가 있으면 R4 로 가지 않는다.

### 언제 쓰나

- **인터넷 규모 통신** — 모든 라우터가 전역 지도를 가질 수 없을 만큼 규모가 큰 망에서, 분산 결정의 합으로 길이 만들어진다.
- **다중 경로 / 장애 우회** — 한 라우터가 죽으면 다른 라우터의 표가 다른 next-hop 을 제시해 자연스러운 우회가 발생한다 (본 시각화는 단순화된 정적 표만 다룬다 — 표가 어떻게 만들어지는가는 라우팅 프로토콜 RIP·OSPF·BGP 의 다른 이야기다).
- **계층적 주소 공간** — IP prefix 가 짧을수록 더 큰 망을 가리킨다. LPM 은 "가장 구체적인 망의 결정" 을 우선해 라우팅 테이블의 행 수를 줄인다.
`;
